# 部署说明 — Server @ 231 小主机 + 各设备 Agent

> 2026-09-19 随首次部署留档。<SERVER_IP> 为 Server 机（小主机 231）的局域网地址，实值见工作区 `00-暂存\handoff.md`（真实 IP 不进 git 的惯例）。

## 架构与端口

```
各设备 Agent (python) ──ws──> Server :9100 (Agent 接入)
浏览器/看板            ──http+ws──> Server :9101 (REST + 2s 广播 + 静态看板)
```

Server 已部署在 231 小主机（Win10 IoT LTSC，Node 22.23.2；主机名/实值 IP 见运维留档，不入 git）。

## Server（231）运维卡

| 项 | 值 |
|---|---|
| 安装目录 | `C:\Users\user\host-monitor`（server\ + client\dist\ + server-run.cmd） |
| 开机自启 | 计划任务 `HostMonitorServer`（SYSTEM 账户，开机触发，**不依赖用户登录**；崩溃 5 秒自动拉起） |
| 日志 | `C:\Users\user\host-monitor\logs\server.log`（含重启标记） |
| 手工重启 | `schtasks /end /tn HostMonitorServer` 后再 `schtasks /run /tn HostMonitorServer` |
| 升级/重部署 | 笔记本重打 zip 后执行 `node 项目\remote-net-tool\231-deploy.mjs`（幂等：停旧→解包→重启） |
| 历史库 | `host-monitor\server\data\host-monitor.db`（SQLite，raw 7 天 / 1 分钟均值 30 天自动滚动） |
| 电源 | 平衡计划，接通电源从不睡眠（实测 AC standby-idle=0） |
| 防火墙 | 实测 9100/9101 局域网直接可达，未加规则；如换机被拦：`netsh advfirewall firewall add rule name="host-monitor" dir=in action=allow protocol=TCP localport=9100-9101`（需管理员） |
| 鉴权 | 读侧（9101 看板与接口）**无鉴权**——原决策维持，勿"顺手"添加。写侧（改名/配对码/演示开关/在场归名/**阈值与探测计划**）要管理口令（S1a，S5 起所有写端点统一走同一道闸 + 按来源地址的失败预算；S6 的 5 个配置写端点由闸的枚举自检自动纳管）。**全进程唯一一处带闸的读** = S6 为编辑表单加的 `GET /api/admin/config`（未掩码），其余读一律开放且探测目标按 S5 G3 掩码（`a.b.*.*`）——这条边界由 `selftest-credential-hygiene` C5 按端点名白名单钉住。Agent 接入侧（9100）自 S2a 起按 `HM_INGEST_TOKEN` 强制：`off` 逃生阀 / `legacy` 默认（陌生 id 一律拒，已入册老 Agent 宽限并点名）/ `strict` 全部要凭据。节点密钥自 S5 起以 `sha256$盐$散列` 落库（**是散列不是加密**：口令与凭据本身仍是明文局域网，无 TLS）。**2026-09-19 起生产为 `strict`**（写在该机的 `server-run.cmd` 里，不在仓库），翻档与回滚动作见"里程碑重部署清单" |

## Agent（每台被监控设备）

前置：Python 3 + `pip install -r agent/requirements.txt`（psutil、nvidia-ml-py、websockets；无 NVIDIA 卡时 GPU 自动降级为静态信息）。

启动命令（在本机 `agent/` 目录下运行）：

```
python main.py --server ws://<SERVER_IP>:9100 --host-id <节点ID> --role <角色>
```

- `--host-id` 缺省时自动用主机名推导（小写、空格转 `-`）；建议显式指定，与看板节点名一致。
- `--role` 可选：`db / inference / desktop / laptop / display / other`（默认 other）。
- `--interval` 推送周期秒，默认 2.0。

**S2b 之后（1.1.0）多两种更省事的用法**——参数可以不再烧进计划任务：

```
# 方式一：一份 agent.json（模板 agent.example.json，拷进 agent/ 目录即可）+ 一次性配对
python main.py --enroll <签发的 12 位配对码>
# 方式二：全部走环境变量 HM_SERVER / HM_HOST_ID / HM_INTERVAL / HM_ROLE / HM_TOKEN
python main.py --print-config      # 排障第一问：这项到底是从哪来的
```

- 配对成功会把节点密钥写进 `identity.json`（与 `host_id` 绑定），**第二次启动不需要配对码**；两份文件都已 gitignore，`identity.json` = 写权限，不要贴进群聊。
- `--pair-only` 只注册不常驻（给安装脚本先配对、再交给计划任务拉起）。两份文件默认落在**代码同目录**（不是 cwd——计划任务的 cwd 不可信），目录只读时用 `--data-dir` 指到可写位置。
- 被服务器拒绝时 Agent 打印中文原因并以退出码 **2** 结束、**不重试**——所以 142 的 systemd / 231 的计划任务会反复拉起它（每轮一行同样的中文日志），这不是"它自己会好"。看到"拒绝接入"就去接入页重新签发码；连码都没了就先 `HM_INGEST_TOKEN=off` 让它活着（隐患 H17）。

当前机群状态（2026-09-19 实态）：

| 设备 | 状态 | 常驻方式 |
|---|---|---|
| 笔记本 124 | `laptop-dev` 在线 | 计划任务 `HostMonitorAgent`（**登录触发**，非开机自启：`wscript run-agent.vbs` → `run-agent.cmd` 的 `:loop`，崩溃 5s 拉起）；代码=本仓库 `agent/`，日志 `00-暂存\agent-laptop\agent-loop.log`；注销/关机后不会自跑，看板会把这台判成"离场" |
| 推理机 142 | `infer-142` 在线 | **systemd 服务 `host-monitor-agent`**（enable + Restart=always，开机自启）：`~/host-monitor/agent` + `~/host-monitor/venv`；日志 `journalctl -u host-monitor-agent`；升级=笔记本重跑 `node 项目\remote-net-tool\142-agent-deploy.mjs`（幂等）；拓扑采集依赖该机 sudo NOPASSWD 跑 `dmidecode -t memory`（每次重启两条 sudo 审计日志，属预期） |
| 小主机 231 | `srv-231` 在线 | **Agent embeddable 方案**：`C:\Users\user\host-monitor-agent`（Python 3.12.10 embeddable + 离线 wheels + agent 代码，`._pth` 含 `..\agent` 路径行）；计划任务 `HostMonitorAgent231`（SYSTEM onstart + 崩溃 5s 拉起）；日志 `host-monitor-agent\logs\agent.log`；升级=笔记本重打 agent231 zip 后跑 `node 项目\remote-net-tool\231-agent-deploy.mjs`（幂等）。无 GPU 场景实测该机反而有 GT 1030，NVML 正常 |

三台均已带节点密钥（`agent_version 1.1.0`、名册 `confirmed`），落点分别是 `124: 仓库 agent/`、`142: ~/host-monitor/agent/`、`231: host-monitor-agent\agent\`——三处都不在升级包里，重跑部署脚本不会把它们带走。


## 上线记录 · 2026-09-19（V2 里程碑：S1a~S4b 上 231 + 三台配对 + 翻 `strict`）

线上包 = 仓库 `ae8ace6`。验收读数：`/api/hosts` 三台在线、`/api/roster` 三台 `has_credential=true`、`/api/enroll` 的 `ingest={mode:strict, accepted:3, accepted_legacy:0, denied:0, drift:0, keyless_agents:[]}`。

- **管理口令已设**：散列在 `server/data/roster.db` 的 `meta.admin_pass`（scrypt，非加密存储），明文只留在工作区 `00-暂存\deploy-231\.admin-passphrase.txt`。每个要写入的浏览器在顶栏 🔑 各输一次；改口令会让其他已开页签的写入全部 403（H13）。**忘记口令不用手改数据库**：见下一节"口令重置"。
- **`strict` 开关位置**：231 的 `C:\Users\user\host-monitor\server-run.cmd` 里 `set HM_INGEST_TOKEN=strict`。改前原文备份在 `C:\Users\user\deploy\server-run.cmd.bak-0919`（旧 SHA256 `53C9…B8D2` / 新 `333A…94D5`）。回滚 = 用备份覆盖后重启 `HostMonitorServer` 任务，代码不用动。
- **我此前预测的"名册迁移闸"没发生**：`roster.js` 首次启动会从历史库播种（日志 `3 node(s), seeded 3 from history`），所以 `legacy` 默认档直接接住了三台 1.x Agent，全程没需要 `HM_INGEST_TOKEN=off` 的引导窗口。下次上新机仍按播种后的实情判断，别照旧文档准备停机窗。
- **231 自监控 Agent 配对时不要停计划任务**：`offline_seconds=15`，一停就产生一条真实"失联"告警。做法是让一次性 `--enroll` 与常驻 Agent **并发**跑（同 host_id、同样的帧，在线状态不断），`identity.json` 落盘后再 `schtasks /end` + `/run` 让常驻进程重载密钥；这一次停仍擦到了 15s 线，台账里 19:23 的 OFFLINE→已恢复就是它，属预期噪声。
- **H8 已在生产证伪过一遍**：`node server/scripts/probe-strict-denial.mjs ws://<SERVER_IP>:9100` → `{"type":"rejected","reason":"unknown_host"}`，台账落一行掩码地址（`192.168.*.*`）。台账里 `neg-test-strict*` 那几行是这个自检，不是有人真在敲门。
- 124 的 `identity.json` 落在仓库工作目录 `agent/`（已核 `git check-ignore`），`git clean -xdf` 会连密钥一起清掉——清目录前先备份。

## 上线记录 · 2026-09-20（S5+S6 上 231，V2 收口）

线上包 = 仓库 `61bf4db`（GitHub 已打 tag `v2` = 同一提交，main 之后只有文档笔）。一句话结论：**上线成功**——三台 Agent 全部带着 `sha256$` 凭据在 `strict` 下重连、重启自启通过、无一台走 legacy 通道；**新暴露四条**（H26 启动期 VACUUM 不彻底、H27 ZT 本地 API 401、H28 `untouched_since_seed` 语义、H29 备份里的明文密钥）全部只做登记，一条没追改。

### 打包与备份（上线前）

- 重打包脚本 = `00-暂存\repack-231.ps1`。它的核心是 robocopy `/MIR` 上那条 `/XF probes.json zt.json history.json settings.json`：这几份是"这台机器是谁"的信息且 gitignored，拿笔记本的副本去 `/MIR` 生产机 = **悄悄改掉线上的探测目标**。
- 包自检三问脚本自带（该在的在／不该在的不在／播种文件是仓库那份并逐哈希）。当晚复核产物：679 条目、全部落在 `host-monitor/` 之下、无 `probes.json`/`zt.json`/`data/`、1,320,288 B、SHA256 `A3F4…B60B`。条目名已是正斜杠（脚本里"PS 5.1 会写反斜杠"那句归一化在 PS 7 下用不上，留着无害）。
- **备份落点**：`C:\Users\user\hm-backup\20260920-013149` = `server-run.cmd` + 计划任务 XML + `config\`（4 份）+ `data\`（6 份，含 `roster.db*` 与 `host-monitor.db*`）= 12 文件 / 13,107,907 B。**备份必须逐文件比哈希，不能只看目录在不在**：前一次 `20260920-013038` 就是 `Copy-Item` 静默失败留下的半份（只有 2 个文件），当晚经"逐文件 SHA256 是真备份的子集"校验后删除。
- ⚠️ 这份备份里的 `roster.db` 是 **S5 迁移之前**的快照，三把节点密钥仍是明文（H29）。它当时是回滚件所以保留 —— **同日上午经他确认后已删，处理过程与留下的能力变化见本节末"同日上午补充"**。以后照这一节做上线时，仍然要按"备份含明文"来对待它，别以为备份天生干净。

### 部署动作

- `node 项目\remote-net-tool\hm231-deploy-s5s6.mjs`：sftp `fastPut` 上传 zip → `Expand-Archive -Force` 到 `C:\Users\user`（**合并，不删**包外的文件，所以生产配置不会被升级带走）。
- 起服务只走计划任务：`schtasks /end /tn HostMonitorServer` → `/run`。**不要手工 `node src/index.js`** —— `strict` 来自 `server-run.cmd` 里的 `set HM_INGEST_TOKEN=strict`，手起的进程没有这个 env，会**静默跑成 `legacy`**（表现一切正常，只有 `/api/enroll` 的 mode 会说实话）。
- 迁移日志一行：`[Roster] Migrated 3 plaintext node key(s) to salted hashes (S5 G1)`。
- 盘上明文清理（H26）：启动期那一次 checkpoint+VACUUM **不够**（二进制搜索仍命中 1 处），当晚补做"停服 → `wal_checkpoint(TRUNCATE)` + `VACUUM` → 再搜 0 命中 → 起任务 → 重启后仍 0 命中"。

### 现场读数（01:40 首启、02:08 重启后各取一次）

| 读什么 | 读数 | 该怎么理解 |
|---|---|---|
| `/api/enroll` | `mode:strict, accepted:3, accepted_legacy:0, paired:0, denied:0, drift:0, keyless_agents:[]` | **这一行就是"S5 没把三台打死"的证据**：三台都以哈希凭据被接受，legacy 通道一次没用过 |
| `/api/hosts` | 在线 3/3、临时 0/0、演示 0/0、静默 0、链路 `worst_level:null`；聚合 CPU 峰 53 / 内存峰 80 / GPU 峰 59 | 三台的 `class=persistent`、`kind=agent`、无逐机覆盖 |
| `/api/roster` | 三台 `has_credential=true`；`nodes` 数组 **7 条** | `nodes` 是**全量缓存、含 retired**（另有 `retired` 数组重复给出那 4 条）→ 7 = 3 真机 + 4 演示（`kind=demo`、`class=retired`、`token=null`、`agent_version=demo-1.0`）。**别把 7 读成"名册里有 7 台机器"** |
| `/api/config` | 探测目标掩码为 `a.b.*.*`；`untouched_since_seed` 首启 `true` → 重启后 `false` | 后者是**预期值**，不代表有人改过配置（H28） |
| `/api/presence` | `{"enabled":true,"source_ok":false,"error":"http-401","last_ok_at":null}` | 已知预期（H27），不是这次上线的新故障 |
| `/api/alerts` | active 2 条：`infer-142 /` 94.3%（阈 90）、`srv-231 F:\` 99.6%；resolved 1 条：`srv-231 rtt_ms` 812→406 | **两条磁盘是真的**；rtt 那条是重启期间 Server 自测的一帧，10 s 自动闭合，属预期噪声。今晚横幅红点该亮 |

演示机群：09-19 曾在生产上开过一次（4 台 `sim-*` 自动入册，日志里那几行 `New node auto-enrolled as persistent: sim-*` 就是它），本次确认 `meta.demo_enabled=0`、四台已 `retired`。道具的历史样本按保留期自然衰减，手工清理语句在下一节末尾。

### 重启自启验证（用户指令："重启即可"）

- `shutdown /r /t 5` → 02:06:49 回来，`/api/health` 约 50 s 内 200（`uptime=52.7`），`HostMonitorServer` 与 `HostMonitorAgent231` 两个任务都在跑（`node=1 python=1`），全程无人工干预。
- 脚本层面踩到的坑：`shutdown` 之后 SSH 会**先回来一次**（关机序列还没走到断网），那时读到的是旧状态、紧接着连接被重启打断。`hm231-cleanup-reboot.mjs verify` 因此改成"先等它真的断开 → 再等它真的回来 → 并用 `LastBootUpTime` 卡住'这必须是刚发生的一次开机'才肯读数"。
- **一条教训，比结论更值得记**：重启后在日志里读到 `[Roster] … (3 node(s))`，而我记得的是 7 行，第一反应是"名册被清了 4 行"。实况是 **`server.log` 跨开机累加**，那一行属于 09-19 的开机块（我按"最后 160 行"过滤，一次跨了三回开机）。真正的判别办法是**别信日志行号，直连现役库**：只读形状探针 + `/api/roster` → 三台 `HASHED`、四台演示 `token=null`，一行没丢。**以后凡是从日志里读"某一次启动的值"，先把它钉到某个开机块上再下结论。**

### 磁盘清理（今晚做到的与没做到的）

- 工具：`项目\remote-net-tool\hm231-cleanup-reboot.mjs`，阶段 `list|probe|roster|log|alerts|clean|reboot|verify`。只删两类：**可再生暂存物**（部署 zip、临时 mjs）与**已证实**的家目录残留（6 个 DB/WAL/SHM + 5 份配置 + 1 个 zip，约 14.5 MB）。
- "现役 vs 残留"的判据（不看内容，四条一起用）：① 文件锁——现役库被进程独占，`Get-FileHash` 直接失败，这本身就是"它活着"的证据；② schema 形状——残留那份 `enroll_token` 仍是 `PLAINTEXT-32hex`，早于 S5；③ `last_seen` 落后 6 小时以上；④ 大小/哈希与现役不一致。删除三条护栏：现役 counterpart 必须存在（**绝不留成孤本被删**）、该文件必须没被任何进程占用（被打开 = 我判错了，立刻停手）、逐文件打印结果。
- **没删、也不该我删的**：`.dsh`/`.dsh.bak-*`（DSH 运行时）、`Downloads`/`AppData`（用户）、`sync-sessions.tar`+`sync-unpack`（约 300 MB 会话归档）、回收站与 Temp（C: 还有 163 GB 空闲，删了没收益只有风险）。
- **要说清的一点**：今晚回收的 ~14 MB 对那两条磁盘 CRIT **零帮助**。`infer-142 /` 与 `srv-231 F:\` 是**内容**问题（影音/游戏/模型），只有本人能决定删哪个卷里的什么。C: 当前 free 162.94 GB / used 60.62 GB。

### 明天上眼清单

1. 看板"设置"页：两栏排版、全局阈值表、逐机阈值/探测计划、"回落到播种值"按钮 —— 视觉与文案是否合用（V3 的 UI/UE 素材就从这一页开始）。
2. 事件流里"该机未再上报"的虚线行 = H14 的重启余温，不鸣响、宽限期到点自行闭合，不用处理。
3. 在场台账 `source unavailable (http-401)` = H27，已知。
4. 两条磁盘 CRIT 是真的：要不要清 `F:\` 和 142 的 `/` 仍是内容决定。**09-20 上午已定**：监控侧走 V3 的"降噪/单独归置"讨论，阈值不改松。
5. 万一某台"没配上来"：先看 `/api/enroll` 的 `keyless_agents`；**S5 迁移前那份库备份已于同日上午删除**（见下一小节），所以那条路上没有"退回旧库"可走，只有重新配对。

### 同日上午补充（办公室经 ZeroTier 口连 231）

- **H29 关闭**：删的是 `C:\Users\user\hm-backup\20260920-013149\data\` 里的 `roster.db` / `-shm` / `-wal` 三个文件（28,672B / 32,768B / 4,120,032B）。**要点是那个 `-wal`**：明文密钥主要躺在尚未合并进主文件的旧帧里，只删 `roster.db` 等于没删——所以备份要连 `-wal`/`-shm` 一起拷，删也要一起删。三条护栏逐文件过：现役 `server\data\roster.db`（53,248B）在 → 不是孤本；三个文件都 FREE（没被进程占用）；逐个打印 `exists_now=False`。删前清单里每份都带 SHA256 前 12 位，事后可查。
- **这一刀换来一条能力变化，必须写清**：退回 S5 **之前**（`ae8ace6`）的路径从此只能靠**重新配对三台 Agent**——老代码不认 `sha256$` 前缀，而那份能喂它的库没了。退回 `61bf4db` 及之后不受影响。这是用户 09-20 上午确认"不再回滚"之后做的决定，不是意外丢失。
- 复扫 `hm-backup` 全树：只剩 `config\probes.json`（225B，真实探测目标 = 家庭网络拓扑，**属 IP 不属凭据**）与样本库 `host-monitor.db*`（约 8.9MB，事件里的地址写入时已掩码）。这个目录现在既不含凭据也不含回滚能力，整目录清不清由他一句话。
- **操作侧一条**：家里的局域网地址在办公室不通，231 要走它的 ZeroTier 口（真实地址只记在 `00-暂存\handoff.md`，本文不放）；脚本已支持 `HM231_HOST=<地址>` 覆盖。ZT 私有网组禁 ICMP，所以 **ping 不通而 TCP 22 通是正常**，别把"ping 不通"读成"机子挂了"。
- 那两条磁盘 CRIT 的处理方向他已定：V3 讨论"**降噪 / 单独归置**"，**不改阈值**（把 90% 改松等于关掉一个真在报警的传感器）。

## S5 上线步骤（凭据治理 + ZeroTier 在场台账）

这一闸改变的是**磁盘上的凭据形态**，所以它的运维风险不在代码而在"库已被新版改过、老代码读不懂"。按下面顺序做，别跳备份。

1. **先备份名册库**（唯一必须在停服前做的事）：`schtasks /end /tn HostMonitorServer` → 复制 `server\data\roster.db`（连同 `-wal`/`-shm` 一起，若存在）为 `roster.db.bak-s5-<日期>` → 再走 `231-deploy.mjs` 重部署。
2. **启动即自动迁移**：Server 起来时把仍是明文的节点密钥改写成 `sha256$<盐>$<散列>`，日志一行 `[Roster] Migrated N plaintext node key(s) to salted hashes (S5 G1)`，随后一次 `wal_checkpoint(TRUNCATE)` + `VACUUM`（不 VACUUM 的话旧明文还躺在自由页和 `-wal` 里，等于没删）。**这句 09-20 在现场被证了一半**：启动期那一次实测**没有**清掉已释放页里的明文镜像（二进制搜索仍命中 1 处），要**停服**再 checkpoint+VACUUM 一次才干净 —— 见 **H26** 与下一节上线记录。
3. **验收三读**：`/api/roster` 三台仍 `has_credential=true`；`/api/hosts` 仍三台在线；对 `roster.db*` 三个文件做二进制搜索，原明文密钥应查不到（本机自检 A 段就是这么断言的）。**在生产上这一读今晚失败过一次**（就是上一条的启动期清理不彻底）；补救动作 = `schtasks /end` → 跑一次 checkpoint(TRUNCATE)+VACUUM（今晚用的是临时脚本 `00-暂存\deploy-231\roster-vacuum.mjs`，长期做法是把它做成 `server/scripts/vacuum-roster.mjs` 并挂到迁移后面，见 H26）→ 再二进制搜索应 0 命中。**别为此每次开机都 VACUUM**：231 是低功耗机，那是启动期的整库重写。
4. **回滚方向只有一个**：老代码不认 `sha256$` 前缀 → 用回老包时必须**连 `roster.db` 一起回滚到备份**，否则三台 Agent 全部认证失败（表现按 H17 = 反复拉起 + 每轮一行拒绝日志）。新代码读老库是兼容的，老代码读新库不是。
5. **口令重置（忘记管理口令时，H13）**：在 231 上 `node scripts\reset-admin-pass.mjs --db server\data\roster.db` 先看状态（不带 `--yes` 只报告、不改），确认后加 `--yes`。这是**重置不是找回**：存的是 scrypt 散列，谁也印不出旧口令；跑完写侧回到默认拒绝，由人在看板 🔑 重新设一次。**节点密钥不受影响**，三台 Agent 不用重新配对。
6. **ZeroTier 在场台账（可选，不做也不影响其他功能）**：把 `server\src\config\zt.example.json` 拷成 `zt.json`（已 gitignore，内含本机 ZT 端口与排除节点 id，属于"这台机器是谁"的信息）。231 的 Server 以 SYSTEM 跑，能读 `%PROGRAMDATA%\ZeroTier\One\authtoken.secret`，因此**不需要**填 `token_file`，也不需要 `HM_ZT_TOKEN`；token 只在进程内读，任何接口/日志/事件行都不会带它。第一次跑必须现场确认一次：接入页右栏"ZeroTier 在场"卡应**非空态**（空态会写明原因：`no-config`/`disabled`/`token-unreadable`/`http-401`/`unreachable`…）。**根/行星节点是否混进列表**也在这一眼确认，混进来了就往 `exclude` 里加它们地址的前 6 位。
   **但这一眼在 231 上目前做不到**（09-20 现场结论，细节在 **H27**）：9993 的 HTTP 口对**任何** token 形态（`?authtoken=`／`Authorization`／`Authorization: token`／`x-authtoken`，`Invoke-WebRequest` 与 `curl` 两实现）一律回 401，而盘上那份 token 经核确实是运行中实例那个 home 里的（`identity.public` 与 CLI 报的节点 id 一致、端口文件解出就是 9993、只有一个 ZT 进程），`zerotier-cli.bat info` 同机正常。整机重启也不改变它。所以"**非空态**"这一读在 231 上要等 H27 的改法（回退到 CLI 通道，已验可通）落地后才能验收；看到 `source unavailable (http-401)` **不是这次上线的故障**。
7. **失败预算的表现要认识**：同一来源地址 60 秒内 8 次口令失败 → 该地址 429，响应体只有一句人话、不带倒计时。别人问"为什么我明明输对了却说失败"，先怀疑他前面输错过、等一分钟即恢复（预算是按地址滚动的，不是永久封）。

阈值侧这两项在 S5 时是"改文件 + 重启"，**S6 之后改它们不用重启**（下面 S6 一节）：`presence.degrade_after_minutes`（缺省 15，进程内长静默降级为缺席，H18；与跨重启的 `absent_after_minutes`=3 是两个问题，别合并）、`events.merge_seconds`（缺省 60，同类事件合并窗口）。

## S6 上线步骤（设置界面 + 配置单一真源）

S6 不改变磁盘上的凭据形态——**上一节（S5）才是这一步里唯一"回滚必须连库退"的风险项**，两步合在一次上线时按 S5 的顺序做，别跳备份。S6 自己的运维风险是另一件事：从这一版起，**界面写的值才是运行时的真相**，文件退居"首次播种 + 逃生舱"。

1. **部署动作与 S5 相同**（备份 `roster.db` 含 `-wal`/`-shm` → `231-deploy.mjs` 重部署 → 起任务）。S6 只在名册库里新建一张空的 `settings` 表，首次开机没有行 → 从 `thresholds.json` / `probes.json` 播种，**行为与今天线上完全一致，零迁移**。
2. **现场两读（各 10 秒）**：`GET /api/config` 看 `untouched_since_seed` —— **首启（库里还没有行、刚播种完那一次）为 `true`；之后每次开机都是 `false`，这是预期值、不代表有人改过配置**（09-20 现场发现，见 **H28**：播种这个动作本身就会写那一行，而判的是"有没有行"）。想确认"没人改过"，今晚可用的两读是：`/api/hosts` 里三台的逐机覆盖为空 + 设置页数值逐格等于 `server\src\config\thresholds.json`（下一条）。第二读：打开"设置"页，全局阈值表的数值应逐格等于 231 上 `server\src\config\thresholds.json`。
3. **改配置不再需要重启 Server**（这一条改变了以前每一版的习惯，值得单独记）：在设置页改一格 CPU 严重档 → 点"应用" → 下一帧判级就用新值，重启后仍是新值。**唯一不在此列的是探测目标**：保存后接口回 `applies_on:'agent_reconnect'`，那台机器的网关/关键机要等它的 Agent 下次接入才换（目标写在 Agent 手里），界面上也照这么写。仍要重启的只剩端口与 `HM_INGEST_TOKEN` 档位。
4. **两个读端点要分清**：`GET /api/config` 不设闸（读侧维持开放），但探测目标已掩码成 `a.b.*.*`；`GET /api/admin/config` 要口令、返回未掩码文档，是编辑表单的取数源（拿掩码值回写等于悄悄把探测臂换成星号）。**后者是全进程唯一一处带闸的读**，`selftest-credential-hygiene` C5 按路径名钉着它并写明理由——将来谁再加第二处带闸的读，自检会红，那是故意的。
5. **回滚方向新加一条，性质与 S5 不同**：老代码根本不读 `settings` 表，所以退老包**不会崩**——但库里的编辑值还活着，将来再升回来时它会突然生效，表现为"上一版明明没有这些阈值"。所以退代码之前先清行（这一步可逆性差，先停服务再删）：
   ```
   schtasks /end /tn HostMonitorServer
   cd C:\Users\user\host-monitor\server
   node -e "import('node:sqlite').then(m=>{const d=new m.DatabaseSync('data/roster.db');d.exec('DELETE FROM settings');d.close();console.log('settings cleared')})"
   ```
   清完再退包即可；下次升回新版会从文件重新播种。（跑起来会带一行 `ExperimentalWarning: SQLite is an experimental feature` —— 那是 Node 22 的固有提示，不是失败；成功的那行是 `settings cleared`。）**同一句话也印在设置页的"帮助"卡上**，不靠人记这份文档。
6. **口令侧无新增负担**：本期 5 个新写端点全部走 S1a/S5 G2 那道闸（闸的枚举自检会自动纳管新端点），朋友仍只需知道一次口令。既有代价不变：改口令会踢掉其他已开页签（H13），同一地址 60 秒内 8 次输错会 429 一分钟（H20、上一节第 7 条）。
7. **重启后看到"该机未再上报"不用处理**（H14 的表现，事先认识比现场判断快）：Server 重启会把它上次没闭合的告警读回来，这些行带虚线小标"该机未再上报"、**不鸣响**、横幅红点在全部严重项都冻结时停止闪烁；宽限期（默 5 分钟，设置页可配 1~240 分）到点后自行闭合成"未上报"，**不会写成"已恢复"**。等 Agent 自己回连即可。真失联（Server 有它的记录、只是不上线）不走这条路，仍照常报 OFFLINE。

## 下次里程碑清单（新增设备 / 再翻档）

顺序不能反：先换 Agent、再翻 Server，否则机器会被自己的策略当场踢下线（且按 H17 的表现是"反复拉起 + 每轮一行拒绝日志"）。

1. Server 重部署（`231-deploy.mjs`）；翻过 `strict` 之后新机器必须**先配对再启动常驻**，否则起不来。
2. 签发配对码：看板"接入"页点"生成配对码"（明文只显示一次，页面同时给出可粘贴的 Windows/Linux 命令）。接口方式：`POST /api/admin/enroll`，头 `x-hm-admin: <管理口令>`，body `{"ttl_minutes":60,"max_uses":3}`。逐台升级并配对：
   - 142：`node 项目\remote-net-tool\142-agent-deploy.mjs` → `node 项目\remote-net-tool\142-agent-enroll.mjs <码>`（脚本自己停 unit、一次性带码跑一次、再起 unit；`ExecStart` 本来就不带码，无需回改）。
   - 231：重打 agent231 zip → `231-agent-deploy.mjs` → `node 项目\remote-net-tool\hm231-agent-enroll.mjs`（并发改法见上一节，别停任务）。
   - 124：本机 `python main.py --server ws://<SERVER_IP>:9100 --host-id laptop-dev --role laptop --enroll <码>` 跑一次，看到 `Paired. Node key saved` 后手工结束该进程，再 `Start-ScheduledTask HostMonitorAgent`。
3. 每台跑 `python main.py --print-config`，确认 `credential= identity.json`；名册里对应节点显示"已带凭据"。
4. 顶栏 `⚿ 未带凭据 N` 归零（等价于 `admin.keyless_agents` 为空）后才翻 `strict`；再看三台仍在线、异常接入台账无新拒绝。
5. 回滚 = 把该 env 改回 `legacy`（或 `off`）重启，不需要回退代码。**S5 之后的回滚多一条**：若已用新包跑过，`roster.db` 里的密钥已是 `sha256$` 形态，退老包必须连库一起退（见上一节第 4 条）。
6. 若本次带上了 S5 的在场台账：接入页右栏"ZeroTier 在场"卡**必须是非空态**，且列表里没有根/行星节点。这一步在本机做不到（普通用户读不到 `authtoken.secret`），是**只能现场验**的一次性确认（`S5-细化设计.md` §8.2）。
7. **新机器接进来之后顺手要做的两件配置**（S6 起都在设置页，不再动文件）：① 档案里填 `site` 与 `role`——`site` 决定它拿哪一份探测计划（本期站点表只读，所以"新增一个站点"仍要走 DB 或文件，见 H6 残留①）；② 若它是异构机（编译/推理/游戏），去"逐机阈值"挪它自己的红线，**不要为它改全局**（H7 的整个出发点）。做完确认它卡片上的探测计划徽标不是"继承全局（可疑）"——那台机器 Agent 的启动日志里打印的是同一句话，两边不一致就是派生逻辑错了。


清理演示道具（若要）：`DELETE FROM samples_raw WHERE host_id LIKE 'sim-%';` + `samples_1m` 同理 + `DELETE FROM nodes WHERE host_id LIKE 'sim-%';`（`S2-细化设计.md` §8）。

## Server 侧配置文件（均在 `server/src/config/`）

| 文件 | 作用 | git 状态 |
|---|---|---|
| `probes.json` | 链路探测目标的**首次播种值**（网关 + key_hosts + 可选 `sites`）。DB 里有 `settings['probes']` 行之后**文件不再被读**，改它不生效——改目标走设置页 | **gitignored**（含真实 IP），模板见 probes.example.json |
| `thresholds.json` | 阈值 + `presence`（缺席/静默两条时间）+ `events.merge_seconds` + `alert.*`（含 `unknown_alert_grace_minutes`）的**首次播种值**。同上，播种后运行时真源在 DB；它另外还是一个用途：**设置页"回落到播种值"就是这个文件当前的内容**（S6 §2.2 的逃生舱，故"回落"= 回到文件值，不是回到出厂值） | 随仓库 |
| `history.json` | 持久化参数覆盖（缺省用内置默认） | gitignored，模板 history.example.json |
| `zt.json` | ZeroTier 在场台账：本机 ZT 端口、轮询间隔、排除节点前缀（S5 §4）。**没进 S6 的配置层**，仍是文件（H24） | **gitignored**（含"这台机器是谁"的信息），模板 zt.example.json；**缺文件=台账关**，不是故障 |

**S6 之后这张表要重新读一遍**：前两个文件从"运行时配置"降级成"首次播种 + 回落目标"，改它们**在已有 DB 行的机器上不会产生任何效果**（不是"要重启才生效"，是"根本不读了"）——要改配置请用设置页，那里改完下一帧即生效、不需要重启。仍然"改文件 + 重启"才生效的只剩三个：`history.json`（采样与保留参数）、`zt.json`（在场台账，H24 未纳管），以及 `server-run.cmd` 里的 `HM_*` 端口与 `HM_INGEST_TOKEN` 档位。
