# 部署说明（v1.0 · 2026-09-19 首次上线 231 实测版）

> 拓扑：各设备 Agent（Python）→ 主动推送 → Server（Node，监听 9100/9101）→ 浏览器看板
> Server 一台机跑全部：REST + WS 广播 + 静态托管看板，无需独立 web server。

## 1. Server 端（一次性）

```bash
# 目录约定（示例：C:\<user>\host-monitor）
host-monitor/
├── server/   # src/ + node_modules/ 直接拷贝即可，Node ≥22（内置 node:sqlite，免 npm install）
└── client/dist/  # vite build 产物，存在则 9101 自动出看板
```

- 配置（`server/src/config/`）：
  - `probes.json` — 链路探测目标（网关 + 关键主机），**真实 IP 不入库**，参考 `probes.example.json`；
  - `thresholds.json` — 阈值；`history.json` — 持久化参数（可选，缺省见 `history.example.json`）。
- 启动：`cd server && node src/index.js`；Windows 开机自启采用计划任务 ONLOGON + `server/run.cmd`（带端口 guard，防双开 EADDRINUSE）。
- 验证：`curl http://<SERVER_IP>:9101/api/health`，浏览器开 `http://<SERVER_IP>:9101`。
- 数据：`server/data/host-monitor.db`（WAL），raw 5s 采样留 7 天、1min 均值留 30 天，自动滚动清理。

## 2. Agent 端（每台被监控设备）

前置：Python 3 + `pip install psutil websockets nvidia-ml-py`（无 N 卡可省最后一项）。

```bash
cd agent
python main.py --server ws://<SERVER_IP>:9100 --host-id <唯一小写id> --role <desktop|laptop|inference|server|db>
```

- Windows 建议做成开机自启（任务计划或 STARTUP 快捷方式）；资源占用实测 ~36MB 内存 / 0.12% CPU / ~1KB/s。
- 首帧上报硬件拓扑（PowerShell CIM，需管理员才有 CPU 温度，否则诚实为 null）。

## 3. 升级 / 重部署

1. 仓库侧 `cd client && npx vite build`；
2. 打包 server/{src,package.json,node_modules} + client/dist 为 zip（**排除 server/data 与真实 probes.json**）；
3. 覆盖解压到目标机 → 杀掉监听 9100 的旧 node → 重新启动（Agent 端会自动重连，秒级中断无感知）；
4. 已验证：重启后活跃告警从 SQLite 回放，历史曲线跨重启连续。

（Windows 目标机远程操作参考 `项目/remote-net-tool/231-deploy.mjs`，本机 ssh.exe 不可用，走 node ssh2。）
