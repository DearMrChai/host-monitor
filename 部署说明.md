# 部署说明 — Server @ 231 小主机 + 各设备 Agent

> 2026-09-19 随首次部署留档。<SERVER_IP> 为 Server 机（小主机 231）的局域网地址，实值见工作区 `00-暂存\handoff.md`（真实 IP 不进 git 的惯例）。

## 架构与端口

```
各设备 Agent (python) ──ws──> Server :9100 (Agent 接入)
浏览器/看板            ──http+ws──> Server :9101 (REST + 2s 广播 + 静态看板)
```

Server 已部署在 231 小主机（Win10 IoT LTSC，Node 22.23.2；主机名/实值 IP 见运维留档，不入 git）。

## Server（231）运维卡

| 项 | 值 |
|---|---|
| 安装目录 | `C:\Users\user\host-monitor`（server\ + client\dist\ + server-run.cmd） |
| 开机自启 | 计划任务 `HostMonitorServer`（SYSTEM 账户，开机触发，**不依赖用户登录**；崩溃 5 秒自动拉起） |
| 日志 | `C:\Users\user\host-monitor\logs\server.log`（含重启标记） |
| 手工重启 | `schtasks /end /tn HostMonitorServer` 后再 `schtasks /run /tn HostMonitorServer` |
| 升级/重部署 | 笔记本重打 zip 后执行 `node 项目\remote-net-tool\231-deploy.mjs`（幂等：停旧→解包→重启） |
| 历史库 | `host-monitor\server\data\host-monitor.db`（SQLite，raw 7 天 / 1 分钟均值 30 天自动滚动） |
| 电源 | 平衡计划，接通电源从不睡眠（实测 AC standby-idle=0） |
| 防火墙 | 实测 9100/9101 局域网直接可达，未加规则；如换机被拦：`netsh advfirewall firewall add rule name="host-monitor" dir=in action=allow protocol=TCP localport=9100-9101`（需管理员） |
| 鉴权 | 读侧（9101 看板与接口）**无鉴权**——原决策维持，勿"顺手"添加。写侧（改名/配对码/演示开关）要管理口令（S1a）。Agent 接入侧（9100）自 S2a 起按 `HM_INGEST_TOKEN` 强制：`off` 逃生阀 / `legacy` 默认（陌生 id 一律拒，已入册老 Agent 宽限并点名）/ `strict` 全部要凭据。**当前生产仍是默认 `legacy`**，翻档动作见"里程碑重部署清单" |

## Agent（每台被监控设备）

前置：Python 3 + `pip install -r agent/requirements.txt`（psutil、nvidia-ml-py、websockets；无 NVIDIA 卡时 GPU 自动降级为静态信息）。

启动命令（在本机 `agent/` 目录下运行）：

```
python main.py --server ws://<SERVER_IP>:9100 --host-id <节点ID> --role <角色>
```

- `--host-id` 缺省时自动用主机名推导（小写、空格转 `-`）；建议显式指定，与看板节点名一致。
- `--role` 可选：`db / inference / desktop / laptop / display / other`（默认 other）。
- `--interval` 推送周期秒，默认 2.0。

**S2b 之后（1.1.0）多两种更省事的用法**——参数可以不再烧进计划任务：

```
# 方式一：一份 agent.json（模板 agent.example.json，拷进 agent/ 目录即可）+ 一次性配对
python main.py --enroll <签发的 12 位配对码>
# 方式二：全部走环境变量 HM_SERVER / HM_HOST_ID / HM_INTERVAL / HM_ROLE / HM_TOKEN
python main.py --print-config      # 排障第一问：这项到底是从哪来的
```

- 配对成功会把节点密钥写进 `identity.json`（与 `host_id` 绑定），**第二次启动不需要配对码**；两份文件都已 gitignore，`identity.json` = 写权限，不要贴进群聊。
- `--pair-only` 只注册不常驻（给安装脚本先配对、再交给计划任务拉起）。两份文件默认落在**代码同目录**（不是 cwd——计划任务的 cwd 不可信），目录只读时用 `--data-dir` 指到可写位置。
- 被服务器拒绝时 Agent 打印中文原因并以退出码 **2** 结束、**不重试**——所以 142 的 systemd / 231 的计划任务会反复拉起它（每轮一行同样的中文日志），这不是"它自己会好"。看到"拒绝接入"就去接入页重新签发码；连码都没了就先 `HM_INGEST_TOKEN=off` 让它活着（隐患 H17）。

当前机群状态（2026-09-19 实态）：

| 设备 | 状态 | 常驻方式 |
|---|---|---|
| 笔记本 124 | `laptop-dev` 在线 | 隐藏窗口后台进程（本会话拉起，日志 `00-暂存\agent-laptop\agent.*.log`）；**未开机自启**，重启后需重跑上方命令 |
| 推理机 142 | `infer-142` 在线 | **systemd 服务 `host-monitor-agent`**（enable + Restart=always，开机自启）：`~/host-monitor/agent` + `~/host-monitor/venv`；日志 `journalctl -u host-monitor-agent`；升级=笔记本重跑 `node 项目\remote-net-tool\142-agent-deploy.mjs`（幂等）；拓扑采集依赖该机 sudo NOPASSWD 跑 `dmidecode -t memory`（每次重启两条 sudo 审计日志，属预期） |
| 小主机 231 | `srv-231` 在线 | **Agent embeddable 方案**：`C:\Users\user\host-monitor-agent`（Python 3.12.10 embeddable + 离线 wheels + agent 代码，`._pth` 含 `..\agent` 路径行）；计划任务 `HostMonitorAgent231`（SYSTEM onstart + 崩溃 5s 拉起）；日志 `host-monitor-agent\logs\agent.log`；升级=笔记本重打 agent231 zip 后跑 `node 项目\remote-net-tool\231-agent-deploy.mjs`（幂等）。无 GPU 场景实测该机反而有 GT 1030，NVML 正常 |


## 里程碑重部署清单（S2：把接入侧翻到 `strict`）

顺序不能反：先换 Agent、再翻 Server，否则三台机器会被自己的策略当场踢下线（且按 H17 的表现是"反复拉起 + 每轮一行拒绝日志"）。

1. Server 重部署（`231-deploy.mjs`），**保持 `HM_INGEST_TOKEN` 缺省 = `legacy`**。
2. 签发一枚配对码：看板第三个视图"接入"页（S2c，随第 1 步那次重部署一起上线）点"生成配对码"，明文只显示一次；页面上同时给出可原样粘贴的 Windows/Linux 命令。接口方式仍然可用：`POST /api/admin/enroll`，头 `x-hm-admin: <管理口令>`，body `{"ttl_minutes":60,"max_uses":3}`。逐台升级 Agent 并配对：
   - 142：`node 项目\remote-net-tool\142-agent-deploy.mjs` → `systemctl restart host-monitor-agent` 前先在命令行带 `--enroll <码>` 跑一次，确认日志出现 `Paired. Node key saved`，再把 unit 的 `ExecStart` 恢复成不带码。
   - 231：重打 agent231 zip → `231-agent-deploy.mjs`，同样先手工 `--enroll` 一次。
   - 124：本机直接 `python main.py --enroll <码>`（它本来就没开机自启）。
3. 每台跑 `python main.py --print-config`，确认 `host_id` 来自 `identity.json`、`credential= identity.json`；看板名册里对应节点应显示"已带凭据"。
4. 顶栏 `⚿ 未带凭据 N` 归零（等价于快照里 `admin.keyless_agents` 为空；接入页右侧那张卡片会逐台点名）后，Server 加 `HM_INGEST_TOKEN=strict` 并重启；再看一眼三台仍在线、异常接入台账没有新拒绝。
5. 回滚 = 把该 env 改回 `legacy`（或 `off`）重启，不需要回退代码。

清理演示道具（若要）：`DELETE FROM samples_raw WHERE host_id LIKE 'sim-%';` + `samples_1m` 同理 + `DELETE FROM nodes WHERE host_id LIKE 'sim-%';`（`S2-细化设计.md` §8）。

## Server 侧配置文件（均在 `server/src/config/`）

| 文件 | 作用 | git 状态 |
|---|---|---|
| `probes.json` | 链路探测目标（网关 + key_hosts） | **gitignored**（含真实 IP），模板见 probes.example.json |
| `thresholds.json` | 告警阈值 | 随仓库 |
| `history.json` | 持久化参数覆盖（缺省用内置默认） | gitignored，模板 history.example.json |

改 `probes.json` / `thresholds.json` 后需重启 Server（重跑 231-deploy.mjs 或 schtasks 重启）生效。
