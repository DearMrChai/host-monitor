# P3 细化设计 — Agent 星型探测 + 链路指标 + V1.5 集群拓扑视图

> ⚠️ **V1 阶段（P 系列）历史稿，2026-09-20 文档对账时加此声明。** 文中的"现状/契约/阈值数值/配置真源"是 P 批当时的形态，**不代表现役**：V2（S1~S6）已收口并上线（生产 231 = `61bf4db` = tag `v2`）。现役事实以 `README.md` + `部署说明.md`（两篇上线记录）+ `技术隐患清单.md`（H1~H29）为准。三条最容易踩的：**阈值现值一律读 `GET /api/config`**（S6 起运行时真源在 DB，`thresholds.json` 只剩"首次播种"与"回落到播种值"两个用途）；**四态色值真源在 `client/src/lib/palette.js` + `style.css`**（由 `client/scripts/check-tokens.mjs` 把关，`lib/status.js` 里没有颜色），卡片排序键是 `attentionRank` 而不是 CRIT>OFFLINE>WARN>OK；文中出现的"V1.5 / V2 / V3"指**页面层级**，与 V2、V3 两轮迭代无关。

> 依据：`产品设计方案-v1.md` §1.4（探测需求）+ §2.2（rtt/丢包阈值）+ §3.1（V1.5 线框）+ §6 评审点②③
> 基线代码：git `6ea5e0a`（P2 检查点）
> 状态：**评审稿，通过后实施，本轮未改代码**
> 日期：2026-09-18

---

## 0. P3 范围边界

| 属于 P3 | 不属于 P3 |
|---|---|
| 第 0 步：内网 ICMP/TCP 探测可行性实测（§1.4 ⚠️ 遗留项） | mesh 两两直测（P5，需探测计划下发） |
| Agent 周期探测：↔Server / ↔网关 / ↔ 关键节点 | 带宽利用率实测（产品方案 §1.4 未列一期） |
| 链路指标进状态机：`components.link` 补全 + 告警复用 P2 引擎 | 链路历史曲线（P5 持久化后） |
| HostCard 链路角标从 `—` 变为真实状态 | V2 详情页链路区块改造（P4 双层化时一并做） |
| V1.5 集群拓扑 3D 视图（新渲染器 + 总览⇄拓扑切换） | 大屏模式 |
| A↔B 连通性经 Server 拼合推算（识别单臂断链） | 探测数据独立通道（仍搭 metrics 帧顺风车） |

---

## 1. 探测架构（第 0 步：先验证，再写探测代码）

### 1.1 可行性实测（实施第一步，需你在场授权）

新增 `agent/scripts/probe-selftest.py`（一次性诊断脚本，非交付件）：从你的机器向**网关、Server 主机、142** 分别做 ICMP（`ping -c/-n 1`）与 TCP 连接（22/80/9100）测试，输出「哪些手段在你的内网可用」清单。**先跑这个再定探测降级链的实际生效范围**——这是 §1.4 ⚠️ 评审遗留项的闭环。

### 1.2 三种探测手段（Agent 内统一实现，不需管理员权限、不需防火墙例外）

| 手段 | 适用链路 | 原理 | 精度/限制 |
|---|---|---|---|
| **WS ping/pong**（首选） | ↔Server | 复用已有 9100 长连接，`await ws.ping()` 测应用层往返 | 零防火墙依赖、跨平台；**最重要的链路完全不碰 ICMP** |
| ICMP | ↔网关、↔关键节点 | 子进程 `ping -c 1 -W 1`（Linux）/ `ping -n 1 -w 1000`（Windows），解析 TTL/时间 | 免管理员；被墙或无权限时降级 |
| TCP 连接探测 | ICMP 失败时的降级 | `socket.create_connection((host, port), timeout=1)` 测握手耗时 | 需要目标有开放端口（见 1.3 分类） |

### 1.3 三态结果分类（防误报核心规则）

每个探测目标每轮返回三类之一：

- **有数据**：rtt 值 + 窗口丢包率 → 正常进状态机/告警；
- **确认不通**：ICMP 通但目标拒绝/超时、或 TCP 端口 closed-refused 且其它手段证实主机不可达 → `loss_pct=100` → 必然 CRIT（链路断）；
- **手段不可判定**（ICMP 被墙 且 无可用 TCP 端口）→ **该目标本轮无数据，不进状态机、不告警**，UI 显示灰色"—"。宁可少报，不能因探测手段受限而误报断链。

### 1.4 探测计划：Server 统一下发（Agent 无需逐机配参）

- 新增 `server/src/config/probes.json`：网关 IP + 关键节点（id/host/TCP 降级端口，如 142 用 22）。
- Agent 注册后，Server 在 `registered` 应答里带 `probe_plan`；Agent 据此探测。**改探测对象只改 Server 一个文件并重启**，各 Agent 自动跟随。
- 无 probe_plan（旧 Agent / 未配置）→ 只测 ↔Server（WS 手段永远可用）。
- **一期实际配置（2026-09-18 用户确认）**：机群 = 笔记本（Agent）、推理机（Agent，vLLM 现状不变）、小主机（Server + Agent）三台；真实目标 IP 记录在 `server/src/config/probes.json`（含局域网地址，已 .gitignore，不入库），仓库随包 `probes.example.json` 占位。结构：

```jsonc
{
  "probe_interval_seconds": 5,
  "window_size": 10,
  "gateway": { "host": "192.168.1.1" },
  "key_hosts": [
    { "id": "infer-N", "name": "infer-N", "host": "192.168.1.N", "tcp_port": 22 }
  ]
}
```

- 笔记本**不作为探测目标**（随时关机/带离属正常，避免 loss 100% 误报风暴）；ZeroTier 链路一期不探测（用户确认，后续要加按 key_hosts 增条目即可）。

### 1.5 节拍与窗口

- 探测周期默认 **5s**（可配 5–10s），每目标每周期 1 发；
- 丢包率 = 最近 **10 发**滑动窗口（约 50s 观测跨度）：`loss_pct = 失败数/10*100`；
- rtt 展示值 = 窗口内成功探测的中位数（抗单发毛刺）；
- 结果随每个 metrics 帧（2s）上报——探测慢帧不拖指标快帧。

### 1.6 A↔B 连通性（一期：经 Server 拼合）

- `rtt(A↔B) ≈ rtt(A→Server) + rtt(B→Server)`（推算值，标注"推算"语义，不做单端 RTT 的精确承诺）；
- **单臂断链识别**：A↔B 不通但 A、B 各自 ↔Server 正常 → 说明公共路径（Server 侧/交换层）无恙；某节点 ↔Server 或 ↔网关 100% 丢包 → 该"臂"故障，拓扑视图直接着红色/灰色标出；
- 真实两两直测留 P5 mesh（需要探测计划错峰下发，避免风暴）。

---

## 2. 数据契约（增量）

### 2.1 Agent → Server：metrics 帧追加 `probes`

```jsonc
"probes": {
  "interval_s": 5, "window": 10,
  "results": [
    { "target": "server",  "name": "Server",  "kind": "ws",   "rtt_ms": 0.8, "loss_pct": 0 },
    { "target": "gateway", "name": "网关",     "kind": "icmp", "rtt_ms": 1.4, "loss_pct": 0 },
    { "target": "infer-142", "name": "infer-142", "kind": "icmp", "rtt_ms": null, "loss_pct": 100 },
    // 手段不可判定的目标直接缺席，不占位
  ]
}
```

### 2.2 快照：`components.link` 补全（P1 预留位）

```jsonc
"link": {
  "level": "CRIT",                    // 各目标最差档；无数据时 null
  "targets": [
    { "target": "server", "name": "Server", "kind": "ws",
      "rtt_ms": 0.8, "loss_pct": 0, "level": "OK" },
    { "target": "infer-142", "name": "infer-142", "kind": "icmp",
      "rtt_ms": null, "loss_pct": 100, "level": "CRIT" }
  ]
}
```

- `evaluateHost`：每目标 `level = worst(levelFor(rtt_ms, thresholds.rtt_ms), levelFor(loss_pct, thresholds.packet_loss))`；两阈值 config 已预留，**零新增阈值**。
- **节点综合档 = 设备组件与 link 的最差值**（产品方案 §2.1"自下而上归并"含链路）——即链路 CRIT 会把卡片边框打红，但角标（§3.1）保证"设备 vs 链路"问题仍可区分。
- `reasons` 追加 `metric: 'rtt_ms' | 'packet_loss'，source: <target>` → 告警身份 `host_id|rtt_ms|server` 等，**P2 引擎零改动复用**（归并/防抖/升档/OFFLINE 特例全继承）。
- 节点 OFFLINE 后其 link 键不再被观测 → 走引擎 missing-key 路径，3 拍内自动 resolved（合理：人已失联，横幅只留"失联"一条，不重复报链路）。

### 2.3 聚合：cluster 增 `link` 摘要

`cluster.link = { worst_level, degraded_count }`（链路异常节点数），供拓扑视图顶部一行话。

---

## 3. 前端

### 3.1 HostCard 链路角标激活（占位兑现）

现 `{{ linkLevel || '—' }}` 改为真实角标：`链路 ● ↘0.8ms`（● 取 link.level 色；无数据灰—）；tooltip 列出各 target 明细（`Server 0.8ms 0% · infer-142 断`）。卡片边框色继续吃防抖后 `status.level`（含链路，§2.2），角标负责回答三问之"是设备问题还是链路问题"。

### 3.2 V1.5 集群拓扑 3D 视图（新渲染器）

- 新增 `client/src/three/ClusterTopologyRenderer.js`，**与主板 TopologyRenderer 完全独立**（不共享场景，互不影响，控制风险）。
- 布局：奶白场景；**Server 枢纽块居中**，网关块挂枢纽侧旁；各节点低面数方块沿圆环布局（按 role 权重排序，位置稳定不跳）。
- 边：节点→枢纽、节点→网关两条星型连线。着色：绿（<WARN）→黄（WARN）→红（CRIT）→**灰色虚线=无数据/不可判定**；线的亮度随 rtt 恶化提高。
- 动效：连线上光点脉冲，**速率 ∝ 1/rtt**（延迟越高脉冲越慢，视觉直读），节点失联时两臂变灰并停止脉冲；
- 节点块顶色 = 节点 `status.level`（设备档），底座描边色 = `link.level`（链路档）——**一台一眼分设备/链路**。
- 每节点 CSS2D 标签：`主机名 · ↘rtt` ；异常节点自动面向相机（沿用现有标签方案）。
- A↔B：不画两两连线（一期是推算值，画实线会误导）；单臂故障通过该节点两臂颜色直接可见。

### 3.3 入口与路由

- 沿用无 router 壳：`route.name` 增加 `'topology'`；ClusterBar 右侧加「总览 ⇄ 拓扑」切换按钮，**顶部横幅（健康灯/告警/静音）两视图共享**，主体区互换；
- 点击拓扑节点块 → 现有 V2 详情；返回回拓扑；
- OverviewView 卡片网格不动。

### 3.4 文案

`client/src/lib/status.js`：`METRIC_LABELS` 补 `rtt_ms:'链路延迟'`、`packet_loss:'丢包率'`；`formatReason` 输出如「↔ infer-142 延迟 62ms（阈值 50）」。

---

## 4. 自测方案（mock 扩展）

mock-agent.mjs 全部节点在 metrics 帧里伪造 `probes`（不经真实网络），新增两节点：

| 节点 | 行为 | 验证点 |
|---|---|---|
| mock-lag | rtt 每 ~12s 在 3→60ms 方波穿越 WARN/CRIT | 6s 窗口后才告警（防抖）；拓扑边绿→黄→红渐变；角标同步 |
| mock-gwdown | 网关 loss 100%、server loss 0% | 单臂红+横幅 CRIT，另一臂仍绿（区分性）；杀 mock 后失联→链路告警 3 拍内消失、只剩失联告警 |

真机侧：验收时在你机器上跑一次 step 0 脚本 + 一个真实 Agent，现场看 ↔Server WS 探测与 ICMP 网关的真实数据进拓扑。

**你的验收清单（约 15–20 分钟）**：
- [ ] step 0 脚本输出你的内网可用手段清单（ICMP/TCP 各目标一行）
- [ ] 真实 Agent 上线 → HostCard 角标显示真实 rtt，拓扑视图出现该节点、绿线脉冲
- [ ] mock-lag 触发链路告警：延迟 6s 窗口、横幅+声音、拓扑边变色
- [ ] mock-gwdown：只红一臂，另一臂不受影响
- [ ] 总览⇄拓扑切换流畅，点节点钻取回跳正常

---

## 5. 工作量（P3 设计通过后）

| 步骤 | 内容 | 预估 |
|---|---|---|
| 0 | probe-selftest.py + 现场跑通验证（**你在场**） | ~20 分钟 |
| 1 | Agent 探测模块（ws/icmp/tcp 三手段 + 窗口 + probe_plan 接收） | ~60 分钟 |
| 2 | Server：probes.json + registered 下发 + status.link 评估 + mock 扩展 | ~40 分钟 |
| 3 | 前端：HostCard 角标 + ClusterTopologyRenderer + 视图切换 | ~90 分钟 |
| 4 | 全链路自测 + 联调验收（**你在场**） | ~20 分钟 |

---

## 6. 评审点（请逐条确认或否决）

1. **↔Server 链路用 WS ping/pong 应用层测量**：最重要的链路完全不依赖 ICMP/TCP 端口，防火墙零风险；网关/关键节点才走 ICMP→TCP 降级链。
2. **探测计划由 Server 配置下发**（probes.json 单点管理），Agent 端不加逐机参数；未配置时只测 ↔Server。
3. **"手段不可判定"三类结果分类**：探测受限的目标显示灰色无数据，**绝不因手段受限而误报断链**。
4. **链路 CRIT 会把节点边框打红**（§2.1 归并含链路），同时角标/拓扑双底座色保留"设备 vs 链路"区分——如果你希望链路问题**不**染红整卡（只黄角标），在此否决。
5. **link 告警直接复用 P2 引擎零改动**（rtt_ms/packet_loss 阈值已在 config），OFFLINE 后链路告警 3 拍自动消解。
6. **A↔B 一期只显示推算语义、不画两两连线**，单臂断链靠两臂颜色识别；真实测留 P5。
7. **拓扑视图入口 = 顶部"总览⇄拓扑"切换**（同页双密度思路的 3D 版），不做第三个平级页面。
