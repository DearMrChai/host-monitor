# V2 迭代讨论纪要 — 2026-09-19

> 角色：后端架构 + 产品技术顾问评审。参与：用户（产品所有者）+ 会话。
> 用途：**决策升版记录**。下一个会话若与本文件冲突，以本文件为准；若要再推翻，先改本文件。
> 基线：main = `2ed74c8`（生产版停在此）。隐患明细见 `技术隐患清单.md`（H1~H11）。
> 本文件不含真实 IP/凭据，可随仓库提交。

## 0. 使用实态（本次讨论的新增事实，后续评估的前提）

- 上线第 1 天，**尚未深度使用**；231 是低功耗小主机，看板是它的附属功能（主业=影音服务，自用、低频）。
- 已产生一次真实决策：看到 142 显存占用高 → 推断朋友 CUDA 内核改造完成 → 去试模型。**→ 产品的真实定位是"变化检测器"，不是"实时仪表"**（纪要 §2.3）。
- 双重职责被用户明确确认：**监控 + 次卧（集群所在房间）洞洞板副屏的氛围装饰**，且"效果不要打折"。
- 手机（安卓）访问已实际发生；局域网覆盖不全，但 **ZeroTier 覆盖很全（约 10 台：安卓 / Ubuntu / Windows）**。
- 机群含**临时机**（办公室 35B 推理机，有别的维护者）与**他人机器**（朋友的台式/笔记本，同时是集群的使用方）。
- 用户是主操作者，朋友偶尔看。**→ v1"3~5 台静态自有机器"的隐含假设作废。**

## 1. 决策变更（相对 `产品设计方案-v1.md`）

| # | v1 决策 | 本次结论 | 理由 |
|---|---|---|---|
| 1 | **纯推送模型**（每设备一 Agent），Server 不持凭据、不 SSH 拉取 | **升版为双通道**：自有机器 = agent 推送；他人/临时机器 = **observed 型，Server 侧 SSH 只读拉取** | 用户明确不愿在他人设备留常驻物（维护者会疑惑/删除），而 SSH 是对方已授权、走后不留痕迹的通道。**决策 1 不被推翻，而是在旁边开一条受限的第二通道** |
| 3 | 阈值全局统一（实态校准） | **升版为 per-node / per-role 覆盖**（H7） | 异构机群里"编译满载"是正常工作状态而非异常；全局阈值必然制造噪声 → 用户开始忽略横幅 → 产品失效 |
| 6 | Server 无鉴权，看板全内网可读 | **升版为"读无鉴权 / 写有口令"**（H8/H10） | 信任边界已从局域网移到 ZeroTier（含他人设备）。读侧保持零摩擦不变；写侧一旦出现（加节点/改阈值/录凭据）必须有口令，否则任何看板访问者可删节点 |
| 4 | 读不到显示 null，不造假 | **保留并扩展**：observed 型采集失败 = **UNKNOWN**，不进四态、不告警、不进健康度（复用 P3 的 IND 语义） | 朋友关机回家 ≠ 集群故障。这是 observed 通道能被信任的前提 |
| 2 | 开发机不做探测目标 | 保留，并扩展：**临时机/他人机不做告警型目标，只做在场（presence）记录** | 同 H1 治理方向 |

## 2. 新增架构决议

**2.1 二期主线 = 机群档案层（`nodes` 表）**
现状是"实时连接的集合"（`store.hosts` 只增不删）。升级为有档案的机群：`host_id / 显示名 / role / owner 归属 / site 网络位置 / class(persistent·ephemeral·retired) / expected / fingerprint / agent_version / 凭据引用 / thresholds_override / mute`。一张表同时解锁 H1/H2/H4/H6/H7/H8，是本轮性价比最高的改造。

**2.2 指纹约束（换卡事件逼出的硬规则）**
`fingerprint` **只能取不随硬件插拔变化的来源**（machine-id / BIOS UUID / installer ID），**禁止用硬件清单派生**。否则用户给 231 换 GT 1030 GDDR5 那天，srv-231 会变成陌生新节点、旧记录沦为幽灵。

**2.3 变化事件流优先于静态仪表**
从已有 `samples_1m` 派生"沿/事件"（跨阈值、上下线、占用区间），总览加"最近 24 小时"。零新采集面。理由见 §0 的那次真实决策。

**2.4 观测层三小步（可分别砍）**
- **presence（先行）**：Server 读本机 ZeroTier 本地 API（`127.0.0.1:9993` + authtoken）→ 当前 overlay 可达成员 = 在场事实。**零安装、零凭据、跨网可用**。不用 Central API（要云端 token + 外网依赖）。
- **observed（次之）**：SSH 拉取，**一期只支持有 sshd 的 Linux 目标**（Windows 第三方机诚实裁剪）。能力=`uname/uptime/free/df/ss/nvidia-smi --query-gpu=csv`，10~30s 一次。失败=UNKNOWN。
- **access（最后，单独确认后才做）**：只在**用户自有机器**上采入站连接（`ss -tn` / `Get-NetTCPConnection`），源 IP 归到 owner → "谁的机器连了我哪台、多久"。**不在别人机器上装东西**，符合用户原则。用户已确认这一步需要他单独点头。
- **硬约束**：三者都是独立台账视图，**不得共享四态归并/告警引擎**，命名上不叫"节点"（防 H1 重演）。

**2.5 凭据保管（用户拍板：存口令，不再要求对方配合加公钥）**
接受理由：账号密码对方已主动给出，再要求配合添加公钥社交成本更高。随之而来的风险由以下硬约束兜底：
- 口令**不进 API 响应**（前端只显示"凭据已配置 · 上次采集时间/结果"）、**不进日志与异常栈**、**不进 git / 分享包 / 备份 zip**；
- 库内做混淆而非裸明文，**但明确声明这不是加密**（对称密钥同机存放无意义）——真实边界：可读 231 磁盘者即得所有已登记机器的登录凭据，用户知情接受；
- observed 执行**只读命令白名单**，禁止任意命令拼接。

**2.6 配置面规则**
- **单一真源**：DB 为运行时唯一真源；`thresholds.json` / `probes.json` 降级为出厂默认 + 首次种子（DB 空时导入，之后不再被读）。
- **热加载**：UI 改完下一帧生效，不重启 Server（顺带消掉"改配置=重启 231"的运维成本）。
- **UI 可改**：节点档案、observed 凭据与"测试连接"、按 site 的探测目标、per-node 阈值、静默、配对 token。
- **保持文件**：`history.json` 采样/保留参数、端口、DB 路径、管理口令本身。
- **永不做**：UI 改代码行为、远程重启 Agent、插件式扩展。

**2.7 加机：自有机器走配对，他人机器走 observed**
`D0'` = Agent 读 `agent.json` + 看板"添加主机"生成短时效 enroll token + 等待接入态 + Windows 自安装物（embeddable python + 离线 wheels + 计划任务，231 已趟通）/ Linux 一行脚本；Agent 自己回连，卡片当场浮现。
`SSH 全自动装 Agent` **降级为不做**：自有机器用户已有更强的部署通道（remote-net-tool / systemd / 计划任务 / wmiexec），他人机器用户不想留物——两个场景都不需要它。用户原始设想里真正有价值的那部分是**"手输地址+口令+测试连接"这个交互**，它落在 observed 通道的设置界面上，完全成立。

**2.8 UI 双轨，视觉不再后置**
- 视觉契约先行一页：四态色、密度分级、动效预算、**"任何视觉元素必须承载一个状态维度；不承载状态的只允许出现在 kiosk 层"**（这是 v0.4"画得像不像"纠偏的制度化）。
- **kiosk 值守屏提前到与功能同期**：它是使用率入口——用户尚未深度使用，而"愿意天天看见"的界面才会养成瞟一眼的习惯，习惯才喂养 L1 的信息密度。
- 3D 唯一保留位置 = kiosk（一个状态体 + 在场可视化），**不做主板、不做零件、不做二次钻取**；范围锁死以保交付。
- L3 主板 3D 下线。几何连续缩放、抽象硬件 3D 替换方案：**不做**。
- 星型拓扑：数据面上正确（A↔B 为推算，画 mesh 即把推算伪装成实测，违反决策 4），叙事面上信息量不足 → **从"一层"降级**为总览里的链路 inset 或按用途/机位分区的空间视图。
- kiosk 性能：用户计划升级 GT 1030 GDDR5（带宽约为 DDR3 版 2×），效果预算放宽，但**自动降级护栏保留**（连续 30s < 20fps → 切纯 CSS 动效）；验收方式=用本系统对比 kiosk 开/关时 srv-231 的 CPU/GPU 占用（监控验证监控自身）。

## 3. 阶段划分（**已被 §5 的"成型冲刺排序"取代，保留作参考**）

| 轨 | 阶段 | 内容 |
|---|---|---|
| 功能 | **V2-a** | 机群档案层（`nodes` 表 + class/expected/owner/site/凭据引用/指纹策略）+ ingest token + agent.json + 版本上报 + per-node 阈值 + 按 site 探测计划 + 手动静默 + 单一真源与热加载 + **写侧口令** |
| 功能 | **V2-b** | 观测层三小步：presence（ZT）→ observed（SSH/Linux）→ access（自有机器入站，待单独确认） |
| 功能 | **V2-c** | 自有机器加机配对（token + 一行安装 / 自安装物） |
| 视觉 | **V2-d** | 视觉契约一页 + L1 信息密度（sparkline / delta / 角色编码 / 异常置顶 / 24h 事件流） |
| 视觉 | **V2-e** | kiosk 值守屏最小版（洞洞板那块屏点亮，`?kiosk` 参数，零额外部署） |
| 收尾 | — | L3 主板下线、代码/注释巡检、H3 探测并发化 |
| 不做 | — | mesh 互探、V4 多机对比、看板登录、几何连续缩放、抽象硬件 3D、SSH 全自动装 Agent、出站告警（H5 用户知情边界） |

**迭代节奏不变**（2026-09-19 用户定）：迭代期只本机开发自测，不重部署 231；测试通过 + 稳定 + 到里程碑才重新部署；push 与部署均需用户单独指示。

## 4. 仍开放

- `access` 台账（谁访问了我的设备）——用户已同意"按建议来"（presence 先行），实施前仍需他单独点头一次。
- 副屏最终由哪台设备驱动（HDMI 直连 231 vs 任意设备开 `?kiosk`）。影响 V2-e 的动效规模，不影响契约。
- v1 §2.4"仅页面内告警"维持；出站通道待机群稳定后重提。

## 5. 战略更新（同日稍后，用户主动提出）→ 成型冲刺排序

**5.1 目标改变。** 用户原话要点："我要推动这个个人项目迅速成型；需求仅仅来自我自己就很局限；我要给朋友展示一个**看起来是成品**的东西，也好向外收集需求、筛选对我有帮助的内容。"
→ 项目从"自用工具"升为"**自用 + 对外展示的需求收集装置**"。这不改变架构方向（档案层、观测层、双轨 UI 全部保留），但**显著改变排序**：演示时最掉价的东西优先修，看不见摸不着的基建可以让位。

**5.2 由"口令治理延后"直接推出的硬约束：observed（SSH 拉取）通道不得排在口令治理之前实施。**
理由：observed 的唯一前提就是 Server 保管别人的口令。用户已把凭据治理定为"面板成型后必做重点"，所以 observed 必须整体后移到那道闸之后。它不是被砍，是被**正确地排到了自己的前置依赖后面**。
→ 连带影响：办公室 35B 那台在成型期**只能以 presence（ZT 在场）形式出现**，不采指标。这是可接受的成型期形态。

**5.3 "看起来是成品"的真实构成（我的判断，用于选题）**
成品感 ≠ 功能多。按掉价程度排序：① **故障感**（幽灵节点灰灯、空态没引导、加载闪烁、console 报错、单位/文案不一致）② **数据单薄**（只有 3 台）③ **交互断层**（加机器要改文件）④ 视觉朴素。
所以成型期投入：
- **最小名册 + 健康度语义修正**（H1 的 1/5 版本：`nodes` 表先只存 class/expected/display，看板上一个"忽略/退役"按钮，不做完整设置界面）→ 消灭故障感；
- **加机配对页 D0' 最小版** → 这是全场最有"产品感"的一屏，且它是**收集需求的外设**：朋友自己粘一行命令接进来 = 他主动告诉你他要什么；
- **演示机群 = 已有的 `server/scripts/mock-agent.mjs`，但节点必须显式带 `DEMO` 标记**（诚实呈现的红线：可以借 mock 撑起密度，不可以伪装成真实设备——这是决策 4 的延伸，也保住对外讲故事时的信用）；
- **L1 信息密度**（sparkline / delta / 角色编码 / 24h 事件流）→ 专业性；
- **kiosk 值守屏** → 惊艳感，也是他自己愿意天天开的原因。

**5.4 成型冲刺排序（当前生效的版本）**

| 序 | 内容 | 判据 |
|---|---|---|
| S1 | 最小名册 + 健康度/在线率语义修正 + 节点忽略/退役 + 手动静默 + ingest token | 拔掉临时机集群仍绿；演示时不出现幽灵节点 |
| S2 | D0' 配对加机（agent.json + token + 等待接入 + 一行安装）+ DEMO 标记的演示机群 | 一台新机 ≤3 分钟出现在看板；截图能填满一屏 |
| S3 | L1 信息密度 + 24h 事件流 | 朋友问"这能看出什么"，你答得出三件事 |
| S4 | 视觉契约一页 + kiosk 最小版 | 洞洞板那块屏点亮且稳 |
| S5 | **闸口：凭据/口令治理 + 写侧口令完整化 + presence 台账** | 过关后才能启动 observed |
| S6 | observed（SSH/Linux）→ access 台账 → 完整设置界面 + 单一真源/热加载 | — |
| 收尾 | L3 主板下线、代码巡检、H3 | — |

**5.5 演示形态待定（影响 H11 的时间点）**
- 形态 A：朋友在 ZeroTier 内访问你的 231 → 暴露面仍是可信小圈子，口令治理按计划留在 S5。
- 形态 B：做一个公网可访问的 demo 实例 → **H11 与看板鉴权立刻从 S5 提前到 B 之前**（公网 + 无鉴权 + 持有他人口令 = 不可接受组合）。若走 B，建议 demo 实例与家集群物理隔离（独立机器/独立容器，只跑 mock，不持任何真实凭据）。
用户尚未选择；若未来提及"给别人开个链接看"，先回到这一条。
