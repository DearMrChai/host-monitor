{
  "name": "host-monitor-client",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "LAN Host Monitor - 3D Digital Twin Dashboard (Vue 3 + Three.js)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "node scripts/check-tokens.mjs && node scripts/selftest-sound.mjs && node scripts/selftest-fps.mjs && node scripts/selftest-silhouette.mjs && node scripts/selftest-nav.mjs && node scripts/selftest-skin.mjs",
    "check:tokens": "node scripts/check-tokens.mjs",
    "check:sound": "node scripts/selftest-sound.mjs",
    "check:fps": "node scripts/selftest-fps.mjs",
    "check:silhouette": "node scripts/selftest-silhouette.mjs",
    "check:nav": "node scripts/selftest-nav.mjs",
    "check:skin": "node scripts/selftest-skin.mjs"
  },
  "dependencies": {
    "three": "^0.170.0",
    "vue": "^3.5.12"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.1.4",
    "vite": "^5.4.8"
  }
}
