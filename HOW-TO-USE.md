# 怎么用这套界面（三条入口）

> 这份文件回答一个很具体的问题：**我到底要怎么打开它，是不是每次都得手动输 IP 和端口。**
> 短答：生产起来之后**只有一个地址**，而且它不需要你启动任何开发进程——静态页面由 Server 自己伺服。
> 文中 `<服务器地址>` 指跑 Server 那台机器在局域网里的地址，端口固定 `9101`。

## 三条入口，各管一件事

| 你想干什么 | 用哪条 | 需要配几次 |
|---|---|---|
| 客厅那块屏常亮当"墙" | `http://<服务器地址>:9101/?kiosk` | **一次**：在那台机器上把它设成固定标签页（或开机自启的浏览器快捷方式） |
| 自己坐下来查某一台机器 | `http://<服务器地址>:9101/` | **一次**：存书签 |
| 抬一眼看有没有事 | 桌面那个全局浮动小窗（`dsh-plugin-host-monitor`） | 已经在了，不用配 |

墙那一面是**只看不点**的：它是给"隔着两米瞟一眼"用的。任何需要点的动作，都在第二条那条路上做。

## 第二条那条路怎么走（桌面/笔记本）

1. 打开书签，落在**「总览」**——那是一张台账表，机器最多、信息最密，适合查数，不适合盯状态。
2. 点顶栏的**「拓扑」**那一格 → 进**陈列架**：一排机体，每台一块牌，形状＝形态、亮暗＝负载、颜色＝健康状态。想快速判断"这堆机器现在怎么样"就看这一屏。
3. 在陈列架上**点任意一台机体** → 进**单机详情**（电路板视角，CPU/内存/GPU/盘/网卡逐项展开）。
4. 点左上那颗 **「← 拓扑」** 回陈列架。回程只有这一颗按钮，没有键盘快捷键——这是有意的：墙上的机器永远不需要键盘。

## 第一次用要先做的两件事（都在「设置」里，都要管理口令）

墙和陈列架上的**名字与形状默认是猜的**，猜得对不对取决于机器自己报上来什么。要让它们稳定写成你要的样子，各有一格：

1. **显示名**：名册里没填显示名时，界面上显示的是 `host_id`，而牌面有 8 字的长度预算，英文字长的 id 会被截成 `laptop-…` 这种。填一个中文名（四到六个字）就不会被截，隔两米也认得出是哪台。
2. **形态档**（决定那台机器画成机架／笔记本／微型哪种剪影）：默认按机名和机号猜。**猜出来的可以错**，而且错了屏上不会报错——所以这一格是"你说过才算数"：填 `rack` / `laptop` / `mini` 三个值之一，就照你填的画，猜测不再参与；留空或填我们不认识的值，才回到猜测。

这两格都是**一次填、长期有效**的，属于"配一次"而不是"每天用"的那类动作。

## 万一屏上什么都不显示

- 先确认 Server 在跑：浏览器直接开 `http://<服务器地址>:9101/api/hosts`，能看到一段 JSON 就是活的。
- 看到一排 `OFFLINE`：多半是 Agent 没在跑或网络不通，**不是界面坏了**。
- 陈列架那排机体不动（3D 起不来，常见于低功耗机器或远程桌面）：改用降级地址 `http://<服务器地址>:9101/?kiosk&flat=1`——同一批机器、同一套牌面，用普通网页元素画，不吃显卡。
- 设置、阈值、接入这些页面需要管理口令；只看不改不用输。

## 界面上不该有的预期

- 墙上（带 `?kiosk`）点了没反应——**这是设计**，不是坏了。
- 一台机器温度爆表但很闲，它在陈列架上会比一台忙碌但正常的机器**更暗**：**亮度只代表负载，颜色才代表健康**，两者是分开的两条通道。
- 数据读不到时显示 `null`/空，不显示 0：**没有读数不等于零负载**。
