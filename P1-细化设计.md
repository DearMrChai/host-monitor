# P1 细化设计 — 指标补齐 + V1 集群总览 2D 视图

> ⚠️ **V1 阶段（P 系列）历史稿，2026-09-20 文档对账时加此声明。** 文中的"现状/契约/阈值数值/配置真源"是 P 批当时的形态，**不代表现役**：V2（S1~S6）已收口并上线（生产 231 = `61bf4db` = tag `v2`）。现役事实以 `README.md` + `部署说明.md`（两篇上线记录）+ `技术隐患清单.md`（H1~H29）为准。三条最容易踩的：**阈值现值一律读 `GET /api/config`**（S6 起运行时真源在 DB，`thresholds.json` 只剩"首次播种"与"回落到播种值"两个用途）；**四态色值真源在 `client/src/lib/palette.js` + `style.css`**（由 `client/scripts/check-tokens.mjs` 把关，`lib/status.js` 里没有颜色），卡片排序键是 `attentionRank` 而不是 CRIT>OFFLINE>WARN>OK；文中出现的"V1.5 / V2 / V3"指**页面层级**，与 V2、V3 两轮迭代无关。

> 依据：`产品设计方案-v1.md` §5 P1（指标补齐：磁盘采集、四态派生 + V1 集群总览 2D 视图）
> 基线代码：git `19e622d`（v0.4 检查点）
> 状态：**评审稿，通过后实施，本轮未改代码**
> 日期：2026-09-18

---

## 0. P1 范围边界（先说清楚"不做什么"）

| 属于 P1 | 不属于 P1（归属） |
|---|---|
| 磁盘空间采集、开机时长/负载采集 | 链路探测、RTT/丢包（P3） |
| 节点四态派生 + 集群健康等级（Server 端） | 告警防抖窗口、告警生命周期、声音、静音开关（P2） |
| 角色标签（Agent 注册配置） | V1.5 集群拓扑 3D 视图（P3） |
| V1 总览页（横幅/卡片网格/侧栏占位）+ 钻取通道 | V2 双层化改造（P4） |
| 阈值 JSON 配置加载（仅用于状态派生） | 告警规则引擎、历史缓冲（P2） |

**已知限制（明示）**：P1 的四态为**逐帧瞬时评定**，无防抖——采样毛刺会让卡片颜色闪一下，P2 引入持续窗口后消除。这是路线顺序决定的，评审时确认接受。

---

## 1. 数据契约（三层各自改什么）

### 1.1 Agent 端

**新增 `agent/collectors/disk.py`**（psutil，跨平台）：

```jsonc
// metrics frame 新增字段 "disk"
{
  "partitions": [
    { "mountpoint": "C:", "fstype": "NTFS", "total_gb": 476.9, "used_gb": 412.3, "percent": 86.5 }
  ],
  "worst_percent": 86.5,        // 所有固定分区占用最大值 → 状态派生用
  "total_gb": 640.0, "used_gb": 500.1
}
```
- 只统计**固定磁盘**（Windows 过滤网络盘/可移动盘；Linux 排除 tmpfs/squashfs/overlay 等伪分区，用 `psutil.STATIC_PARTITIONS`）；
- 每分区保留原始值，聚合值由 Server 派生（Agent 不做业务判断，维持"Agent 只采集"原则）。
- **磁盘 IOPS 不在 P1**：属 L3 钻取明细（V3 抽屉随 P4 才有消费者），先不采集避免无主数据。

**新增 `agent/collectors/system.py`**：

```jsonc
// frame 新增字段 "system"
{ "uptime_seconds": 345600, "load_avg": [1.2, 0.9, 0.8] }   // Windows: load_avg = null
```

**`main.py` 两处改动**：
- CLI 新增 `--role`，取值 `db | inference | desktop | laptop | display | other`（默认 `other`），进 `register` 消息（不进 metrics 帧）；
- `collect_all()` 合入 `disk`、`system` 两字段。

**兼容性**：老 Agent（无 disk/role）连新 Server 不报错——Server 侧所有派生对缺失字段容忍（§1.2）。

### 1.2 Server 端

**新增 `server/src/config/thresholds.json`**（§2.2 阈值基线原样落地，可手改、改后重启生效——热加载不做，YAGNI）：

```json
{
  "cpu_usage":  { "warn": 80,  "crit": 95 },
  "cpu_temp":   { "warn": 75,  "crit": 85 },
  "mem":        { "warn": 85,  "crit": 95 },
  "disk":       { "warn": 80,  "crit": 90 },
  "gpu_temp":   { "warn": 80,  "crit": 93 },
  "rtt_ms":     { "warn": 5,   "crit": 50 },
  "packet_loss":{ "warn": 1,   "crit": 5 },
  "offline_seconds": 15
}
```
（rtt/丢包两项 P1 无数据源，占位存在，P3 直接消费。）

**新增 `server/src/status.js`** —— 四态派生单一职责模块：

```
evaluateHost(host)  →  status 对象（挂到 host.status）
evaluateCluster(hosts) → cluster 对象（挂到 snapshot.cluster）
```

每节点输出结构（前端与 P2 告警引擎的共同数据源，**一次定契约**）：

```jsonc
"status": {
  "level": "WARN",                    // 最差档归并：组件 → 节点
  "components": {
    "cpu":  { "level": "OK",   "value": 71 },
    "mem":  { "level": "OK",   "value": 61 },
    "disk": { "level": "WARN", "value": 86.5 },
    "gpu":  [ { "id": "gpu0", "level": "OK",   "value": 78 },
              { "id": "gpu1", "level": "WARN", "value": 89 } ],
    "link": { "level": null }          // P1 恒 null → 前端显示"链路 —"角标，P3 填充
  },
  "reasons": [                         // 人类可读触发项，V1 侧栏与 P2 告警列表复用
    { "metric": "gpu_temp", "source": "gpu1", "value": 89, "threshold": 80, "level": "WARN" },
    { "metric": "disk", "source": "C:", "value": 86.5, "threshold": 80, "level": "WARN" }
  ]
}
```

- OFFLINE 判定沿用现有 `store.checkTimeouts()`（>15s），status.level 直接置 `OFFLINE`，不参与阈值比较；
- 温度/数值为 `null` 的组件（笔记本 GPU 省电态）跳过，不判级；
- 集群聚合负载：`{ cpu: {avg, peak}, mem: {avg, peak}, gpu: {avg, peak} }`，仅统计在线节点，null 安全。

snapshot 顶层新增：

```jsonc
"cluster": {
  "health": "WARN",                    // 所有在线节点 + OFFLINE 节点的最差档
  "online": 5, "total": 6,
  "aggregate": { "cpu": {"avg": 43, "peak": 91}, "mem": { "...": 0 }, "gpu": { "...": 0 } }
}
```

**`store.js`**：register 时存 `role`（缺省 `other`）；`getSnapshot()` 调 status.js 注入 `host.status` 与 `cluster`。REST `/api/hosts` 同构返回。

### 1.3 Client 端 —— V1 集群总览

**导航方案：不引入 vue-router**。App.vue 收敛为壳，持 `view = 'overview' | { name:'detail', hostId }` 状态机：
- 现有 3D 单主机视图整体抽为 `views/DetailView.vue`（**只搬家不改内部逻辑**——内部双层化是 P4 的事；顺手解决 `hosts[0]` 硬编码 → 按 hostId 取节点）；
- 点击卡片 → detail；detail 顶部返回按钮 → overview。**默认落地页 = V1**（值守场景）。

新增文件：

| 文件 | 职责 |
|---|---|
| `views/OverviewView.vue` | V1 页布局：TopBar + 卡片网格 + 右侧栏 |
| `components/ClusterBar.vue` | 集群红绿灯（health 大色块+文字）、在线率 `5/6`、CPU/内存/GPU 聚合负载条 |
| `components/HostCard.vue` | 主机名 / 角色徽章 / 四态边框 / CPU·MEM·GPU·DISK 迷你条 / 链路角标（P1 显示 `—`）/ reasons 前 2 条角标文字 |
| `components/AlertPanel.vue` | P1 占位：按 cluster 中非 OK 节点列 `status.reasons`，按级别排序，点击=钻取；P2 替换为完整告警生命周期列表 |
| `lib/status.js` | 前端色码与排序共享模块：四态色值（并入奶白主题）、节点排序键（CRIT > OFFLINE > WARN > OK，同级 role 权重 60% 以上时按 `inference > db > desktop > laptop > display > other`，再同级按名字）|

- 数据源：复用现有 9101 WS snapshot（2s 广播），V1 不新增请求；
- 迷你条颜色与 3D 视图发光色码统一走 `lib/status.js`（当前 App.vue 里 `barClass` 的 70/90 硬编码**废弃**，以四态阈值为准）；
- 卡片网格 `repeat(auto-fill, minmax(240px, 1fr))`，全屏即大屏模式（P1 只做 CSS 密度，不做模式开关）。

---

## 2. 顺带发现的技术债（**本轮明确不修**，仅登记）

1. `cpu.py` / `memory.py` 仍在调 **wmic**（Win 25H2 已废弃，交接文档 §5 明令用 CIM）——topology.py 已是 CIM 正确写法。影响：Windows 上 CPU 温度回退和内存插条明细静默失败（前端已容错，显示 null）。建议 P2 前单独一次小修。
2. Client 硬编码 `ws://localhost:9101`——局域网其他机器打开页面连不上 Server。修法很简单（从 `location.hostname` 推导），但属独立缺陷，等你点头才捎带改。
3. Windows `getloadavg` 不存在 → `load_avg: null`，前端显示 `—`，不假装采数。

---

## 3. 验证方案（实施后自测 + 你验收）

**我的自测（模拟数据驱动，不依赖真机）**：
- 写 `scripts/mock-agent.mjs`：向 9100 注册 3 个假节点（1 全绿、1 GPU 高温 WARN、1 注册后静默 → OFFLINE），验证 Server 派生 + 前端渲染色码/排序/钻取全链路；
- 真机笔记本 Agent 跑通 disk/system 新字段采集（Windows 实采）。

**你的验收清单（约 15 分钟，需在场）**：
- [ ] 打开 5173，落地页是 V1 总览：集群红绿灯 = WARN（因 mock 异常节点）
- [ ] 异常节点卡片红/黄边框且**置顶**，链路角标显示 `—`
- [ ] 点正常节点卡片 → 进入 3D 详情且是该机的数据，返回正常
- [ ] 关掉 mock 的"静默节点"：15s 后变灰 OFFLINE
- [ ] 改 thresholds.json（如 disk warn→70）重启 Server，卡片颜色跟变

---

## 4. 实施排期与工作量（P1 通过后）

| 步骤 | 内容 | 预估 | 需你在场 |
|---|---|---|---|
| 0 | 提交《产品设计方案-v1.md》到 git（当前唯一未跟踪文件） | 2 分钟 | 确认提交即可 |
| 1 | Agent：disk.py + system.py + --role | ~30 分钟 | 否 |
| 2 | Server：thresholds.json + status.js + store 接线 | ~40 分钟 | 否 |
| 3 | Client：DetailView 抽离 + V1 四组件 + lib/status | ~1 小时 | 否 |
| 4 | mock-agent 脚本 + 全链路自测 | ~30 分钟 | 否 |
| 5 | 联调验收（§3 清单） | ~15 分钟 | **是** |

一个会话内可完成 1–4，验收通过后 git 提交 P1 检查点，再进 P2 细化设计。

---

*本文档评审通过后生效；实施按 §1 契约执行，超出本文范围的改动先回来修订。*
