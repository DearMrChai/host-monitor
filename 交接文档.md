# 交接文档 — 局域网主机监控系统（供新窗口"专家团"模式接续使用）

> 日期：2026-09-18
> 交接目的：**暂停当前开发，在新窗口以"专家团"模式（需产品经理参与）重新设计产品**
> 配套阅读：`项目阶段性汇报.md`（技术细节更全）；本文档侧重"为什么走到这一步 + 新会话该怎么接手"

---

## ⚡ 核心纠偏（本次交接最重要的一条，新会话先读这里）

用户自我诊断（原话，2026-09-18）：
> "我觉得我的'产品设计'方向发生了问题，我一直在纠结**画得像不像**，
> 但我忘记了我的核心是**服务的监控**：我想监控每台设备的情况、
> **内网集群的健康** 和 **通信状况**。但是我抓不住重点。"

已达成的共识：
- 过去数轮迭代（CPU 形象、内存容器、PCB 走线…）都在打磨**组件外观**，这是手段替代了目的。
- 产品真正的核心 = **监控语义**，共三层：
  1. **单设备状态**：CPU/GPU/内存/磁盘/网络的实时负载与健康（已有采集能力）；
  2. **集群健康**：整个内网有多少节点在线/异常/告警，一眼可判（当前缺失总览层）；
  3. **通信状况**：设备之间的链路质量——带宽/延迟/丢包/断线（**当前 Agent 只采集本机网卡流量，没有节点间主动探测，是能力缺口**）。
- 3D 数字孪生只是**展示外壳**：先回答"这个视图要让用户 3 秒内看到什么"，再决定"长什么样"。
- 本轮专家团的第一任务：**从监控语义出发重新设计产品**（指标体系 → 告警体系 → 信息架构 → 视图结构），视觉拟真度排最后。

---

## 0. 给新窗口 AI 的启动指令（可直接粘贴）

```
请阅读 E:\DSH-workspace\代码仓库\host-monitor\交接文档.md（先读"核心纠偏"一节）
和 项目阶段性汇报.md，以"专家团"模式接续本项目。
产品核心已重新定义为：设备状态监控 / 内网集群健康 / 节点间通信状况。
本轮任务：产品经理主导，从"要监控什么、给谁看、异常了怎么办"出发，
输出：指标体系 → 告警体系 → 信息架构 → 视图结构（3D 只是外壳，最后讨论；
注意节点间延迟/丢包探测是当前 Agent 的能力缺口）。
本会话先不写代码，产出产品设计方案供我评审。
```

---

## 1. 项目是什么

- **来源**：由产线数字孪生项目 `galv-3d-viewer`（Vue3+Three.js+Vite）复制派生。**原产线项目不可改动。**
- **目标**：监控局域网内多台主机的 CPU / GPU（可多卡）/ 内存（可多条）/ 网络速度。
- **用户本人环境**：Windows 11 25H2 笔记本（i9-12900H + RTX 3050 Ti + 2×8GB DDR4）为当前示例机；未来部署到一台老机器（Phenom II X4 645 + 8G DDR3，准备换 GT 1030 GDDR5）做监控服务端/展示端；局域网内还有 笔记本/台式机/数据库主机/vLLM推理服务器 等异构节点。
- **节奏约定**：小步快跑、分阶段实现、每阶段验证后再进下一阶段；先做框架后补细节。

## 2. 当前技术资产（可运行，保留复用）

### 2.1 三层架构
| 层 | 技术 | 位置 | 说明 |
|---|---|---|---|
| Agent | Python (psutil + nvidia-ml-py + websockets) | `host-monitor/agent/` | 采集 CPU/GPU/内存/网络；**开机自检硬件拓扑**（Windows 用 PowerShell Get-CimInstance，wmic 已废弃）上报 |
| Server | Node.js (express + ws) | `host-monitor/server/` | 9100 收 Agent；9101 对浏览器提供 REST `/api/hosts` + WS 推送；**纯内存存储，无持久化** |
| Client | Vue3 + Three.js + Vite | `host-monitor/client/` | 单 3D 视口，`src/three/TopologyRenderer.js` 按 JSON 拓扑配置程序化生成主板场景 |

### 2.2 启动方式
```
cd server && node src/index.js              # 中继
cd client && npx vite --host                # http://localhost:5173
cd agent  && python main.py --server ws://localhost:9100
```
（截至交接时三个服务都在运行；不需要时可 `Get-Process node,python | Stop-Process -Force`）

### 2.3 拓扑配置 Schema（前端数据驱动的关键，多机型扩展的基础）
见 `client/src/config/sample-topology.json`（台式机）与 `inference-server-topology.json`（EPYC+4×H100 NVLink 示例）。
字段：`host_type / cpu / memory(sticks,channels) / gpu[] / storage[](via cpu|pch) / network[] / pch / interconnects[]`。
Agent 自检产出的就是同构 JSON，前端无需区分"配置来源"。

## 3. 已确定的产品/设计决策（重要！新设计应尊重或显式推翻）

1. **可视化风格**：对比过"平面爆炸图 vs 3D 立体"，用户选定 **3D 立体**，理由：后期多机拓展时要有"多台主机通过网络相连"的整体感。
2. **单台主机 = 一块 3D 主板的数字孪生**，组件布局参考真实 ATX 走位（CPU 左上、内存右侧竖排、PCIe 横排、M.2、PCH、网卡）。
3. **组件形象演进**（当前 v0.4 状态）：
   - 主板：浅绿 PCB + 铜走线/过孔/SMD/安装孔/丝印；
   - CPU：参考用户提供的实物图 —— 绿色基板贴板（无悬空）+ 四边金色鸥翼引脚 + **圆角银色 IHS 顶盖（顶面 Canvas 印字：CPU+型号+序列）**+ 金色 pin-1 定位点；**无散热片/风扇**；
   - 内存：**池化为 2 个无盖窄容器（罐子），与 CPU 同排对齐**；两罐液位恒相同 = 整池占用率。
4. **实时数据驱动视觉**：组件发光颜色 绿→橙→红 随负载；**CPU 报警闪烁**（占用>90% 或 温度>85°C 时顶盖红色脉冲+标签闪烁）；风扇转速随负载（GPU 保留）。
5. **背景**：奶白浅色 `#f5f0e6`（用户明确不要黑色）。
6. **连接线**：Manhattan 直角 PCB 铜走线（非 3D 管道），按总线类型着色（DDR蓝/PCIe绿/青/DMI橙/NVLink紫）。
7. **性能约束**：展示端可能是老机器 → 坚持**程序化几何体**（低面数），不引入 GLB 重模型。
8. **科学准确性优先于迎合**（用户原话）：例如纠正过"M.2 内存"→ M.2 是固态；内存池化后**无法也无需区分单条存了多少**（interleaving 按 cache-line 跨条分布，OS 只有总用量）。

## 4. 为什么暂停 —— 待重新设计的核心问题

用户对"设备监控信息这块的样式效果"不满意，要停下来从**产品层面**重想，而不是继续调 3D 细节。

新设计需要回答的问题（建议产品经理主导）：
1. **产品定位与用户**：这是给谁看的？（家庭自托管玩家 / 小团队运维 / 大屏展示值班）不同用户对"信息密度 vs 观赏性"的权重完全不同。
2. **核心使用场景**：日常盯屏？告警响应？资产总览？容量规划？——决定信息层级。
3. **监控信息呈现范式**（当前痛点）：
   - 信息"刻"在 3D 组件上 vs 悬浮标签 vs 2D HUD 面板 vs 3D 场景+2D 详情双层设计；
   - 数字为主还是颜色/动效为主；
   - 单主机视图与多主机总览视图如何切换（钻取关系）。
4. **告警体系**：阈值谁定？分级？通知渠道（页面闪烁之外：声音/IM/webhook）？
5. **多机拓扑与通信**：网络互联怎么画（交换机/网段/流量）？节点间延迟/丢包需要**主动探测**（Agent 互 ping / 轻量 probe），当前只有本机网卡流量——需求确认后要补进 Agent；异构机型（笔记本/台式/数据库/推理服务器）模板如何模块化拼装？
6. **数据与持久化**：目前无持久化；是否需要历史曲线、离线告警记录？
7. **范围裁剪**：3D 数字孪生是"必需核心"还是"差异化外壳"？是否配一个 2D 表格/卡片视图作为信息主体、3D 作为总览入口？

## 5. 已知技术债 / 坑（新会话避免重蹈）

- **文件在 workspace 外**：`host-monitor` 不在 IDE 工作区（工作区是 `galv-3d-viewer`），编辑工具会报 `can not edit the file outside the projects`。**解法**：在工作区内写临时 Python 脚本（字符串替换/整文件写入）→ `python xxx.py` 执行 → 删除脚本。
- **Windows 25H2**：`wmic` 已废弃，硬件检测必须用 PowerShell `Get-CimInstance`；PowerShell 内联 heredoc 易错，落成 .py 文件再跑。
- **端口占用**：重启 server 前杀干净 `node`/`python` 进程，否则 EADDRINUSE 9100。
- **pynvml 已改名**：用 `nvidia-ml-py`，并 filterwarnings 抑制 FutureWarning。
- **存储接口识别**：Windows 下 NVMe 可能误报 SATA（连线正确，名字不准），待改进。
- **笔记本 GPU** 省电态 usage 为 null，前端已容错。
- 项目**无 git 仓库**；如需版本管理，建议新会话第一件事 `git init` 并提交当前检查点。

## 6. 关键文件索引

| 文件 | 作用 |
|---|---|
| `client/src/three/TopologyRenderer.js` | 核心 3D 渲染器（~1000 行，v0.4：PCB 主板/CPU 实物形象/内存池化容器/Manhattan 走线/实时指标/报警闪烁） |
| `client/src/App.vue` | 单视口 + WebSocket 连接 + HUD（CPU/GPU/RAM/NET 条形读数）+ 总线图例 |
| `client/src/style.css` | 浅色主题 CSS 变量、topo-label、alarm-blink 动画 |
| `client/src/config/*.json` | 拓扑模板（台式机 / 推理服务器） |
| `agent/collectors/topology.py` | 硬件自检（PowerShell CIM）→ 拓扑 JSON |
| `agent/main.py` | 注册(含拓扑)+周期上报指标 |
| `server/src/store.js` | 主机/拓扑/指标内存存储，15s 离线判定 |
| `server/src/index.js` | 双 WS 端点 + REST |
| `项目阶段性汇报.md` | 上一轮生成的中文阶段汇报 |

## 7. 交接清单（新窗口开始时确认）

- [ ] 读两份文档（本文 + 阶段汇报）
- [ ] 打开 http://localhost:5173 看当前效果（若服务没起来，按 §2.2 启动）
- [ ] 专家团就位：产品经理主导 §4 的 7 个问题，先出**产品设计方案**（用户画像/场景/信息架构/呈现范式选型），评审通过后再动代码
- [ ] 动代码前建议先 `git init` 固化 v0.4 检查点

---
*本文档由上一会话自动生成，内容截至 v0.4（2026-09-18）。*
