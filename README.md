# LAN Host Monitor — 局域网 / ZeroTier 多机监控看板

给自己和朋友的一小群机器用的：**先回答"现在有什么不对"，再回答"具体是什么"**。
它不是企业级监控，而是一个**变化检测器**——常驻机器在不在、负载有没有越过这条线、链路臂还通不通、以及过去 24 小时里哪一刻变了。

```
各设备 Agent (Python) ──ws──> Server :9100 (接入；生产档 = strict，全部要凭据)
                                │  ├─ SQLite：历史库 + 名册库（含配置真源）
浏览器 / 看板        ──http+ws──> Server :9101 (REST + 2s 广播 + 静态看板)
```

## 运行要求

- **Node 22+**（Server 用内置 `node:sqlite`，所以不引第三方 ORM；依赖只有 `ws`/`express`/`cors`）
- Python 3.9+ + `agent/requirements.txt`（`psutil`、`websockets`、`nvidia-ml-py`；无 NVIDIA 卡时 GPU 自动降级为静态信息）
- 一台常开的机器跑 Server（本项目实际部署在低功耗小主机上，监控是它的**附属功能**，不是专职服务器）

## 快速开始

```bash
# 1) Server
cd server && npm install && npm run dev        # :9100 接入 / :9101 看板

# 2) 前端（开发模式；生产用 npm run build 后由 Server 直接托管 client/dist）
cd client && npm install && npm run dev        # http://localhost:5173

# 3) 每台被监控机器上的 Agent
cd agent && pip install -r requirements.txt
python main.py --server ws://<SERVER_IP>:9100 --host-id <节点ID> --role <角色>
```

`--role` 可选 `db / inference / desktop / laptop / display / other`。参数也可以写进 `agent.json` 或用环境变量，优先级 **CLI > env > agent.json > 默认**；`python main.py --print-config` 会逐项打印当前值**以及它是谁给的**。

**接入需要凭据时**（`HM_INGEST_TOKEN=legacy|strict`，默认档拒掉陌生 id）：在看板"接入"页用管理口令签发一次性配对码，然后
`python main.py --server ws://<SERVER_IP>:9100 --enroll <码>` 跑一次——身份与节点密钥落进 `identity.json`，之后正常常驻启动即可。

## 界面

| 页 | 回答什么 |
|---|---|
| 总览 | 集群健康吗（一盏灯 + `在线 n/m`）· 哪台机器有问题 · 它现在为什么被判定成这样 · 今天这一刻发生了什么（事件流） |
| 节点详情 | 这台机器的硬件拓扑与 2h/7d/30d 曲线 |
| 陈列架（顶栏「拓扑」那格） | 一排机位、每台一格：机体按真比例画成**三档卧式剪影**（薄板 / 方盒 / 超宽）。**形状＝你在节点档案里声明的形态**（没声明时按机名推断），**面色＝健康档**，**亮度＝负载**（没报负载是中灰，不是零负载）。中心块、网关块、连线与脉冲点都不再画——链路是单机详情里的读数，不在这面屏上（V3 包 1，任务书 B §1.3） |
| 值守屏（`?kiosk`） | 挂在副屏上只负责"看起来是活的、出事了看得出来" |
| 接入 | 配对码、宽限期内的老 Agent、未带凭据的来源、异常接入台账、ZeroTier 在场台账（231 上目前显示"来源不可用（http-401）"= H27 已知预期，不是新故障，见隐患清单） |
| 设置 | 全局阈值与探测计划、逐机阈值覆盖、节点档案（显示名/归属/站点/角色/常驻或临时）、静默 |

四态判级只有一处实现，且每个非 OK 状态都自带**成因**（现役标签：`CPU占用` / `CPU温度` / `内存` / `磁盘` / `GPU温度` / `失联` / `链路延迟` / `丢包率`；链路那两条在行上印 `↔目标名`，网关与关键机都算目标——**没有"NAS"这种内建成因**）。"缺席"与"出事"是分开的两件事：长期不上报的常驻机器计入在线率与事件流，但不进集群健康度。

> **这三条入口各自怎么走（墙 / 桌面 / 浮动小窗）、屏上什么都不显示时先查哪一句**：见 [`HOW-TO-USE.md`](./HOW-TO-USE.md)。

## 自检

改完代码不靠手点，靠这套：

```bash
cd server && for f in scripts/selftest-*.mjs; do node "$f"; done   # 七套，全部对临时库跑
cd client && npm run check                                        # token 契约 + 声音契约 + FPS
```

它们是可执行的产品契约：任何一条红了就是契约被破坏，不允许"绕过那条断言"。**现役基线读数（2026-09-20 复跑）**：server 七套 = 14 + 48 + 50 + 59 + 10 + 100 + 28 = **309 条全过**，client 三套 = 10 + 9 + 6 = **25 条全过**，各套件对临时库跑，可与生产库并存。

**H22 修后复跑（2026-09-22，本机）**：server = 14 + **65** + 50 + 59 + 10 + 100 + 28 = **326 条全过**（config 套新增 H 段 17 条：局部阈值写入被拒并点名缺项 / 缺阈值判级返回 null / 两条评估入口各自有护栏且异常落成 `system/evaluator_error`），client = 23 + 9 + 6 = **38 条全过**（token 套在此期间由 10 涨到 23，与 H22 无关）。三条正对照（拿掉 `levelFor` 守卫 / 拿掉 `snapshotOrNone` 的 try / 只留一条入口的护栏）逐条红在预期位置，读数见 `server/scripts/selftest-config.mjs` 的 H 段抬头。

**V3 翻暗第 3 刀后复跑（2026-09-22，本机·当前基线）**：client = **20 + 9 + 6 = 35 条全过**，server 未动仍 **326 条**。token 套由 23 降到 20 的原因：`--ok-ink/--warn-ink/--crit-ink` 三支按色表 R-2 与 §3 整族作废（暗地上面色本身就够亮，"字直接用面色"，第二套值反而违反一色一事实），它们与 `palette.INK` 的三对配对断言随之删除，54 处 `var(--*-ink)` 改指 `var(--green)/var(--orange)/var(--red)`。**同值改名，渲染结果零变化**——那三支改前的十六进制值与面色逐字节相同，所以断言数减少不等于闸变松：配对表是断言数的唯一来源（脚本内没有写死的期望总数），剩余 20 对逐对仍红在正对照下（临时把 `palette.STATE.OK` 或 `--green` 改成别的值，各自只余 19 过 1 红）。

## 诚实边界（不伪装）

- **明文局域网，无 TLS。** 配对码、节点密钥、指标都是。节点密钥以 `sha256$盐$散列` 落库、管理口令以 scrypt 落库——**那是散列不是加密**，谁也印不出原值，但线上传输仍是明文。要收口得靠 TLS 或每机一次性握手。
- **读侧无鉴权**（朋友路过看一眼 = 零摩擦，是需求）；**写侧全部要管理口令**，且只有一处带闸的读（设置页的编辑源，理由写在自检里）。口令失败按来源地址计预算。
- 真实 IP 与凭据**不进 git、不进日志与异常栈、不进 API 响应、不进分享包**；含地址的文件（`probes.json`/`zt.json`/`identity.json`）一律 gitignored，仓库里只有 `*.example.json`。
- **"某族写法已经全部清完"这种断言，唯一依据不许是"我的搜索没命中"。** 一个搜索模式可以跑成功、然后自信地返回 0，而 0 恰恰是最容易被信任、也因此最危险的读数。本仓库实测过两次：`rgba(0,0,0` 把带空格的变体 `rgba(0, 0, 0, .07)` **整片漏掉**（一处存量从"24 处"变成 30 处）；另一条嵌了 POSIX 字符类的模式在 `git grep -E` 下**匹配 0 条且不报错**，而它本该命中的那支色确实活在代码里。⇒ **规矩（用户 2026-09-22 定，所有人都要守，包括 agent）：说"这一族没有了"之前，先喂一条已知应该被命中的样本给同一个模式，并把那条样本写进结论一起交。** 同一标准的两面：**验收里凡是"数有几处"的读数，必须带一条能重跑的命令**，否则它只是措辞，不是读数。
- 演示/模拟节点的 `host_id` 永远以 `sim-` 开头、显示名由名册强制加 `模拟-` 前缀（`server/src/demo.js` 的四台道具），并且可以一键关掉；关掉时它们的告警以"已退役"闭合，**不会伪装成"已恢复"**。
- 出站告警（微信/邮件）**故意不做**：这台机器的机群满载是正常工作状态，夜半推送等于为人陪编译。

## 文档索引（为什么这样设计，都在这些文件里）

| 文件 | 是什么 |
|---|---|
| `技术隐患清单.md` | **H1~H29 隐患台账**：每条写着"哪一批关掉了哪一半、还剩哪一半"。**V3 选题以它为准** |
| `部署说明.md` | 231 运维卡、Agent 逐台动作、上线与回滚步骤、**两篇上线记录（09-19 / 09-20）** |
| `S1-…S6-细化设计.md` | 每一批的判据、契约、偏差与未验证清单（**§8 是实施后回填的，别看代码猜**）；每份末尾另有"§ 收口销账" |
| `V2-迭代讨论纪要-20260919.md` | 第二轮迭代的决策升版与排期（S1…S6）。写于 09-19，**完成状态一律看上面两篇上线记录** |
| `产品设计方案-v1.md` | **产品定位与"诚实边界"的出处（仍有效）**；具体数值与界面清单已被 V2 取代 |
| `P1~P5-细化设计.md` | V1 阶段的批次契约（**历史稿**：阈值现值一律读 `GET /api/config`，四态色值真源在 `client/src/lib/palette.js`） |
| `交接文档.md` / `项目阶段性汇报.md` | 2026-09-18 v0.4 的一次性交接与对外汇报（**均停留在 V1，照做会重做 V1**）；现役交接见工作区 `00-暂存\handoff-v3讨论.md` |

## 当前状态与下一步

**V2 已收口并上线**：S1 名册与写侧闸 → S6 设置界面与配置单一真源，生产 231 = `61bf4db`（GitHub tag **`v2`** = 同一提交）。三台 Agent 以 `sha256$` 散列凭据在 `strict` 下接入、重启自启已实测通过——读数在 `部署说明.md`"上线记录 · 2026-09-20"。**功能冻结已生效**：V2 之后新发现的缺陷（H26~H29）一律只登记、不追改。

**V3 的主题 = UI/UE 视觉迭代**（用户 2026-09-20 定）。边界一句话："基于现有信息做视觉迭代，信息架构非必要不更改，但不限制死"——落到执行：**只用现有字段/现有接口，不新增数据源**；发现某个结构本身在误导人时上报，那是发现不是越界。

候选项以 `技术隐患清单.md` 为准，V3 首批建议顺序：**H22**（一次"只改一项"的阈值写入会让 Server 反复崩溃且重启不救——V3 会让人真的在设置页动手，所以它排在设计工作之前）→ **H27**（在场台账回退 CLI 通道，做完 S5 的在场页在生产上才第一次有数据）→ **H28**（`untouched_since_seed` 语义 + 设置页三态文案）→ **H26**（停服清理脚本化）→ 磁盘告警的**降噪/单独归置**（阈值不许改松）。

**别在 V3 自作主张的**：历史数据导出（已判伪需求，勿复活）、`observed`（SSH 拉取，前置闸 H11 未落地且属新数据源）、access 台账（实施前需用户单独点头）。
