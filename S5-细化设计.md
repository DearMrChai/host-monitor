# S5 细化设计 — 闸口批：凭据/口令治理 + 写侧口令完整化 + presence 台账

> 上位文件：`V2-迭代讨论纪要-20260919.md` §2.5（凭据边界）、§2.6（观测层三小步与"独立台账"硬约束）、§5.2（observed 不得排在口令治理之前）、§5.4 的 S5 行。
> 基线：main = `5ea683f`（生产包 `ae8ace6`，231 已上线，三台 Agent 1.1.0 已配对，接入侧 `strict`）。
> **验收判据（纪要原话）**：过关后才能启动 observed。
> 本文件不含真实 IP/凭据。所有新增自检脚本同样**不得**硬编码真实地址（沿用 `probe-strict-denial.mjs` 的做法：地址由参数给）。

## 0. 这一闸要过的到底是什么

闸口的原意不是"把口令功能做完"，而是：**在考虑让 Server 保管任何第三方凭据之前，先证明本项目对自己已经持有的凭据是干净的**。所以本期判据拆成四条可核对的：

| # | 过关条件 | 由谁保证 |
|---|---|---|
| G1 | 静态盘上读不到可用的接入凭据：`roster.db` 里既没有管理口令明文，也**没有节点密钥明文** | `roster.js` 改哈希存储 + 启动迁移；`selftest-credential-hygiene.mjs` C1 断言 |
| G2 | 每一个会改状态的 HTTP 端点都在口令后面，且口令校验不能被当成免费的 CPU 消耗器 | `index.js` 单一 `requireAdmin` + 新增失败预算；自检 C2 枚举断言 |
| G3 | 任何读端点/日志/事件行的响应体里不出现口令、节点密钥、完整指纹、完整对端地址 | 自检 C3 逐个端点扫（含真实进程 + 真 WS 接入） |
| G4 | presence 台账独立成立：不进四态、不进健康度/在线率、不产告警、UI 上不叫"节点" | `zerotier.js` 与 `store.js` 无引用边 + 自检 C4 |

**G3/G4 是可执行的**，不是一句承诺。这是本期与"我们约定不写口令"之间的区别——H11 的过渡期兜底条款此前只有散文背书。

## 1. 明确不做（防止被当成漏做）

| 砍掉 | 理由 |
|---|---|
| observed（SSH 拉取）通道 | 它是这道闸的**下游**，不是闸本身。且 V2 期内没有可验证的第二方目标机——写出来就是"未经真机验证"的代码。建议整体移入 V3，见 §7 决策项 D1。 |
| access 台账 | 纪要 §4/§2.6 明文"实施前需用户单独点头一次"，该点头尚未发生。不做。 |
| 口令加密存储 / 静态加密 | 无远端密钥前提下是障眼法（H11 已定）。本期做的是**不可逆化 + 面收缩**，不是伪装加密，UI 与文档措辞一律不出现"已加密"。 |
| 多用户/账号化（谁在操作） | 属 H11 长期项，本期仍是"一把口令"。 |
| 服务端"忘记口令"自助通道 | 在局域网明文传输上开一个能重置口令的口 = 把 H13 从"麻烦"升级成"漏洞"。给**本机脚本 + 文档**（§3.3），不给网络接口。 |
| 看板登录 / session cookie | 形态 A（不暴露公网）下不改变威胁模型，且会破坏"朋友拿到链接就能看"的展示用途。 |
| presence 进事件流 | §2.6 硬约束：三者是独立台账，不得共享归并与告警。`events` 表算共享叙事面，本期不写，登记为 V3 可选（D2）。 |

## 2. G1：节点密钥改为不可逆落库

**现状**：`nodes.enroll_token` 存 32-hex **明文**节点密钥（S2 §2）。管理口令已经是 scrypt，两者不对称——拿到 `roster.db` 的人虽然改不了看板设置，却**可以直接冒充三台 Agent 写入数据**（在 `strict` 下这正是唯一有效力的凭据）。

**改法**：

- 新落库形态 `sha256$<salt-hex>$<hash-hex>`，salt 为每行随机 16 字节。
- **为什么是 sha256 而不是 scrypt**：节点密钥是 128 位随机值，不是人挑的口令，不存在字典攻击面；而 `keyMatches` 落在 Agent 重连路径上，scrypt 会在每次重连时白花 80ms（231 是低功耗机）。口令侧继续用 scrypt，两侧策略不同是**有意的**，代码里写明。
- 校验：`keyMatches` 对候选值同 salt 派生后 `timingSafeEqual`。长度不符/形态不认识一律 false。
- **兼容与迁移**（关键，涉及生产三台）：
  1. 启动 `reload()` 后做一次 `#migratePlainKeys()`：把仍是 32-hex 明文形态的行**就地哈希**。因为我们手里就有明文，迁移不需要 Agent 参与，也不改变 Agent 侧任何东西（它只认自己那份 `identity.json`）。
  2. `keyMatches` 保留明文比对分支（防御回滚、以及迁移被跳过的极端情况），命中即顺带升级该行。
  3. 重新配对（`--enroll`）写入的就是哈希形态。
- **回滚安全性**：新库→旧代码 = 旧代码拿哈希当明文比，认证失败。因此这条改动**必须随里程碑上线**，且 `部署说明.md` 写清"回退代码需同时回退 `roster.db` 备份"。上线步骤里加一步：先备份 `roster.db`，翻完再看一眼三台仍在线。
- `nodeKeyOf()` 不再返回可用凭据（返回的是哈希），唯一调用点 `keyMatches` 内部化；`has_credential`/`keylessAgents()` 判据不变（非空即有）。

## 3. G2：写侧口令完整化

### 3.1 端点审计结论（本期实测）

现有 POST 共 7 个：`/api/admin/passphrase`、`/api/roster/:id/class|mute|name`、`/api/admin/enroll`、`/api/admin/enroll/revoke`、`/api/admin/demo`。除口令端点本身（首次设置时服务端还没有口令可比，且 `setPassphrase` 要求"已设置则必须带对的原口令"）外全部挂 `requireAdmin` → **写侧口令目前是完整的**。完整性不是本期新增的，本期新增的是**"以后不漂移"的保证**：`selftest-credential-hygiene.mjs` C2 从 Express 路由表里枚举所有非 GET 路由，断言中间件链含 `requireAdmin`（新加写端点忘了挂 → 自检红）。

### 3.2 失败预算（scrypt 的拒绝服务面）

`requireAdmin` 每次调用做一次 scrypt。231 是低功耗小主机，且端口对局域网开放：任何人拿curl打 `POST /api/roster/x/class` 都能让我方 CPU 满转，同时把"猜口令"变成免费尝试。加一层**先于 scrypt 的失败预算**：

- 按对端 IP 计：滚动窗口 60s 内失败 8 次 → 该 IP 后续请求直接 429，不再进 scrypt。
- 只计失败；成功不写状态。表大小上限 512 条，超限时清最旧（内网场景不需要 LRU 精度）。
- 429 响应体不含"还差几次"之类的提示，避免给探测者反馈。
- 对端地址只用于内存计键，**不打印、不落库、不进事件**（G3）。

### 3.3 G4 的另一半：口令重置的可执行文档（H13）

- `server/scripts/reset-admin-pass.mjs`：**本机**运行（`--db` 指到 roster.db，需文件系统权限），必须 `--yes` 才写；动作=删 `meta.admin_pass` 一行 → 回到"未设置→写侧默认拒绝"的已知安全态，再由看板首次设置流程重新立口令。
- 不提供任何 HTTP 面；不提供"找回旧口令"（不可逆，也没有可找回的东西）。
- `部署说明.md` 增补：谁可以跑、跑完会发生什么（所有设备写操作立刻变灰，需重输新口令）、以及"这不是恢复，是重置"。

## 4. G4：presence 台账（ZeroTier 在场）

纪要 §2.6 原方案：Server 读本机 ZeroTier Local API → 当前 overlay 可达成员 = 在场事实，零安装、零凭据、跨网可用。

### 4.1 数据面

- 新模块 `server/src/zerotier.js`，只读 HTTP：`POST http://127.0.0.1:<port>/getPeers`，头 `Authorization: <token>`。
- 令牌来源优先级：`HM_ZT_TOKEN`（env）> `zt.json` 里的 `token_file` > 平台默认路径（`%PROGRAMDATA%\ZeroTier\One\authtoken.secret` / `/var/lib/zerotier-one/authtoken.secret`）。端口默认 9993，可 `api_port` 覆盖（自检用 stub）。
- 配置文件 `server/src/config/zt.json`，**gitignored**（内含节点 id 与端口），模板 `zt.example.json`。缺文件 = `enabled:false`，端点返回"未启用"而不是报错。
- 在场判据：`!localPeer && latency >= 0`（ZT 对未知/不可达报 -1）。根服务器与行星节点会一起出现在 `getPeers` 里 → 提供 `exclude` 列表（节点 id 前缀）由配置排除，默认排除本机。
- 轮询默认 30s，失败不刷屏：连续失败只在状态里体现（`source_ok:false` + 最后一次错误类别），一行日志。

### 4.2 归名与持久化

- 自动归名：peer 的 `name` 与名册里某节点 `hostname` **唯一**匹配 → 关联；否则"未归名"。歧义（同名多台）时不猜，标 `ambiguous`。
- 手工映射：`presence_alias(zt_addr TEXT PRIMARY KEY, host_id TEXT, label TEXT, updated_at INTEGER)` 落 `roster.db`（低频写，与名册同库），写侧走口令：`POST /api/presence/alias {addr, host_id|null, label}`。
- 状态持久：`presence_state(zt_addr PRIMARY KEY, name, host_id, first_seen, last_seen, last_latency)`，每台最 60s 一次写。这样"它今晚在不在"重启后仍答得出。
- **不叫节点**：响应字段一律 `peer`/`device`；`hosts`/`cluster`/`alerts` 三个数组里绝不出现它（自检 C4 断言 `store.js` 与 `zerotier.js` 之间无 import 边，且 `/api/hosts` 快照里不含 ZT 地址字段）。

### 4.3 呈现

`接入`页右侧新增一张"ZT 在场"卡：来源状态（已启用/未启用/读不到）+ `在场 n / 可见 m` + 逐行（显示名或"未归名 c3d1…"、延迟、最后可见、是否已归名）。未启用时**空态说清为什么**（缺 `zt.json`），不留一块空白——空态没引导正是 §5.3 列的第一位掉价项。

本期不加第四个顶部页签：它是台账不是工作台，放在"接入"页与口令/凭据同一叙事带里最省认知。

## 5. presence 判定侧的两处补漏（H18 与死行）

### 5.1 H18：进程内长静默 → 降级为缺席

现状：缺席卡片只由**名册驱动的合成**产生（"这台常驻机在本进程里从没出现过"）。而 `store.hosts` 从不清理，所以一台**在本进程运行期间**被关掉的常驻机永远是"实时 OFFLINE"记录 → CRIT 卡片 + 循环铃声 + 进健康度。7×24 的 231 上，关机过夜=响一夜。

改法（复用，不新造一套）：

- `store.checkTimeouts()` 里，对**已在名册里确认为常驻**、且 `now - lastSeen > degrade_after_minutes*60_000`（默认 15）的实时记录，置 `absent_record = true`、`last_seen = lastSeen`。
- 于是它自动走上 S3 已有的那条路：`evaluateHost` 里 `absent_record` → 无 reasons → 告警以 `cancelled='absent'`（"改判缺席·不再报警"）闭合、健康度不计入、在线率仍计入分母、卡片灰虚线、铃声停。
- 恢复上报时（`updateMetrics`）清掉 `absent_record`，重新变回实时记录；再次越线会再生成一次缺席事件（`oncePerEpisode` 的 6h 窗口限制它不刷屏）。
- 配置进 `thresholds.presence`：`degrade_after_minutes`。**语义分工写进注释**：`absent_after_minutes`(3min) 管"跨重启后这台机器算不算缺席"，`degrade_after_minutes`(15min) 管"本进程里看着像挂了多久才改口"。两个阈值不是重复，是两个问题。
- 客户端唯一要改的措辞：`HudPanel` 的 `absent_record` 文案"自 Server 重启后未再上报"对降级记录是错的 → 改成都说得通的写法（区分依据用新字段 `absent_by`：`restart`/`silence`）。
- 不做：把降级记录从 `hosts` 里删除（会让在线率分母说谎，正是 H12 的反面）。

### 5.2 enroll_codes 死行

`enroll_codes` 只增不删（撤销才删）。清理挂到 history 的 retention 钩子上（S3 定的"一个时钟"）：过期超过 30 天的行删除。明文码本就只显示一次，删行不影响任何读侧（`activeCodes()` 只查未过期）。审计留在 `events`（`code_issued`/`code_revoked`），不依赖码行。

## 6. 自检与验收

| 用例 | 位置 | 断言 |
|---|---|---|
| C1 密钥哈希 | `selftest-credential-hygiene.mjs` | 配对后 DB 里无明文密钥；明文老库启动后被就地迁移且认证仍通过；`keyMatches` 对错误密钥 false |
| C2 写侧完整 | 同上 | 枚举非 GET 路由 → 每条链上含 `requireAdmin`（口令端点在白名单里且理由写明） |
| C3 无泄漏 | 同上 | 真进程 + 真 Agent 接入后，逐个读端点扫响应体：不含口令、节点密钥、完整指纹、完整对端 IP；`/api/events` 的 detail 里 `addr` 均为掩码 |
| C4 presence 独立 | 同上 + `selftest-zt-presence.mjs` | `store.js` 不 import `zerotier.js`；快照里没有 ZT 字段；stub ZT 服务下在场判据/归名/歧义/掩码正确；未启用时空态结构化返回 |
| C5 失败预算 | hygiene | 连打 9 次错口令 → 第 9 次 429 且未进 scrypt（用计数桩断言） |
| C6 H18 降级 | `selftest-roster.mjs`（presence 段）+ `selftest-events.mjs` | 注入时钟推进 16min → 该 host `absent_record`、告警 `cancelled='absent'`、健康度不含它、在线率仍含它；重新上报后恢复实时；措辞字段 `absent_by='silence'` |
| C7 死行清理 | `selftest-history.mjs` | 过期 31d 的码行被清，未过期与 29d 的保留 |
| C8 重置脚本 | hygiene（跑在临时库上） | 无 `--yes` 拒绝；带 `--yes` 后 `hasPassphrase()===false` 且写侧回到默认拒绝 |

**判据回读**：G1–G4 全绿 → 闸口过 → observed 才允许排期（本期结论是**不排**，见 §7）。

## 7. 交给用户拍板的一项（不替他决定）

**D1｜observed（SSH 拉取）建议从 V2 移出，落 V3。** 三点理由：
1. 这道闸要防的就是"Server 保管别人的口令"，而 V2 的闸内容只做完了"自己已有凭据的干净化"，第三方凭据的治理（分权限、审计、轮换）没有落地——现在开 observed 等于自己拆闸。
2. 形态 A 下 observed 的真实收益只有一台（办公室 35B），而它已被 §2.6 的 presence 通道覆盖成"在场可见、不采指标"，这是用户认可的成型期形态。
3. 没有第二台真机可验：Windows 侧裁剪、Linux 侧白名单命令、失败=UNKNOWN 三条都要真机才算数，写出来只能标"未验证"。项目里没有先例，本期也不开这个头。
→ 若用户坚持 V2 内做 observed，前置条件是先把凭据治理做到"第三方凭据有独立存储、可逐台撤销、有审计"，那是 3~5 天量级的额外工作。

## 8. 偏差与未验证清单（实施后回填，不得事前省略）

### 8.1 实施中发现并当场修掉的（设计里没写、但不修就是坑）

| 项 | 结论 |
|---|---|
| G1 只改 `SELECT` 侧不够：明文残页 | 迁移重写会把旧行留在自由页和 `-wal` 里，`grep` 二进制仍能捞到明文密钥。修法：`#migratePlainKeys()` 改完行后一次性 `PRAGMA wal_checkpoint(TRUNCATE)` + `VACUUM`。自检 A 段直接扫 `roster.db*` 三个文件的原始字节，不再只看查询结果。 |
| `loadCfg()` 的 disabled 分支返回残缺对象 | 少了 `timeout_ms` → `AbortSignal.timeout(undefined)` 立即 abort，之后任何"重新启用"（`setEndpoint`）都会以 `unreachable` 全败。由 `selftest-zt-presence.mjs` 首跑 18 条红抓到（它正是从"无配置"这个分支起步的）。修法：`loadCfg()` 任何分支都返回完整形状。**这是本期唯一由自检抓出的真 bug**，且它在"没装 ZeroTier 的机器"上是静默的——只在启用后才炸，属于典型的延迟引爆。 |
| `maskAddr` 保留两段（`127.0.*.*`） | 设计写的是"掩码"，没定粒度。保留前两段等于暴露 /16：小圈子 LAN 段号本身就那么大，识别不出具体机器，但能识别"是不是我家这段"。若将来形态变化（跨地域 / 公网），改法在 `ingest.js` 一处。自检 D9 钉住了当前形态，改动必须显式改断言。 |
| 口令失败预算放在 scrypt **之前** | 设计写了预算，没写顺序。顺序反了则预算只保护"记账"不保护 CPU，等于没做。C5 用 `checkPassphrase` 计数桩断言"9 次错只调了 8 次 scrypt"。 |
| 429 响应体不含数字 | "还剩 N 秒"会把预算变成可探测的时钟，也徒增泄漏面。固定一句人话。 |

### 8.2 本期未验证（交接时按此核对，别当已验证）

| 项 | 状态 |
|---|---|
| ZT Local API 真机读取 | **本机未验证**：`authtoken.secret` 对普通用户不可读（实测 `Permission denied`），因此 124 上无法端到端跑通；生产 231 的 Server 以 SYSTEM 运行 → 理论上可读，**里程碑上线时必须现场确认一次**（看"ZT 在场"卡是否非空态）。C4 只覆盖逻辑（stub HTTP 服务）。 |
| `getPeers` 是否混入根/行星节点 | **未验证**（同上原因）。已按可配置 `exclude` 处理，现场第一次看就要确认。 |
| 节点密钥哈希对生产三台 | 本机只有 mock/临时库验证；真机影响面 = 231 的 `roster.db`。上线步骤含备份与"三台仍在线"复核。 |
| presence 首次真机跑 | 归名命中率未知（取决于各机 ZT `name` 是否等于主机名）。命中率低不影响正确性，只影响省事程度；手工映射是兜底路径。 |
| "ZT 在场"卡的视觉排版 | **人眼未过**：内置浏览器视口 0×0（页签不可见），几何量测无意义。已用 DOM/计算样式核实：结构正确、四分支（按主机名/手工/同名多台/不可达）里"手工"与"不可达"两支实测渲染、`zp-*` 全部走灰阶 token（无四态色，G4 的呈现侧约束成立）、写侧 403→口令弹窗→重试回放闭环实测通过。**布局宽窄、行内换行、按钮文案长度仍需现场人眼一次。** |
| `absenceCause` 的两支措辞 | **未在浏览器里见到缺席支**：本机快照要出现 `absent_record` 需要"上报过一次后再静默 15min（silence）"或"重启 Server 后 3min 仍未上报（restart）"，两条都要等时钟，本期没等。已核实：`lib/status.js` 两支字符串、`HudPanel.vue` 走 `host.absent_record ? 该常驻节点… : 该临时节点已离场`、`vite build` 通过、后端字段由 `selftest-roster.mjs` 断言。**HudPanel 缺席行 + HostCard 头部文案 = 上线时人眼一次。** |

### 8.3 自检结果（本期实跑）

`selftest-roster 100/0`、`selftest-events 59/0`、`selftest-history 10/0`、`selftest-credential-hygiene 44/0`、`selftest-zt-presence 28/0`、`selftest-agent-pair 14/0`；客户端 `check-tokens 10/0`、`selftest-fps 6/0`、`vite build` 通过。

