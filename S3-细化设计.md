# S3 细化设计 — 24h 事件流 + L1 信息密度 + 缺席可见（H12/H15/H17 归宿）

> 上位文件：`V2-迭代讨论纪要-20260919.md` §5.4 的 S3 行、§2.3「变化事件流优先于静态仪表」。
> 隐患条目：`技术隐患清单.md` H12（常驻机不重连即隐形）、H15（接入台账是内存环形缓冲）、H17（被拒只在本地日志里）。
> 基线：main = S1a/S1b + S2a/S2b/S2c。本文件不含真实 IP/凭据。
> **验收判据（纪要原话）**：朋友问"这能看出什么"，你答得出三件事。

## 0. 三件事先行（本阶段的选题准绳）

S3 不做"更多图"，只做三件能被指着回答的事：

1. **谁在过载、谁掉过**——24h 事件流里的阈值跨档与在线/离线翻转（含异常置顶，红色不必自己找）。
2. **这台机器今天怎么变的**——卡片上的 2h sparkline + 相对上一段的 delta，替代"只有一个瞬时数"。
3. **谁进过这个机群、谁被拒了、谁改过名册**——跨重启的事件台账（H15/H17 的归宿），
   以及**一台常驻机不在了这件事本身**（H12）。

判据落空的那件（信息密度堆满但答不出三件事）不在本阶段解决范围内，宁可少做。

## 1. 范围切分与提交边界

| 子段 | 内容 | 提交 |
|---|---|---|
| **S3a（后端）** | 事件表 + 写入点（presence/ingest/roster）+ 跨档行复用 `alert_events` + 缺席可见（H12）+ `GET /api/events` + 自检 | 一个 commit |
| **S3b（前端）** | 事件流面板 + sparkline/delta + 异常置顶 + 缺席卡呈现 + 实机验证 | 一个 commit |

## 2. 真源划分：一件事只有一处记录（H9 的教训前置）

| 事件类别 | 真源 | 是否新表 | 说明 |
|---|---|---|---|
| 阈值跨档（触发/恢复/终止） | **已有** `alert_events` | 否 | 已经是去抖过、带 `started_at/resolved_at/cancelled` 的持久事实。再从 `samples_1m` 另起一个跨档检测器 = 第二个真相，且它没有去抖，会和用户眼前的告警互相打脸 → **不做** |
| 在场翻转（上线/下线） | 新 `events` | 是 | 主机表只有"现在"，翻转必须落盘才谈得上 24h |
| 缺席（H12） | 新 `events` | 是 | 常驻机自 Server 启动后从未回来：今天完全隐形 |
| 接入（拒绝/配对/宽限/漂移） | 新 `events` | 是 | `ingest.events` 内存环保留（接入页"本次启动"视图仍用它），落盘这份才是 24h 答案 |
| 名册写操作（改档/改名/静默/退役/签发/撤销/口令/演示开关） | 新 `events` | 是 | 写侧是有口令的、低频的、该被审计的 |

`events` 表（**在 history DB**，与 `alert_events` 同库同保留任务；roster DB 继续只承担低频档案，不让热路径争它的写锁）：

```sql
CREATE TABLE IF NOT EXISTS events (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  ts INTEGER NOT NULL,                 -- 最后一次发生时间（合并计数时会前移）
  kind TEXT NOT NULL,                  -- presence | absent | ingest | roster
  host_id TEXT,
  level TEXT NOT NULL DEFAULT 'info',  -- info | warn | crit：呈现权重，不是四态
  code TEXT NOT NULL,                  -- 稳定的机器码，文案在读取时渲染
  detail_json TEXT NOT NULL DEFAULT '{}',
  count INTEGER NOT NULL DEFAULT 1     -- 合并计数的同类事件
);
```

**存 code + detail，不存中文句子**：措辞要能改，历史行不能改。`GET /api/events` 负责渲染文本。

## 3. 写入点与噪声纪律

事件流一旦自己变成噪声源就完蛋（S1 的全部理由）。四条硬规矩：

1. **启动基线**：`store` 首轮只登记各节点的当前状态，不产事件。否则每次重启 Server 都给全部机器记一条"上线"。
2. **同类合并**：`(host_id, code)` 在 60s 内的重复拒绝只把 `count+1` 并把 `ts` 前移，不追加行。
   一台没凭据的机器被 supervisor 反复拉起（H17）在 24h 里应当是**一行"被拒 34 次"**，不是 300 行。
3. **缺席只报一次**：每个缺席周期一条 `absent`；6h 内不重复播报（跨重启也成立，判据是 DB 里上一条同 host 的 `absent`）。节点回来后周期结束。
4. **不上采样噪声**：跨档来自 `alert_events`（已去抖），采样帧、心跳、`last_seen` 落盘都不进事件流。

保留：`event_retention_days` 默认 7 天（`history.json` 可覆写；raw 是 7d、1m 是 30d，事件比 1m 便宜但不该比 raw 更长寿地占着）。清理挂在 `history` 的保留任务上（`history.onCleanup(fn)` 注册，避免 `events → history → events` 的循环 import）。

## 4. 缺席可见（H12）——以及一处有意与设计稿不同的取舍

### 4.1 合成缺席记录

`store.getAnnotatedHosts()` 现在只遍历内存主机表。改为：名册里**未被退役、已确认（`confirmed=1`）**、主机表里没有、且 `last_seen` 距今超过 `presence.absent_after_minutes`（默认 3 分钟）的节点，合成一条缺席记录并入列表：

```
{ host_id, display_name, presence_class, kind, owner, site, agent_version,
  last_seen, online:false, metrics:null, absent_record:true }
```

三个条件各挡一类误报：`confirmed` 挡掉 S1 §4 从历史库里种子回来的僵尸 mock（它们从没被谁确认过）；
`absent_after_minutes` 挡掉重启瞬间 Agent 还在重连的几秒；主机表判重保证不与既有记录双计。

### 4.2 分母要进，健康度不进（关键取舍）

- **进**：`cluster.total` / `在线 n/m` / 灰色卡片 / 事件流那条"缺席已 n 小时"。这才是"机群有几台、现在在几台"的诚实答案。
- **不进**：`cluster.health` 的最差档归并，因此**不顶灰顶部横幅、不触发循环铃声**。

理由（对 H12 建议原文"直接进 OFFLINE 档"的偏离）：常驻机里有一台是**关着睡觉的**（142 夜里断电、朋友的机器带回家）是常态，不是事故。
今天它不报警仅仅是因为它"从没连过"——那是 H12 的 bug；把 bug 修掉就顺手让它天天把横幅钉成灰色 + 彻夜鸣响，等于用 H1 换 H12，
并把 S1 §3.1 的全部理由（"临时机把横幅钉死灰灯"）从后门放回来。**"它不在这儿"≠"它出事了"。**
真正该响的仍然是那台**在场后掉线**的机器——那条路径（`markDisconnected` → OFFLINE reason → 告警）一行未动。

实现沿用 S1 §3.2 已有的 `status.absent` 机制：`absent = host.absent_record || presence_class === 'ephemeral'` → 不产 reasons；
`evaluateCluster` 的 `levelForHealth` 对 `absent_record` 返回 null。卡片仍显 OFFLINE 灰档，文案是"缺席"（不是"离线"）。

### 4.3 名册视图与详情页

缺席记录是合成出来的，`GET /api/roster` 早已按名册出全部节点，故名册侧无需改。详情页对缺席节点沿用既有的"节点暂不可见"诚实说明（S2c 已验）。

## 5. 读接口

```
GET /api/events?window=24h|7d&host=<id>&limit=120
→ { window, from, events: [{ ts, kind, host_id, level, code, text, count?, ...detail }], truncated }
```

- 合并 `events` 表 + 由 `alert_events` 渲染的跨档行，按 `ts` 倒序，`limit` 上限 400。
- 文本渲染在服务端（同一份 code→中文映射也被接入页复用，避免两处措辞漂移）。
- **地址只到网段**（沿用 `maskAddr`）、**不输出任何 token/口令/指纹值**，只输出其状态；`detail_json` 里不放对端地址以外的可识别信息。
- 读侧无鉴权（决策 6 升版：读开放/写有口令）。事件里含"谁被拒了"，属机群运行事实，与既有 `/api/enroll` 同档。

## 6. S3b 前端（本文件先定契约，实现见 §10 实施记录）

1. **事件流面板**：总览右侧或独立 tab，24h/7d 切换，按 `kind` 轻过滤（全部/告警/在场/接入/名册），每行 = 时间 + 主机显示名 + 文本 + 级别色点。空态写"这 24 小时机群没有异常"（诚实空态，不是"暂无数据"）。
2. **sparkline**：卡片上的 2h CPU（或该卡主指标）迷你折线。数据来自**已有的** `/api/history/<id>?range=2h`——批量端点 `POST /api/history/batch` 若要做需另议（首屏 3~7 台 × 每 30s 一次，单请求 96 点，够用；不做预取合并）。
3. **delta**：同区间内"近 30 分钟均值 vs 前 30 分钟均值"，↑↓ 箭头 + 百分号，方向色**不借用四态色**（避免"红了才像出事"）。
4. **异常置顶**：卡片排序 = 级别档（CRIT>WARN>OFFLINE/缺席>OK）→ 同级内按负载。演示机群与真机同列同规则（它们已在分母之外，不参与健康度）。
5. **角色/归属编码**：卡角标 `owner`/`site` 只在非默认值（≠ `me`/`home`）时出现，默认不打扰。

## 7. 验收映射

| 判据/隐患 | 由什么满足 | 怎么测（自检段号，实跑见 §9） |
|---|---|---|
| 三件事之①跨档与翻转 | `alert_events` 渲染 + presence 事件 | 自检 D 段：直接写 `alert_events` 后 `events.read()` 出跨档/恢复/终止行，且 `events` 表内无 `alert_%` 行；F 段在真 Server 上读 `/api/events` |
| 三件事之②今天怎么变的 | sparkline + delta | §10.3 真机验证：真帧出图 + delta 空态按可画点数收敛（自检测不到呈现，只能实跑） |
| 三件事之③接入审计 | ingest/roster 事件落盘 | 自检 C 段（含 `detach()` → 重新 `attach()` 后旧行仍读得到 = H15 的正面回答）+ E 段写侧审计 + F5~F9 HTTP 侧 |
| H12 缺席可见 | 合成记录 + `absent` 事件 | 自检 B 段 + G 段边界；F1/F2 端到端：出现在 `hosts`、进分母、**不进 health**、不生成告警 |
| 缺席不得被读成"它回来了"（S3b 实测发现） | 第四种告警闭合 `cancelled:'absent'` | 自检 B6b~B6e：失联告警遇改判缺席 → 以 `absent` 闭合，事件流里读得到"改判缺席"、**读不到**"回到阈值内" |
| H17 被拒不再只在本地日志 | 合并计数的一行 | 自检 A2（同 code 连打 5 次 → 1 行 `count=5`）+ C2 |
| 不重新制造 H1 | absent 不进健康度 | 自检断言：唯一常驻机缺席时 `cluster.health !== 'OFFLINE'` 且 `total` 仍计入 |
| H9 不分裂真源 | 跨档只读 `alert_events`，无第二检测器 | 代码审查 + §2 表 |

## 8. 风险与回滚

| 风险 | 处置 |
|---|---|
| 事件表无界增长 | 保留 7d 且 `#cleanup` 必跑；被拒合并计数使最坏情况（一台机器 5s 一轮重启循环）≈ 每小时 1 行 |
| 缺席合成把已退役/僵尸 mock 拉回看板 | `confirmed=1` 是硬门槛；退役过滤在 `roster.active()` 之前已生效 |
| 重启后首轮事件风暴 | 启动基线（§3.1），首轮静默 |
| 事件流把地址/凭据带进只读 API | 只存 `maskAddr` 后的网段；断言扩到 `/api/events`：自检 E6（表内扫描）+ F8（HTTP 响应内扫描）均无明文密钥/口令 |
| 前端首屏拉 24h 历史变卡（既有 30d `getSeries` 隐患） | sparkline 只用 2h；不新增 JSON 列查询 |
| 回滚 | 事件写全是"旁路记账"，`events.record` 在 `history.enabled=false` 下 no-op；缺席合成有 `presence.enabled` 开关（默认开）可一键回到 S2 行为 |

## 9. 实施记录（S3a · 2026-09-19 本机完成，未上线）

### 9.1 改动清单

新增 3 个文件、改 8 个文件（服务端 + 配置 + 自检；**前端一行未动**，事件流的呈现是 S3b）。

| 文件 | 改了什么 |
|---|---|
| `server/src/events.js`（新，262 行） | 流本体：`attach(history)` 建表、`record()` 带 60s 同类合并与 `onceMs` 一次性语义、`read()` 合并 `events` + 由 `alert_events` 渲染的跨档行、`purge()` 挂保留任务、`render()` code→中文（文案集中处，接入页可复用） |
| `server/src/store.js` | `#absenceRecords()`（H12 合成缺席记录）+ `#presenceEvents()`（启动基线、`came_online`/`went_offline`/`absent`）；两者都挂在既有 `getAnnotatedHosts()` 上，REST 与 WS 快照同一路径 |
| `server/src/status.js` | `absent` 判定扩到 `absent_record`；`levelForHealth()` 对缺席记录返回 `null`（§4.2 的落点） |
| `server/src/ingest.js` | 三类写入点入账：`paired`、`legacy_accept`、`#log()` 里 `refused`/`drift`（地址一律 `maskAddr` 后入库） |
| `server/src/roster.js` | 写侧审计：改档/改名/静默/撤销/签发/口令/演示开关各一条；`setClass`/`setDisplay` 加 `silent` 选项 |
| `server/src/history.js` | `event_retention_days`（默认 7）+ `onCleanup(fn)` 钩子（`#cleanup()` 末尾按序跑，异常各自吞掉） |
| `server/src/index.js` | 启动时 `events.attach(history, { nameOf, reasonText })`；新增 `GET /api/events` |
| `server/src/config/thresholds.json` | `presence: { enabled: true, absent_after_minutes: 3 }` |
| `server/scripts/selftest-events.mjs`（新，427 行） | 见 §9.3 |

### 9.2 与设计稿不同处（都是实现期发现的、值得留档的偏差）

1. **`events` 表多了 `first_ts`**。合并计数会把 `ts` 前移，只留 `ts` 就丢失"这件事第一次发生在什么时候"。UI 上一行应当读作"09:12 起 34 次"，而不是"最后一次 11:47"。
2. **§3 的"缺席只报一次"用 `(code, host)` 上一条 DB 行的时间做判据**，窗口 6h（`oncePerEpisode`）。这样跨重启也成立——原来只写了"跨重启也成立"，没定机制。
3. **`went_offline` 只对 `ephemeral` 节点记**。常驻机在场后掉线，`alert_events` 里本来就有 OFFLINE 那一行（§2 的"一件事一处记"），再记一次就是自己打自己的脸。缺席（启动后从未回来）才是这里的新事实。
4. **只读响应比 §5 多一个 `persisted` 标志**（`truncated`/`count` 照给）。`history.enabled=false` 的开发模式下事件流必然为空，前端需要区分"确实没发生事"和"根本没落盘"——诚实空态（§6.1）依赖这个标志。`limit` 默 120、上限 400，窗口非法回落 24h（F11 断言）。
5. **文案里的节点名在读取时才取**（`nameOf`）。历史行存 `host_id`，改名之后旧行自然读作新名——这正是"不存中文句子"换来的东西，写侧只把 `from`/`to` 存进 `detail_json`。
6. **`roster → events → history` 的 import 环是靠"显式 attach"拆掉的**，不是靠懒加载。`events.js` 不 import `history.js`；`index.js` 把 `history` 递进来。自检里可以换库重挂（C6/C7），生产路径只有一处接线。

### 9.3 验证

`node scripts/selftest-events.mjs` → **pass=49 fail=0**（绿了才删临时目录，红了留证据）。分段：
A 记录/合并/一次性/保留天数（6）· B H12 合成与分母-健康度分离（8）· C 接入审计与跨进程持久（7）· D 跨档行只源自 `alert_events`（5）· E 名册写侧入账与"台账里无明文密钥"（6）· G `presence.enabled` 开关双向 + 未确认/刚注册两类误报边界（4）· F 真 Server 端到端（11，起子进程 9310/9311，含缺席卡、跨进程读到拒绝、配对码接入、错口令 403、HTTP 改档入账、按节点过滤、非法 window 回落）。

回归（同批实跑，全绿）：`selftest-roster.mjs` 100/100、`selftest-history.mjs` 10/10、`selftest-agent-pair.mjs` 14/14、`config-selftest.py` 16/16。
8 个改动文件逐个 `node --check` 通过。

自检期修掉的都是测试自身的坑（不是产品缺陷），留两条以防再犯：注册帧的凭据必须放进 `msg`（helper 的 option bag 不算），否则 legacy 档下就是 `unknown_host`；`gone-box` 必须在起子进程**之前**写进名册，因为 Server 启动时才把名册读进缓存——这条已经写成测试注释。

> S3b 在同一个自检里加了 4 条（B6b~B6e，见 §10.4 第 2 项），本段计数由 **49 → 53**。

### 9.4 已知未闭合

- **S3b 未做**（写这一节时的状态；现已由 §10 做掉）：`/api/events` 当时没有任何调用方，事件流在 UI 上看不见；缺席卡片沿用 S1b 的灰档呈现。§6 的 sparkline/delta/异常置顶同样当时未动。
- **真机验证未做**（S3a 当时全部证据来自本机一次性库与子进程 Server）：**§10.3 已补**本机真 Server + 真浏览器的结构验收；231 上的真部署仍未做（本期不重新部署，见迭代节奏约定）。
- **`last_seen` 的粒度**：名册按 30s 节流落盘（`SEEN_WRITE_MS`），缺席窗口 3 分钟，误差 ±30s 可接受；但它意味着 `absent_after_minutes` 不应低于 1 分钟，否则一次落盘抖动就能误判缺席。
- **合并窗口是固定 60s**，没有做成配置项。噪声实测成问题再放开（当前最坏情况：一台没凭据的机器反复重启 ≈ 每小时 1 行）。
- **事件流仍不含 `observed`（SSH 拉取）类节点**：那是 S6，且受 H11 凭据治理闸口约束，本期不碰。
- 回滚口子照 §8：`presence.enabled=false` 回到 S2 行为；`history.enabled=false` 时 `record()` 是 no-op，不写不读不崩。

---

## 10. 实施记录（S3b · 2026-09-19 本机完成，未上线）

本期第一次把改动放进**真浏览器 + 真 Server**跑，因此也是第一次抓到三类"自测全绿但呈现说错话"的问题（§10.4）。

### 10.1 改动清单

前端新增 4 个文件、改 7 个文件；服务端在验证中回改 3 个文件（呈现的错话有一半写在服务端文案层）。

| 文件 | 改了什么 |
|---|---|
| `client/src/lib/events.js`（新） | 事件流的**唯一**前端真源：一个 reactive store + 引用计数轮询（15s），窗口/类别状态共享；读失败保留上一次好数据；`useEvents()`/`eventsFor()` |
| `client/src/components/EventStream.vue`（新） | 面板本体：24h/7d、kind chips（可 `lockKind` 锁死，接入页用）、每行 = 色点 + 时间 + 节点名 + 文本 + `×N（HH:MM 起）`；四种诚实空态（未落盘 / 这台机器没事 / 机群没事 / 这一类没记录）；行可点钻取 |
| `client/src/lib/series.js`（新） | 卡片 2h 历史缓存：按卡序 `setInterest` + 30s 串行轮询（一次一个请求，`seq` 令牌丢弃过期批次）；失败照样盖 `fetchedAt`（否则退化成请求风暴）；`sparkOf()` 选负载最高的那一维（CPU/GPU/内存），`deltaOf()` 取四分之一窗对前半窗；`coords()` 空桶留洞 |
| `client/src/components/Sparkline.vue`（新） | 内联 SVG 迷你折线：**按洞拆段**（跨失联期直连正是这块面板不该讲的那种谎）；hover 给 2h 最小/最大/现值；方向色 `#8b5cf6`/`#0d9488`，不借四态色 |
| `client/src/lib/status.js` | 新增 `formatAgo()`、`unitFor()`（见 §10.4-1）、`ATTENTION_TEXT`、`attentionRank()`、`loadOf()`；`sortHostsForOverview()` 改为"关注度带 → 负载 → 角色 → 名字"，`LEVEL_RANK` 不再决定卡片顺序（那只是轻一版的 H1） |
| `client/src/components/HostCard.vue` | 接 sparkline（卡片只读不拉：拉取由视图发布关注集）；缺席文案分`缺席`/`离场`两支；空态收敛为一层（§10.4-6） |
| `client/src/components/HudPanel.vue` | 本机事件（`host_id` 过滤、12 行）放在 v-if/v-else 链**之外**，缺席与失联节点也看得见它；有事件流时不再重复"本机告警"那段；缺席措辞分开 |
| `client/src/views/OverviewView.vue` | 右栏 `.ov-rail`（告警面板 + 机群事件）；`watch(sorted)` 按卡序发布关注集，挂载起轮询、卸载停 |
| `client/src/views/EnrollView.vue` | 接入选 `lockKind="ingest"` 的事件流替换原来的内存环形缓冲列表；"本次启动累计"降级为补充行（跨进程事实已经由事件流承担） |
| `client/src/views/DetailView.vue` | 级别词与钻取遮罩区分缺席/离场 |
| `client/src/components/AlertBanner.vue` | 顶栏横幅改走 `unitFor()`（原来写死 `%`） |
| `server/src/alerts.js` | 缺席闭合：`cancelled='absent'`（§10.4-2） |
| `server/src/events.js` | 失联三句专用文案 + OFFLINE 事件按 crit 上色 + `stale` 措辞改准（§10.4-2/3/4） |
| `server/src/roster.js` | 首次设置口令降为 info，换口令才 warn（§10.4-5） |
| `server/scripts/selftest-events.mjs` | +4 条（B6b~B6e）钉住"缺席不是恢复" |

### 10.2 与 §6 契约不同处

1. **排序带把"缺席"独立成一档**，位置在 OFFLINE 之后、OK 之前（§6.4 写的是"OFFLINE/缺席"同档）。理由：缺席不是故障，与真失联同档等于把"它不在这儿"和"它出事了"印成同一句话。临时在线与离场各占最低一档。
2. **§6.5 的 `owner`/`site` 角标没做**，推到 S6。当前没有任何写入口（名册默认 `me`/`home`），"非默认才出现"意味着永远不出现——先做一个能写的入口，再谈角标。
3. **delta 的"30 分钟"是按返回点数取 1/4 窗**，不是硬编 30 分钟。满 96 点时正好 30min vs 30min；点数不足 4 直接不显示（Server 刚起时那两小时本来就没有内容）。
4. **批量端点仍然不做**（与 §6.2 一致）。实测量级：5 卡 × 每 30s × 单请求，SQLite 侧毫无压力，多一条查询路径却是长期维护成本。
5. **三处事件流共用一个 store**，不是每个面板各自轮询。`?host=` 服务端过滤已具备（F10），当前在客户端过滤——机群一旦超过 400 行窗口，把过滤下推即可，不必改契约。

### 10.3 真机验证（首次）

环境：本机一次性库（`E:/DSH-workspace/00-暂存/s3b-verify/`，`HM_ROSTER_DB` + `HISTORY_CONFIG` 指过去，`server/data/` 全程未碰），`HM_DEMO=1`、`HM_INGEST_TOKEN=legacy`、9100/9101。真 Agent 用 Node 22 全局 WebSocket 写了个探针：配对一个常驻确认节点 `office-desk`，上报 50 秒后永久静默。

实跑看到的：

- **缺席卡**：`缺席 · 上次在场 4 分钟前`＋`计入在线率，不计入集群健康度`，排序第 3（CRIT、WARN 之后，OK 之前），不画 sparkline；HUD 给 `该常驻节点自 Server 重启后未再上报 · 上次在场 16:42（6 分钟前）`。集群健康度横幅确实不因它变灰（顶栏仍只剩 1 项严重）。
- **事件流三处呈现**都有内容；kind chips 客户端过滤正确（告警 4 行 / 全部 18 行）；24h↔7d 切换、⟳ 立即刷新均生效。
- **合并计数的两难分界跑通了**：同一 `host_id` 连打 6 次被拒 → **1 行 `×6`**；换 5 个不同 `host_id` → **5 行**。这正是 §3 想要的区分（一台机器的重启循环是一件事，五台陌生机器是五件事）。
- **跨进程持久**：Server 连开五次，`/api/events` 始终读得到前几个进程写的行（H15/H17 的活证据，不再只是自检断言）。
- 探针自身踩的两条（记下来防再犯）：改档接口字段是 `cls` 不是 `class`；Node 的全局 `WebSocket` 是浏览器 API，没有 `.on()`。

**验证限制（必须说清）**：内置浏览器页面表面不可见（viewport 0×0），截图与真实点击不可用，改判据为"DOM 结构 + 程序化 click + 计算样式"。因此**视觉美观、观看距离、kiosk 密度都未验收**，与 S4 的视觉契约一起做。粘性右栏在 <900px 回落为静态流，这条只在 CSS 层确认，未在真窄屏确认。

### 10.4 验证期发现并修掉的问题（都是"讲错了事实"，不是样式）

1. **顶栏把温度印成百分比**：`模拟-朋友的游戏机 CPU温度 95%`。横幅自己写死了 `%`（只有 `offline` 例外）。改法是把单位规则收进 `status.js:unitFor()`，`formatReason()` 与横幅共用——一件事一处定义，与 §2 同一条原则。
2. **缺席被说成"恢复了"（本期最严重的一条）**：Server 重启 → `restoreActive()` 把旧的 失联 告警放回 → 缺席合成让该节点不再产出事由 → 引擎按"连续 N 轮未见事由"走了**恢复**分支，事件流写下 `在线状态（heartbeat）回到阈值内（204）`，而 204 秒正是它当时的失联时长——正好是反的那句话。修：缺席记录是一等事实，给它第四种闭合 `cancelled:'absent'`（`alerts.js` 用缺席集合判定，`events.js` 写"失联告警终止：改判缺席"，`status.js:resolvedText` 写"已改判缺席·不再报警"）。自检 B6b~B6e 钉住，且**断言读不到"回到阈值内"**——只断言新句子上线、不断言旧句子消失，是这类修复最常见的假绿。
3. **失联的触发行自相矛盾**：`越过阈值 阈值 15 触发值 3`（OFFLINE 跳过防抖，记的是触发瞬间的陈旧秒数，3 < 15）。`metric==='offline'` 时改走三句专用文案：`节点失联（超过 15s 无上报）` / `节点恢复上报，失联告警结束` / `失联告警终止：…`。
4. **失联在事件流里是灰点**：级别映射只认 CRIT/WARN，`OFFLINE` 落到 info。它与横幅、卡片、铃声同属最高档，色点跟着升为 crit。
5. **首次设置管理口令被标成琥珀色**：审计行该在，严重度不该。`was_set` 为假（从无到有）时降为 info，替换已在用的口令才 warn。
6. **卡片历史空态有两层**（折线自己给 `—`，卡片又给"2h 无历史"）：`sparkOf()` 增加 `drawn`（可画点数），卡片按它决定画线还是给理由。原来的 `points.length` 判据会被"两个空桶"骗过。

### 10.5 一条被实测澄清的语义边界（新增隐患 H18）

缺席与失联**不是同一件事的两个阶段，而是两条路**：

- Agent 在本进程运行期间掉线 → 实时记录 `online:false`，永远是 失联（CRIT、进健康度、进分母）；`store.hosts` 不删行，所以它不会自己变成缺席。
- 只有 Server 重启（或该节点本轮从未上报）且距 `last_seen` 超过 `absent_after_minutes` → 才合成缺席。

后果：**关机过夜的常驻机器，在 7×24 运行的 231 上会响一整夜**——这正是 H12 想关掉的那半个场景。S3 关掉的是"跨重启的静默"，没关"进程内的长静默"。已记为 **H18**，建议处置进 S5 闸口批：进程内失联超过一个更长阈值（建议 15 分钟）后降级为缺席（同样以 `cancelled='absent'` 闭合、卡片措辞不变、健康度剔除），并把 `absent_after_minutes` 与这个降级阈值一起做成可配。

### 10.6 已知未闭合

- 视觉验收未做（见 §10.3 限制），与 kiosk 一并落在 S4。
- 事件流不含 `observed`（SSH 拉取）类节点：S6，且受 H11 凭据治理闸口约束。
- `enroll_codes` 死行清理仍挂 S5（H15 残留）。
- 合并窗口 60s 仍非配置项。
- 事件流面板没有"点开一行就把详情曲线定位到那一刻"（行里已带 `ts`，缺的是详情侧的游标入口），属 S4/S6 打磨。
- `DrawerV3.vue` 的 `产品方案 §1.3` 仍漏在用户可见文案里（HUD 那条本期已清）；等 S4 文案统一时一并处理。
- 探针脚本 `00-暂存/s3b-verify/{probe-agent,noise-agent}.mjs` 在仓库外、不随包分发；下次真机验证可复用（要求 Server 在 9100/9101，且口令处于未设置态时才能建口令）。
- 回滚：S3b 纯呈现 + 一个告警闭合分支。撤客户端即回到 S3a 外观；`presence.enabled=false` 仍是那条总闸（缺席不再合成 → `absent` 闭合自然不触发）。
