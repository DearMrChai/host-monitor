# S1 细化设计 — 机群名册最小版 + 健康度语义修正

> 状态：**S1a（后端）已实现并自测通过，S1b（前端呈现与操作）进行中**（2026-09-19）。实施期遵守迭代节奏：本机自测，不重部署 231。实施记录与偏差见 §10。
> 上游依据：`V2-迭代讨论纪要-20260919.md` §5.4 的 S1、`技术隐患清单.md` H1/H10。
> 一句话目标：**把"当前连着的就是我的机群"改成"我有名册的机群"**，让临时机/他人机不再污染集群健康与在线率——这是对外展示时最掉价的缺陷。

---

## 1. 范围

### 做
- `nodes` 表（机群名册）与其读写层，含 `presence_class` / `kind` / `owner` / `site` / `settings_json`
- 集群健康度与在线率的**参与规则**改造（核心）
- 节点呈现新增第四种形态：**离场（ABSENT）**——只呈现，不告警、不进归并
- 卡片上的最小操作：常驻 / 临时 / 退役 / 静默（2h、今天、取消）
- **写侧管理口令**（因为本阶段第一次出现写接口）
- 存量 3 台的**无损入册迁移**

### 明确不做（含归属阶段）
| 不做 | 为什么 | 归哪 |
|---|---|---|
| ingest token **强制启用** | 启用即要求同时改 3 台生产 Agent 的启动参数（计划任务 / systemd unit），**违反"迭代期不重部署生产"的节奏约束**。本阶段只在 register 报文里**埋 `token` 可选字段并落库存证**，不校验、不拒绝 | S2（那时本来就要重做 Agent 配置与部署物） |
| `agent.json` / Agent 侧配置化 | 同上（要动生产） | S2 |
| per-node 阈值覆盖 | 名册的 `settings_json` 已为它留位，但告警求值改造与它一起做得更顺 | S3 |
| 配置单一真源 + 热加载（H9） | 与设置界面绑定才有意义；本阶段 JSON 文件真源地位**不变** | S6 |
| 完整设置页 / 加机向导 | S2 交付 | S2 |
| machine-id / BIOS 指纹强校验（H4） | 需要 Agent 上报，同生产改动问题；字段先留 `NULL` | S2 |

**双真源的临时性声明**：本阶段结束后，节点档案在 DB、阈值与探测计划在 JSON。这是**已知且登记过的过渡态**（H9），S6 收口。

---

## 2. 数据模型

> **实现修正（S1a）**：本表最终落在**独立的 `server/data/roster.db`**，不是 `host-monitor.db`。
> 原因：历史库每 2s 有一次写入脉冲，名册写入是"用户动作 + 节流 last_seen"级别；分文件后两条写路径不抢同一个 WAL 写锁，且名册可以单独备份/删除（它是档案，不是数据）。
> 历史库只在**启动时**被再打开一次做种子查询。代价：多一个文件、多一个 `HM_ROSTER_DB` 环境变量（自测用）。

```sql
CREATE TABLE IF NOT EXISTS nodes (
  host_id        TEXT PRIMARY KEY,          -- 沿用现有 Agent 推导值，语义不变（改名=改 display_name）
  display_name   TEXT NOT NULL,
  role           TEXT,                      -- db/inference/desktop/laptop/display/other（沿用 v1 枚举）
  owner          TEXT DEFAULT 'me',         -- 归属；'me' | 朋友标识。影响呈现与静默策略
  site           TEXT DEFAULT 'home',       -- 网络位置；S6 的探测计划按它派生（本阶段只存不用）
  kind           TEXT DEFAULT 'agent',      -- agent | demo(S2) | observed(S6) —— 提前留字段，避免日后迁移
  presence_class TEXT NOT NULL DEFAULT 'persistent',  -- persistent | ephemeral | retired
  confirmed      INTEGER DEFAULT 0,         -- 实现新增：见下方说明
  enrolled_at    INTEGER,
  last_seen      INTEGER,                   -- 独立于内存 store：Server 重启后仍知道"上次见到是什么时候"
  agent_version  TEXT,                      -- 本阶段可为 NULL，S2 起上报
  enroll_token   TEXT,                      -- 实现新增：S1a 只存证不校验，S2 起用于配对
  fingerprint    TEXT,                      -- 本阶段恒 NULL，S2 起启用（规则：禁止硬件清单派生）
  settings_json  TEXT NOT NULL DEFAULT '{}' -- 预留：muted_until / thresholds_override 等
);
```

`confirmed` 是实现时新增的一列，设计里没写但缺它整个引导条不成立：
名册里的每个节点都需要一个"用户看过并认可这个归类"的位，否则**已确认的节点下次启动又出现在待确认列表里，永远点不完**。
任何一次显式的 class 设定（常驻/临时/退役）都视为确认。

`settings_json.mute` 结构：`{ "until": <epoch_ms> | "today" }`（设计里的 `reason` 字段留空待定）
—— 用 JSON 扩展列而非加列，是为了 S3/S5 加 per-node 阈值、静默理由时不再改表。

## 3. 语义规则（本设计的全部难点都在这三节）

### 3.1 在线率与集群健康：谁参与归并

| presence_class | 总览出现 | 计入在线率分母 | 参与 `cluster.health` 归并 | 失联 >15s 时 |
|---|---|---|---|---|
| `persistent` | 是 | **是** | **是** | `OFFLINE` 告警 + 声音（现状行为保持） |
| `ephemeral` | 是（"离场"呈现） | 否 | 否 | 记 `absence` 事件，**不告警、不响铃、不进横幅** |
| `retired` | 否 | 否 | 否 | 无任何行为；历史仍可按 host_id 查（30d 内） |

新公式：
- `cluster.online / cluster.total` = **persistent 节点** 中 在线/总数
- 新增 `cluster.presence = { online: n, total: m }` = ephemeral 的在场计数（仅用于显示"在场 +1"）
- 横幅文案：`在线 3/3` 改为 `在线 3/3`（分母语义变了但数字看起来一样）+ 若有 ephemeral 在场则追加 `· 在场 1`

> `evaluateCluster` 的 `health` 只遍历 `presence_class === 'persistent'` 的节点。**这一条是 H1 的根治点。**

### 3.2 ABSENT（离场）不是第五个告警等级

实现上刻意做成**呈现层状态**，不进 `RANK`：
- `status.js` 在生成 offline reason 时按 class 分流：persistent → `metric:'offline'`（进 reasons，被告警引擎接管）；ephemeral → `status.absent = true` 且**不产生 reason**。
- 卡片：置灰 + "离场 · 上次在场 14:32"（`last_seen` 来自 DB，Server 重启也答得出——这是现状做不到的）。
- 告警列表、横幅、声音、`worstLevel` 全部不受影响。

### 3.3 静默（mute）语义

- 生效范围：该节点的**全部告警**不进横幅/列表/声音，仍进事件流（S3）与历史，节点卡片显示 🔕 与到期时间。
- **不静默 OFFLINE**（persistent 节点失联是可用性事实，静默它会掩盖真故障）。若以后需要，走 `retired` 或 `ephemeral`。
- 实现（S1a 落地版，比原设计更强一点）：`alerts.js` 对静默节点**整条状态机跳过**——既不建 pending、也不推进、也不老化消解，因此已 active 的告警是**冻结**而不是"恢复"；解除静默后它照原样回到可见列表（`getLists()` 只是不输出它）。原设计写的"跳过 activate()"不够：那样静默期间的告警会被老化成 resolved，等于伪造恢复。
- 静默集合每帧重算（`tick()` 里 `muteSet()`），不能只在去抖闸门通过时算——否则用户刚点的 🔕 最多要 1.8s 后才在界面上生效（自测 E6 抓到过这个）。

## 4. 存量迁移（无损，幂等）

Server 启动流程：
1. 建表；
2. 若 `nodes` 为空 → `seedRosterFromHistory()`：
   `SELECT host_id, MAX(ts) FROM samples_1m GROUP BY host_id`（并集 `samples_raw`）→ 每个历史出现过的 host_id 插入为 `persistent`，`display_name` 取 `store` 缓存不可用时的 host_id，`last_seen` 取该 host_id 的最大 ts。
3. 之后任何 register：host_id 不在 `nodes` → 自动插入为 `persistent`（**保持现行为，零惊喜**），同时在看板顶部挂一条引导："发现新节点 `<hostname>` → 设为临时 / 保持常驻"。这条引导是给 35B、朋友机器准备的。

**种子不会造成告警风暴（实测确认，别被"8 个节点"吓到）**：内存 `store` 才是节点出现与否的入口，名册行不在 store 里就既不上卡片也不产生 OFFLINE 告警（自测 E9 断言了这一点：重启后无 Agent 连接 → `hosts=0`、健康度 OK）。
但**引导条会**被老种子刷屏：本机开发库实测种出 8 个 host_id，其中 7 个是历次迭代留下的 `mock-*` 假机器，它们永远不会回来。
所以 `unconfirmed()` 加了 **24 小时新鲜度闸门**（自测 A8）——只有近一天内见过的未确认节点才挂引导；老行留在名册里（历史可查、host_id 不复活冲突），但不打扰人。

**验收要求**：迁移前后 `laptop-dev / infer-142 / srv-231` 三个 host_id 不变、历史曲线连续、当前告警状态不变。

## 5. API 与写侧口令

### 5.1 新接口（全部挂在 9101，实现后的实际签名）
```
GET  /api/roster                     → { nodes, retired, admin:{passphrase_set} }   无需口令（读侧仍全开放）
POST /api/roster/:hostId/class       → { cls }                    需口令（cls: persistent|ephemeral|retired）
POST /api/roster/:hostId/mute        → { hours } | { cancel:1 }   需口令（hours 传字符串 'today' = 今天之内）
POST /api/roster/:hostId/name        → { name }                   需口令（改名是四个卡片操作之外的必需项）
POST /api/admin/passphrase           → { passphrase, old }        不需要口令（见下）
```
两处与设计不同，都是实现时必须定的：
- **`/api/admin/passphrase` 不在 `requireAdmin` 里面**——否则第一次设口令就成了死锁（要设口令得先有口令）。它的自我闸门是：库里已有口令时**必须带对旧口令**才允许改（自测 D6）。
- 静音入参用 `hours`/`cancel` 而不是裸 `until`：客户端不该拼 epoch 毫秒，"今天之内"由服务端解释（`'today'` → 当日 24:00，自测 A10），跨端时钟差不再是变量。

### 5.2 口令：默认拒绝，而不是默认放行
- DB 里存 `scrypt` hash + salt（`node:crypto`，零新依赖）。
- **未设置口令时，三个写接口一律 403**，看板上的操作项置灰并提示"先设置管理口令"。
  - 理由：S1 第一次引入写能力，默认开放=路过的人能点"退役"你的节点；默认拒绝=你自己启动后花 10 秒设一次。
- 设好后存 `localStorage`，写请求带 `x-hm-admin` 头。不引入 session/cookie（个人工具，过度工程）。
- 口令不进任何 GET 响应；校验失败只回 403，不回"口令错误原因"。
- **诚实说明**：这防的是误操作与熟人路过，不防主动攻击者（无 TLS、内网明文）。真正的对抗性防护只在形态 B（公网）才需要，而形态已定为 A。

### 5.3 广播帧变更
`store.getSnapshot()` 的 `hosts[i]` 增加 `presence_class / display_name / owner / absent / muted_until`；`cluster` 增加 `presence`。
**兼容性**：老前端遇到新字段忽略即可；本阶段前后端同仓同版，不做双版本兼容。

## 6. 模拟/演示节点（本阶段只定契约，实装在 S2）

- `nodes.kind = 'demo'`，`display_name` **强制带前缀**：`模拟-<角色>` 或 `效果设备-<名字>`（Server 侧在 register 路径上对 `kind='demo'` 的名字做校验/自动加前缀，不靠人自觉）。
  **S1a 已实现的是这一条**（`roster.ensure(kind:'demo')` → `模拟-` 前缀，自测 A5）。
- 全局开关 `include_demo`：**S1a 未做，随 S2 的模拟机群一起做**。理由很直接——本阶段还没有任何 demo Agent 会连进来，先做一个永远为真的开关是自欺；而 S2 的模拟机群落地时必然要改 `getSeries`/聚合两处，那时一并做更省。
  红线不变：**S2 交付的演示模式必须自带这个开关，缺开关的演示不算完成**。
- 红线（决策 4 延伸）：demo 节点不得伪装成真实设备，任何聚合数字都要能回答"这里面有几台是模拟的"。

## 7. 影响面清单（改哪些文件）

| 文件 | 改动 |
|---|---|
| `server/src/store.js` | 注册/指标更新时同步 `nodes.last_seen`；提供 `annotateWithRoster()`；`retired` 节点从 `getAnnotatedHosts()` 过滤 |
| `server/src/status.js` | `evaluateCluster` 只归并 persistent；offline reason 按 class 分流；新增 `presence` 统计 |
| `server/src/alerts.js` | mute 跳过 activate；absence 不入键 |
| `server/src/roster.js`（新） | 建表/迁移/CRUD/口令校验；`settings_json` 读写 |
| `server/src/index.js` | 4 个新路由；启动时迁移；snapshot 富化 |
| `client/src/views/OverviewView.vue` | 卡片 ⋯ 菜单、在场计数、新节点引导条、口令设置弹层 |
| `client/src/components/HostCard.vue` | display_name 与 ABSENT 呈现、🔕 角标、demo 前缀 |
| `client/src/lib/status.js` | 排序与色码不新增等级（ABSENT 复用灰），仅加 mute/demo 的展示分支 |

**不动**：`agent/*`（全部生产改动推到 S2）、`history.js`（表结构不变）、`prober.py`、`thresholds.json`、`probes.json`。

## 8. 验收（本机 dev 环境，逐条可跑）

1. 名册里把 `laptop-dev` 设为 `ephemeral` → 停掉它的 Agent → **集群横幅保持 OK、无声音、卡片显示"离场 · 上次在场 xx:xx"**；
2. 恢复为 `persistent` → 再停 → 立即 OFFLINE + 横幅 + 声音（原行为未退化）；
3. 设为 `retired` → 卡片消失、分母 -1、`GET /api/history/laptop-dev?range=2h` 仍返回数据；
4. 重启 Server → 名册仍在（现状是重启清空）、三台自动回册、无幽灵；
5. 未设口令时点"退役" → 403 且按钮置灰提示；设口令后正常，刷新页面不需重输；
6. 清库冷启动（无历史）→ 首次注册即入册，不报错、不重复插行；
7. `node --check` 全绿 + 三关自查（boot-check 脚本），生产 231 不动。

## 9. 我判断本阶段最大的风险

**语义变更会让用户已有的"红/灰直觉"位移**：ephemeral 化后失联不再报警，如果你把某台你以为"该报警"的机器标成临时，就会漏报。**缓解**：卡片上永远显示 class 徽标（常驻/临时/已退役），不允许隐式；退役/临时操作有二次确认。**不要为了少两个徽标而把呈现做干净**——这是这个产品能不能被信任的地方。

## 10. S1a 实施记录（后端，2026-09-19）

**自测**：`cd server && node scripts/selftest-roster.mjs` → **45 项全绿**。它自己起一个一次性 Server（`HM_AGENT_PORT=9300`/`HM_CLIENT_PORT=9301`，端口现可用环境变量覆盖）、用真实 `ws` Agent 连接、跑在系统临时目录的两个 throwaway DB 上，**不碰 `server/data/`**。P5 老套件 `selftest-history.mjs` 同步回归 10/10 仍绿。

分组：A 名册数据层（种子/自动入册/显示名不被覆盖/demo 前缀/退役/静默三种入参/新鲜度闸门/`publicOf` 不泄漏 token 与指纹）· B 语义纯函数（常驻 vs 临时失联、健康度分母、聚合仍含临时机）· C 告警引擎（去抖 3 周期、静默不建 pending、冻结不消解、解除后可见）· D 口令（默认拒绝/长度/改要旧口令/scrypt 落盘）· E 真 Server 端到端（注册入册→改类别→离场→静默→退役→重启→重连改名保留）。

**代码改动面**：新增 `server/src/roster.js`（建表/种子/CRUD/口令/公开投影）、`server/scripts/selftest-roster.mjs`；改 `status.js`（offline 按 class 分流 + `evaluateCluster` 只归并 persistent + `presence`）、`store.js`（register/markSeen 挂名册、retired 过滤、快照富化 `admin`/`new_nodes`/逐节点名册字段）、`alerts.js`（`muteSet` 每帧重算、静默冻结、offline 等级直通、`getLists` 隐藏静默项）、`index.js`（端口可覆盖、register 转发 token/agent_version/fingerprint/kind 但**不校验**、5 个新路由 + `requireAdmin`）。Agent 与前端在 S1a **零改动**。

**自测暴露并修掉的两个真问题**（不是测试写错）：
1. `E6`：静默集合只在去抖闸门通过时重算 → 刚点的 🔕 最多 1.8s 不生效。改为 `tick()` 每帧算。
2. 本机开发库实测种子（§4）：8 个 host_id 里 7 个是历史 `mock-*` → 引导条会被死机器刷屏。加 24h 新鲜度闸门。

**验收对照**（§8）：1/2/3 的**语义部分**由 B+C+E 覆盖，但"卡片显示离场、按钮置灰、声音"属 S1b 前端；4 由 E8/E9 覆盖；5 的接口部分由 D+E3 覆盖，"刷新页面不需重输"是 localStorage，归 S1b；6 由 A3 覆盖（冷启动=空临时库）；7 `node --check` 全绿，生产 231 未动。

**遗留到 S1b/S2 的诚实清单**：`include_demo` 开关（S2）；`mute.reason` 字段（S3 事件流一起用）；ingest token 强制（S2）；per-node 阈值（S3）；名册备份与 `roster.db` 的运维说明要在 S6 写进部署文档。
