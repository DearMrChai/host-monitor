# 局域网主机监控系统 — 产品设计方案 v1

> 状态：**评审通过，待实施**（2026-09-18 固化自上一会话评审稿）
> 前置阅读：`交接文档.md`（核心纠偏一节）、`项目阶段性汇报.md`（v0.4 技术资产）
> 本文档为产品层契约：实施阶段（P1–P5）按本文执行，偏离需先回改本文档。

---

## 0. 产品定位与用户（设计出发点）

- **首要用户**：个人自托管运维者（本人），值守异构内网集群：笔记本 / 台式机 / 数据库主机 / vLLM 推理服务器 / 老机器展示端。
- **核心使用场景**：浏览器或副屏**常驻盯屏**（值守态），异常时钻取定位；偶尔投屏展示。
- **3 秒必答问题**（所有视图围绕这三问取舍）：
  1. 集群现在健康吗？（一眼红绿灯）
  2. 哪台节点不对？（异常节点置顶/高亮）
  3. 是设备本身的问题还是链路的问题？（节点状态 vs 通信状态分离）
- **告警渠道（已确认）**：第一期仅页面内分级高亮 + 声音，不引入 IM/webhook 外部依赖。
- **原则**：3D 数字孪生降级为展示外壳；信息呈现以"2D 数字为主、颜色/动效为辅"，拟真度排最后。

---

## 1. 指标体系（三层）

### L1 集群层（总览所需）

| 指标 | 定义 | 数据来源 | 现状 |
|---|---|---|---|
| 在线率 | 在线节点 / 配置节点总数 | Server 15s 离线判定 | 已有 |
| 集群健康等级 | 所有节点状态取最差档（OK/WARN/CRIT/OFFLINE） | 由 L2 派生 | **缺失，需新增派生逻辑** |
| 节点间 RTT / 丢包率 | 节点↔节点链路质量 | Agent 主动探测 | **能力缺口，需新增（见 §1.4）** |
| 集群聚合负载 | 全集群 CPU/内存/GPU 平均与峰值占用 | 各 Agent 汇总 | 可派生 |

### L2 节点层（单机健康）

- **节点级**：综合状态、开机时长、系统负载、**磁盘空间占用（缺口，psutil 可补）**
- **组件级**（沿用 v0.4 已有采集）：
  - CPU：占用 / 温度 / 频率
  - GPU：多卡 占用 / 显存 / 温度
  - 内存：池占用率（整池语义，见 §1.3 "不做"项）
  - 网络：网卡上/下行速率

### L3 明细层（钻取所需）

- CPU 分核占用
- 每 GPU 卡明细
- 每磁盘：容量占用 / IOPS
- 每网卡明细
- 链路明细：本节点↔网关、↔Server、↔关键节点（DB/推理机）的 RTT / 丢包 / 带宽利用率

### 1.3 科学性约束（维持 v0.4 结论，不重开）

- **不做单条内存占用**：双通道 interleaving 按 cache-line 跨条分布，单条"存了多少"科学上不可测、也无需区分；OS 只有总用量。内存一律按池呈现。

### 1.4 探测需求说明（针对"节点间通信"能力缺口）

只定需求，实现细节在 P3 阶段设计细化时确定。

- **第一期：星型探测**
  - 每个 Agent 周期 ping Server；Server 汇总推算节点间连通性（A↔B 质量 ≈ 经 Server 拼合，能识别"单臂断链"）。
  - 探测对象：Server、默认网关、以及配置指定的关键节点（DB / 推理机）。
- **第二期（P5 可选）：mesh 探测**
  - Agent 两两直测；由 Server 下发探测计划（错峰、限并发），避免探测风暴。
- **共同需求**：
  - 探测周期 5–10s；
  - ICMP 失败时降级为 TCP 端口探测（连接成功即视为通，测 TCP 握手 RTT）；
  - Windows/Linux 统一在 Agent 内实现，**不依赖系统防火墙例外配置**；
  - ⚠️ 评审遗留确认项：实际网络环境是否允许 ICMP/任意 TCP 端口探测（P3 开工前验证一次）。

---

## 2. 告警体系

### 2.1 状态机（四态，全局统一色码）

`OK 绿` / `WARN 黄` / `CRIT 红` / `OFFLINE 灰`

节点状态 = 其组件与链路状态的最差值（**自下而上归并**）；集群健康等级 = 所有节点状态的最差值。

```
                 ┌───────────────┐
   连续 N 周期    │   恢复正常     │
   超 WARN 阈值  ▼   (连续 N 周期  │
┌──────┐ ──────► ┌──────┐ ◄───── │ ┌──────┐
│  OK  │          │ WARN │ ──────┼─┤ CRIT │
│ 绿   │ ◄──────  │ 黄   │ 超 CRIT│ │ 红   │
└──────┘  恢复    └──────┘  阈值 └─┘      │
   ▲                                       │ ◄─ 超 CRIT 阈值（跳过 WARN）
   └───────────────────────────────────────┘
                                           
任何状态 ──心跳缺失 >15s──► OFFLINE（灰）──重新上报──► 按阈值重新评定
```

- WARN→CRIT：连续 N 周期超 CRIT 阈值；OK→CRIT 可直接跳档（如温度骤升）。
- 降级（CRIT→WARN→OK）同样需满足持续窗口，防抖。

### 2.2 阈值基线（第一期：硬编码默认值 + JSON 配置覆盖）

| 指标 | WARN | CRIT |
|---|---|---|
| CPU 占用 | >80% | >95% |
| CPU 温度 | >75°C | >85°C |
| 内存占用 | >85% | >95% |
| 磁盘空间 | >80% | >90% |
| GPU 温度 | >80°C | >93°C |
| 节点间 RTT（内网） | >5ms | >50ms |
| 丢包率 | >1% | >5% |
| 节点失联 | — | >15s 即 OFFLINE |

### 2.3 防抖与抑制规则

- **持续窗口**：连续 N 个采样周期（默认 3 次）超限才触发；恢复同理。避免抖动刷屏。
- **归并**：同节点同类告警合并为一条；组件告警不单独占列表位，归入节点告警明细。
- **告警生命周期**：触发 → 持续（显示时长）→ 恢复（进入历史，5 分钟后折叠）。

### 2.4 异常了怎么办（告警可达性设计）

1. **CRIT / OFFLINE**：顶部告警横幅 + 节点卡片红/灰边框 + **声音**
   - CRIT：循环提示音；WARN：单次提示；
   - 右上角静音开关；
   - 浏览器自动播放策略：首次点击页面解锁音频（UI 上明示"点击激活声音提醒"）。
2. **钻取**：点击告警条目 → 直接跳转该节点 V2/V3 视图并高亮异常组件。
3. **告警历史**：Server 内存环形缓冲；持久化列入 P5 可选，第一期不做（重启即失，已知并接受）。

---

## 3. 信息架构（两级钻取 + 全局框架）

```
V1 集群总览（首页，值守态）
 ├─ 顶部：集群健康红绿灯横幅 + 在线率 + 聚合负载条 + 静音开关
 ├─ 主体：节点卡片网格（每卡：主机名 / 角色标签 / 四态色 /
 │        CPU·内存·GPU 迷你条 / 磁盘 / 该节点链路状态角标）
 ├─ 侧栏：活动告警列表（按级别排序，点击钻取）
 └─ 钻取 → V2 节点详情
V2 节点详情
 ├─ 左：3D 主板数字孪生（复用 v0.4 资产，作外壳）
 ├─ 右：2D 指标面板（数字为主：组件读数、温度、链路 RTT/丢包）
 └─ 钻取 → V3 组件/链路抽屉（当前明细；历史曲线待 P5 持久化阶段）
```

- **展示态**：V1 全屏即大屏模式（卡片放大、隐藏侧栏细节）——同一页面两种密度，不单独做大屏版。
- **角色标签**（DB / 推理 / 台式 / 笔记本 / 展示端）：Agent 注册时人工配置，影响卡片排序权重（关键节点异常时置顶）。

### 3.1 视图线框描述

**V1 集群总览（2D 卡片网格，值守主界面）**

```
┌──────────────────────────────────────────────────────────────┐
│ [● 集群健康:WARN]  在线 5/6   ▓▓▓▓░ CPU 62% ▓▓░░░ MEM 41%  [🔊] │  ← 顶部横幅
├──────────────────────────────────────────┬───────────────────┤
│ ┌──────────┐ ┌──────────┐ ┌──────────┐  │ 活动告警 (2)       │
│ │infer-142 │ │ db-main  │ │ desk-01  │  │ ● CRIT infer-142   │
│ │[推理] 红框│ │[DB]  绿  │ │[台式] 绿 │  │   GPU2 89°C 12m    │
│ │CPU ▓▓▓░  │ │CPU ▓░░░  │ │CPU ▓▓░░  │  │ ○ WARN desk-01     │
│ │MEM ▓▓░░  │ │MEM ▓▓▓░  │ │MEM ▓▓░░  │  │   内存 87% 3m      │
│ │GPU ▓▓▓▓  │ │DISK ▓▓░░ │ │DISK ▓░░░ │  │                    │
│ │链路 ✓ ↗3 │ │链路 ✓ ↗1 │ │链路 ⚠ ↗5 │  │ 恢复记录(折叠)      │
│ └──────────┘ └──────────┘ └──────────┘  │                   │
│ ┌──────────┐ ┌──────────┐               │                   │
│ │ laptop   │ │ old-tv   │  （异常卡自动  │                   │
│ │[笔记本]  │ │[展示端]   │   前置重排）   │                   │
│ └──────────┘ └──────────┘               │                   │
└──────────────────────────────────────────┴───────────────────┘
```
- 卡片四态边框色 = 节点综合状态；右上角链路角标 = 该节点链路最差档（与设备状态分离呈现，回答"是设备问题还是链路问题"）。
- 点击卡片 → V2；点击告警条目 → V2 并高亮对应组件。

**V1.5 集群拓扑（3D，通信状况视图，待评审立项）**

```
        [db-main]═══════●═══════[infer-142]
                         ║  (Server 汇聚点)
   [laptop]──────╮      ║      ╭──────[desk-01]
                 ●══════╬══════●
   [old-tv]──────╯             ╰──────(网关节点)
```
- 各主机 = 简化 3D 节点块（沿用低面数程序化几何），链路连线**按 RTT/丢包着色**（绿→黄→红→灰=断），连线**脉冲动画表流量方向与强度**。
- 定位：把"多机相连的整体感"从观赏性资产变成通信监控资产——用户当初选 3D 的动机在此兑现。

**V2 节点详情（3D + 2D HUD 双层）**

```
┌──────────────────────────────┬───────────────────────┐
│                              │ infer-142  [推理] WARN │
│      3D 主板视图（v0.4）      │ ── 组件读数（2D 数字）──│
│   颜色/动效负责"哪里在报警"    │ CPU  71%  82°C  3.4GHz │
│                              │ GPU1 64%  78°C  8/24G  │
│                              │ GPU2 91%  89°C ⚠ 22/24G│
│                              │ MEM  61% (池)          │
│                              │ DISK ▓▓ 74%            │
│                              │ ── 链路 ──             │
│                              │ ↔Server 1ms 0%丢包     │
│                              │ ↔gw     2ms 0%丢包     │
└──────────────────────────────┴───────────────────────┘
```
- 信息范式改动：从 v0.4 的"数字刻在组件上"改为 **3D 颜色/动效（定位报警点）+ 2D HUD（精确读数）双层**。
- 点击 3D 组件或 HUD 行 → V3 抽屉。

**V3 组件/链路抽屉（自 V2 底部滑出）**
- CPU 分核条形 / 单卡明细 / 单盘容量·IOPS / 单链路 RTT 序列（当前窗口，无历史）。

---

## 4. 视图结构总则（3D 是外壳）

- **V1 总览 = 纯 2D**，不强行 3D——值守场景效率优先。
- **V2 节点详情 = 现有 3D 主板视图 + 2D HUD 面板**（双层化改造，P4）。
- **V1.5 集群拓扑 = 新增 3D 视图**（P3，是否立项见 §6 评审点③）。
- 视觉沿用：奶白浅色主题（`#f5f0e6`）、程序化低面数几何体（老机器渲染约束）、四态色码全局统一。

---

## 5. 实施阶段路线（方案通过后按序循环：设计细化 → 实施 → 验证）

| 阶段 | 内容 | 依赖 |
|---|---|---|
| P1 | 指标补齐（磁盘采集、四态派生）+ V1 集群总览 2D 视图 | 无 |
| P2 | 告警体系（阈值配置、归并防抖、横幅/列表/声音） | P1 |
| P3 | Agent 星型探测 + 链路指标 + V1.5 集群拓扑视图 | P1 |
| P4 | V2 节点详情双层化改造（v0.4 3D + 2D HUD 面板） | P2 |
| P5（可选） | 持久化与历史曲线、mesh 探测升级 | 评审决定 |

每阶段循环约定：先出该阶段细化设计 → 用户确认 → 实施 → 验证 → 再进下一阶段。

---

## 6. 评审结论（2026-09-18 已确认项）

原评审稿留下四个确认点，评审结论如下：

1. **阈值基线数值**（§2.2 表）：按 v1 基线执行，实施中通过 JSON 配置微调。
2. **星型探测是否满足网络拓扑**：一期星型方案通过；ICMP/TCP 探测在实际网络环境的可行性作为 P3 开工前的验证项（§1.4 ⚠️）。
3. **V1.5 集群拓扑视图**：立项，随 P3 交付。
4. **角色标签人工配置**：接受，Agent 注册时配置。

---

*v1 固化于 2026-09-18。实施过程中的产品层决策变更须先修订本文档并升版。*
