# S4 细化设计 — 视觉契约一页 + kiosk 值守屏最小版（氛围职责）

> 上位文件：`V2-迭代讨论纪要-20260919.md` §2.8「UI 双轨，视觉不再后置」、§5.4 的 S4 行、§5.3「kiosk 值守屏 → 惊艳感，也是他自己愿意天天开的原因」。
> 基线：main = S1a/S1b + S2a/S2b/S2c + S3a/S3b。本文件不含真实 IP/凭据。
> **验收判据（纪要原话）**：洞洞板那块屏**点亮**且**稳**。
> 上位约束（P1 验收时用户留下的那条）：**改什么效果由用户主导，不得自行加装饰性需求**。所以本阶段的第一产出是"规则"，不是"新长相"——kiosk 复用既有视觉语言，不引入新配色、新字形、新依赖。

## 0. 做什么、不做什么

**做**：

1. **视觉契约一页**：把散在 11 个文件里的"状态色 / 字号 / 动效 / 术语"收敛成一份可核对的规则，并配一条**防漂移自检**（契约不落成可执行的检查，三周内就会重新漂走——这是 H9「配置真源分裂」在客户端的同一件事）。
2. **kiosk 值守屏最小版**：`?kiosk` 一个参数、全屏、只读、零新增部署、3D 状态体 + L0 状态条 + 自动降级护栏。
3. **三处诚实修正**（§3）：3D 里伪造的硬件标识、用户可见文案里的内部文档编号、把样例拓扑铺成真机零件。

**不做**（写清理由，防止下一轮又被当成"漏做"）：

| 砍掉 | 理由 |
|---|---|
| kiosk 自动轮播 / 多页切换 | 值守屏的作用是**瞟一眼**，轮播把"看一眼"变成"等一等"，还让错过的那一屏变成新隐患。 |
| kiosk 的 3D 交互（旋转/点击钻取） | 纪要 §2.8 明文：3D 唯一保留位置=kiosk，**不做主板、不做零件、不做二次钻取**。所以 kiosk 里关掉 OrbitControls（省每帧 damping，也防误触把画面转到看不清的角度）。 |
| 夜间暗色主题 | 真问题（副屏在卧室=一盏灯），但它落在"P1 验收那条约束"的边界内——**配色方向要用户点头**。本期只把颜色令牌化并留 `data-tone` 钩子，不实现夜档。 |
| L3 主板 3D 下线 | 属收尾批。kiosk 复用 `ClusterTopologyRenderer`（星型、无阴影贴图、三者里最便宜），**不**复用 `TopologyRenderer`（每主机、带 1024² 阴影贴图、dispose 是空的）。 |
| 预取合并端点 `POST /api/history/batch` | 与 S3b 同结论：3~7 台 × 每 30s，单请求够用，不做。 |
| 新增任何前端依赖 | 现有 `three@0.170` + `vue@3.5` 覆盖全部需求。 |

## 1. 视觉契约（一页，可核对版）

### 1.1 色彩：四态 + 三个"非状态色"，且没有第五态

| 令牌 | 值 | 只允许承载 |
|---|---|---|
| `--green` OK | `#3fb950` | 正常 |
| `--orange` WARN | `#d29922` | 警告 |
| `--red` CRIT | `#f85149` | 严重 |
| `--text3` OFFLINE | `#8a8a8a`（面）/`#9aa0a6`(3D) | 失联 |
| `--ok-ink` / `--warn-ink` / `--crit-ink` | `#1a7f37` / `#9a6700` / `#b62324` | 同一状态的**文字**档（浅底上四态色对比度不够，所以每态有"面/墨"两值，这是一对而非两态） |
| `--accent` | `#0969da` | 可点、可写、"尚未设置"——**不表示健康度** |
| `--up` / `--down` | `#8b5cf6` / `#0d9488` | delta 方向，S3b 已定：**不得借用四态色**，且只允许贴在数字/折线上，不得出现在边框、圆点、进度填充上 |

三条硬规则：

1. **新颜色必须先回答"它承载哪个状态维度"**；答不出就回退灰阶。这条是纪要 §2.8「不承载状态的只允许出现在 kiosk 层」的可执行版本。
2. **缺席不是第五态**：与失联共用灰，靠**形状**区分（卡片虚线边框、3D 缩小 0.72）。沿用 S1 §3.2 / S3 §4.3，本契约把它钉死。
3. **kiosk 层是唯一允许"纯装饰"的地方**，且装饰不得占用四态色通道（一个一直在呼吸的东西，用户会在三秒内把它读成状态）。

### 1.2 单一真源与防漂移（本期唯一新增的自检）

- **DOM 侧**：`style.css` 的 `:root` 是唯一声明处；组件里不得再出现状态色字面量。现状：三个"墨"色在 11 个文件里出现 **39 次**——本期全部换成令牌。
- **WebGL 侧**：canvas 内没有 DOM 层，读不到 CSS 变量 → 新增 `lib/palette.js` 作为第二消费面的真源（十六进制字符串 + `toRGB()` + `levelColor(level)`），三个 `three/*.js` 里的 `LC`/`GRAY`/`ABSENT`/背景色全部由它派生。
- **两侧防漂移**：`scripts/check-tokens.mjs` 解析 `style.css` 的 `:root` 与 `palette.js` 的 `INK`，逐项比对（含直接状态色 `green/orange/red/accent`），任一不一致 exit 1。只写正则，不引入 postcss。
- **灰阶与装饰色不在本期收编范围**（`#6b7a8f` 中枢块、`#efe8d9` 地面…），避免把一次契约落地扩成一次全量重着色。

### 1.3 密度四档：观看距离决定这一档允许回答什么问题

| 档 | 载体 | 距离 | 只回答 | 下限 |
|---|---|---|---|---|
| **L0** | kiosk（新增） | ≥3m | "有没有事 / 几台在" | 状态字 26px、数字 20px、节点名 ≤8 字、每屏 ≤3 个动效 |
| **L1** | 总览卡片 | 1–2m | "这台现在怎样、怎么变来的" | 10px（现有最低，不再往下） |
| **L2** | HUD 侧栏 | 臂展 | "为什么、最近本机发生了什么" | 11px |
| **L3** | 详情/抽屉 | 坐着看 | "逐盘、逐核、逐 GPU、逐核频" | 无 |

规则：**升一档只能多问一句，不能改口径**——同一指标在两档里出现时，必须来自同一个格式化函数（`formatReason`/`unitFor`/`formatAgo`），这条 S3b 已经吃过一次教训（横幅的 `CPU温度 95%`）。

### 1.4 动效预算（GT 1030 上的硬约束）

允许清单，除此之外不得新增持续动画：

| 动效 | 预算 | 位置 |
|---|---|---|
| 连接点呼吸 2s | 1 个 | ClusterBar |
| CRIT 圆点呼吸 1s | 每告警 1 个 | 横幅/健康度 |
| 条形宽度过渡 `.6s` | 事件驱动，非循环 | 卡片/聚合条 |
| 脉冲点速率 ≈ 1/RTT | **每边 1 个，每节点 ≤2 个** | 3D 星型 |
| 告警闪烁 `alarm-flash .5s` | 仅 CRIT 标签 | 3D 标签 |

禁则：① **不可见时必须暂停 rAF**（`visibilitychange`），屏保/切标签页不该烧 GPU；② kiosk 整屏不用 `backdrop-filter`（全屏每帧合成，是 1030 上最容易掉到 20fps 以下的一项）；③ 降级必须**可见**（角落标"已降级·动效关闭"），不许静默换脸。

降级护栏（纪要 §2.8 那句"连续 30s <20fps → 切纯 CSS 动效"的可执行版）：`KioskView` 每帧采样，30s 滚动窗口平均 <20fps → 卸掉 WebGL、切 CSS 平面墙（同一份快照、同一套令牌、同一套术语），并记录一次降级到浏览器 console（不进事件流——事件流是机群事实，本机掉帧不是）。

### 1.5 文案契约

术语表（唯一写法）：`常驻 / 临时 / 已退役`（归类）、`在场 / 离场 / 缺席 / 失联`（四种"在不在"，语义各不相同，见 S3 §4.3）、`演示道具`（显示名一律 `模拟-` 前缀）、`管理口令`、`静默`。

三条禁则：

1. 用户可见文案**不得引用内部文档编号**（`产品方案 §1.3`、`S3 §4.2` 这类，是给自己看的）。
2. **不得出现伪造的硬件标识**（型号、步进、序列号）；拿不到真值就整行留空——这条是决策 4「不把推算伪装成实测」在视觉层的延伸，对外展示时是信用问题。
3. 空态必须说明**为什么是空的**（S3b 已立四条，本期并入契约，新增面板照此办理）。

## 2. kiosk 值守屏最小版

### 2.1 入口与外壳

`?kiosk`（读 `location.search`，**不引入 router**——目前只有 4 个视图、一层 `ref` 状态机，router 的收益抵不过新增依赖）。命中时 App 只渲染 `KioskView`：不渲染 ClusterBar / AlertBanner / PassphraseDialog / 声音解锁提示——**写侧在 kiosk 里不存在**（读开放、写有口令，决策 6 不变）。同时 `documentElement.dataset.kiosk='1'`，为夜档与全屏样式留钩子。

数据路径完全复用：App shell 的 WS 快照与 `/api/hosts` 首帧照旧，`lib/events.js` 的 15s 轮询照旧，只是消费方换成 L0 版式。声音：kiosk 不自动解锁（浏览器策略也不允许），保留一个显式喇叭按钮，用户自己决定要不要让卧室响。

### 2.2 版式（16:9 与 21:9 都不裁切）

- **主区**：3D 星型状态体（方块=设备档、底座=链路档、脉冲速率=延迟倒数、缩小 0.72+浅灰=离场）。相机固定俯视角、无控件。
- **顶栏 L0**：`集群正常/警告/严重` 大字 + `在线 n/m` + 一条最近事件（正文 ≤26 字，超出截断，完整内容在总览看）。
- **底栏 L0**：CPU / 内存 / GPU 三条聚合（均值 + 峰值刻线），复用 ClusterBar 的 `agg` 语义，只放大字号、去掉标签文字。（实施未去掉标签，理由见 §7.2-3。）
- 空态（无常驻节点 / WS 断开）走 §1.5 第 3 条。

### 2.3 断连必须自己说话（本期最重要的一条实现）

一台常年插电、没人碰的副屏，最坏的失效不是黑屏，是**安静地显示一张过期的"一切正常"**。所以 WS `onclose` 起 kiosk 必须显示"已停止刷新 X 秒 · 画面为最后一次快照"，并让画面进入灰档。这与 H12 是同一类问题（"没消息"被读成"好消息"），只是搬到了呈现端。

> 实施修正：判据不是 `onclose` 而是**最后一次快照到达的时刻**——`onclose` 看不见"连上了但不再吐数据"这种失效。见 §7.2-1。

### 2.4 稳定性护栏（本期真正的改动量）

| 问题（现状） | 处置 |
|---|---|
| 无任何 `webglcontextlost` 处理 → 驱动重置/睡眠唤醒后**永久白屏**（三个渲染器都有） | kiosk 里 `preventDefault` + 停 rAF + `webglcontextrestored` 后重建场景与标签（实施改为主动 `restoreContext()` 且**不重建**，见 §7.2-2）；恢复失败 → 落 CSS 墙并可见标注 |
| `ClusterTopologyRenderer.update()` 删节点只 `scene.remove`，不 dispose 几何/材质/CSS2D DOM；`stubs` 建了**从不删除** | 补 `dispose()` 到位（节点与 stub 同规则），名册反复变更不再单调漏 |
| rAF 无条件 60fps、不可见时不暂停 | `visibilitychange` 暂停；kiosk 里渲染循环按 30fps 上限（墙屏不需要 60，直接省一半） |
| 背景/地面/网格色各文件写死 | 由 `palette.js` 派生（§1.2） |

### 2.5 明确不做

kiosk 不钻取（点方块不跳详情）、不编辑、不显示阈值数字、不显示 host_id/IP 类可识别信息（读接口本身已只到网段，§S3-5）。

## 3. 三处诚实修正

1. **`TopologyRenderer._makeCpuTexture`**：CPU 顶盖第二、三行印的是手写常量 `SRK2L  3.50GHZ` 与 `L123B456`——真机的"步进/频率/序列号"，一眼可信、全是编的。改为真值：型号行已有，次行用 `cpu.cores`/`cores_physical` 渲染"N 线程 · M 物理核"，缺数据则整行留空。
2. **`DrawerV3.vue:186`**：`内存按池呈现（双通道 interleave，单条占用不可测——产品方案 §1.3）` → 去掉内部编号引用，保留结论。**结论本身是对的**，那是已声明的取舍（`TopologyRenderer` 的 `count = Math.min(sticks.length, 2)`），不是 bug，不在本期改呈现方式。
3. **`DetailView.buildTopologyFromMetrics`（实施期发现的第三条，同一类问题）**：Agent 没上报拓扑时，它把 `sample-topology.json` 的整份内容当作"派生自 metrics 的结构"铺进去——于是真机的 L3 主板上印出了 `i9-14900K / RTX 4090 / 990 Pro 2TB / I225-V / DDR5-6000`，全是那台虚构工作站的零件——用户的机器上根本没有这些型号，也没有这些盘。改法：metrics 只能证明它证明的东西（CPU 型号与核数、内存池总量、GPU 名与显存），其余槽位留空；一条都画不出来时**不画 PCB**，由遮罩说明为什么是空的（§1.5 禁则 2 + 禁则 3）。`sample-topology.json` 从此只服务演示道具。

## 4. 验收映射

| 判据 | 由什么满足 | 怎么测 |
|---|---|---|
| **点亮** | `?kiosk` → 全屏 L0（3D 状态体 + 顶/底栏） | 真机起 Server + mock + 浏览器实开 `?kiosk`；DOM 结构断言（本会话已知截图不可用） |
| **稳（不白屏）** | contextlost 恢复 + visibilitychange 暂停 + 30fps 上限 | 控制台 `renderer.forceContextLoss()` → 断言自动恢复且 `connected` 快照继续刷新 |
| **稳（不漏）** | 节点/stub dispose | 反复增删节点 20 次 → `renderer.info.memory.geometries` 回到基线 ±常数 |
| **稳（不掉帧）** | fps 采样 + 降级护栏 + 降级可见 | `?kiosk&flat=1` 直接进 CSS 墙（等于人工触发一次降级路径），断言无 WebGL 且有"已降级"标记 |
| **契约生效** | `:root` 唯一声明 + `palette.js` + `check-tokens.mjs` | 自检绿 + 组件内状态色字面量 grep 归零（灰阶/装饰色白名单除外） |
| **诚实** | §3 三处 + kiosk 保留 `模拟-` 前缀 | DOM 断言文案中无 `SRK2L`/`L123B456`/`§`；演示道具在 kiosk 顶栏计数与卡片前缀同时可见 |

## 5. 风险与回滚

| 风险 | 处置 |
|---|---|
| 令牌化改动面铺得太大、一次重着色把验收搞浑 | 严格限定在**状态色三墨 + 四态**；39 处字面量替换后必须在浏览器里逐面板核对计算样式，不看代码看渲染 |
| 1030 上 3D 根本不达标 | 降级护栏不是兜底而是设计的一半：CSS 墙是完整可用的 L0；`?kiosk&flat=1` 直接进 |
| 复用 `ClusterTopologyRenderer` 把它改坏，殃及现有拓扑视图 | 改动集中在 dispose/调色板/暂停三处，不改几何与语义；拓扑视图回归用 DOM 断言（它仍在总览旁边） |
| kiosk 让过期画面看起来正常 | §2.3 是硬要求，不是可选打磨；没有它就不点亮"稳"这个字 |
| 回滚 | kiosk 是纯增量视图 + App 一层分支：撤 `?kiosk` 即回到 S3b 全部行为。`palette.js`/`check-tokens.mjs` 不参与运行时正确性。§3 三处文案/呈现独立可 revert |

## 6. 提交边界

| 子段 | 内容 | 提交 |
|---|---|---|
| **S4a** | 契约落地：`:root` 令牌扩充 + 39 处字面量替换 + `lib/palette.js` + 三个渲染器改派生 + `scripts/check-tokens.mjs` + §3 三处诚实修正 | 一个 commit |
| **S4b** | kiosk：`KioskView.vue` + `lib/fps.js` + App 外壳分支 + 渲染器稳定性护栏（contextlost / dispose / visibilitychange / 30fps） | 一个 commit |
| 实施记录 | 回写本文件 §7 | 随 S4b |

> 实施修正：`ClusterTopologyRenderer.js` 的调色板派生（S4a）与生命周期护栏（S4b）在同一个文件里交错，git 不逐块暂存就拆不开；且 `check-tokens` 的"three/*.js 无状态色字面量"断言要求渲染器改色与它同批落地，否则 S4a 那个提交自带一条红。故**稳定性护栏随 S4a 进**，S4b 只含 kiosk 本体。见 §7.2-4。

## 7. 实施记录（S4a + S4b · 2026-09-19 本机完成，未上线）

一句话：本期"好看"那一半只有脚本能证明的部分（字号下限、无字面量、单一真源）算完成，"稳"那一半第一次有了**可复现**的验证；而收获最大的三条全部落在"看着在动、其实讲错了话"这一类（§7.4）。

### 7.1 改动清单

**S4a · 契约落地 + 渲染器生命周期**（16 改 5 新）

| 文件 | 改了什么 |
|---|---|
| `style.css` | `:root` 补齐三档墨色 `--ok-ink/--warn-ink/--crit-ink` 与 delta `--up/--down`，DOM 侧唯一声明处 |
| `lib/palette.js`（新） | WebGL 侧真源：`STATE`/`INK`/`GROUND`/`NEUTRAL` + `toRGB()`/`levelColor()`；canvas 读不到 CSS 变量，所以它是"第二消费面"而不是"第二份事实" |
| 11 个组件/视图 | 39 处状态色字面量 → `var()`。其中 `TrendChart` 的 `stroke` 必须走 `:style`（呈现属性不解析 `var()`），`DrawerV3` 的序列色同批交给它 |
| `three/ClusterTopologyRenderer.js` | 颜色由 palette 派生；`interactive`/`fpsCap` 两个构造选项；contextlost→自动 `restoreContext()`＋10s 引信；`visibilitychange` 暂停；`_disposeObject()` 统一回收几何/材质/CSS2D DOM；stub 剪枝；`dispose()` 补齐；`getDelta()` 钳制步长 |
| `three/TopologyRenderer.js` | palette 派生；`_makeCpuTexture` 只印 Agent 报过的字段（§3.1）；trace shimmer 的自反馈（每帧把自己上一轮的 `emissiveIntensity` 再乘一遍、只有一个下限）改成纯函数 |
| `views/DetailView.vue` | §3.3：不再把 `sample-topology.json` 铺成真机零件；一条结构都没有时**不画 PCB**，遮罩说明为什么是空的 |
| `components/DrawerV3.vue` | §3.2：可见文案去掉内部编号，结论保留 |
| `scripts/check-tokens.mjs`（新） | 10 条断言：两侧配对逐项比对 / 组件内无状态色字面量 / `three/*.js` 只经 palette / 可见文案无文档编号 |

**S4b · kiosk**（4 改 4 新）

| 文件 | 改了什么 |
|---|---|
| `views/KioskView.vue`（新） | L0 一屏：顶栏（健康度大字 + 在线 n/m + 演示道具计数 + 一条最近事件 + 喇叭）、主区 3D 或 CSS 墙、降级标注 `已降级·动效关闭`、§2.3 停止刷新遮罩、底栏三条聚合。地址形状兜底 `addressShaped()`：命中就**整条不显示**而不是打码。不钻取、不编辑、无阈值数字（§2.5） |
| `lib/fps.js`（新） | 满窗 30s 均值 <20fps → 一次性降级；判定走定时器而非帧本身（"彻底不画"没有帧可 notice）；时钟可注入 |
| `lib/topology-vm.js`（新） | `hosts[]` → 场景节点 / 平面墙两份映射的**唯一出处**：拓扑 tab、kiosk 的 3D、kiosk 的 CSS 墙从此共用一套口径，`nameMax` 只改字数不改口径 |
| `scripts/selftest-fps.mjs`（新） | 6 条：健康不降 / 12fps 必降且报对数 / 停帧必降 / 满窗前不判 / 一次性 / 瞬时抖动不得永久降级 |
| `App.vue` | `?kiosk` 分支（写侧整体不渲染）+ `data-kiosk` 钩子 + `lastSnapshotAt` 打点（WS 快照与 REST 首帧都算一次到达） |
| `lib/status.js` | `shortName()`（L0 八字）；`loadOf()` 转导出给墙用 |
| `lib/events.js` | `streamAgeMs(now)`；读失败每串只 `console.warn` 一次——掉帧与取数失败不进事件流（事件流装机群事实，不装这个标签页） |
| `package.json` | `check` = `check-tokens` && `selftest-fps`（另留两条单项入口） |

### 7.2 与契约不同处（5 条，都改了本文件相应位置）

1. **§2.3 的判据从 `onclose` 换成"最后一次快照到达的时刻"**。`onclose` 看不见"连上了但不再吐数据"，而 Server 活着 + Agent 全哑恰好是 H12 那一类失效的呈现端版本。副作用：`connected` **不再传给 kiosk**，新鲜度只有 `lastSnapshotAt` 一个输入——留着布尔量，迟早有人拿它当新鲜度用。REST 首帧同样打点，所以"WS 从来没连上"也会在 10 秒后亮横幅。
2. **§2.4 的"contextrestored 后重建场景与标签"改为主动 `restoreContext()` 且不重建**：浏览器只有在 `preventDefault` 之后才发 `restored`，而 `restored` 到来时 three.js 的 JS 侧对象本来就还在、GL 资源下一帧自动重传——"重建"等于把没坏的东西扔掉一遍。护栏因此是：lost → 停帧 + 1.5s 后请求恢复；10s 没回来才落 CSS 墙。
3. **§2.2 底栏的"去掉标签文字"没做**：CPU/内存/GPU 三个词留着，只放大数字。三米外一根没有文字的进度条回答不了"哪一维高了"，而这三条本来就是 L0 唯一还在讲指标的地方。字号下限按稿执行。
4. **§6 的提交边界**：渲染器稳定性护栏随 S4a 进（见 §6 尾注），S4b 只含 kiosk 本体。
5. **§2.2 顶栏那条事件的预算从"正文 ≤26 字"改成"整串 ≤40 字"**：原本限的是正文，实现里正文前面还有时间戳与节点名，而缺席行的正文本身就以节点名开头（于是名字被印了两遍，去掉前缀后整串变长）。可视宽度本来就由 `nowrap + ellipsis` 封住，JS 这条只是防超长串挤开布局。

### 7.3 验证：跑过什么、没跑过什么

**脚本层（可重复）**：`npm run check` → `check-tokens` 10/0 + `selftest-fps` 6/0；`npm run build` 绿（707KB / gzip 204KB）；每个改动模块 `node --check`。

**真浏览器层（内置浏览器：DOM + 计算样式 + 控制台断言）**

- `?kiosk&flat=1`：canvas 数 0、`已降级 · 动效关闭 · 手动指定 ?flat=1（跳过 3D）`、四态类名正确（`lv-crit/lv-warn/lv-ok/lv-absent` + `离场` 注记）、节点名截到 8 字、健康度 26px / 数字 20px / 聚合值 20px、`header.cluster-bar` 与 `form.pd` 均不在 DOM、`data-kiosk=1`、演示道具计数在场。
- `?kiosk`：canvas 挂上、无降级标注、`interactive:false`（无 OrbitControls、无点击监听）、30fps 上限、页面隐藏时护栏不误触发（等 12s，console 无 `[kiosk]` 警告，只有浏览器自己的 fullscreen 警告）。
- 泄漏：增删节点 20 轮 → `info.memory.geometries` 稳定在 12；`dispose()` 后容器 DOM 归 0、geometries/textures 归 0；stub 剪枝 1→0。
- contextlost → restored 整条异步链跑通，两个 hook 都触发（§7.4-3）。
- **§2.3 断连真跑**：`taskkill` 掉隔离 Server → 横幅出现（`已停止刷新 1 分钟 · 画面为最后一次快照`，`.stale` 灰档 + 画面压暗）→ 重启 Server → **不刷新页面**横幅自己消失；同一期间事件行独立说出 `事件流已停止更新 2 分钟`，恢复后回到正常行。这条同时证明了"降级可见"与"过期画面不装正常"，是本期最要紧的一次实测。
- 全库 grep：`SRK2L`/`L123B456`/`i9-14900K`/`4090`/`990 Pro` 只剩在注释与 `sample-topology.json`（现为演示道具专用）里，渲染路径不再引用。

**没跑过的（不算验收通过）**

- **画面本身**：内置浏览器 `document.hidden===true` → rAF 不跑、CSS2D 标签不排布、截图不可用。所以"26px 在三米外好不好读""动效观感""21:9 是否裁切"全部未验收——需要用户自己的副屏实看。这条与 S1/S3 遗留的"视觉待验收"是同一件事，本期仍挂着。
- **真 GT 1030 帧率**：降级阈值只能靠注入时钟自测，1030 上 30fps 跑不跑得动未知；出口已经备好（`?flat=1` 是完整可用的 L0，不是残缺版）。
- 隔离 Server 用的是本机一次性库（`AppData/Local/Temp/hm-s4`，`HM_ROSTER_DB`+`HISTORY_CONFIG`+`HM_DEMO=1`），`server/data/` 全程未碰，未部署 231。

### 7.4 验证期发现并修掉的问题（都是"会讲错话"级，不是样式）

1. **`fpsCap` 的第一版会自己冻住动画**：用"跳过这帧渲染"实现上限，而 `getElapsedTime()` 仍每帧走时 → 脉冲点跳步甚至停摆；切后台再回来 dt 巨大，全场瞬移。改成 `getDelta()` 累加 + 0.1s 钳制（钳制本身就是"唤醒后不瞬移"的护栏）。
2. **partial window 会误杀 3D（本期最值钱的一条）**：`minSpanMs` 原本 20s < `windowMs` 30s，意思是"头 20 秒平均难看"就能触发一次性降级——而降级不许回头，于是冷启动那几秒 shader 编译能把墙永久变成 CSS。改成 `minSpan === window`（判据必须是满窗），selftest 第 6 条钉住"10 秒 5fps 抖动不得降级"。**这条是自测用例先红后绿逼出来的**，靠肉眼永远碰不上。
3. **`restoreContext()` 从来没人调用**：最初探针读到的是"handler 没触发"——事件是异步的，等一下就有。真问题是：生产代码只被动等 `restored`，而浏览器并不会自己把上下文还回来。也就是说 §2.4 那条"驱动重置 = 永久白屏"的结论，本来只差一行没修。
4. **`_clearEdges()` 只 dispose 线，漏了脉冲精灵**；**stub 布局键用 `stubs.size`**，一旦开始剪枝就撞键 → 换成单调 `_stubSlot`。
5. **"几何泄漏 delta=12"是量出来的假红**：`info.memory` 只在真渲染过后才计数，隐藏页里基线读成 0。正确判据是"20 轮增删后不再增长"，不是"回到 0"。
6. **缺席事件把节点名印了两遍**（正文本身以名字开头）。
7. **ticker 被节流时横幅会晚报**：`now` 由 1s 定时器推进，隐藏页里定时器节流到约 1 分钟一次 → 测试中看到横幅比预期晚出现。真实 kiosk 常驻可见不受影响；而且年龄取自 wall clock、不是 tick 计数，所以节流**只会晚报、不会错报**——这也是当初不写 `staleSec += 1` 的理由。

### 7.5 已知未闭合 / 移交

- S3b 挂下来的视觉验收（"效果待后期迭代、内容待定"）现在有了口径和屏幕，但**仍需真人眼过一遍**：下次连副屏时一起做，做完才算 S4 点亮。
- 夜档不做（§0 已声明）；`data-kiosk` 钩子已留。
- L0→L1 的入口（"三米外看见 CRIT，拿手机走向 231 看详情"）属 S6，本期 §2.5 明确不钻取。
- `MonitorScene.js` 死代码仍在，收尾清。
- 生命周期护栏只补在 `ClusterTopologyRenderer`；`TopologyRenderer`（L3 主板）同病未动——它是坐着看的视图、失效代价低一档，且正讨论下线。
- `check-tokens` 只比对了四态 + 三墨 + 场景底色；灰阶/装饰色按 §1.2 有意留在范围外。
- 回滚：撤 `?kiosk` 分支即回 S3b 全部行为；`palette.js`/`check-tokens.mjs` 不参与运行时正确性；§3 三处各自可独立 revert。

---

## § 收口销账（2026-09-20 文档对账）

只改文档、未动一行代码。**本文件有两条验收判据今天已经失效，必须先读这一段再看 §7.4/§7.5。**

- **§7.4 首条 + §7.5 首条（"下次连副屏时一起做，做完才算 S4 点亮"）——这条判据的前提没了**：09-20 定的 V3 主题是**看板 UI/UE 迭代**，而副屏/kiosk 方向的视觉投入已被明确冻结（见 `00-暂存\handoff-v3讨论.md` §0 与决策"装饰需求排后"）。所以"S4 点亮"不能再挂在"等一块副屏"上：同一天用户对屏幕类验收给了**默认放行**（原话"屏幕默认验收我通过"），口径记为"**默认通过、非人眼逐条确认**"，S4 到此按销账收口，不再有待办挂账。详见 `00-暂存\V3-前置-文档对账.md`。
- **`?kiosk` 在 231 生产上从未真机跑过**：副屏没接、也没人打开过那个 URL。09-20 上线记录里读的总览/接入/设置都是普通桌面形态。**别把"S4 已随 v2 上线"读成"kiosk 已被验证"**——前者成立、后者不成立，降级阈值至今只有注入时钟的自测读数。
- **真 GT 1030 帧率**：仍未验证，且随副屏形态一起挂起（那块卡就是副屏那台机器的）。出口照旧是 `?flat=1`（完整 L0，不是残缺版）。
- **`MonitorScene.js` 死代码**：**仍在**，且现在能确定它没人 import（09-20 全库 grep 只剩它自己的类定义）。"收尾清"这件事没做，V3 期间可以顺手删——它不参与运行时。
- **`TopologyRenderer`（L3 主板）的生命周期护栏仍未补**：状态未变（`views/DetailView.vue:133` 还在用它），"是否下线"的讨论也没结论。本期没动。
- **§7.3 那批"全库 grep 只剩注释与 sample 文件"的私有信息核对**：09-20 重跑过同一组 grep（`SRK2L`/`L123B456`/`i9-14900K`/`4090`/`990 Pro`），结论仍成立——命中全部落在 `client/src/config/sample-topology.json`（演示道具专用）与三处注释里（`three/TopologyRenderer.js:436`、`views/DetailView.vue:41`、`:67-68`，后者是"为什么不继承 sample 文件"的那段说明），渲染路径不再引用。`check-tokens.mjs` 现在是四态/色值契约的执行者（10 条断言，客户端 25 条全绿）。
- **schema / 契约侧无新增偏差**：本文件的四态与 `palette.js` 是唯一色值真源这一条，README 原先指向的是 `p1-细化设计.md` 的色值表——09-20 已把 README 改成指向 `client/src/lib/palette.js`（那份 V1 文档只作为历史稿保留，其阈值表已过期，读 `GET /api/config`）。
