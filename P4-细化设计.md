# P4 细化设计 — V2 节点详情双层化（3D 外壳 + 2D HUD 面板）

> 状态：自主跑模式（2026-09-18 用户授权：方案环节默认通过，文档照常留档）
> 上游契约：产品设计方案-v1 §3「V2 节点详情 / V3 抽屉」+ §1.4 L3 明细层
> 前置：P1（四态派生）、P2（告警引擎）、P3（链路指标）均已提交（64cf3b1）

---

## 0. 范围

| 做 | 不做（理由） |
|---|---|
| DetailView 重排为「左 3D + 右 2D HUD」双层布局 | V4 多机对比（无产品定义） |
| V3 组件/链路抽屉（底部滑出，点击 3D 组件或 HUD 行打开） | 历史曲线（P5 持久化后接入，本期链路小图仅用客户端环形缓冲） |
| TopologyRenderer 增加组件点击 raycast + 组件级报警闪烁 | 3D 资产重做（v0.4 几何原样复用） |
| Agent 补 L3 数据：CPU 分核占用、磁盘聚合 IOPS | 单盘 IOPS（Windows/Linux 设备名→挂载点映射不可靠，只给整机聚合，诚实呈现） |
| DetailView 接入本机告警列表（active + resolved 过滤） | 内存单条占用（§1.3 科学约束，维持池语义） |

## 1. 数据契约变更（增量）

### 1.1 Agent（`collectors/cpu.py`、`collectors/disk.py`）

```jsonc
// cpu.collect() 增加：
"per_core": [12.5, 30.1, ...]      // psutil.cpu_percent(interval=0.5, percpu=True)，长度=逻辑核数
// usage_percent 改为 per_core 均值（与整次采样一致，语义不变）

// disk.collect() 增加：
"io": { "read_mb_s": 3.2, "write_mb_s": 1.7 }  // 整机聚合，disk_io_counters 两采样差分（同 network.py 模式）；首帧 null
```

- 除 cpu 外其余字段不变；server 对未知字段透传，无需改动（store.js 原样存 metrics）。
- mock-agent.mjs 同步补 `per_core`（7 节点各生成 8-16 核伪随机）与 `io`。

### 1.2 客户端侧（无 server/agent 契约改动）

- **链路 RTT 序列**：DetailView 内本地环形缓冲，从 2s 广播帧累积 `status.components.link.targets[].rtt_ms`，每 target 最多 60 点（≈2 分钟窗）。零协议改动，符合"当前窗口，无历史"的 V3 定义。
- **单机告警**：App 把现有 `alerts`（active+resolved）传入 DetailView，按 `host_id` 过滤。

### 1.3 TopologyRenderer API 增量

```js
enableClicks(cb)          // raycast 命中组件 → cb(componentKey)；5px 拖动防误触（同 ClusterTopologyRenderer 模式）
updateMetrics(metrics, alarmMap)  // alarmMap: {cpu:bool, mem:bool, gpu0:bool, ...} 组件级闪烁；
                                  // null/undefined 时回退旧行为（整机 CRIT 则 CPU 闪）
```

- componentKey 取值：`'cpu' | 'memory' | 'gpu0'… | storage id | nic id | 'disk' | 'network'`（storage/network 组件点击统一归入 `disk` / `network` 抽屉，映射规则在 DetailView 侧做）。

## 2. V2 布局（双层化）

```
┌────────────────────────────────────────┬──────────────────┐
│ 顶栏：← 返回 · 主机名 [角色] · 四态徽章 · LIVE           │
├────────────────────────────────────────┼──────────────────┤
│                                        │  2D HUD 面板      │
│        3D 主板（TopologyRenderer）      │  （数字为主）      │
│   颜色/闪烁负责"哪里在报警"              │  ── 组件 ──       │
│   点击组件 → V3 抽屉                    │  CPU  71% 82℃ 3.4G│
│                                        │  GPU1 64% 78℃ 8/24│
│                                        │  GPU2 91% 89℃ ⚠   │
│                                        │  MEM  61% 39/64G  │
│                                        │  DISK 74% 读3.2写1.7│
│                                        │  NET  ↓12.3 ↑1.2  │
│                                        │  ── 链路 ──       │
│                                        │  ↔server 1ms 0%  │
│                                        │  ↔gw     2ms 0%  │
│                                        │  ↔infer  1ms 0%  │
│                                        │  ── 本机告警 ──    │
│                                        │  ● CRIT rtt 12m  │
│                                        │  ✓ 已恢复 内存 …  │
└────────────────────────────────────────┴──────────────────┘
  左列 flex:1                    右列固定 300px，窄屏(<1100px)折叠为底部横条
```

- HUD 每行可点击 → 打开对应 V3 抽屉并高亮该行；行内四态色点 = 该组件 level（server 派生，前端不再判阈值）。
- 在线率/OFFLINE：host 离线时 HUD 显示失联时长（lastSeen），3D 场景整体降饱和（scene 材质 emissive 归零 + 标签"失联"），不做假数据。
- 视觉延续奶白主题（#f5f0e6），HUD 背景 var(--bg-glass)，沿用现有 CSS 变量。

## 3. V3 抽屉（底部滑出，max-height 42%，可关闭）

| 抽屉 | 内容 |
|---|---|
| CPU | 分核占用条形组（per_core，每核一竖条+百分比，核多时分两行）+ 频率 + 温度 |
| GPU | 该卡明细：usage / mem util / VRAM 用·总 / 温度 |
| 磁盘 | 分区表：挂载点 / fstype / 已用·总 / 百分比条；顶部整机 IO（读/写 MB/s） |
| 网络 | 当前 ↓↑ Mbps + 累计收发 GB |
| 链路 | 该 target：kind / 当前 rtt / 窗口丢包 / **RTT 迷你折线（60 点 SVG）** / 阈值线（5/50ms 取自快照 thresholds） |
| 系统 | uptime / 平台 / load avg（Windows 无 → "—"）/ host_id |

- 一次只开一个抽屉；再点其他行切换内容；点 3D 组件与 HUD 行等价。
- 阈值来源：快照消息里已有 `thresholds` 字段，App 透传给 DetailView（小改：applySnapshot 存一份）。

## 4. 组件与文件清单

| 文件 | 动作 |
|---|---|
| `agent/collectors/cpu.py` | 改：per_core |
| `agent/collectors/disk.py` | 改：io 聚合（模块级上采样缓存） |
| `server/scripts/mock-agent.mjs` | 改：per_core + io 伪数据 |
| `client/src/three/TopologyRenderer.js` | 改：enableClicks + alarmMap 组件级闪烁 |
| `client/src/views/DetailView.vue` | **重写**：双层布局 + HUD + V3 抽屉 + 链路环形缓冲 |
| `client/src/components/HudPanel.vue` | 新增：右列 HUD（组件/链路/告警三段），props 驱动 |
| `client/src/components/DrawerV3.vue` | 新增：底部抽屉（按 kind 渲染六类明细） |
| `client/src/App.vue` | 小改：DetailView 增传 `:alerts` 与 `:thresholds` |
| `client/src/lib/status.js` | 小改：导出 `linkLevelOf(target)` 等抽屉用的小工具（如需） |

## 5. 自测方案（自主闭环）

1. `node --check` 全部 JS + `python -m py_compile` 改动 py；`vite build` 通过。
2. server + mock-agent 起链，REST 快照断言：每 host `metrics.cpu.per_core.length == cores`、`metrics.disk.io` 第二帧起非 null。
3. 真实 agent（本机）跑一帧验证 per_core 在 Windows 实际可用、采样间隔未变慢（帧周期仍 ≈2s）。
4. 内置浏览器（非用户外部浏览器）：进 detail → 断言 HUD 行数、点 GPU 行出抽屉、点 3D CPU 出分核条、mock-lag 主机链路行色 = WARN/CRIT、RTT 折线随帧增长；截图留档 00-暂存。
5. 控制台零错误为验收线之一（P3 的 HMR 教训：改 lib 导出后整页刷新再测）。

## 6. 评审点（默认自决，用户可回看）

1. **右列固定 300px + 窄屏折叠**——自决通过：值守屏均 ≥1920，折叠仅是兜底。
2. **单盘 IOPS 降级为整机聚合**——自决通过：设备名映射跨平台不可靠，宁缺毋假。
3. **链路历史用客户端环形缓冲而非 agent samples 下传**——自决通过：零协议改动，符合"当前窗口"定义；P5 有持久化后切 server 端历史。
4. **per_core 用 interval=0.5 的 percpu 采样**——自决通过：与现有 0.5s 阻塞采样合并，帧周期不变。
5. **离线节点 3D 降饱和 + HUD 显示失联时长**——自决通过：复用 OFFLINE 语义，不造数。

## 7. 风险与对策

- **raycast 命中母板装饰物（SMD/走线）**：只对 `components` Map 里登记的 group 做递归命中，装饰网格不注册即不可点。
- **闪烁逻辑回退**：alarmMap 缺失键视为 false，保留旧整机闪烁路径给"整机 CRIT 但组件级信息不足"的旧数据。
- **CPU 抽屉在 24 核笔记本上的排版**：分核条按容器宽度自适应列数，≥32 核时分页签（当前机群最大 14 核，防御即可不做分页）。
