# P5 细化设计 — 持久化与历史曲线（SQLite）

> ⚠️ **V1 阶段（P 系列）历史稿，2026-09-20 文档对账时加此声明。** 文中的"现状/契约/阈值数值/配置真源"是 P 批当时的形态，**不代表现役**：V2（S1~S6）已收口并上线（生产 231 = `61bf4db` = tag `v2`）。现役事实以 `README.md` + `部署说明.md`（两篇上线记录）+ `技术隐患清单.md`（H1~H29）为准。三条最容易踩的：**阈值现值一律读 `GET /api/config`**（S6 起运行时真源在 DB，`thresholds.json` 只剩"首次播种"与"回落到播种值"两个用途）；**四态色值真源在 `client/src/lib/palette.js` + `style.css`**（由 `client/scripts/check-tokens.mjs` 把关，`lib/status.js` 里没有颜色），卡片排序键是 `attentionRank` 而不是 CRIT>OFFLINE>WARN>OK；文中出现的"V1.5 / V2 / V3"指**页面层级**，与 V2、V3 两轮迭代无关。

> 状态：自主跑模式（2026-09-18 用户授权：方案环节默认通过，文档照常留档）
> 上游契约：产品设计方案-v1 §5 数据层 + P2 §164「历史=内存 5 分钟折叠，P5 才持久化」+ P4 §1.2「链路小图为客户端环形缓冲，P5 切 server 端历史」
> 前置：P1（ac6fdd6）、P2（6ea5e0a）、P3（64cf3b1）、P4（b2fac48）、技术债（d3e4c0d）均已提交
> 参数默认值由实施方自决（用户授权），调参入口见 §6

---

## 0. 范围

| 做 | 不做（理由） |
|---|---|
| Server 端 SQLite 持久化（node:sqlite 内置，零新依赖） | mesh 两两直测（产品方案标 P5 可选，一期星型已够用，需要时单开阶段） |
| 指标时序两级存储：5s 原始（存 7 天）+ 1min 均值（存 30 天） | 更长保留（1 年/归档导出 CSV——无产品定义） |
| 告警持久化：active/resolved 生命周期落库，Server 重启不丢 | IM/webhook 外部通知（一期明确不做，沿用 P2 边界） |
| `GET /api/history/:host_id` 分桶查询接口 | 多用户/鉴权/权限（局域网内工具，已知接受） |
| DrawerV3 图表切到 server 历史（链路）+ 新增趋势小图（CPU/GPU/内存/磁盘） | V1 总览页历史图（无产品定义，V1 保持实时） |
| 保留期作业（小时级 roll + 过期删除）+ WAL | 每分核/显存占用的历史曲线（数据量大、图形价值低，只存摘要字段） |

## 1. 存储选型（自决点 1）

**`node:sqlite`（Node 内置 DatabaseSync）**，不用 better-sqlite3。

- 实测：本机 node v22.23.2 建库/插入/查询通过，仅一条 ExperimentalWarning（不阻塞）。
- 理由：零依赖零编译——231 小主机部署时 `npm install` 只有 express/cors/ws 三个老依赖，不引入原生模块安装风险。
- 代价：实验性 API，跨大版本可能变动——本项目单文件建表+十几个 prepared statement，锁死在自有 schema，风险可控。

写入模型：DatabaseSync 是同步 API。单行 INSERT 在局域网 7~20 节点规模下每次 <0.1ms，广播循环里批量写（事务包裹），不构成事件循环卡顿源。不做异步队列（过度设计）。

## 2. 表结构

```sql
PRAGMA journal_mode = WAL;

-- 5s 原始采样（两级之一）
CREATE TABLE IF NOT EXISTS samples_raw (
  host_id   TEXT NOT NULL,
  ts        INTEGER NOT NULL,          -- 服务器接收毫秒时间戳
  cpu_usage REAL, cpu_temp REAL, cpu_freq REAL,
  mem_percent REAL,
  disk_worst REAL,                     -- 最大分区占用 %
  io_read REAL, io_write REAL,         -- MB/s 整机聚合
  net_down REAL, net_up REAL,          -- Mbps
  gpu_json  TEXT,                      -- [{"i":0,"u":64,"t":78},...]（u=usage t=temp）
  link_json TEXT,                      -- [{"t":"server","r":1.2,"l":0},...]（r=rtt l=loss）
  PRIMARY KEY (host_id, ts)
);

-- 1min 均值（二级，由 roll 作业生成，列同上，数值为分钟均值）
CREATE TABLE IF NOT EXISTS samples_1m ( ... 同上 ... );

-- 告警事件（生命周期 upsert，非 append-only）
CREATE TABLE IF NOT EXISTS alert_events (
  id TEXT PRIMARY KEY,                 -- host_id|metric|source（同 P2 合并键）
  host_id TEXT, hostname TEXT, metric TEXT, source TEXT,
  level TEXT, state TEXT,              -- active / resolved
  value_at_trigger REAL, latest_value REAL, threshold REAL,
  started_at INTEGER, resolved_at INTEGER
);
CREATE INDEX IF NOT EXISTS idx_alert_time ON alert_events(started_at);
```

- JSON 列只装变长数组（GPU 卡数、链路目标数不定），其余全列式，分桶查询直接 `AVG()`。
- CPU per_core、显存占用、分区明细**不入历史**（§0 表），抽屉实时值仍走广播帧。

## 3. 写路径（自决点 2：在广播循环落盘，不在 Agent 连接处）

`index.js` 的 2s 广播 tick 里，`getSnapshot()` 之后调 `history.onBroadcast(snapshot)`：

- 每 host 维护 `lastSampleAt`，距上次落库 ≥ `sample_interval_s`（默认 5）才写当帧最新 metrics——**取瞬时值非窗口均值**（简单，5s 粒度下误差可忽略，风险 §10-R2 声明）。
- 只写 online 且有 metrics 的 host；OFFLINE 不产生空行（缺口由分桶 AVG 自然吸收）。
- 好处：与广播共用同一次评估结果，Agent 侧零改动、零协议变更。

告警落库钩子在 `alerts.js`：`activate()`、escalate、resolve、以及 latest_value 更新时 upsert `alert_events`（防抖 pending 态不落库）。

## 4. 降采样与过期（小时级作业）

`setInterval(roll, 3_600_000)` + server 启动后 60s 首跑：

1. **roll**：把 samples_raw 中「已完整且尚未入库」的分钟 `GROUP BY host_id, ts/60000` 求 AVG 写入 samples_1m（`INSERT OR IGNORE`，重跑幂等）。
2. **删**：`DELETE FROM samples_raw WHERE ts < now - raw_retention_days*86400e3`；samples_1m 同理用 agg_retention_days。
3. `PRAGMA wal_checkpoint(TRUNCATE)` 收尾，防 WAL 文件膨胀。

roll 用「补历史空洞」式条件（1m 表里该 host 已存的最大分钟之后才聚合），重启/补跑安全。

## 5. 查询 API

```
GET /api/history/:host_id?range=2h|24h|7d|30d
→ {
  host_id, range, bucket_ms, points: 96,
  cpu:  { ts:[], usage:[], temp:[] },
  mem:  { ts:[], percent:[] },
  disk: { ts:[], worst:[], io_read:[], io_write:[] },
  net:  { ts:[], down:[], up:[] },
  gpu:  { "0": { ts:[], usage:[], temp:[] }, ... },   // 并集出现过的卡号
  link: { "server": { ts:[], rtt:[], loss:[] }, ... }
}
```

- 数据源选择：range ≤ 24h → samples_raw，否则 samples_1m；`bucket_ms = 时间跨度/96`（图表恒 ≤96 点）。
- 分桶在 SQL 里做（`GROUP BY (ts - ts%bucket)`），数值列 AVG；gpu_json/link_json 只能应用层展开——策略：**链路与 GPU 趋势先取原始行（该 range 内 ≤ 每桶 1 行粗采样：SQL 里 LIMIT 96 偏移取行）再按桶平均**，实现为一次全行扫描（7 节点规模 range=30d 也才 ~43k 行，<100ms）。
- 400 未知 host → `{}` 各序列空数组；404 不区分（历史可能早于注册）。

## 6. 配置（`server/src/config/history.json`，gitignore；模板 `history.example.json`，同 probes.json 模式）

```jsonc
{
  "enabled": true,              // false = 纯内存模式（完全回到 P4 行为）
  "db_path": "data/host-monitor.db",   // 相对 server/ 根目录（绝对路径亦可）
  "sample_interval_s": 5,       // 落盘采样间隔
  "raw_retention_days": 7,      // 5s 粒度保留天数
  "agg_retention_days": 30,     // 1min 粒度保留天数
  "chart_points": 96            // 历史接口每序列点数上限
}
```

- 读取失败（无文件）→ 用内置默认值 + 打一行日志，不崩（同 probes.json 模式）。
- **给用户的话**：想改采样频率/保留时长，编辑 `server/src/config/history.json`（复制 example 起步），重启 Server 生效。
- 环境变量 `HISTORY_CONFIG` 可指向备用配置文件（自测用短保留期跑过期路径）。

## 7. 告警重启语义（自决点 3）

Server 启动时把 `alert_events` 中 `state='active'` 的行回填进 AlertEngine entries（state=active、underCycles=DEBOUNCE_CYCLES-1、补记 started_at）：

- 重启期间恢复的告警 → 首拍后 2 个干净节拍即 resolve（自愈，最迟 6s），生命周期补全落库。
- 重启前 pending 的窗口不恢复（本来就未触发过，无所谓）。
- `/api/alerts` 响应不变（内存 5 分钟折叠窗）；新增 `GET /api/alerts/history?limit=200`（按 started_at 倒序，含未恢复项）供查全量历史。

## 8. 客户端改造

- **TrendChart.vue（新，~80 行）**：通用 SVG 迷你趋势图。props：`{ ts:[], vals:[], warn?, crit?, unit, label }`；y 轴自适应（max 至少 crit*1.2），阈值虚线、折线按越段染色（复用 P4 链路图画法，抽成组件）。
- **DrawerV3**：
  - 链路抽屉：环形缓冲 → `fetch /api/history` 的 link 序列；头部加 `2h | 24h | 7d | 30d` 段选择器（默认 2h）；打开时拉一次 + 60s 定时刷新；组件卸载清 timer。
  - CPU / GPU / 内存 / 磁盘抽屉：kv 明细下方各挂 TrendChart 区（CPU=usage+temp 两图，GPU=usage+temp，内存=percent，磁盘=worst+io_write）。range 选择器共用一个（当前抽屉局部状态即可）。
  - system 抽屉：挂 `GET /api/alerts/history?limit=50` 过滤本机的「本机告警史」列表（时间+级别+指标+值）。
- **App.vue**：零改动（历史全部子组件自取 REST，不进广播协议）。
- 离线节点：历史照常可查（这是持久化的意义），DrawerV3 不受 `hud-offline` 影响，抽屉内注明数据截至 `last_seen`。

## 9. 数据量估算

7 节点常开：raw = 7 × 12 行/分 × 1440 × 7 天 ≈ **85 万行**（每行 ~120B，含索引 ≈ 150MB 内）；1m 表 30 天 ≈ 30 万行。231 小主机完全无压力。wal_checkpoint 防抖。mock 7 节点同量级。

## 10. 决策评审点（自主跑=自批留档）

1. **node:sqlite 而非 better-sqlite3**——自决通过：零依赖优先于「稳定性名义」，实机验证过。
2. **落盘在广播 tick 而非 WS 收帧处 + 瞬时值非均值**——自决通过：一次评估两处用；5s 粒度取瞬时值对趋势图形无损，声明为已知近似。
3. **告警重启回填 active 而非只读历史**——自决通过：P2 文档明示此缺口归 P5；自愈窗口 6s 可接受。
4. **per_core/显存/分区明细不进历史**——自决通过：历史图价值在主线趋势；数据体积减半以上。
5. **range 上限 30 天、图表恒 ≤96 点**——自决通过：一期局域网工具无长审计诉求。
6. **mesh 探测不做**——自决通过：产品方案标「可选，评审决定」，与持久化正交，不捆绑交付。

## 11. 风险

| # | 风险 | 缓解 |
|---|---|---|
| R1 | node:sqlite 实验性 API 变更 | 封装集中在 `history.js` 单文件；换 better-sqlite3 只动一层 |
| R2 | 5s 瞬时采样漏掉毛刺峰值 | 与告警链路无关（告警走实时 2s 评估），仅影响图形，声明接受 |
| R3 | roll 作业与写入并发（同库同步 API） | DatabaseSync 单连接串行，天然无竞态 |
| R4 | GPU/链路 JSON 展开扫全行慢 | range>7d 走 1m 表（行数为 raw 的 1/12）；96 点上限在应用层 |
| R5 | 时钟回拨/重复 ts | PRIMARY KEY (host_id,ts) + INSERT OR IGNORE 幂等 |

## 12. 自测矩阵（mock 环境）

| 用例 | 期望 |
|---|---|
| 起 Server（无 history.json，用默认）+ mock 7 节点 30s | raw 表每 mock 节点 ≈ 6 行/30s；REST 2h range 返回非空序列 |
| 链路图 | mock-lag RTT 方波（3↔60ms）在 2h 图可见台阶；客户端不再依赖环形缓冲 |
| 告警重启存活 | mock-crit active 时 kill Server → 重启 → 告警史在 `/api/alerts/history`，active 列表含 mock-crit（其磁盘 96% 持续）|
| 恢复自愈 | 停 mock-crit → 重启 Server 前手动不恢复 → 重启后 ≤10s 内该回填告警 resolve 并落库 |
| 过期与 roll（HISTORY_CONFIG 指短参数：sample 1s/raw 保留≈0/roll 手动触发） | raw 清空、1m 有聚合行、DELETE 生效 |
| enabled:false | 行为回退 P4，无 DB 文件创建 |
| 前端 | 浏览器：链路/CPU/GPU/内存/磁盘五类抽屉趋势图出图、range 切换、离线节点仍可查；console 零错误 |
| 卫生 | data/、history.json 在 .gitignore；git diff 无真实 IP |
