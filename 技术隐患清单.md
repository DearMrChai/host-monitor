# 技术隐患清单 — host-monitor

> 2026-09-19 第二轮迭代入场审计（后端架构视角）产出。
> 用途：**随迭代逐项消化**，不是待办清单的全集。每项标了影响面与建议处理时机。
> 现状基线：**2026-09-20 01:40 S5+S6 已上线 231（生产 = 代码锋面 `61bf4db`，其后只有文档笔）**，GitHub 已推 tag `v2`（= 同一提交），main 之后只有文档笔、不再逐笔标 SHA。现场读数：三台 Agent 全部以哈希凭据在 `strict` 下重连（`/api/enroll` = accepted 3 / denied 0 / drift 0）、名册三台 `has_credential=true`、无任何逐机覆盖。`GET /api/config` 的 `untouched_since_seed`：**首启 `true`，02:08 重启后为 `false`——后者是预期值，不代表有人改过配置**（语义坑见 **H28**，本文件先前版本把 `true` 写成了基线，已纠正）。上线过程的备份/回滚点、清理与重启验证、只做现场验证的结论见 `部署说明.md`"S5+S6 上线记录（2026-09-20，V2 收口）"。
> **V2 到此收口，功能冻结**：V3 的主题是 UI/UE 迭代（用户 2026-09-20 定），下面的"建议时机"里凡是 V3 的功能项都默认排在设计巡检之后。
> 本轮新增条目 H22~H25 是 S6 实施过程中**发现并只做登记、没有追改**的（用户指令：有问题留到 V3）；H22 是其中唯一一条建议列入 V3 首批的。**H26/H27 是上线当晚在现场取到的两条**，都属于"本机测过、真机才暴露"；**H28** 是同一次上线的**读数对比**才暴露的语义坑（单次开机看不出来），**H29** 是清理那一段顺手查出来的凭据面问题——四条都是 V2 冻结后才登记的，一条都没追改；**H29 已于 09-20 上午关闭**（执行的是收尾动作：删含明文密钥的备份，见该行末格与 `部署说明.md`"同日上午补充"），其余三条留在 V3 池里。
> 2026-09-20 下午**文档对账**（V3 入场前的信息对齐，只改文档、未动一行代码）：把本文件与 README / 部署说明 / S1~S6 细化设计 / V1 历史稿逐条对过，本文件内改了 4 处与事实不符的表述——H22 的复现例证（`mem_usage` 是不存在的键）、H22 修法的覆盖面（漏了 `index.js:539` 这条不经过广播定时器的入口）、H15 的"仍未做"与同格末尾的"S5 已做"并存、H28 里"要改 `部署说明.md`"这半件其实已经做完。另重跑全套自检作为这份文档的可复现基准：**服务端 309 条 / 客户端 25 条全绿**（分套件读数见 `README.md`"自检"段）。对账全表与 H22 施工图落在工作区 `00-暂存\V3-前置-文档对账.md`。
> 2026-09-21 深夜再新增 **H31**（复核第 2.5 刀时顺手查出，同属"翻暗自身的账"，也只登记未改）：**"黑色 alpha"这一整族在暗地上失去了表达余量**（**09-22 凌晨订正：规模 24 → 30 处**，两个 CR 的**配对**当时也写反了——正确读数是"槽底对**面板** 1.024／峰线对槽底 **1.111**"，登记原文"槽底对地面 1.10／峰线对槽底 1.03"保留在 H31 行的证据格里；地面 hex 应为 `--bg #0b1015`，不是 `#0d1117`。三条读数现在都在 `00-暂存/contrast-check.mjs` 的 A 段里，**可一键重跑**）。它是纸面时代用来写"压暗＝槽／凹陷／抬起／反馈"的，而 `#0b1015` 已经没有多少可压的空间——实测见上（非文本图形可读下限约 3.0）。所以后果不是美观，是**一条事实在墙上已经不存在了**，而这条产品定位就是"变化检测器"。三把刀的判据都是"状态色"，黑 alpha 不承载状态，于是**它从没进过任何一张清单**——这一条与 H30 同形：**规则没有落地机制**。修法方向由色表推（峰线反到亮侧／槽靠 `--border` 描边／投影族单列并如实登记它不再承担层次），已把规则补进色表 R-2 推论。**成本侧（09-22 二次订正后的最终数）**：30 处里**只有 14 处是机械的**——`.hud-bar` 第 1 刀就用 `background: var(--bg)` 当槽（CR 1.173，已在安静面带内），同一件事仓库里已有看得见的那种写法。**要他眼睛的只有 3 处**＝水位峰线 2 ＋ 离线视口 `filter` 1；另 **1 处遮罩不改**（方向合 R-2）、**3 处 `ephemeral` 缺席标签我判为已合规**（已带 1px 虚线描边，R-2 推论②要的边界已由描边承担——**这一档是语义判读、不是扫描结果，列出来供他否**）、**10 处 hover／投影建议明确不做**。逐条站点与重跑命令在任务书 §6.10。**这里第一版写的是「20 处有现成解」＝把「同一种写法」当成「同一种语义」合并归类的后果，虚报了 6 处 ⇒ R-6 第七次命中，且这次的形态在归类不在扫描。**
> 2026-09-22 凌晨：**H22 已修并本地提交（`9cba7c8`，未 push、未部署）**——V3 首批第一条落地，三半（写侧全覆盖点名拒／缺档判 `null`／两条入口各自包护栏 + `system/evaluator_error` 事件）都在，读数见该行末格与 `README.md`。这条是**台账里第一条在登记之后由 agent 施工、再由人逐条复核销账的 V3 隐患**，所以复核留下的三件事值得进 header：① **行号会烂**——本行与 selftest 抬头里写死的 `:539`/`:544` 是登记时的位置，改完代码后一个指向注释块、一个指向 `alerts.tick` 那行；selftest 那两处已改成按名字指认，本行内的保留为"登记时位置"。**以后指认入口用函数名，不用行号。** ② 复核时查出**第三条评估入口**：`index.js:162` 的 `/api/hosts` 路由裸调 `store.getAnnotatedHosts()`，而 `alerts.tick` 就在它里面（`store.js:201`）——它在 express 的 try/catch 内，所以后果是 500 不是死进程，"两条崩溃入口"这个说法仍然成立；但 H6b 的结构断言只数 `store.getSnapshot()`，**看不见这一条**，已登记不追改。③ 顺手确认了一件好事：设置页的 payload 是按 `spec.metrics` 逐项 `deepSet` 出来的（`SettingsView.vue:149-166`），与库里有没有那一行无关，所以 ① 这条收紧**不可能被设置页自己触发**，而旧 DB 行里那个洞在设置页存一次就自愈——"boot 要不要重校验既有行"因此从必修降为可选。
> 2026-09-22 早晨：**H30 的第一条按用户裁定收掉（`2b515ef`，本地未 push）——`.conn-dot.ok` 由 2s 闪改常亮**（他的原话："常亮代表正常连接，闪烁有点奇怪"）。施工时查出一件登记时没记的事：**`.conn-dot` 有两个家**——全局 `style.css` 里那条 `.conn-dot.ok`（服务 `DetailView` 顶栏那颗）与 `ClusterBar.vue` 的 scoped 一份（ClusterBar 自己那颗），**scoped 优先级更高，只改全局等于没改**；两支 `@keyframes pulse` 都保留（正对照：`ClusterBar` 的 `.hp-dot` 1s 与 `EnrollView` 的等待点 1.6s 仍在用）。客户端 35 条断言重跑同读数（20+9+6）；**视觉未人眼**。本行只收这一条，**H30 仍开着**——六处关键帧／五个动画名那份清单和"通道闸"要不要现在动，等用户对"并刀/单刀"这一问表态（他早晨问的是这个词什么意思，解释已钉在任务书 §6.10 上方）。
> 2026-09-22 上午：**H31 的 14＋2＋3 收掉了（本次提交）——第 2.6 刀落地，翻暗的黑色 alpha 族第一次有了"看得见的槽"和"重新存在的水位峰线"**。三组读数（逐处 CR 现在挂在 `00-暂存/contrast-check.mjs` 的 D 段，可重跑）：**14 处槽底/牌底由 `rgba(0,0,0,α)` 改指 `var(--bg)`，对合成玻璃底 `#15202b` 从 CR 1.011~1.022（＝看不见）升到 1.158（安静面分带内）**；**2 处水位峰线取 `--text3`（用户裁的档，不是算术过线那档），对修后槽底 CR 6.237 ≥ 载义图形下限 3.0——这条事实从墙上消失了又回来了**；**3 处局部地面（`.hc-menu` 纯白浮层／口令卡输入框／`.k-stale` 米色纱）收进地面族 token**，其中 `.k-stale` 是**本来就在坏**：米色纱把"已停止刷新"那句载重警告压到 CR 1.03，等于这条警告从来没显示过。**不动项按裁定原样保留**：hover 5＋投影 5＋`ephemeral` 3＋遮罩 1＝14 条，`git grep` 黑 alpha 由 30 → 14，**算术闭合**。**复核侧留三条**：① **它报的一个数我复算不出**——红字压 `color-mix(--red 10%)` 芯片它记 3.78，四支候选地面里最低是 4.376（对 `--panel`），**结论方向一致（仍 <4.5），但我不采它这个数**（R-6：只采能重跑的读数）；② **它自己报了施工自伤**——用 `git checkout --` 删临时正对照样本时连带回滚了已做的 `.ap-count`，当场发现并重做 ⇒ **工作区里不要用 `git checkout --` 收拾临时样本**；③ **台账数字被它订正一处**：任务书 §6.10 把 `.hc-role` 记成 `.05`，HEAD 实为 `.03`（`.05` 属于不动清单里的 `.hc-class.ephemeral`）——**照"哪一处"动手是对的，照数字动手就会错**。⚠️ **视觉一律未人眼**。交判四项（缝线档位／离线卡 1.112／字段底 `--bg` vs `--bg-glass`／`DetailView` 那处 `filter`）记在 H31 行末格与本文件 header 之下，本文件不替他裁。
> 2026-09-22 上午（后段）：**H31 交判四项由他一次裁完（原话"全部按默认执行"），其中只有一项产生代码改动；顺手抓出本文件自己的一处结构损坏。** ① **落地项＝`DetailView.vue` 缺席视口的 `filter`**：撤 `opacity(.8)`、留 `saturate(.15)`（去色＝"这台不在"是合法信息，压暗＝"这里没东西"撞 R-2 推论）——客户端 **35 条断言重跑同读数（20+9+6）**，⚠️ **视觉未人眼**。②③④（抽屉缝线档位／缺席卡槽底／两个输入框底）**按默认＝不改**，判据已写进 H31 行末格。⑤ **裁之前我把两个数跑飞并当场订正**：槽底 **1.112 → 1.088**（错把卡底当 `--panel`、错认命中的 class 是 `.lv-offline{opacity:.75}`，该卡实际赢在 `.absent{opacity:.62}`）**已掉出安静面下限**；"其余 **13 个控件**用 `--bg-glass`"是并族出来的假数，真值 **39 处声明／17 文件**。缝线那条则是**数字对、尺拿错**——它不承载意义，按安静面判 **1.224 本就合格，从头没有欠账**。
> 同一轮查出：**本文件 H32 那一行的 markdown 表格是断的**（我上一批写进去的）。原因两处叠在一起——**"可重跑命令"里的反斜杠丢了（`color:s*` 扫不到带空格的写法，正是 R-6 那一族）＋ 正则里的竖线没转义把行切成 9 格**。⇒ **教训：台账里写的命令也是命令，落笔时就得跑一遍；而 markdown 表格是一个"没人检查的格式"**。修法已随本批做掉（转义＋还原 `\s`，正对照是一条 `color: #ffffff`，命中数仍是 6）。**顺手留下一个可跑的检查器 `00-暂存/md-table-check.mjs`（不在仓库内）**；**要不要把它升成仓库闸，交他判——本文件不自行新增判据。** 另：我写那段订正说明时把竖线原样放进反引号，**订正说明自己又把表格切坏了一次**，同一坑一段话里踩两次，是这条纪律存在的理由。
> 2026-09-22 夜（V3 包 3 步 2）：新增 **H35**——道具台数这个事实被自检抄了三份字面量，于是任务书 §5.3 裁定 3 授权的那"一处 demo.js 改动"，实际落地成本是"改三处断言"。施工 agent 按铁律没动自检，把 3 条红留在 `6206e70` 上等裁（前后逐套读数与三个处置见 H35 行）。
> 注：本文件不含真实 IP/凭据，可随仓库提交。

> 2026-09-21 晚（V3 视觉迭代期间）新增 **H30**。这一条的来源方式值得单记一句：**它不是跑出来的故障，是拿他的一次人眼读数反查代码查出来的**——R-4 把"闪"定为最稀缺通道并为此重校了一处（`alarm-flash`），但同一条通道在客户端里另有**六处关键帧定义、五个动画名、四档周期、五档幅度，零处受闸**，其中一套还在替"连接正常"这句心跳闪。（**本格首次登记时数字是"四套／三份"，且把他看的红点认成了 ClusterBar 的 `hp-dot`；逐条打开看语义后是上面这个规模，实际那个点来自 `AlertBanner` 的 `.bn-icon`——更正过程记在 H30 行的证据格里，留痕不抹。）所以本条登记的与其说是一个缺陷，不如说是**一条规则缺落地机制**：定了"省着用"，却没有清单管着这个通道有几个家。纯呈现面，未改一行代码，判法交用户。

| # | 隐患 | 影响 | 证据 | 建议时机 |
|---|---|---|---|---|
| H1 | **幽灵节点**：`store.hosts` 只增不删，节点一旦注册永久留在名册；`cluster.total`= 历史出现过的一切节点；`evaluateCluster.health` 取全体最差档且 OFFLINE 权重最高 → 改名/退役/长期关机的机器把顶部横幅钉死灰灯，并触发 OFFLINE 循环铃声 | 直接污染产品第一问"集群健康吗"；在线率分母随 Server 运行时长只涨不降；退役节点改名即永久残留直到重启 Server | 登记时：`store.js` 无 remove API（今天仍然没有：退役改的是 `roster.js` 的名册行，不是内存表）；`status.js:148` worst 归并含 OFFLINE（S6 后 `evaluateCluster` 挪到 `status.js:174`）；`sound.js` CRIT/OFFLINE 循环 | **S1a/S1b 已做（2026-09-19）**：持久化 roster（`nodes` 表）+ 常驻/临时/退役三态，健康度与分母只算常驻在线。**H12 已由 S3a 补上（2026-09-19）**：presence 改为名册驱动，缺席常驻进分母、有灰卡、有一条事件，但**不进健康度**（S3 §4.2 的取舍），本项至此闭合 |
| H2 | **Agent 无配置文件、无版本上报**：`--server/--host-id/--role/--interval` 全靠命令行烧进计划任务与 systemd unit；register 帧无 `agent_version` | 机群混版本不可见 → 升级无法验收；任何"加机向导/远程装"都要靠拼命令行字符串，易错难回滚 | ~~`agent/main.py:121-135`~~ 现为 `agent/config.py`；`index.js` ack 字段 | **S2b 已做（2026-09-19）**：`agent.json`/env/CLI 三层解析（`--print-config` 说明每项来源），`identity.json` 存身份与节点密钥，register 帧带 `agent_version`+`fingerprint`，Server 存并在名册展示（端到端断言 P2b/P8）。**2026-09-19 里程碑：三台生产 Agent 已升到 1.1.0**（名册 `agent_version` 全部非空、`identity.json` 三处落盘、`--print-config` 三台实测 `credential= identity.json`），"混版本可见"这半在真数据里成立 |
| H3 | **探测环串行**：每轮耗时上界 = Σ 各目标超时（ICMP 3s + TCP 2s 级），目标数线性放大滞后；`_interval` 之后才 sleep | 3 台无症状；key_hosts 增长后链路指标变陈旧、滑窗统计失真 | `prober.py:107-115` | **低**：机群 >5 台或做 mesh 前改 `asyncio.gather` + 总预算封顶 |
| H4 | **host_id 身份可碰撞**：默认由 hostname 推导，且 9100 曾无鉴权 → 同名机器/伪造注册互相覆盖同一节点记录 | 数据正确性（非安全议题，决策 6 另议）：表现为读数抖动、拓扑出现重名幽灵 | `config.py:default_host_id`（仅未配对时）；`ingest.js:#drift` | **S2a＋S2b 已做（2026-09-19）**：伪造注册被 ingest 凭据挡掉；一条连接改写别的节点被 `frameHostMatches()` 封堵并记台账；真 Agent 现已上报 `fingerprint`，同一 id 从另一台硬件上来会在异常接入台账里以"指纹漂移"点名（**接受但不静默**，S2 §4：拒绝 = 自我致瘫）。**残留**：`host_id` 由 hostname 推导只发生在未配对的首次启动，配对后被 identity 钉住（C9）；"真机重装/换盘"这条路径只做过服务端侧模拟（F 段），未做真机实测 |
| **H5** | **监控系统自身无人监控**：Server 有计划任务崩溃拉起（已覆盖进程崩溃），但 231 断电/睡眠/整机死机 = 全静默；告警渠道仅页面内，人不在屏幕前等于没有告警 | 异常可达性在"人不在跟前"的时段归零 | 产品 v1 §2.4 决策"仅页面内" | **用户已知情并接受为产品边界**（2026-09-19 拍板：调试期维持界面内，不做出站）。理由：异构机群满载是常态，夜半推送 = 为人陪编译。→ 真正的诉求转为**告警过滤/静默/归属**（见 H7），出站通道待机群稳定后重提 |
| **H8** | **信任边界已移动，ingest 曾无凭据**：Agent 接入端口 9100 无 token；机群实际跨 ZeroTier（约 10 台含朋友设备），不再是纯局域网信任域。任何 ZT 组成员可注册任意 `host_id`、伪造或劫持既有节点数据 | 决策 6（无鉴权）的前提"安全边界=局域网"已不成立；后果是数据可信度，不是泄密 | 登记时：`index.js:44-47` 无校验（S2a 后同一位置是 `config.attach`/`events.attach` 的接线，接入鉴权落在 `index.js:78` 的 `ingest.authorize`）；用户口述机群含朋友设备与临时机 | **S2a 已做（2026-09-19）**：配对码→每节点密钥的两段式凭据 + `ingest.js` 三档强制（默认 `legacy`：陌生 host_id 一律拒绝，已入册老 Agent 宽限并在看板点名）+ metrics 帧身份校验（连带修掉 H4 的重写面）。**2026-09-19 里程碑已收口**：三台生产 Agent 升到 1.1.0 并各自带密钥，生产 `HM_INGEST_TOKEN=strict`，现场负例自检 `server/scripts/probe-strict-denial.mjs` 返回 `rejected/unknown_host` 且名册未被写入（`denied` 计数器 +1）。本条关闭；冒充已知节点那半边归 H16 |
| **H6** | **全局单探测计划**：Server 把同一份 `probes.json` 推给所有 Agent（网关/关键机按家庭局域网 IP 写死）。Agent 一旦从别处接入（办公室、朋友家、ZeroTier 侧），它的"网关臂"探的是你家的网关 → 语义错乱 | 机群跨网后链路层结论不再可信 | 登记时：`index.js:30-36,71` 单对象广播（S6 后此处已改为 `config.probePlanFor(node)` 三级派生，广播的是逐节点计划） | **S6 已做（2026-09-19，本机）**：三级派生 **节点专属 > 站点(`sites[site]`) > 全局**（`config.probePlanFor`），register ack 里带 `probe_plan_source`，看板与 Agent 打印同源（自检 D 段 10 条 + E9 端到端）。**没被掩饰的那一半**：来源=全局且该节点站点与 Server 不同时标"可疑"（J5），因为此时链路结论仍然不可信。**残留（V3）**：① **站点计划本批只读**——设置页能编辑全局与逐机两层，`sites` 只能在界面上"看见"（列名 + 一句去处），新增站点仍要写 DB 行或改文件；把 `site` 名字编进节点档案而不给站点表编辑口，是本期知道自己缺的那块。② 计划改完在 **Agent 下次接入时**才换目标（不是立即），界面与 Agent 日志都如实这么写。③ 探测目标仍是"一台机器一个网关"，没有 mesh（H3 的并发预算先于它） |
| **H7** | **阈值全局单一**：所有机器共用一份 `thresholds.json`。异构机群（推理机/编译机/游戏机）里满载是**正常工作状态**，不是异常 → 必然产生"陪朋友编译"式噪声告警 | 告警可信度下降 → 用户开始忽略横幅 → 产品失效 | 登记时：`status.js:11` 直接 import 全局 `thresholds`（无逐机路径）。S6 后同一行改为 import config 层的 `mergeNodeThresholds`，逐机叠加的读取入口在 `store.js:187` | **S6 已做（2026-09-19，本机）**：**逐机阈值覆盖**——`nodes.settings_json.thresholds` 存差量，评估时 `mergeNodeThresholds` 叠在全局之上，`pushReason` 给原因打 `custom:true`，所以卡片/告警行上那条"**该机自定义**"标记回答了"为什么这台不到 80 就红"（没有标记的话，逐机阈值会变成下一排看不懂的字）。**允许只改一侧**（`{cpu_usage:{crit:95}}` = 编译机让它晚点红，黄档仍跟机群走），这在设计时是刻意的：强制成对会把"我只想挪一条线"变成"你得先知道全局值"。**"手动静默"这半其实在 S1 就有了**：逐机限时静默（`muted_until`，到当天 24:00）+ 卡片 🔕 + 事件流 `muted`/`unmuted`；静默期内阈值既不评估也不解析（**冻结而不是伪恢复**），且 **OFFLINE 不受静默影响**——可用性值得被叫醒，负载不值得。**未做（V3 候选）**：`per-role` 模板。今天的能力是"一台一台点"，缺的是"给所有 `role=inference` 一次套一份"——三台机器上这点代价为零，朋友的机群长到十台就不是了。**生效时机与 H6 不同，别混**：阈值由 Server 侧每个快照重读（`store.js:187` → `roster.nodeThresholds()`），改完下一拍（≤2s）即生效，已活动的告警也会按新阈值重新评估（该解除就解除）；探测计划要等 Agent 重连，因为目标写在 Agent 手里 |
| **H9** | **配置真源即将分裂**：V2-a 引入 `nodes` 表后，运行时配置同时存在于 JSON（thresholds/probes）与 DB → 两边都能改 = 行为分裂、排障不可能 | 中后期必然踩到；且现在所有配置改动都要重启 Server | 登记时：`status.js:11`、`index.js:30-36` 均为启动期一次性读文件。S6 后 `index.js:34-38` 是 `config.attach({get,set})`（把 DB 注进 config），`status.js:11` 只 import config 层，两处都不再碰文件 | **S6 已做（2026-09-19，本机）**：`server/src/config.js` 成为运行时唯一真源，`thresholds`/`probes` 各占 `settings` 表一行 JSON；**文件只在 DB 无该行时作首次种子**（`loadSeeds()` 在 import 期填一份，`attach()` 之后文件即不再是真源——这条由自检 A 段用"改文件不改行为"证伪，不是靠声明）；写侧只有 `setThresholds`/`setProbes` 两个入口，**整体替换 + 逐键校验 + 原子拒绝**（不合格返回人话错误列表， live 对象一个字节都不动）；生效方式是 `replaceInto` 原地改字段，所以 import 了 `thresholds` 的模块无需重连、**没有轮询定时器、不需要重启 Server**（自检 B 段）。设置页就是这条链路的界面。**三条诚实例外（不写下来"H9 关闭"就是谎）**：① `resetThresholdsToSeed()` 会**重新读 `thresholds.json`**（`config.js:240`）——"回落到播种值"的真实含义是"回到当前文件值"，文件在这里是逃生舱而不是运行时真源，这个区别必须留在文档里；② `zt.json` 由 `zerotier.js:48-51` 直读，不经 config、界面上也没有它；③ `BROADCAST_INTERVAL_MS` 是启动常量，而 `alert.tick_ms` 已可配 → 两个相邻节律一个能改一个不能。②③ 合记为 **H24**。**运维侧新增一条闸**：回滚代码但不清 `settings` 行 → 老代码根本不读它（看似无恙），可一旦再升回来，上次编辑的值还活着 = 现场和上一版预期不符。写进 `部署说明.md`，且设置页的"帮助"卡把这句话印给了人 |
| **H10** | **写侧将无防护**：设置界面（加节点/改阈值/录凭据/删节点）一旦上线，任何能打开 9101 的人都能改机群——读开放是需求，写开放不是 | 朋友路过看一眼 = 零摩擦（保留）；但误操作可删节点、可改他人机器凭据 | `index.js:requireAdmin` 中间件；`roster.js:hasPassphrase/checkPassphrase` | **S1a 已做**：写侧管理口令（scrypt 存哈希、默认拒绝、口令未设时一切写 403），S2a 把新增的签发配对码/演示开关同规则纳入。**S5 G2 收口（2026-09-19，本机）**：不再靠"记得加闸"——自检枚举全部非 GET 路由并要求每条链上含 `requireAdmin`（口令端点是唯一白名单、理由写明），且断言闸只有**一个函数实例**（防将来偷偷长第二个）；新增的在场归名写端点已被这套枚举自动纳管。**读侧维持无鉴权**（原决策） |
| **H11** | **【重点风险 · 已排为面板成型后的第一优先】第三方机器口令落在 231**：用户 2026-09-19 拍板"存口令，不再要求对方加公钥"，并明确"成型期先专注完善项目，凭据治理当成面板成型后必做重点工作" | 能读 231 磁盘者 = 取得所有已登记机器的登录凭据；对外展示/收集需求会**放大接触面**（更多人知道这台 Server 的存在与其上持有他人机器密码）；"加密"在无远端密钥前提下是障眼法，不做伪装 | 决策升版见纪要 §2.5 / §5 | **S5 已做"自己已有凭据的干净化"这一半（2026-09-19，本机）**：节点密钥改散列落库（H16）、写端点全量走同一道闸（H10 残留清零）、读侧/日志/事件行无凭据泄漏（自检 A~E 段）。**第三方凭据的治理仍未落地**（独立存储、逐台撤销、审计）→ 因此 **observed（SSH 拉取）继续不得排在其前**，且 S5 §7 明确建议把 observed 移到 V3（等用户拍板 D1）。过渡期兜底不变：口令不进 API 响应 / 不进日志与异常栈 / 不进 git 与分享包 |
| **H12** | **长期不上报的常驻节点会"静默消失"**：presence 与分母都来自**内存主机表**（`store.hosts`），不是名册。Server 重启后一台常驻机再没回来 → 没有卡片、不进 `在线 n/m` 分母、也不会新建 OFFLINE 告警。S1b 只保住了"重启前已活动的那条告警"（冻结 5 分钟），**没有告警的常驻节点仍然完全隐身** | 恰是最该报警的情形（机器被搬走/重装/长期关机）没有任何交代；对外展示时是硬伤 | `store.js` 主机表只由 register/metrics 填充；`status.js:evaluateCluster` 只遍历传入 hosts | **S3a 已做（2026-09-19）**：`store.getAnnotatedHosts()` 改为名册驱动——`confirmed` 且非 demo 且 `last_seen` 超 `presence.absent_after_minutes`（默 3min）的节点合成一条 `absent_record`；灰卡 + 进 `在线 n/m` 分母 + 事件流每缺席周期一条"已 N 分钟没有上报"。**与建议原文的偏离**：不进 `cluster.health`（"它不在这儿"≠"它出事了"，否则把 H1 从后门放回来；理由见 S3 §4.2）。可当场回退：`presence.enabled=false`。**S3b 已补呈现（2026-09-19）**：卡片写"缺席 · 上次在场 4 分钟前 / 计入在线率，不计入集群健康度"，与临时节点的"离场"分词；三处事件流（总览右栏 / HUD 本机事件 / 接入台账）上线；并且修掉一条真机才暴露的反向谎话——缺席合成让旧 失联 告警走了解析分支，事件流写下"回到阈值内（204）"，现改以第四种闭合 `cancelled:'absent'`（"改判缺席"）收口，见 S3 §10.4-2。**但只关了"跨重启的静默"，进程内的长静默仍是彻夜红灯 → 见新条目 H18** |
| **H13** | **管理口令在多设备间会互相踢**：口令明文存在各设备 localStorage，任一处更换即让其他设备的写操作 403。S1b 把"无限弹层"改成了明确报错（缺陷 11），但**服务端没有重置/找回通道**——忘了就只能上服务器改库 | 朋友或第二个自己误改口令 = 看板写侧集体锁死；对外收集需求时会放大 | `admin.js` 403 分支；`roster.js` 只有 `setPassphrase(需旧口令)` | **可修的那一半已修（S5 §3.3，2026-09-19，本机）**：`server/scripts/reset-admin-pass.mjs` —— 无 `--yes` 只报告、带 `--yes` 才把 `meta.admin_pass` 删掉回到"未设置"（默认拒绝），**不碰节点密钥**（三台 Agent 不需重配）；步骤进 `部署说明.md`"S5 上线步骤"第 5 条。**为什么是脚本不是接口**：一个"忘记口令"的 HTTP 路由就是那道 guarding 所有写入的门上的洞，而能跑这个脚本需要的文件系统权限本来就已经等于能直接改库。**未修的一半**：多设备之间仍会互相踢（要口令明文落 localStorage 才会）；真正的解是账号化，随 H11 的第三方凭据治理一起谈 |
| **H14** | **冻结告警的铃声代价**：`unknown_alert_grace_minutes`（默认 5min）内，一个已经消失的演示/模拟节点的 CRIT 会保持活动，而客户端 CRIT/OFFLINE 是**循环铃声** | 每次重启 Server 后最多 5 分钟无意义鸣响；演示机上更常见（mock 随时关） | `alerts.js:unknownGraceMs()`（原 `UNKNOWN_GRACE_MS` 常量，S6 起读可配标量）；`App.vue:setCritLoop`；判定的客户端一侧 `lib/status.js:critLoopWanted` | **S2a 已消掉最常见触发源**——演示机群关闭时其节点退役、告警立刻以 `retired` 闭合，不再冻结 5 分钟（`selftest-roster` G6e）。**S6-5 已做本体的语义收口（2026-09-19，本机）**：把"冻结"从引擎内部状态提升为**契约字段**——`getLists()` 里 `state==='active' && _unknownSince!=null` → `frozen:true` + `frozen_since`，且 `restoreActive()` **恢复即标冻结**（以前要等第一个 tick 才发现；"快照已服务、首次 advance 未到"这段窗口正是重启后循环铃声的起跑线）。**采的立场是"降档，不撤条"**：因为"我们没再看见它"≠"它恢复了"，把还开着的告警撤掉是另一种谎——所以行仍在，只是虚线 chip"该机未再上报" + 整行变暗 + 横幅的圆点**只在全部 CRIT 都冻结时**才停闪烁；声音侧循环铃声与单次提示都不响。判定挪到 `client/src/lib/status.js`（`isFrozen`/`critLoopWanted`）而不是留在 `App.vue`，为的是它能被单测而不是靠匹配源码字符串。**安全方向守住两条**：已知但离线的机器**不算**冻结（该响照响，自检 G7）；一条冻结**不许**把旁边活着的那条一起消音（自检 S3 混排用例）。宽限期现在可配（`alert.unknown_alert_grace_minutes`，1~240 分，就在设置页阈值表下面），到期闭合为 `cancelled:'stale'`，措辞是"未上报"而**不是**"已恢复"（自检 S7 + G4）。**真机读数（不是推断）**：重启后 `[Alerts] Restored 2 active alert(s) from disk` → 两条 `"frozen":true`、横幅印"…（该机未再上报）"、`.bn-icon.still` 生效，未静音状态下 11s 内新建 oscillator **0 个**；正对照"1 条活 + 1 条冻结"= 24 音，证明消音不是"根本没响"。**残留 → H25**：冻结期间那条行会丢"**该机自定义**"标记（`alert_events` 无该列，本期不做迁移）。自检：`selftest-config` G 段 8 条 + 新增 `client/scripts/selftest-sound.mjs` 9 条 |
| **H15** | **接入台账是内存环形缓冲**：`ingest.events` 只留最近 20 条、不落库 → Server 重启即清空；`enroll_codes` 只判过期不删除，死行永久留在表里（行很小但无界） | "昨天谁试过接进来"这种排障问题答不了；对外展示够用（现场演示不看重启前），运维期会不够。**S2c 上线后多一条呈现面风险**：接入页那块卡片如果只写"暂无记录"，重启后的空列表会被读成"从来没人被拒过"——正好相反 | `ingest.js:EVENTS_MAX`（现为 `:46`，仍在，已退化成"本次启动"视图）；登记时：`roster.js` 无码表清理路径 → S5 §5.2 起有 `purgeExpiredCodes()`（`roster.js:420`，挂在 `index.js:498` 的 retention 钩子上） | **S3a 已做主体（2026-09-19）**：拒绝/配对/宽限/指纹漂移四类都落 `events` 表（history 同库），`GET /api/events` 跨进程可读，内存环形缓冲退化为接入页的"本次启动"视图（仍保留）。噪声纪律：同 `(host, code)` 60s 内合并 `count+1`，一台没凭据的机器被 supervisor 反复拉起 ≈ 每小时 1 行而非 300 行（自检 A2/C2）。保留 7d 挂在 `history.onCleanup`。**当时仍未做**（09-20 对账标注：本格末尾已记录该缺口由 S5 §5.2 收口，此处保留原措辞是为了留下"登记时点"，别读成两条并存）：`enroll_codes` 死行清理（行很小、无界，留 S5 凭据治理批一并做）；S2c 的"本次启动"兜底文案到 S3b 有真事件流后再换 → **S3b 已换（2026-09-19）**：接入页改为持久化事件流（`EventStream lock-kind="ingest"`，24h 可跨重启）＋一行"本次启动累计"，重启后的空列表不再可能被读成"从来没人被拒过"。**`enroll_codes` 死行已由 S5 §5.2 清掉（2026-09-19，本机）**：`roster.purgeExpiredCodes(now)` 挂到 `history.onCleanup` 上（同一个 retention 时钟，不新开第二个定时器），过期超 30d 的码行删除；读侧本来就只看未过期（`activeCodes()`），撤销走 `events` 审计（`code_issued`/`code_revoked`），所以删行不擦掉任何历史。自检 G 段：−31d 删、−29d 与有效码保留 |
| **H16** | **节点密钥明文存储、明文过线**：`nodes.enroll_token` 以明文躺在 `roster.db`，配对时在局域网 WS 上明文传输（无 TLS）。能读到 231 磁盘、或能在链路中间监听者 = 可顶替任意已配对节点上报假数据 | H8 关的是"凭空造节点"，**没有关"冒充已知节点"**；`legacy` 档下连冒充都不需要密钥（见 S2 §3 残留风险） | `roster.js:newNodeKey/keyMatches`；`ingest.js` 模块头 "Honest limits" | **存储侧已收口（S5 G1，2026-09-19，本机）**：落库形态 `sha256$<16B 盐>$<散列>`，逐行独立盐、恒定时间比对；启动一次性迁移老明文行（`#migratePlainKeys`）＋ 迁移后 `wal_checkpoint(TRUNCATE)`+`VACUUM`（否则明文还躺在自由页与 `-wal` 里，"改了 SELECT"是假收口），并在匹配时顺手升级未迁移行。**2026-09-20 现场把这一半的强度降一档**：启动期那一次实测**没有**清掉已释放页里的明文镜像（三行活动值都已是散列，但主文件 offset 8142 仍可捞出一个曾经有效的密钥），停服后手工再跑一次才归零 → 存储侧收口的完整动作是"迁移 + **停服**清理"两步，见 **H26**。**为什么是 SHA-256 而不是 scrypt**：节点密钥是 128bit 随机十六进制，不是字典里挑的词，暴力成本一样；而 scrypt 每次 Agent 重连都要跑一遍，231 是低功耗小主机——**管理口令那侧继续用 scrypt**，因为它确实是人选的词。**传输侧仍未收口**：配对与上报仍是明文局域网、无 TLS，"过线"这半个 H16 没关，也不做伪装式加密（诚实表述写进 `部署说明.md` 鉴权行）；真收口要 TLS 或每机一次性握手，属成型后工作 |
| **H17** | **"被拒即退出"把重试风暴换成了 supervisor 重启循环**：S2b 让 Agent 收到 `rejected` 后打印中文原因并以退出码 2 结束，不再自己 5 秒重试。但 142 是 `Restart=always`、231 是计划任务崩溃 5 秒拉起 → 一台没凭据的机器会"起—被拒—退"循环，每轮一行中文日志 | 好处：拒绝原因每次都单独打印在退出前，比"伪装成正常运行的静默重试"易读；坏处：supervisor 会反复拉起（systemd 默认配额下可能进 `failed`，也可能一直循环，231 的计划任务一定一直循环），且看板上它只是 OFFLINE，原因**过去**只在本地日志里 | `agent/main.py` `except Denied`；`部署说明.md` §Agent 运行方式 | **S2b 已知代价，本期不修**：改成"退避重试＋首次即报"要动 Agent，而本期不碰三台生产机（迭代节奏约定）。事件流侧的承接条件在 S3a 已经具备。**S3a 把"只在本地日志里"这半彻底消掉了**：每次拒绝都进 `events` 表并按 60s 合并计数，重启 Server 后仍可从 `/api/events` 读出"intruder 接入被拒：未知节点，且未带有效配对码 ×N"（自检 C2 + F4 端到端）。剩余部分（Agent 侧退避重试而非"起—拒—退"循环）仍未做，需要改 Agent 且本期不动生产机。**S3b 补上了呈现**：合并后的 ×N（HH:MM 起）在接入页常驻可见，且不再依赖"本次启动"（`/api/events` 跨重启）。翻 `strict` 前的动作：先确认顶栏 `⚿ 未带凭据 N` 归零（等价于快照 `admin.keyless_agents` 为空），再翻档；翻错用 `HM_INGEST_TOKEN=off` 当场回退（S2 §8）。逐台步骤已写进 `部署说明.md`"里程碑重部署清单" |
| **H18** | **进程内的长静默仍是彻夜红灯**（S3b 真机验证时澄清的语义边界）：缺席只由**名册驱动的合成**产生，而合成条件是"该节点不在实时主机表里"。`store.hosts` 从不清理（`checkTimeouts` 只把 `online` 翻 false），所以一台在本进程运行期间被关掉、过夜的常驻机器**永远走不到合成那条路**，它一直是实时 OFFLINE 记录 → CRIT 卡片 + 循环铃声 + 进健康度 | 与 H12 同源、但场景相反：H12 是"该报警的没报"，H18 是"该淡出的不淡"。7×24 运行的 231 上，关机过夜 = 响一夜，而"它不在这儿"并不是"它出事了"（S3 §4.2 的取舍在这里被绕开了一半） | `store.js:checkTimeouts`（无 remove）+ `#absenceRecords(liveHosts, …)`（现 `store.js:206`）的"不在实时表里才合成"条件；实测见 S3 §10.5 | **S5 已做（2026-09-19，本机）**：`store.checkTimeouts()` 里，已确认 + 常驻 + agent 类记录静默超过 `presence.degrade_after_minutes`（默 15）即打 `absent_record=true`、`absent_by='silence'`，**复用 S3 那条缺席通路**（不改生命周期、只加一个标记，所以 H1 已闭合的那半边没被牵动）→ 无原因可评的 OFFLINE 告警以 `cancelled='absent'` 闭合、健康度剔除、仍进在线分母、铃声停。两条阈值各答一问：`absent_after_minutes`=3（跨重启："它今晚在不在"）vs `degrade_after_minutes`=15（进程内："别再为它响了"）。措辞侧新增 `absent_by` 前端分支（"Server 运行期间静默超时" / "自 Server 重启后未再上报"），避免把两种成因印成同一句话。自检：`selftest-roster` presence 段（注入时钟 16min）+ `selftest-events` C6 |
| **H19** | **`identity.json` 存了 `server` 却不参与解析**：配对时把当时的 Server 地址一起写进身份文件（`main.py:158` `save_identity`），但 `config.resolve()` 的优先级链是 CLI > env > `agent.json` > 默认，**identity 只提供 `host_id` 与密钥**，`server` 字段被存而不用。三台生产机现在靠计划任务/unit 里烧死的 `--server` 活着 | 单看今天无害；但它埋了一条很难查的路径：哪天按 S6 的"参数不再烧进任务"做清理（unit 改成裸 `python main.py`），三台会一起回落到默认 `ws://localhost:9100`——Server 在别的机器上，表现为"改了个启动参数，全机群集体失联"，且 `--print-config` 会诚实显示 `server = (from default)`，容易被当成无关噪声略过 | 2026-09-19 里程碑三台 `--print-config` 实测输出；`config.py` 解析链 | **低优先级可选**（原挂 S6"启动参数瘦身"，**S6 实际没做这一项** → 改挂 V3 与 H24 同批，不要单独动）：要么让 identity 的 `server` 参与解析（排在 `agent.json` 之后、默认之前，并在 `--print-config` 里点名来源），要么干脆不存这个字段——**存而不用最坏**，因为它同时具备"看起来会生效"和"实际不生效"两个属性。**本期现状**：S6 没有动这条，但 Agent 的启动横幅本来就把解析后的地址打在明面（`main.py:116` `Server: …`，S2b 起），S6 新增的探测计划打印（`main.py:171`）紧随其后 → 真做"裸 `python main.py`"时会连续两行指着同一个事实（回落到 `ws://localhost:9100` + 计划来源），比原来只看得到一行更好认 |
| **H20** | **口令失败预算按"来源地址"计数**（S5 G2 引入的新形态，登记备查，本期不改）：60s 内同一来源 8 次失败 → 该地址 429。当前形态 A（每个浏览器各自直连 231）下正确；**如果将来中间加一层反向代理、或多人经同一出口（公司 NAT / 同一台 ZT 网关）进来，一个人的连输会锁住同地址的其他人** | 低摩擦误伤：被锁的人看到的是"口令未被接受"，猜不到是别人输错的 | `index.js:221-248`（`ADMIN_FAIL_*`，键为 `req.socket.remoteAddress`），429 的两个出口在 `:256` 与 `:276` | **低优先级可选**：机群或用户数上来后改法有两种——按 `(地址, 会话)` 复合键，或在反代场景信任 `x-forwarded-for`（**只有真的加了可信代理才许信这个头**，否则等于把预算送给伪造头的人）。现在就加"信任转发头"是把误伤换成绕过，不做 |
| **H21** | **在场台账是一份会长久的留存记录**（S5 §4 引入）：`presence_state` 跨重启保留每台对端的 `first_seen`/`last_seen`/`name`，`roster.db` 因此多了"哪些机器什么时候在线"的历史；同时 `zt.json` 内含本机 ZT 端口与排除节点 id | 与"读侧无鉴权"这条既有决策叠加后，能打开 9101 的人 = 能看到这份在场历史（**不含 IP、不含密钥、token 不进任何响应**，这是 S5 G3 已断言的边界）。小圈子用途下已接受；对外发截图/分享包时这页要过一眼 | `roster.js` 的 `presence_state` 表；`/api/presence` 只回 ZT 节点 id（10 位十六进制）而不回 IP/token（hygiene D 段逐端点扫描） | **【不建议现在实现】"在场历史可导出/可清除"这类治理功能**——它是账号化（H11/H13 长期解）之后才有意义的话题。当前只记：分享包不含 `roster.db`、`zt.json` 已 gitignore |
| **H22** | **一次"只改一项"的阈值写入会把 Server 打死，而且坏值落库、重启不救**（S6 实施过程中发现，本期只登记未改）：`setThresholds()` 是**整体替换**语义，但 `validateThresholds()` 对**缺失**的键直接 `continue`（只校验带来的那些），而 `replaceInto()` 会把 target 里 next 没有的键 `delete` 掉 → 一个只带 `{mem:{…}}`（或只带一个标量如 `{offline_seconds:15}`）的 POST 让 `thresholds.cpu_usage` 从内存与 DB 行里一起消失。**⚠ 09-20 文档对账更正**：本条原先举的例子是 `{mem_usage:{…}}`，而 `mem_usage` 是个**不存在的键**——未知键在 `config.js:136-139` 就被判"未知配置项"、整个请求 400 拒掉，照旧例子写回归用例会误判成"已修"。`METRICS` 的真实键名是 `cpu_usage / cpu_temp / mem / disk / gpu_temp / rtt_ms / packet_loss`（09-20 从 231 的 `GET /api/config` 现场核过，顶层键为 `cpu_usage cpu_temp mem disk gpu_temp rtt_ms packet_loss offline_seconds alert events presence`）。`status.js levelFor()` 读 `th.crit` 无护栏，下一拍 `evaluateHost()` 抛 TypeError，而它跑在 `index.js:544` 那个**没有 try/catch 的裸 `setInterval`** 上 = 未捕获异常 → 整个 Server 进程死；231 的计划任务 5 秒拉起 → 起来就死 | 这是本期**唯一一条"一个写请求能让 Server 反复崩溃且自恢复无效"**的路径。说清它不是什么：不是安全洞（过得了管理口令才碰得到），也不是 UI 缺陷（设置页永远发整张表）。正因如此现有自检与浏览器验收都**看不见**它——只有手搓 curl、老客户端、或将来任何"只改一项"的调用方能碰到，而那恰好是"对外收集需求"之后最可能出现的场景。现场形态很难看：看板全断 + Agent 连不上 + 日志每 5 秒同一行栈 | `config.js:109`（undefined → continue）、`config.js:150`（delete 缺键）、`status.js:25`（`value > th.crit` 无守卫）、`index.js:544`（裸 timer） **【本行行号＝2026-09-20 登记时位置；修后已移位，指认请认函数名：`validateThresholds` / `replaceInto` / `levelFor` / `snapshotOrNone`】** | **【已修 2026-09-22 凌晨·本机 commit `9cba7c8`·未 push 未部署】三半都落地，各自承重（缺一半都还能死）**：① `config.validateThresholds` 要求 `METRICS` 全覆盖，缺项**点名**拒绝（"阈值表必须整张提交，缺 cpu_usage、cpu_temp"），既不修也不接受；② `status.levelFor` 缺档返回 `null` ＝ "这一档不判"，既不是永远红也不是崩，`worstLevel` 跳过 null 所以卡片其余部分照判；③ `index.snapshotOrNone(where)` 把**两条入口各自包**（2s 广播 ＋ `clientWss.on('connection')`），抛了只跳这一拍，并 `console.error` ＋ 落一条 `system/evaluator_error`（`events.js` 有自己的中文行，重复按 `merge_seconds` 并成一行 ×N），客户端 `KIND_TABS` 同时登记第 6 格"系统"。**读数（复核时本机重跑）**：server 309 → **326 全过**（config 套 48 → 65，H 段 17 条），client **38 全过**（token 套 10 → 23 是第 2/2.5 刀带来的，与 H22 无关），`vite build` 通过。三条正对照（拿掉②的守卫／拿掉③的 try／只留广播那一拍）逐条红在预期位置。**复核另查两件**：`alertEngine.tick` 的调用链已亲眼确认（`store.js:201` ← `getAnnotatedHosts` ← `getSnapshot:278`），所以③那句"两条入口"是准确的；但 `/api/hosts` 那条路由（`index.js:162` 裸调 `getAnnotatedHosts()`）是**第三条评估入口**，它在 express 的 catch 里＝后果是 500 不是死进程，而 H6b 的结构断言只数 `store.getSnapshot()`、看不见它 —— **登记不追改**。**未做两件**：(a) boot 不重校验既有 DB 行（② 已保证不崩，且设置页存一次即自愈：它的 payload 按 `spec.metrics` 逐项 `deepSet`、与库里有没有那一行无关 → 这条从必修降为可选）；(b) ③ 触发期间看板停在最后一帧、不出 stale 横幅（"跳拍"与"拉闸"之间的中间态，属 H27 心跳族而不是本条）。**交人眼一次**：设置页第 6 个 tab 在窄宽度下会不会换行、"系统"格里那行中文读起来像不像"我们自己的机器坏了" —— 本机没在浏览器里看过，**标未验证**。 原文（登记时的判法，保留）：**【高优先级必做】建议列为 V3 首批第一条**。修法两半且都要：① `validateThresholds` 要求 `METRICS` 全覆盖，缺项即拒并在错误列表里点名"缺 cpu_usage"；② `levelFor` 对缺失阈值返回 `null`——"没有这条阈值"既不等于"永远红"，也不等于"崩"。只修②是把崩溃换成静默失效，只修①是让下一个调用方继续踩。另外给评估器包一层 try/catch + 落一条"评估器异常"事件：**监控系统不该因为一次评估失败而整个下线**，这条与 H5"系统自身无人监控"同族。**⚠ 09-20 文档对账更正（这一格原先的覆盖面写窄了两处，照原文施工会留下活的崩溃路径）**：原文点名"那个 timer 与 `alerts.tick`"，但 ① `alerts.tick` 没有自己的定时器——它是 `store.js:201` 在 `getSnapshot()` 里同步调用的，与 :544 本来就是同一个调用点，"包两处"实际只包了一处；② 真按原文只包 `index.js:544`，Server 仍会被打死，因为同一段评估代码还有一条不经过该 timer 的入口：`index.js:539`（`clientWss.on('connection')` 回调里直接 `store.getSnapshot()`）= **任何一个浏览器刷新页面就能再触发一次**。所以 try/catch 的落点是 :544 与 :539 两处（另有 `history.js:111` 那个每小时 roll 定时器，但它已在 `rollAndCleanup()` 内部 `history.js:188` 被 catch 包住，不要重复包）。事件落点用现成的 `events.record()`（`server/src/events.js:127`），不新建机制；但客户端的 `KIND_TABS`（`client/src/lib/events.js:20-26`）要同时登记新 kind，否则变成"服务端记了、界面上没有一格归置它"。本期未修的原因：①是对校验契约的收紧，而 S6 的自检全部建立在"整体替换"语义上，收尾期改它收益/风险不合（用户指令：有问题留 V3） |
| **H23** | **开发用 mock 已被 S2a 的身份绑定半废**：`server/scripts/mock-agent.mjs` 在**一条 WS** 上多路复用 7 个 `host_id`，而 `frameHostMatches()`（H4 的封堵面）认定"一条连接只能改写它注册的那个节点" → 除第一个之外全部 `REFUSED … host_mismatch` | 不伤生产（真 Agent 一台一条连接），伤的是**开发侧预置条件的诚实度**："多机场景"实际塌成单机，而跑 mock 的人看不出来。H14/H18 那类"多节点 + 缺席 + 冻结"的场景已经无法用 mock 复现，只剩 selftest 的合成帧能演 | 本次 S6-4 浏览器验收实测；改用每主机一条连接的临时脚本（`00-暂存/s6-one-frame.mjs`）才拿到 3 台 | **低优先级可选**：改法二选一——每 host 建一条连接（与真 Agent 同形，推荐），或在 mock 头部明写"本脚本的多节点模拟已退化，多节点场景走 selftest"。**不建议现在实现**"Server 侧允许一条连接多身份"来救它：那是为了迁就一个测试脚本把 H4 刚关上的边界重新打开 |
| **H24** | **真源收敛还差两处**（H9 的余量）：① `zerotier.js:48-51` 直读 `server/config/zt.json`（缺文件即关、连定时器都不建），config.js 不知道它、设置页也没有它；② `BROADCAST_INTERVAL_MS = 2000` 是 `index.js` 的启动常量，而同族节律 `alert.tick_ms`/`events.merge_seconds` 已可配——两个挨着的钟，一个能改一个不能。再加上 H9 例外①（`resetThresholdsToSeed()` 重读文件，"回落播种值"实际是"回到当前文件值"） | "改一处配置要去看三个地方（DB 行 / thresholds.json / zt.json）"正是 H9 要避免的分裂形态的小号版本；今天规模下不致错，但它让"**设置页是唯一的配置入口**"这句对外承诺不能全说。zt 那处还有一层：它是**读侧无鉴权**下唯一能关掉在场台账的东西，而这个开关不在界面上 = 不知道它存在的人会以为在场台账总是开着 | 上列行号；`config.js:240` | **低优先级可选**（V3，与 H19 的"启动参数瘦身"同一批）。**不建议把 zt.json 并进 DB**：它是本机专属、含端口与排除节点 id、且 gitignored，搬进 `roster.db` 等于把"这个库可随包分发"变成"这个库带着上一台机器的网络拓扑"——那是真源分裂的新形态而不是收敛。正确做法是给它一个"经 config 读、不缓存、不写库"的位置，界面上只留一句"由本机 `zt.json` 决定" |
| **H25** | **冻结行会丢"该机自定义"标记**（H14 的已知残留）：`custom` 是评估时由 `pushReason` 打上去的**内存态**，`alert_events` 表没有这一列，所以 `restoreActive()` 从库里恢复出来的那条不知道自己当初是自定义阈值触发的 | 纯呈现、窗口短（默认可配最长 5 分钟内，且只影响冻结行）：一条"该机未再上报"的行同时少了"为什么这台的阈值和别人不一样"的解释。与 H14 已修的"伪恢复"不同，**这条不是谎话，只是话没说全** | `alerts.js:getLists()` 的 `...(e.custom === true ? { custom: true } : {})` 依赖内存 entry；`history` 的 `alert_events` DDL 无 `custom` 列 | **不建议现在实现**：加列是一次 schema 迁移，V2 收尾期不该为一条几分钟的呈现缺口动库。登记即可。真要修时优先选"**从 `nodes.settings_json` 现算**"而不是加列——那是把标记从"触发时的快照"变成"当前事实"，顺带让宽限期之后仍活动的行也标对 |
| **H26** | **启动期那一次 `wal_checkpoint(TRUNCATE)` + `VACUUM` 不足以把明文节点密钥从盘上清干净**（S5 上线当晚在现场取到的第一条）：`#migratePlainKeys()` 的整库重写发生在**启动过程中**，此时 WAL 里还有本次启动刚写的内容、且库仍在被写；实测生产 `roster.db` 主文件 offset 8142 处残留一条明文 32 位十六进制节点密钥的旧页镜像——所有**活动行**都已是 `sha256$盐$散列` 形态（`key-shape-probe` 三台全部 `HASHED`），残留的是**已释放页里的历史字节** | 不是"迁移没生效"，而是"H16 存储侧收口"这句承诺的强度被高估了一档：能读 231 磁盘的人仍能捞出一个曾经有效的节点密钥，而我们在文档里写的是"磁盘上真的没有明文了"。风险面与 H16 同源（磁盘可读 = 已经输掉机器），但**措辞必须降级**，否则这条隐患会被当成已闭合 | 现场序列：`SCAN before: main_hits=1 at 8142 wal_hit=false` → 停服后跑 `00-暂存/deploy-231/roster-vacuum.mjs`（`PRAGMA wal_checkpoint(TRUNCATE)` + `VACUUM`）→ `SCAN after: main_hits=0` → 三台 Agent 重连后再扫 `现役 roster.db / -wal / -shm = NO-HIT`。自检未覆盖：现有断言查的是"SELECT 出来的字段是散列"，没有查页复用 | **低优先级可选**（V3 顺带做，不单独动库）。**修法**：把这一次 checkpoint+VACUUM 从启动路径挪到**迁移发生了改写的那一次**之后立即执行、且不要在还有其它写者的时候执行——现实做法是提供 `server/scripts/vacuum-roster.mjs` 并在**部署说明**里把它写成密钥迁移的固定后一步（本次是手工补的）。**不建议**为了这一条把 `VACUUM` 常驻启动路径：231 是低功耗小主机，整库重写每次启动都跑一遍是拿启动时间换一次性的清理效果。机理为什么是"启动期那一次不够"仍未定论，本期只做登记（用户指令：V2 收尾不追改） |
| **H27** | **231 本机 ZeroTier 的 TCP/9993 HTTP 口对任何请求都回 401，而同机 CLI 一切正常**（上线当晚现场取到的第二条；**原来的"内存 token 与盘上文件分家"假设已被今晚的整机的重启证伪**）：02:06:49 重启把 `ZeroTierOneService` 带回来了（新 pid 4352、02:07:11 起），`/api/presence` 仍是 `http-401`、`last_ok_at:null`。之后把"token 不对"和"送 token 的方式不对"分开验了一整轮（diag3~diag8，全只读）：① 四种送法全 401 —— `?authtoken=`（原样 + `EscapeDataString`）、`Authorization` 裸值、`Authorization: token <t>`、`x-authtoken`，`Invoke-WebRequest` 与 `curl.exe` 两条实现一致；② 没有实例/端口歧义 —— 9993 只有一个监听者（pid 4352 = `C:\ProgramData\ZeroTier\One\zerotier-one_x64.exe`，LocalSystem/Auto），`zerotier-one.port` 那 4 字节是 ASCII `9993`，`identity.public` 的节点 id 与 CLI 报的地址一致 → 盘上这份 token 就是运行实例那个 home 里的；③ token 可读、24 字符、纯字母数字（无 URL 保留字符）→ 编码/转义不是原因；④ **我们的实验其实没有判别力**：空 token 与明显错的 token 同样 401，所以"401"无法区分"token 不对"和"这个 build 压根不看这几个字段"；⑤ CLI 通道是通的：`zerotier-cli.bat info` → `200 info <node-id> 1.16.1 ONLINE`，`-j getPeers` exit=0、约 94 ms、3.4 KB JSON（**前面带版本横幅，要剥到第一个 `[` 才能 parse**） | 在场台账（S5 G3 / H21）在 231 上目前**没有数据可读**：`/api/presence` 回 `{"enabled":true,"source_ok":false,"error":"http-401","counts":{"visible":0,"present":0,"named":0}}`，日志一行 `[Presence] ZeroTier source unavailable (http-401)`。**表现是设计对的**——按降级分支显示"来源不可用（http-401）"，而不是把"看不到"印成"没人进来"，所以这条不影响任何已有功能，只让 S5 的在场页在生产上暂时是空账 | 2026-09-20 01:40 首读 + 02:08 重启后复读（同值）；诊断脚本 `项目/remote-net-tool/hm231-zt-diag.mjs` ~ `hm231-zt-diag8.mjs`（全只读，token 全程在 231 本机展开、任何输出/命令行里都不落值；diag4 的 ③ 有一条 PS 解析错，不影响结论，已由 diag6 ①② 补上） | **V3 第一批里成本最低的一条，而且改法已经从"猜"变成"验过可通"**：`zerotier.js` 的取数加一条回退分支 —— HTTP 回 401 时改跑 `zerotier-cli.bat -j getPeers`（execFile，剥掉版本横幅后 `JSON.parse`），一次约 94 ms、30s 一轮，负载可忽略；输出侧仍守"token 不进任何接口/日志"的老约束，节点地址照旧掩码。**不要再做的事**：为了这个 401 去重启 ZT 服务（今晚已证无效，而且会让全机群 overlay 掉线数十秒）。**不建议现在实现**"界面上加一个 ZT token 配置口"——那是把一个诊断问题变成一个功能面，而且 231 上的在场页本来就不是 V3 的主题（UI/UE）。**给明天看板上眼的人**：在场台账显示 `source unavailable (http-401)` 是**已知预期**，不是这次上线的新故障 |
| **H28** | **`untouched_since_seed` 从第二次开机起永远是 `false`，哪怕没人改过一个字**（S6 配置层的语义坑，上线当晚两次读数对比暴露）：`config.js` 的 `loadFromStore()` 判的是"**DB 里有没有这一行**"，而**播种这个动作本身就会写那一行**（`store.set(KEY_TH, JSON.stringify(seed))`）→ 首启 `true`、重启后 `false`。这不是运行时错误（取数、优先级、回落三件事都对），是**这个布尔量的名字比它的语义强**：读起来像"没人动过"，实际含义是"库里有行" | 两处会误导人：① `部署说明.md` S6 §2 的现场验收写的是"`untouched_since_seed` 应为 `true`"——从第二次开机起这条**永远不成立**，照文档判会把"没人改过配置"读成"配置被人动过"，进而怀疑整批上线读数的基线；② 设置页那句 `v-else` 文案"已由设置页/接口写入 DB：文件不再被读取（H9）"，在"播种后没人改"的情况下是**假陈述**，朋友看到会以为自己或别人动过阈值 | 01:40 首启读数 `true`（当时写进了本文件的现状基线）→ 02:08 重启后 `/api/config` = `untouched_since_seed=false`，而同一次 `/api/hosts` 显示三台仍无任何逐机覆盖、阈值数值仍等于 `thresholds.json`；代码侧 `config.js` 的 `seededFromFile` 只在 `raw == null` 分支置 `true`（`snapshot()` 里 `untouched_since_seed: seededFromFile`），`SettingsView.vue:339-344` 两分支文案 | **V3 顺带改，不必单独立项**（一次写入 + 一处文案）：让那个布尔量名副其实——要么播种时记 `meta.config_touched=0`、只有走设置页/`/api/admin/*` 的写才置 1；要么读的时候比较 `raw === JSON.stringify(seed)`，逐字节等于播种值仍算"未动"。同时把设置页文案改成三态（仍是文件播种值／播种后没人改过／已被写入），并修正 `部署说明.md` S6 §2 的期望值为"首启 true、之后 false 属预期"（**09-20 对账：文档这一小半已经做完了**——`部署说明.md` 的上线记录与读数表两处都按"后者是预期值"印着，V3 只剩"布尔量改名/改判 + 设置页三态文案"这一半，别再去改文档）。**不建议现在改**：V2 已冻结，而且它是可读性问题、不是正确性问题 |
| **H29** | **回滚备份里躺着迁移前的明文节点密钥，而且没有任何机制会提醒我们它在那儿**（S5 的"散列落库"承诺只覆盖**现役库**，不覆盖盘上的副本）：`C:\Users\user\hm-backup\20260920-013149\data\roster.db*` 是 S5 启动迁移**之前**手工拷的快照 → 里面三把节点密钥仍是明文；同一天家目录里还另有一份同形残留（`roster.db`/`probes.json`/`thresholds.json`，形状探针实测 `PLAINTEXT-32hex` + 真实网关与关键机 IP），**今晚已按三条护栏删除**（现役 counterpart 必须存在 + 该文件未被任何进程占用 + 逐个打印结果），备份这份属回滚件、当时不能删 | 谁拿到 231 这块盘（或这一份备份）= 同时拿到**三台机器的写权限凭据**（节点密钥即写身份）+ 家庭网络拓扑（真实 IP）。这与我们对朋友的承诺（"名册里只存散列"）不冲突，但**只在现役库这个范围里不冲突**——这句话以前没写在任何地方 | 家目录残留的形状探针对照（`HASHED` vs `PLAINTEXT-32hex`）与清理清单：`00-暂存/deploy-231/cleanup-list.txt`；备份的文件清单见 `部署说明.md` 2026-09-20 上线记录的备份一节（12 文件 / 13.1 MB，含 `data\roster.db`） | **已关闭（2026-09-20 上午，办公室经 ZeroTier 口操作，用户确认"不再回滚"后执行）**： 删的是 `hm-backup\20260920-013149\data\roster.db*` 三个文件（逐文件先打印大小 + SHA256 前 12 位 + 占用状态：`roster.db` 28,672B / `-shm` 32,768B / `-wal` 4,120,032B —— 明文密钥主要在 `-wal` 那份未合并的旧帧里，所以只删主文件等于没删），三条护栏全过（现役 `data\roster.db` 53,248B 存在 = 不是孤本、三个文件都 FREE、逐个打印 `exists_now=False`）。 复扫结果：`hm-backup` 全树只剩 `config\probes.json`（225B，真实探测目标 = 家庭网络拓扑，**不是凭据**）与样本库 `host-monitor.db*`（约 8.9MB，事件里的地址在写入时已掩码）。整目录要不要清掉仍等他一句话——它现在既不含凭据也不含回滚能力。 **这一刀换来一条必须写清的能力变化**：退回 S5 之前（`ae8ace6`）的路径从此**只能靠重新配对三台 Agent**，因为老代码不认 `sha256$` 前缀而那份能喂它的库已经没了；退回 `61bf4db` 之后的版本不受影响。写进 `部署说明.md`"上线记录 · 2026-09-20"末尾。 **保留的可选护栏（低优先级，V3 顺带）**：部署脚本收尾扫一次——`ROOT` 之外出现 `roster.db*`/`probes.json`/`zt.json` 就打印并要求确认（今晚那份家目录残留正是早期手工部署留下的，robocopy 的 `/MIR` 只管 `ROOT` 内部，看不见它）。 **不建议**做"备份加密"：那是新范围，而且这台机上的备份是给人手工救火用的，加一道加密就变成先解密才能回滚。 |
| **H30** | **"闪"这条被 R-4 定为最稀缺的通道，在客户端里有六处关键帧定义、五个动画名、四档周期，一处受闸的都没有**（V3 翻暗期间 09-21 夜由他的现场读数反查出来）：R-4 重校的那套是 `style.css` 的 `alarm-flash`（1.2s／透明度 1→0.72），**只有一个消费者**——`TopologyRenderer.js:941` 给 3D 标签加 `.alarm-blink`。而看板上真正在闪的东西走的是**另外几套**：顶部报警横幅那个小红点用 `AlertBanner.vue:53` 的 `blink`（**1s／1→0.3**）；`pulse` 在 `style.css:110` 与 `ClusterBar.vue:135` **逐字重复定义**两份（1→0.4），第三份在 `EnrollView.vue:516`（1→0.3），使用者 = `ClusterBar.vue:148` 的 CRIT 健康点 **1s**、`ClusterBar.vue:134`＋`style.css:109` 的 `.conn-dot.ok` 绿点 **2s**、`EnrollView.vue:515` 配向导点 **1.6s**；`KioskView.vue:327` 的 `.k-health.beat` 是第四套 `k-beat`（**1s／1→0.45**）。合计 **6 处关键帧定义／5 个动画名／4 档周期（1s·1.2s·1.6s·2s）／5 档幅度（0.3·0.4·0.45·0.55·0.72）** | 不是色值问题、是**语义问题**，两条各自踩在 V3 的主判据上：① **绿点在闪**——`.conn-dot.ok` 表达的是"连接正常"，那是一句心跳不是一个事件 ⇒ 把最稀缺的通道花在安静态上，且让"绿"进入"正在发生"的读法（违 R-1＋R-4）；② **CRIT 红点在长期磁盘 CRIT 下会永远闪** ⇒ 叙事态占用告警态通道，正是"余光策略"要防的形态。**诚实的一半**：他 09-21 的读数是"警告，又不是那么燥"——**没觉得吵**，因为那个点只有 9px。所以这条不是"现网在折磨人"，是**规则没有落地机制**：R-4 说了"省着用"，但没人守着这个通道有几个家 | `AlertBanner.vue:53-55`（`.bn-icon` = **1s／1→0.3**，另有 `:54` 的 `.bn-icon.still{animation:none}` 停闪分支）、`ClusterBar.vue:134-135,148`、`style.css:109-110,244-249`、`EnrollView.vue:515-516`、`KioskView.vue:327-328`（`.k-health.beat` = `k-beat` **1s／1→0.45**）、`TopologyRenderer.js:941`；`check-tokens.mjs` 只比 **DOM↔WebGL 的色值**（登记时 23 对；**09-22 凌晨第 3 刀 `3341803` 删掉 INK 三对，现 20 对**，客户端断言 38→35），**完全不管周期与幅度**；`selftest-sound.mjs` 管的是声音侧循环，也不管运动通道。**⚠ 09-21 夜自我更正两处**：① 本格初版把他看的红点认成 `ClusterBar` 的 `.hp-dot`，**实际是 `AlertBanner` 的 `.bn-icon`**（他的原话是"顶部红色**报警横幅**的小红点"，横幅=AlertBanner，我没照着话去定位）；② 初版数出"四套周期、三份重复定义"是**一次 grep 粗扫的结果**，逐条打开看语义后真实规模是 **6 处关键帧定义 / 5 个动画名 / 4 档周期（1s·1.2s·1.6s·2s）/ 5 档幅度（0.3·0.4·0.45·0.55·0.72）**，其中 `blink` 与 `k-beat` 是粗扫时漏掉的。**教训同 §6.6 那两次同源**：按"扫出来的数字"写台账，数字本身就是错的；台账里的证据格必须逐条开过。 | **登记，不顺手改**（09-21 夜他刚认可当前读数，改完必须他再看一次，属于"要过他"的那一档）。**修法三半**：① 六处关键帧收成一处、周期与幅度统一到 R-4 那一档（1.2s／1→0.72）；② `.bn-icon` 那条**不要新造机制**——它已经有一个被自检守着的停闪分支 `.still`（H14 用它收"全部 CRIT 都已冻结"），对症的改法是**把"长期"也接进这个分支**，而不是删动画；③ `.conn-dot.ok` 绿点**是否改常亮，判权交他**——因为"它在闪"可能正是他要的"这面墙还活着"的证据，那是价值不是噪声，我没有依据替他消掉。**要补的是闸不是色**：给这条通道立一份清单式断言（允许的动画名、周期与幅度的取值范围、以及"只有告警类可无限循环"），否则下一次重校仍然只覆盖到 `alarm-flash` 一处。**并刀／单刀交他判**（同一批文件，并刀边际成本≈0）。**不建议**为它加配置项（决策 10"不做通用化"）<br>**✅ 09-22 下午：本条第 2 步（通道清单）交付，在任务书 §6.12，零改动、闸仍未写。** 本格 header 里那三个数**按现况作废并给出真值**：绿点停闪（`2b515ef`）之后，循环类＝**5 条通道／3 档周期（1s·1.2s·1.6s）／4 档幅度（0.30·0.40·0.45·0.72）**——原先记的"四档周期含 2s"里那 2s 是绿点的，已不存在；"五档幅度含 0.55"里那个 **0.55 是 `.bn-icon.still` 的静态 opacity，不是一档摆动**（数字对、尺拿错，与 H31 那两个 CR 同种错）。定义份数也订正：**7 处 `@keyframes`／5 个动画名**，其中 `style.css:105` 那份 `pulse` **现在是零消费者的孤儿**（两支在用的是 SFC 各自的本地同名定义，scoped 编译会改名，全局这份够不着）。**清单最大的增量不在 CSS 这半边**：3D 里另有 4 条连续通道从来不在任何账上（A0 链路脉冲点＝合规；**A1 风扇 `rotation.y += 0.04 + usage/100*0.22` 未乘 dt ⇒ 帧率越高转得越快，同一台机器在不同 fps 下"有多忙"读出来不一样**；A1 shimmer＝不表达任何事实的常量运动；A1 组件 CRIT 脉动周期 ≈1.047s 与 DOM 那条 1.2s **同屏差拍**）。**并发性两条**：墙上（`?kiosk`）**只有 1 条**在闪（`KioskView` 不挂 `ClusterBar`／`AlertBanner`）⇒"闪太多"这个担心在墙上不成立，成立的是交互页；**A1 单机页最多 4 条同屏**。另登记 `prefers-reduced-motion` 全仓 **0 处**（存量、非翻暗引入）。**四条待判（一条都不替他收敛）已在 §6.12 末段列出**，其中我认为唯一不属审美的是风扇那条（乘 dt 属读数正确性），但它同样要他点一次头。 |

| **H31** | **翻暗漏了"黑色 alpha"这一整族（**实测 30 处，登记时记成 24**——我的 grep 只认 `rgba(0,0,0` 紧排逗号式，漏掉 6 处带空格的写法，见色表 R-6），因为这个方向的表达余量已经被暗底吃光**（第 2.5 刀复核时顺手查出来的，09-21 夜登记）：这些声明写于米色纸面时代，语义是**"压暗＝凹陷／槽／抬起／反馈"**——在 `#f5f0e6` 上黑色 alpha 有巨大的可用范围，在 `--bg #0d1117` 上**压暗只剩下约 4% 的余量**。实测算术（**09-22 凌晨订正：合成底是 `--panel #16212d` 而不是 `--bg`，负载槽本来就画在卡片面板上**；读数已并进 `00-暂存/contrast-check.mjs` 的 A 段，**从此可一键重跑，不再是 prose**）：负载槽底 `rgba(0,0,0,.07)` → `#141f2a`，**对面板 CR 1.024**（低于安静面下限 1.10）；**历史水位峰线 `rgba(0,0,0,.35)` → `#0d141b`，对槽底 CR 1.111**（非文本图形的可读下限通常取 3.0）。⚠️ **09-22 凌晨订正：本格初版的两个 CR 是"数字对、配对错"**——登记成"槽底对地面 1.10／峰线对槽底 1.03"，实际是"槽底对**面板** 1.024／峰线对槽底 **1.111**"。**结论不变（都远低于 3.0）**，但别人照公式复算会以为脚本坏了；错因＝算完直接手写进表格、没落进可重跑的算式。**原文（登记时的写法，保留）**：负载槽底 → `#0c1015` 对地面 CR 1.10；峰线 → `#080b0f` 对槽底 CR 1.03。同一族里 hover 反馈是 `.03~.05` 档，比峰线更弱 | 三件事按严重度排：**① 数据不见了**——峰线是"这台机历史上到过这个水位"的唯一呈现，CR 1.111（订正前的登记值是 1.03，同一结论）等于**这条事实在墙上已经消失**，而它正是这个产品第一问（"有没有变化"）的载体；**② 读不出"离满还差多少"**——负载条的 fill 是状态色（看得见），槽底是黑 alpha（看不见），于是只剩一截亮条、没有刻度终点，**而"槽＝未占用"这个方向本身是对的（合 R-2 更暗＝没信息），错的只是量级**；③ hover／投影的"抬起"与"凹陷"反馈在暗地上基本归零（`box-shadow` 四支黑投影 `.08~.18`）——A0 零输入所以墙上无妨，桌面视图（HostCard／EventStream 的 hover）仍然要。**它不是"没上漆"，是这一族从来没进过任何清单**：第 1、2、2.5 刀的判据都是"状态色"，而黑 alpha 不承载状态，所以三把刀都按规矩放过了它 | `components/ClusterBar.vue:188`（槽 .07）与 `:191`（**峰线 .35**）、`views/KioskView.vue:407`/`:413`（同形，墙上 3px）、`components/HostCard.vue:351`（槽 .07）＝**数据族四处**；中性底／hover：`AlertPanel.vue:86,94`、`EventStream.vue:121,138,151`、`HostCard.vue:311,315,325,386`、`HudPanel.vue:216`、`DetailView.vue:285`、`EnrollView.vue:507`、`ClusterBar.vue:152`、`KioskView.vue:324`；投影：`App.vue:236`、`HostCard.vue:296,379`、`PassphraseDialog.vue:89`；模态遮罩：`PassphraseDialog.vue:82`（`.28`，方向与 R-2 一致，**这条不用改**）。逐条列法是因为本轮已经吃过两次"用一种写法去数一个有多种写法的事实"（§6.6／H30）。⚠️ **09-22 凌晨自我订正：本格初版的"24"仍然是错的，真值 30**——**唯一误差＝模式只认紧排逗号式 `rgba(0,0,0`，漏掉 6 处带空格的 `rgba(0, 0, 0, α)`**（`DrawerV3.vue:299/321/334/338`、`HudPanel.vue:196`、`TrendChart.vue:73`）。⚠️ **订正自己的订正（09-22 凌晨，同一次复核内）**：我上一版把原因写成"两处独立误差：① 数的是命中行数不是出现次数；② 空格变体"。**①是假的**——重跑证明紧排式的**出现次数＝命中行数＝24**，没有一行有两支。**我为了把"24"解释圆，顺手编了一个没验证的机制。** 这比数字本身更贵：**错的归因会指错防护方向**（照它去加一条"行数≠次数"的对照，防的是一件不存在的事，而真正漏的那一类——空白符变体——照旧没人防）。⇒ **R-6 的适用范围由此扩到"原因"：不只数字要正对照，"我为什么数错"这句话同样要能重跑。**可重跑命令：`git grep -o -E 'rgba?\(\s*0\s*,\s*0\s*,\s*0' HEAD -- client/src \| wc -l` ＝ **30**。**而"斜杠式另扫过一遍为零"这一句当时是一条没有正对照的 0**（R-6 抓的就是它）：09-22 拿一条已知该命中的 `rgb(0 0 0 / 35%)` 喂给同一个模式，命中 1 ⇒ **这条"零"如今成立且有对照了**，但登记当时它只是"我没扫到"。**"没扫到"与"不存在"差一个正对照**，而台账读者分不出这两者。 | **登记，不在第 2.5 刀顺手做**（它的判据是状态色，塞进来会让那一刀的"零观感变化"前提失效）。**修法方向由色表推，不由眼睛试**：① **峰线必须反到亮侧**——它是"有信息"的标记，按 R-2 就该比地面亮，建议取 `--border` 或 `GROUND.plate` 之上那一档，别再往暗处走；② **槽底改"比地面亮一档"或干脆描一条 1px `--border`**（R-3：层次靠面、边界靠描边），保留"未占用＝更暗"的语义靠 fill 的缺失来表达，不靠黑 alpha 的量级；③ hover／投影这一族**单列、低优先级**：暗底上投影本来就是弱信号，改成亮边是审美倒退，**建议只把桌面视图的 hover 底改到能看见的档**，四支 `box-shadow` 维持现状并在此留一句"暗地上它不再承担层次表达"（不许假装它还在工作）。**④ 同一次复核还翻出反方向的漏项两处（亮端残留，刺眼级；两处都不属于"状态色"，所以三把刀按判据都没管）**：`views/KioskView.vue:392` 的 `.k-stale{background: rgba(245,240,230,.55)}`——**这就是旧 `--bg` `#f5f0e6` 的 rgba 写法**，快照一旧就在黑墙上糊一层米色纱（本机预览因此只敢开 `/`、不敢开 `?kiosk`）；`components/PassphraseDialog.vue:98` 的 `rgba(255,255,255,.6)`——口令卡＝黑墙上一块近白纸片，与第 2 刀修掉的 `.topo-label` 同罪。**建议并入第 2.6 刀一起判**（同属"地面族亮端"，改法都是回到 `--panel`/`--bg` 那一侧，不新增色值）。**顺带补一条规则进色表**（已做，见 R-2 推论）：**"有信息"的标记不许用"比底色更暗"来表达**——否则下一次还会以新形态出现（第 2.5 刀那 30 条中性黑就是这次没判的那一半）。**判时**：与第 3 刀同批或单列第 2.6 刀，交他定；纯呈现层，不碰契约四耦合点<br>**⚠️ 09-22 上午：本行的 14 机械＋2 峰线＋3 局部地面已落地（＝上面 header 那一天的提交）**，剩余为**已裁的不动项**（hover 5／投影 5／`ephemeral` 3／遮罩 1）。**落地时留下四项交判，本行不替他裁**：① `DrawerV3.vue` `.dw-table td` 的 1px 缝线取 `--border` 后 CR **1.224**（在安静面分带内、但低于细描边带 1.60~3.50 下限）——同族兄弟 `.dw-table th` 与全仓结构缝线本来就 100% 用 `--border`，**改成它就是"与同族一致"，要更亮只能抢 `--absentLine`/`--noData`（撞 R-1）或发新色（撞 R-5）**，所以停在这里；② `HostCard.vue` `.hcb-bar` 在**离线卡**上因整卡 `opacity:.75` 落到 **1.112**（贴安静面下沿）；③ 两个输入框底我给了 `var(--bg)`，仓库其余 13 个控件的既有配方是 `var(--bg-glass)`——**统一它只要改两行，但它属"惯例一致性"不属本刀判据**；④ `.hcm-item:hover` 的 `rgba(0,0,0,.05)` 在菜单底翻成 `--panel` 后**确定成空操作**（本来就 <1.10），属 hover 族令箭不许动。`DetailView.vue` 那处 `filter: saturate(.15) opacity(.8)` **仍未裁、未动**。<br>**✅ 09-22 上午（后段）：以上四项他一次裁完，原话"全部按默认执行"＝四条都采纳我给的默认值；本行至此没有未决项。** 落地情况：**只有 ④ 产生一笔代码改动**（`DetailView.vue` 的 `filter` 撤掉 `opacity(.8)`、留 `saturate(.15)`——去色＝"这台不在"合法，压暗＝"这里没东西"撞 R-2 推论），客户端 35 条断言重跑同读数（20+9+6）；①②③ 按默认**不改**。**⚠️ 但裁之前我把两个数跑飞了，此处一并订正，别照旧数动手**：② 的 **1.112 重跑不出，真值 1.088**——我当成了 `--panel` 卡底（实为 `--bg-glass` 合成 `#15202b`）、当成 `.lv-offline{opacity:.75}` 命中（该卡同时带 `.absent{opacity:.62}`，写在后面所以它赢）⇒ **它已掉出安静面下限，不是"贴下沿"**；裁的结论不受影响（缺席卡本该退到背景里，前提是"这是一张缺席卡"仍读得出来：虚线描边＋灰态＋"已停止心跳"）。③ 那句"其余 **13 个控件**用 `--bg-glass`"**是并族并出来的假数**，可重跑真值＝**`background: var(--bg-glass)` 39 处声明／17 个文件**，且其中多数是面板／条／卡底而非控件。① 另有一处**分类错**（数字对、尺拿错）：缝线不承载意义，该按**安静面 1.10~1.70** 判 ⇒ **1.224 本来就合格，这项从头就没有欠账**。 |
| **H32** | **主按钮／选中态的白字压在自己的强调色上，CR 2.526——文字族不达标，而且这一条与翻暗无关**（第 2.6 刀复核时顺手扫出来的，只登记未改）：6 处 `color: #fff` 压在 `background: var(--accent)`（`#58a6ff`）上。它**在米色地上也是 2.526**（这一对不依赖页面地面），所以不是翻暗翻坏的，是**一直存在、以前没人拿分带量过** | 影响的是**"当前选中／主行动"这一族入口**：ClusterBar 视图切换的选中态、EventStream 的筛选选中态、口令卡主按钮、配对向导主按钮、总览与设置页的主按钮。11~14px 的字压在上面，按 WCAG 文字档下限 4.5 判**不达标**，按载义图形 3.0 判也差一点；它是墙上／桌面上**唯一表示"你在这里"的一族**，读不出＝导航成本回到记忆 | 可重跑：`git grep -n -E 'color:\s*#(fff\|FFF\|ffffff\|FFFFFF)' -- client/src` ＝ **6 条命中，逐条都含 `background: var(--accent)`（我另数了一遍 `grep -c accent` ＝ 6）**。⚠️ **本行首次登记时这条"可重跑命令"自己是坏的，两处**：① 反斜杠丢了（写成 `color:s*`）⇒ **它扫不到带空格的 `color: #fff`**，与 R-6 那 6 处空格变体同形；② 壳里的竖线没转义 ⇒ **直接把本行表格切成 9 格**（`00-暂存/md-table-check.mjs` 一跑就抓到，跑之前没人发现。⚠️ **我写这段订正说明的时候，把那个竖线原样放进反引号里，于是订正说明自己又把表格切成 7 格**——同一个坑在一段话里踩第二次，是这条纪律存在的理由，不是笑话）。修正后的模式命中仍是 6（逐条核过都带 `--accent` 底），并拿一条 `color: #ffffff` 做过正对照。⇒ **记一条纪律：写进台账的命令也是命令，落笔时就得跑一遍。**CR 复算＝`(1.05)/(L(#58a6ff)+0.05)`＝**2.526**（同一支蓝在暗地上当文字用是够的，**不够的是白字压蓝底**）。⚠️ 本行是我这一轮唯一"扫出来的是别人的旧账、不是本刀造成的"条目，登记而不追改 | **低优先、独立一刀**：修法要么换字色（`--bg` 压 accent＝够），要么换 accent 那一档——**都是色值决策，交他眼睛**，且改的是"选中态"这种他一眼就能看出对不对的东西，不值得夹在任何刀里顺手做。翻暗收口之后、A0 静态稿之前判即可 |

| **H33** | **详情页那块电路板上"线重叠"是有机械成因的，而且其中一层根本不表达数据**（09-22 傍晚他自己给出的两条现状读数，反查代码证实）：同一块主板上有两层走线，**两层都不做避让**。① 装饰层 `_addDecoTraces` 是 **60 条伪随机线（`seed = 42`）＋ 30 个随机过孔**，`opacity 0.4`，**一条都不表达任何指标**；② 总线层 `_addTrace` 全部画在**同一个高度 `y = 0.04`**，且 Manhattan 路由写死"先横后竖"、**第一条水平段永远落在 `z = from.z` 那条线上**——而绝大多数总线的 `from` 是同一个 CPU 点，**于是每条总线的前半段彼此完全重叠，后画的盖住先画的** | 影响的正是 V3 给 A1 定的那个角色：P4"节点详情双层化"（`b2fac48`）之后主板已经**从"数据屏"降成 3D 外壳＋点击拾取**，数据在 2D HUD 与组件／链路抽屉里。所以重叠**不读错任何数字**（读数在 DOM，决策 1 第 2 条），它伤的是"这块板值不值得占这么大一块画面"——而总线层是**唯一表达真实连接关系**的那一层，被装饰层糊住＝把承重件混进背景件。⚠️ **连带一条更硬的**：那 60 条随机线是 V3 静态稿禁令里"没有数据的装饰"的正主（任务书 §4），它在详情页不在陈列架上，**所以不违本轮竖切的稿子约束，但 A1 那一轮必须判它去留** | 可重跑（三条，都是无管道的单模式，直接粘）：`git grep -n "_addDecoTraces"`（定义＋调用共 2 处）、`git grep -n "_addTrace"`（定义 1 处＋各总线调用点）、`grep -n "for (let i = 0; i < 60" client/src/three/TopologyRenderer.js`（装饰条数）。重叠结论来自逐条读 `_addTrace` 的 `segments.push` 两行，**不是画面上数出来的**；正对照＝我按 `from` 相同这一条件先排掉了"随机线互相压"这个解释，再确认总线层的共起点才是系统性重叠源。**未做**：截图像素级取证（本机浏览器截图返回空这一已知限制），所以"重叠在他屏上有多显眼"＝**他的眼睛读数，我这里只有成因** | **登记，不追改**（V2 之后新发现缺陷一律只登记这条纪律照守）。**分两半，两半都归 A1 那一轮**：① 装饰层去留＝观感决策，交他眼睛（若留，也该把 60 条降到不与总线同屏抢读）；② 总线层若留，最小改法＝**给各总线换个 `y` 常量错开高度**（一行数量级的改动，不新增机制、不加材质），但"错开之后好不好看"不由我判。**shimmer 那条通道他已裁"留"**（09-22 傍晚），所以本行不是要撤微光，是**微光底下这两层谁该让位**。与 H30 的关系：M8（shimmer）说"这条通道不表达事实"，本行说"它踩在一条表达事实的线上"——**同一条 mesh 上的两件事，别并刀** |
| **H34** | **陈列架的"折行阈值"是一个看着像旋钮、其实没接线的常量**（09-22 深夜复核包 2 的排布轴时，用一个变异实验抓出来的）：`SHELF.wrapAtContainerWidthPx: 960` 自带注释"= `maxSlotsPerRow × slotWidthPx`"，读起来像"改这个数就改了什么时候折第二排"——**但折行算式不读它**，`silhouette.js:166` 是现算的（`Math.floor(containerWidthPx / slotWidthPx)` 再按 `maxSlotsPerRow` 截顶）。全仓**只有自检读它**（`selftest-silhouette.mjs:191` 拿它跟现算值对账） | 后果不是显示错，是**下一个人会改错地方并且看不出来**：这个值是任务书 §5.4「排布轴唯一的家」那张表旁边最容易被伸手改的一个数，改完墙上纹丝不动，而没有任何东西报错——和 H9 同形（**一个事实两个家，其中一个不干活**），只是这次两个家都在同一个文件里、相距 15 行。⚠️ **同时要说清它没有造成的那部分**：它**不影响**判据①，也**不影响**墙上现在的排布，所以这条不值得单开一刀 | 可重跑（两条，都是无管道单模式）：`git grep -n "wrapAtContainerWidthPx" -- client` ＝ **3 处**（定义 1 ＋ 断言 2），**生产消费者 0**；正对照＝同一条模式扫 `maxSlotsPerRow` 命中 **7 处**（含 `:166` 那条真算式与渲染器 `:234` 的注释），**证明这条 grep 看得见这类常量、那个 0 不是探针坏了**。**触发方式＝变异实验而不是扫表**：把 `silhouette.js:150` 的 `maxSlotsPerRow: 4 → 3`（只改这一个数），跑 `node client/scripts/selftest-silhouette.mjs` ⇒ ① `git diff --name-only` 全程只有 `silhouette.js` 一个文件（**生产侧确实只需要改一处，§5.4 排布轴成立**）；② 2 条断言 FAIL（`960 ≠ 720` ＋ 八点探针表列／排数全对不上），**证明这条闸有牙、不是永真式**；③ 那个惰常量当场暴露。实验后已还原：`git status` 干净、70 条断言回到 20/9/6/35、`REAL_EXIT=0` | **一行删除，建议夹进包 3**（同文件族、不新增判据、不动 §5.4 三条落点、不碰契约四耦合点）：删掉 `wrapAtContainerWidthPx`，让 `selftest-silhouette.mjs:190` 那条断言自己算 `maxSlotsPerRow × slotWidthPx`。**判据侧一句实话**：现在守着这个配对的**只有那条断言**，所以"删常量"不能删成"删对账"——真源只能是算式，不能是一个没人读的字面量。**若他更愿意留着它**（当作可读文档），那至少要降级成注释：**常量摆在那里＝承诺它能改行为，而它不能**。纪律照旧＝登记不追改，因为它是包 2 自查时我自己抓的、且顺手成本≈0，所以这里给的是"建议随包 3"而不是"另起一刀"。**09-22 夜销账（`2aa0862`，本地未 push、未部署）**：常量已删（原 `silhouette.js:151` 那行），并且**没有删成"删对账"**——`selftest-silhouette.mjs:189-207` 那条换成一对话必须同时成立的读数：反半边 `'wrapAtContainerWidthPx' in SHELF` 必须为 false（谁复活它就红），正半边 `slotWidthPx 240 × maxSlotsPerRow 4` 两把尺还在、现算出的 960 **在边界上真的是墙**（960px→4 列 1 排／959px→3 列 2 排）。条数 35 不变、客户端基线 **82 条全绿**。**三发内存变异实测（`HM_MUT` 改 `SHELF`，不动仓库文件）**：`revive`（把键塞回去）⇒ 全套只有我这条红（其余 34 绿）＝**再次实测"这个键有没有人读"只有这一条**；`ruler`（槽宽 240→300）⇒ 2 红（我这条＋八点探针）＝正半边不是装饰；`maxslots`（每排 4→3）⇒ 2 红，且**墙自己移到 720**（`720px→3列2排／719px→2列2排`）＝改排布不需要改第二遍。**销账时的读数口径三条，留给下一个复跑的人**：① `git grep -n "wrapAtContainerWidthPx" -- client` 现在也是 **3 行**，与本行登记时的"3 处"**同数不同物**——那 3 行现在全在自检那一条断言块里（注释／`in` 检查／PASS 文案），定义那处没了，`-- client/src` 是 **0 处**；② 不带 pathspec 的全仓跑会连本台账一起命中（就是本行），别当成生产消费者；③ 正对照 `maxSlotsPerRow -- client` 由 7 行涨到 **10 行**，多的 3 行是**新断言的文案自己引用了它**——这类命中数会被注释与文案推高，读之前先分清是代码还是话。**本行不给"报错报反了"那类预言打勾**：我只实测了"删掉常量而不同步改断言"会红成 `undefined ≠ 960`（推演，不是读数——我按同一笔同时改的做法没给它红出来的机会）。 |
| **H35** | **道具台数这个事实被自检抄了三份字面量**（V3 包 3 步 2 落地当场撞出来的，`6206e70`）：任务书 §5.3 裁定 3 说"给道具组补一台活着报数的笔记本＝**一处 demo.js 道具表改动**"，代码上确实只改了一处（`server/src/demo.js:90-99` 加一行 SCENARIO 条目），但 `server/scripts/selftest-roster.mjs` 里把"共 4 台"当断言写了三遍，于是加一台道具的真实成本是**改三处断言** | 后果不是显示错，是**下一个人会挑代价最小的那条路**：要么不敢加道具（判据① 的复测样本继续只有那台离场卡），要么把三处改成从夹具 derived 顺手把"恰好几台"的保护一起扔掉。与 H9／H34 同形＝**一个事实多个家**，不同之处在于这次的家是**判据本身**——所以它比 H34 更贵：H34 那个多出来的家不干活，这里三个多出来的家**都在干活，只是每次加机器都要同时改它们** | 可重跑（改动前／后各一遍，实测）：`cd server && for f in scripts/selftest-*.mjs; do node $f; done` ⇒ 加行前 **7 套 14/65/50/59/10/100/28 ＝ 326 条全绿、7 个 exit=0**；加行后同一命令六套读数**一字未变**，只有 selftest-roster 变 **97/3、exit=1**，三条红＝`F8`（diagnostic 里列出那 5 个 模拟- 名字）、`F8b`、`G6` ⇒ **归因唯一＝+1 台道具**，不是别的改动。站点：`git grep -n "\.total === 4" -- server/scripts` ＝ **2 处**（`:606`、`:713`）＋ `git grep -n "props.length ===" -- server/scripts` ＝ **1 处**（`:601`，另有 `:714` 的 `+ 4`）。**正对照**（R-6）：`git grep -n "SCENARIO" -- server` 命中 **7 处、全在 `server/src/demo.js`**（真源 `const SCENARIO` 在 `:67`，`SCENARIO.length` 在 `:152`／`:213`）——证明这条模式系列**看得见"台数"的真源**，断言里出现的是抄件而不是引用，不是探针坏了 | **本包不处置，交人裁**（铁律：确认自检本身有问题就停下报告，不许改自检让它通过）。三个处置：**(a)** 三处字面量 → 从夹具 derived，**(b)** 三处 4 → 5（最小改动，但下次加道具再撞一次），**(c)** revert `6206e70`（判据① 失去复测样本）。**agent 的建议是 (b) 打底 ＋ 把方向定成"名单钉死、台数派生"**：断言改成"道具名册恰为 `sim-vectordb, sim-game, sim-media, sim-notebook, sim-tmpnote` 这五个 id"，台数从 `demoFleet.ids().length` 拿——这样"不小心多注册一台"仍然当场可见（名单会变），而加道具只改夹具一处＋名单一处。改断言语义属他的裁量，本行只登记。**包 4 预告**：若加 roster 形态列，本行这三处会再撞一次。 **⇒ 09-22 深夜销账（他裁"名单钉死＋台数派生"，即上表建议里那个方向被提成正修，(a)(b) 两案都没取）**：`server/scripts/selftest-roster.mjs` 里 F8 换成钉**名单**（`PROPS` 五个 id 逐字面量，台数与在线数一律从 `demoFleet.ids()` 派生），F8b／G6 的 `=== 4` 全部改成派生值（`fixtureIds.length`／`propCount`）。计数不变：本套仍 **100 条**，七套仍 **326 全绿**，客户端仍 **82**（20+9+6+35+12）。**牙已实测在**（正对照，不是推理）：往 `SCENARIO` 插一台 `sim-mutant`（名字照规带 `模拟-` 前缀，好把失败隔离到"名单不认"这一因）⇒ 重跑 **只有 F8 一条红、exit=1**，diagnostic 同时打印 `got`／`fixture` 两串；F8b 与 G6 因为台数派生**照常绿**——这正是"派生"该有的分工：数量自己跟、名单要人签。随后逐字符还原，`git diff --stat -- server/src/demo.js` 空，复跑回 **100／326 全绿**。上表"包 4 预告"那句**因此作废**：加形态列不动台数，而即便再加道具，要改的也只有名单那一处。剩下的成本从"改三处断言"降到"改一处夹具＋签一次名单"，也就是**加一台道具必须过一次人**——这是本案真正想买的东西。 |

## 待决项（非隐患，属架构记账）

- **历史库体积**：SQLite 无 auto_vacuum，`DELETE` 不回收空间；raw 7d + 1m 30d 量级下可控（3 台 ≈ 数十 MB 量级），但换机群规模后需复核。当前无需动。
- **`getSeries` 30d 区间**：会把区间内所有含 `gpu_json/link_json` 的行全部读入并逐行 `JSON.parse`（3 台 30d ≈ 13 万行）。首屏若默认拉长区间会卡顿；改法=按区间降采样或限制 JSON 列查询。
- **首次 roll 的 catch-up**：`#roll` 一次读入 `rolledThrough` 之后的全部 raw 行（全新安装且已有 7 天数据时是整表）。可分批。
- **哪些配置改完还要重启（S6 之后这一条已经缩得很小，但必须写准）**：`thresholds` 与 `probes`（含逐机阈值、逐机/站点探测计划、`alert.tick_ms`、`events.merge_seconds`、`presence.*`、陌生告警宽限期）**全部热生效、不需要重启 Server**（H9 已收口，自检 B 段）。仍然要重启的两类：① `HM_*` 启动环境——端口与 `HM_INGEST_TOKEN` 档位（翻档按 S2 §8 就是重启，别指望界面）；② Agent 侧——`--interval` 与 `agent.json` 里的地址要重启那个进程。另有一条容易误读：**探测计划的换目标不是热生效，而是"下次接入生效"**（目标写在 Agent 手里，`applies_on:'agent_reconnect'`），所以"热加载"这三个字对阈值成立、对探测计划不成立，界面上分别写清了。
- **演示机群会在历史库里留 30 天尾巴**：道具照常落 `samples_raw`/`samples_1m`（详情页要有曲线），关掉只是停止生成＋退役节点，已写入的行按自然保留期衰减。清理语句在 `S2-细化设计.md` §8；4 台道具 ≈ 每分钟 4 host-minute，roll-up 开销可忽略。**不做**"关演示即删数据"——那是把用户以为已经删掉的东西变成静默破坏。
- **kiosk 的新鲜度横幅依赖标签页定时器**：`KioskView` 的 1s ticker 推动"已停止刷新 X 秒"，而 Chrome 对**隐藏或被遮挡**的标签页把定时器节流到约 1 分钟一次（实测：内置浏览器页面 `document.hidden===true` 时横幅比预期晚出现约 1 分钟）。年龄取自 wall clock 而非 tick 计数，所以只会晚报、不会错报；但"值守屏必须始终前台、不被遮挡"从此是一条部署要求而不只是使用习惯。真机验收时确认一次（副屏 + `?kiosk` 前台）。见 `S4-细化设计.md` §7.4-7。**2026-09-19 更新（用户口述）**：他其实不需要"副屏"这个显示形态（"你可能误会我的意思，不过没关系～保留吧"）→ 功能保留、**围绕 kiosk 的视觉投入冻结**，那条真机验收也随之降级为"顺手看一眼"，不再是一次需要他安排的验收；"值守屏必须前台"这条部署要求仍然成立，只是不再有义务为它配一块屏。
- **L3 主板 3D 没有本期这套生命周期护栏**：`contextlost` 自愈、`_disposeObject`、`visibilitychange` 暂停只补在 `ClusterTopologyRenderer`（kiosk 与拓扑 tab 共用那一个）。`TopologyRenderer` 同病未动——它是坐着看的视图、白屏代价低一档，且已在讨论下线（收尾段）。若最终保留，需要把同一套护栏搬过去而不是重写一遍。
- **密钥迁移那一次 `VACUUM` 是启动期的整库重写**（S5 G1）：只在"确实有明文行要迁"时跑一次，量级 = 名册库大小（当前数十 KB，秒级）。它换来的是"磁盘上真的没有明文了"——**这句在 2026-09-20 现场被实测削弱了一档**：启动期那一次之后 `roster.db` 主文件仍剩一条明文密钥的旧页镜像（offset 8142），是在停服后手工再跑一次 `checkpoint+VACUUM` 才归零的，见 **H26**。所以准确的表述是"活动行全是散列 + 一次**停服**清理才是完整收口"。它同时是**开机路径上的一次性写放大**，231 上如果将来名册变大（几百台）就要看它是否影响启动。第一次上线现场确认两行日志：`Migrated N plaintext node key(s)` 与随后端口就绪（本次实际印的是 `Migrated 3 plaintext node key(s)`，三台全部升级）。
- **在场台账给 231 增加了一个每 30s 的读点**：ZeroTier 本地 API 一次 POST + 命中可达时才写 `presence_state`（写侧被 `PRESENCE_WRITE_MS=60_000` 节流，一台稳定在场的机器 ≈ 每分钟一行 UPDATE，不是每 30s）。不装 Agent、不采指标、不进告警评估，所以功耗面与主机采集不在一个量级；但"多一个常驻轮询"这件事本身在低功耗机上是要记的账，`zt.json` 缺文件即完全关闭（连定时器都不建）。
- **设置页的浏览器验收是在临时库里做的，不是 `server/data`**（S6-4/S6-5）：`00-暂存/s6-ui-harness.mjs` 在 `%TEMP%/hm-s6-ui-*` 里新建 roster+history、灌三台假节点（`ui-laptop`/`ui-infer`/`ui-office`，探测目标全是随手编的假地址，与真实机群无关）再起 Server+vite。**为什么绕这一圈**：设置页写的是**真配置行**，而本机 `server/data` 那份库是 231 上线演练的基准——不能为了看界面把真网关地址印到屏幕上，也不能让一次点错的"应用"改动演练基线。harness 里那个口令**只活在那个临时目录的库里，不是任何真实凭据**（脚本里能看到明文，仓库里没有，也不该有）。要复现 H14 的铃声读数再加 `00-暂存/s6-one-frame.mjs <cpu> <host_id>`（每主机一条连接，原因见 H23）。两个脚本都带绝对路径、留在 00-暂存，不随仓库提交。
- **`server/data` 与临时库的关系要留一句**：本机日常自测一律用临时目录，`server/data` 只在"和生产同形"的验收里才碰。将来若做"配置导入导出"，这条纪律要变成显式的 `--db` 参数而不是靠约定。
