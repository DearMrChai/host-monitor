# 技术隐患清单 — host-monitor

> 2026-09-19 第二轮迭代入场审计（后端架构视角）产出。
> 用途：**随迭代逐项消化**，不是待办清单的全集。每项标了影响面与建议处理时机。
> 现状基线：生产部署停在 `2ed74c8`（231）；开发区 HEAD = S1a/S1b（`9552c7f`/`0790208`）+ S2a 服务端，尚未上线。
> 注：本文件不含真实 IP/凭据，可随仓库提交。

| # | 隐患 | 影响 | 证据 | 建议时机 |
|---|---|---|---|---|
| H1 | **幽灵节点**：`store.hosts` 只增不删，节点一旦注册永久留在名册；`cluster.total`= 历史出现过的一切节点；`evaluateCluster.health` 取全体最差档且 OFFLINE 权重最高 → 改名/退役/长期关机的机器把顶部横幅钉死灰灯，并触发 OFFLINE 循环铃声 | 直接污染产品第一问"集群健康吗"；在线率分母随 Server 运行时长只涨不降；退役节点改名即永久残留直到重启 Server | `store.js` 无 remove API；`status.js:148` worst 归并含 OFFLINE；`sound.js` CRIT/OFFLINE 循环 | **S1a/S1b 已做（2026-09-19）**：持久化 roster（`nodes` 表）+ 常驻/临时/退役三态，健康度与分母只算常驻在线。**残留**：H12（常驻机重启后不重连仍隐形）是本项的最后一块，排 S3 |
| H2 | **Agent 无配置文件、无版本上报**：`--server/--host-id/--role/--interval` 全靠命令行烧进计划任务与 systemd unit；register 帧无 `agent_version` | 机群混版本不可见 → 升级无法验收；任何"加机向导/远程装"都要靠拼命令行字符串，易错难回滚 | `agent/main.py:121-135`；`index.js:71` ack 字段 | **高**：D0 加机的前置项（`agent.json` + version 随 register 上报，Server 存并展示）。服务端侧已就绪（`roster` 存 `agent_version`/`fingerprint`，ack 回 `node_key`），**待 S2b Agent 侧** |
| H3 | **探测环串行**：每轮耗时上界 = Σ 各目标超时（ICMP 3s + TCP 2s 级），目标数线性放大滞后；`_interval` 之后才 sleep | 3 台无症状；key_hosts 增长后链路指标变陈旧、滑窗统计失真 | `prober.py:107-115` | **低**：机群 >5 台或做 mesh 前改 `asyncio.gather` + 总预算封顶 |
| H4 | **host_id 身份可碰撞**：默认由 hostname 推导，且 9100 曾无鉴权 → 同名机器/伪造注册互相覆盖同一节点记录 | 数据正确性（非安全议题，决策 6 另议）：表现为读数抖动、拓扑出现重名幽灵 | `main.py:26-31`；`index.js:47-71` | **部分（S2a）**：伪造注册被 ingest 凭据挡掉；一条连接改写别的节点已被 `frameHostMatches()` 封堵并记台账；指纹漂移服务端会留痕。**残留**：真 Agent 尚未上报指纹（S2b），绊线在此之前空转；跨机同 id 碰撞的检测待 S2b 上报后才能生效 |
| **H5** | **监控系统自身无人监控**：Server 有计划任务崩溃拉起（已覆盖进程崩溃），但 231 断电/睡眠/整机死机 = 全静默；告警渠道仅页面内，人不在屏幕前等于没有告警 | 异常可达性在"人不在跟前"的时段归零 | 产品 v1 §2.4 决策"仅页面内" | **用户已知情并接受为产品边界**（2026-09-19 拍板：调试期维持界面内，不做出站）。理由：异构机群满载是常态，夜半推送 = 为人陪编译。→ 真正的诉求转为**告警过滤/静默/归属**（见 H7），出站通道待机群稳定后重提 |
| **H8** | **信任边界已移动，ingest 曾无凭据**：Agent 接入端口 9100 无 token；机群实际跨 ZeroTier（约 10 台含朋友设备），不再是纯局域网信任域。任何 ZT 组成员可注册任意 `host_id`、伪造或劫持既有节点数据 | 决策 6（无鉴权）的前提"安全边界=局域网"已不成立；后果是数据可信度，不是泄密 | `index.js:44-47` 无校验；用户口述机群含朋友设备与临时机 | **S2a 已做（2026-09-19）**：配对码→每节点密钥的两段式凭据 + `ingest.js` 三档强制（默认 `legacy`：陌生 host_id 一律拒绝，已入册老 Agent 宽限并在看板点名）+ metrics 帧身份校验（连带修掉 H4 的重写面）。**剩余**：三台生产 Agent 换带 `agent.json` 的新版后翻 `strict`（里程碑重部署时，动作已写 `S2-细化设计.md` §3） |
| **H6** | **全局单探测计划**：Server 把同一份 `probes.json` 推给所有 Agent（网关/关键机按家庭局域网 IP 写死）。Agent 一旦从别处接入（办公室、朋友家、ZeroTier 侧），它的"网关臂"探的是你家的网关 → 语义错乱 | 机群跨网后链路层结论不再可信 | `index.js:30-36,71` 单对象广播 | **高**：与名册同批，探测计划改为**按节点派生** |
| **H7** | **阈值全局单一**：所有机器共用一份 `thresholds.json`。异构机群（推理机/编译机/游戏机）里满载是**正常工作状态**，不是异常 → 必然产生"陪朋友编译"式噪声告警 | 告警可信度下降 → 用户开始忽略横幅 → 产品失效 | `status.js:11` 全局 import | **高**：需 per-node/per-role 覆盖 + 手动静默 |
| **H9** | **配置真源即将分裂**：V2-a 引入 `nodes` 表后，运行时配置同时存在于 JSON（thresholds/probes）与 DB → 两边都能改 = 行为分裂、排障不可能 | 中后期必然踩到；且现在所有配置改动都要重启 Server | `status.js:11`、`index.js:30-36` 均为启动期一次性读文件 | **高**：与名册同批定死"DB 唯一真源 + JSON 仅作首次种子 + 热加载" |
| **H10** | **写侧将无防护**：设置界面（加节点/改阈值/录凭据/删节点）一旦上线，任何能打开 9101 的人都能改机群——读开放是需求，写开放不是 | 朋友路过看一眼 = 零摩擦（保留）；但误操作可删节点、可改他人机器凭据 | `index.js:requireAdmin` 中间件；`roster.js:hasPassphrase/checkPassphrase` | **S1a 已做**：写侧管理口令（scrypt 存哈希、默认拒绝、口令未设时一切写 403），S2a 把新增的签发配对码/演示开关同规则纳入。**读侧维持无鉴权**（原决策） |
| **H11** | **【重点风险 · 已排为面板成型后的第一优先】第三方机器口令落在 231**：用户 2026-09-19 拍板"存口令，不再要求对方加公钥"，并明确"成型期先专注完善项目，凭据治理当成面板成型后必做重点工作" | 能读 231 磁盘者 = 取得所有已登记机器的登录凭据；对外展示/收集需求会**放大接触面**（更多人知道这台 Server 的存在与其上持有他人机器密码）；"加密"在无远端密钥前提下是障眼法，不做伪装 | 决策升版见纪要 §2.5 / §5 | **成型后必做（闸口级）**。成型期先记不实施。**连带硬约束：observed（SSH 拉取）通道不得排在口令治理之前做**——它需要保管别人的密码，见纪要 §5.2。过渡期兜底：口令不进 API 响应 / 不进日志与异常栈 / 不进 git 与分享包 |
| **H12** | **长期不上报的常驻节点会"静默消失"**：presence 与分母都来自**内存主机表**（`store.hosts`），不是名册。Server 重启后一台常驻机再没回来 → 没有卡片、不进 `在线 n/m` 分母、也不会新建 OFFLINE 告警。S1b 只保住了"重启前已活动的那条告警"（冻结 5 分钟），**没有告警的常驻节点仍然完全隐身** | 恰是最该报警的情形（机器被搬走/重装/长期关机）没有任何交代；对外展示时是硬伤 | `store.js` 主机表只由 register/metrics 填充；`status.js:evaluateCluster` 只遍历传入 hosts | **高**：S2/S3 把 presence 改为**名册驱动**（`nodes` 里有、主机表里没有 = 缺席常驻 → 直接进 OFFLINE 档），并配一条"缺席超过 X 小时"的独立提示 |
| **H13** | **管理口令在多设备间会互相踢**：口令明文存在各设备 localStorage，任一处更换即让其他设备的写操作 403。S1b 把"无限弹层"改成了明确报错（缺陷 11），但**服务端没有重置/找回通道**——忘了就只能上服务器改库 | 朋友或第二个自己误改口令 = 看板写侧集体锁死；对外收集需求时会放大 | `admin.js` 403 分支；`roster.js` 只有 `setPassphrase(需旧口令)` | **中**：短期把重置命令写进部署文档（删 `meta` 表 `admin_pass` 行即回到"未设置"）；长期随 H11 做账号化 |
| **H14** | **冻结告警的铃声代价**：`unknown_alert_grace_minutes`（默认 5min）内，一个已经消失的演示/模拟节点的 CRIT 会保持活动，而客户端 CRIT/OFFLINE 是**循环铃声** | 每次重启 Server 后最多 5 分钟无意义鸣响；演示机上更常见（mock 随时关） | `alerts.js` `UNKNOWN_GRACE_MS`；`App.vue:setCritLoop` | **低~中**：**S2a 已消掉最常见触发源**——演示机群关闭时其节点退役、告警立刻以 `retired` 闭合，不再冻结 5 分钟（`selftest-roster` G6e）。**仍未做**：给"冻结/陈旧"告警加视觉标记并在声音侧降级、或把 grace 配到 1-2 分钟（S2c 一并看） |
| **H15** | **接入台账是内存环形缓冲**：`ingest.events` 只留最近 20 条、不落库 → Server 重启即清空；`enroll_codes` 只判过期不删除，死行永久留在表里（行很小但无界） | "昨天谁试过接进来"这种排障问题答不了；对外展示够用（现场演示不看重启前），运维期会不够 | `ingest.js:EVENTS_MAX`；`roster.js` 无码表清理路径 | **低~中**：S3 的 24h 事件流正是它的归宿（拒绝/配对/漂移都是事件），届时把码表清理并进同一批 retention |
| **H16** | **节点密钥明文存储、明文过线**：`nodes.enroll_token` 以明文躺在 `roster.db`，配对时在局域网 WS 上明文传输（无 TLS）。能读到 231 磁盘、或能在链路中间监听者 = 可顶替任意已配对节点上报假数据 | H8 关的是"凭空造节点"，**没有关"冒充已知节点"**；`legacy` 档下连冒充都不需要密钥（见 S2 §3 残留风险） | `roster.js:newNodeKey/keyMatches`；`ingest.js` 模块头 "Honest limits" | **与 H11 同批（S5 闸口前不做伪装式加密）**。本期已做的补偿：密钥不进任何读接口/日志（G5 断言）、写侧要口令、`strict` 档把"无密钥即可写"这条路封死（翻档动作在里程碑）。真正的收口需要 TLS 或每机一次性握手，属成型后工作 |

## 待决项（非隐患，属架构记账）

- **历史库体积**：SQLite 无 auto_vacuum，`DELETE` 不回收空间；raw 7d + 1m 30d 量级下可控（3 台 ≈ 数十 MB 量级），但换机群规模后需复核。当前无需动。
- **`getSeries` 30d 区间**：会把区间内所有含 `gpu_json/link_json` 的行全部读入并逐行 `JSON.parse`（3 台 30d ≈ 13 万行）。首屏若默认拉长区间会卡顿；改法=按区间降采样或限制 JSON 列查询。
- **首次 roll 的 catch-up**：`#roll` 一次读入 `rolledThrough` 之后的全部 raw 行（全新安装且已有 7 天数据时是整表）。可分批。
- **`--interval`/`thresholds.json`/`probes.json` 均需重启生效**：改配置=重启 Server，运维成本随节点数上升。若做名册化，顺带把"配置热加载"列入同一改造。
- **演示机群会在历史库里留 30 天尾巴**：道具照常落 `samples_raw`/`samples_1m`（详情页要有曲线），关掉只是停止生成＋退役节点，已写入的行按自然保留期衰减。清理语句在 `S2-细化设计.md` §8；4 台道具 ≈ 每分钟 4 host-minute，roll-up 开销可忽略。**不做**"关演示即删数据"——那是把用户以为已经删掉的东西变成静默破坏。
