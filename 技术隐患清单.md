# 技术隐患清单 — host-monitor

> 2026-09-19 第二轮迭代入场审计（后端架构视角）产出。
> 用途：**随迭代逐项消化**，不是待办清单的全集。每项标了影响面与建议处理时机。
> 现状基线：**2026-09-19 V2 里程碑已上线 231**，生产包 = `ae8ace6`（= S1a/S1b + S2a/b/c + S3a/b + S4a/b），三台 Agent 1.1.0 且已配对、接入侧 `strict`。**S5（凭据治理 + 在场台账 + H18）已在本机完成并全绿自检，尚未上线** → 下一次上线 = S5 闸口批之后的里程碑，逐台动作与回滚路径见 `部署说明.md`"S5 上线步骤"（**这一步回滚必须连 `roster.db` 一起退**）。
> 注：本文件不含真实 IP/凭据，可随仓库提交。

| # | 隐患 | 影响 | 证据 | 建议时机 |
|---|---|---|---|---|
| H1 | **幽灵节点**：`store.hosts` 只增不删，节点一旦注册永久留在名册；`cluster.total`= 历史出现过的一切节点；`evaluateCluster.health` 取全体最差档且 OFFLINE 权重最高 → 改名/退役/长期关机的机器把顶部横幅钉死灰灯，并触发 OFFLINE 循环铃声 | 直接污染产品第一问"集群健康吗"；在线率分母随 Server 运行时长只涨不降；退役节点改名即永久残留直到重启 Server | `store.js` 无 remove API；`status.js:148` worst 归并含 OFFLINE；`sound.js` CRIT/OFFLINE 循环 | **S1a/S1b 已做（2026-09-19）**：持久化 roster（`nodes` 表）+ 常驻/临时/退役三态，健康度与分母只算常驻在线。**H12 已由 S3a 补上（2026-09-19）**：presence 改为名册驱动，缺席常驻进分母、有灰卡、有一条事件，但**不进健康度**（S3 §4.2 的取舍），本项至此闭合 |
| H2 | **Agent 无配置文件、无版本上报**：`--server/--host-id/--role/--interval` 全靠命令行烧进计划任务与 systemd unit；register 帧无 `agent_version` | 机群混版本不可见 → 升级无法验收；任何"加机向导/远程装"都要靠拼命令行字符串，易错难回滚 | ~~`agent/main.py:121-135`~~ 现为 `agent/config.py`；`index.js` ack 字段 | **S2b 已做（2026-09-19）**：`agent.json`/env/CLI 三层解析（`--print-config` 说明每项来源），`identity.json` 存身份与节点密钥，register 帧带 `agent_version`+`fingerprint`，Server 存并在名册展示（端到端断言 P2b/P8）。**2026-09-19 里程碑：三台生产 Agent 已升到 1.1.0**（名册 `agent_version` 全部非空、`identity.json` 三处落盘、`--print-config` 三台实测 `credential= identity.json`），"混版本可见"这半在真数据里成立 |
| H3 | **探测环串行**：每轮耗时上界 = Σ 各目标超时（ICMP 3s + TCP 2s 级），目标数线性放大滞后；`_interval` 之后才 sleep | 3 台无症状；key_hosts 增长后链路指标变陈旧、滑窗统计失真 | `prober.py:107-115` | **低**：机群 >5 台或做 mesh 前改 `asyncio.gather` + 总预算封顶 |
| H4 | **host_id 身份可碰撞**：默认由 hostname 推导，且 9100 曾无鉴权 → 同名机器/伪造注册互相覆盖同一节点记录 | 数据正确性（非安全议题，决策 6 另议）：表现为读数抖动、拓扑出现重名幽灵 | `config.py:default_host_id`（仅未配对时）；`ingest.js:#drift` | **S2a＋S2b 已做（2026-09-19）**：伪造注册被 ingest 凭据挡掉；一条连接改写别的节点被 `frameHostMatches()` 封堵并记台账；真 Agent 现已上报 `fingerprint`，同一 id 从另一台硬件上来会在异常接入台账里以"指纹漂移"点名（**接受但不静默**，S2 §4：拒绝 = 自我致瘫）。**残留**：`host_id` 由 hostname 推导只发生在未配对的首次启动，配对后被 identity 钉住（C9）；"真机重装/换盘"这条路径只做过服务端侧模拟（F 段），未做真机实测 |
| **H5** | **监控系统自身无人监控**：Server 有计划任务崩溃拉起（已覆盖进程崩溃），但 231 断电/睡眠/整机死机 = 全静默；告警渠道仅页面内，人不在屏幕前等于没有告警 | 异常可达性在"人不在跟前"的时段归零 | 产品 v1 §2.4 决策"仅页面内" | **用户已知情并接受为产品边界**（2026-09-19 拍板：调试期维持界面内，不做出站）。理由：异构机群满载是常态，夜半推送 = 为人陪编译。→ 真正的诉求转为**告警过滤/静默/归属**（见 H7），出站通道待机群稳定后重提 |
| **H8** | **信任边界已移动，ingest 曾无凭据**：Agent 接入端口 9100 无 token；机群实际跨 ZeroTier（约 10 台含朋友设备），不再是纯局域网信任域。任何 ZT 组成员可注册任意 `host_id`、伪造或劫持既有节点数据 | 决策 6（无鉴权）的前提"安全边界=局域网"已不成立；后果是数据可信度，不是泄密 | `index.js:44-47` 无校验；用户口述机群含朋友设备与临时机 | **S2a 已做（2026-09-19）**：配对码→每节点密钥的两段式凭据 + `ingest.js` 三档强制（默认 `legacy`：陌生 host_id 一律拒绝，已入册老 Agent 宽限并在看板点名）+ metrics 帧身份校验（连带修掉 H4 的重写面）。**2026-09-19 里程碑已收口**：三台生产 Agent 升到 1.1.0 并各自带密钥，生产 `HM_INGEST_TOKEN=strict`，现场负例自检 `server/scripts/probe-strict-denial.mjs` 返回 `rejected/unknown_host` 且名册未被写入（`denied` 计数器 +1）。本条关闭；冒充已知节点那半边归 H16 |
| **H6** | **全局单探测计划**：Server 把同一份 `probes.json` 推给所有 Agent（网关/关键机按家庭局域网 IP 写死）。Agent 一旦从别处接入（办公室、朋友家、ZeroTier 侧），它的"网关臂"探的是你家的网关 → 语义错乱 | 机群跨网后链路层结论不再可信 | `index.js:30-36,71` 单对象广播 | **高**：与名册同批，探测计划改为**按节点派生** |
| **H7** | **阈值全局单一**：所有机器共用一份 `thresholds.json`。异构机群（推理机/编译机/游戏机）里满载是**正常工作状态**，不是异常 → 必然产生"陪朋友编译"式噪声告警 | 告警可信度下降 → 用户开始忽略横幅 → 产品失效 | `status.js:11` 全局 import | **高**：需 per-node/per-role 覆盖 + 手动静默 |
| **H9** | **配置真源即将分裂**：V2-a 引入 `nodes` 表后，运行时配置同时存在于 JSON（thresholds/probes）与 DB → 两边都能改 = 行为分裂、排障不可能 | 中后期必然踩到；且现在所有配置改动都要重启 Server | `status.js:11`、`index.js:30-36` 均为启动期一次性读文件 | **高**：与名册同批定死"DB 唯一真源 + JSON 仅作首次种子 + 热加载" |
| **H10** | **写侧将无防护**：设置界面（加节点/改阈值/录凭据/删节点）一旦上线，任何能打开 9101 的人都能改机群——读开放是需求，写开放不是 | 朋友路过看一眼 = 零摩擦（保留）；但误操作可删节点、可改他人机器凭据 | `index.js:requireAdmin` 中间件；`roster.js:hasPassphrase/checkPassphrase` | **S1a 已做**：写侧管理口令（scrypt 存哈希、默认拒绝、口令未设时一切写 403），S2a 把新增的签发配对码/演示开关同规则纳入。**S5 G2 收口（2026-09-19，本机）**：不再靠"记得加闸"——自检枚举全部非 GET 路由并要求每条链上含 `requireAdmin`（口令端点是唯一白名单、理由写明），且断言闸只有**一个函数实例**（防将来偷偷长第二个）；新增的在场归名写端点已被这套枚举自动纳管。**读侧维持无鉴权**（原决策） |
| **H11** | **【重点风险 · 已排为面板成型后的第一优先】第三方机器口令落在 231**：用户 2026-09-19 拍板"存口令，不再要求对方加公钥"，并明确"成型期先专注完善项目，凭据治理当成面板成型后必做重点工作" | 能读 231 磁盘者 = 取得所有已登记机器的登录凭据；对外展示/收集需求会**放大接触面**（更多人知道这台 Server 的存在与其上持有他人机器密码）；"加密"在无远端密钥前提下是障眼法，不做伪装 | 决策升版见纪要 §2.5 / §5 | **S5 已做"自己已有凭据的干净化"这一半（2026-09-19，本机）**：节点密钥改散列落库（H16）、写端点全量走同一道闸（H10 残留清零）、读侧/日志/事件行无凭据泄漏（自检 A~E 段）。**第三方凭据的治理仍未落地**（独立存储、逐台撤销、审计）→ 因此 **observed（SSH 拉取）继续不得排在其前**，且 S5 §7 明确建议把 observed 移到 V3（等用户拍板 D1）。过渡期兜底不变：口令不进 API 响应 / 不进日志与异常栈 / 不进 git 与分享包 |
| **H12** | **长期不上报的常驻节点会"静默消失"**：presence 与分母都来自**内存主机表**（`store.hosts`），不是名册。Server 重启后一台常驻机再没回来 → 没有卡片、不进 `在线 n/m` 分母、也不会新建 OFFLINE 告警。S1b 只保住了"重启前已活动的那条告警"（冻结 5 分钟），**没有告警的常驻节点仍然完全隐身** | 恰是最该报警的情形（机器被搬走/重装/长期关机）没有任何交代；对外展示时是硬伤 | `store.js` 主机表只由 register/metrics 填充；`status.js:evaluateCluster` 只遍历传入 hosts | **S3a 已做（2026-09-19）**：`store.getAnnotatedHosts()` 改为名册驱动——`confirmed` 且非 demo 且 `last_seen` 超 `presence.absent_after_minutes`（默 3min）的节点合成一条 `absent_record`；灰卡 + 进 `在线 n/m` 分母 + 事件流每缺席周期一条"已 N 分钟没有上报"。**与建议原文的偏离**：不进 `cluster.health`（"它不在这儿"≠"它出事了"，否则把 H1 从后门放回来；理由见 S3 §4.2）。可当场回退：`presence.enabled=false`。**S3b 已补呈现（2026-09-19）**：卡片写"缺席 · 上次在场 4 分钟前 / 计入在线率，不计入集群健康度"，与临时节点的"离场"分词；三处事件流（总览右栏 / HUD 本机事件 / 接入台账）上线；并且修掉一条真机才暴露的反向谎话——缺席合成让旧 失联 告警走了解析分支，事件流写下"回到阈值内（204）"，现改以第四种闭合 `cancelled:'absent'`（"改判缺席"）收口，见 S3 §10.4-2。**但只关了"跨重启的静默"，进程内的长静默仍是彻夜红灯 → 见新条目 H18** |
| **H13** | **管理口令在多设备间会互相踢**：口令明文存在各设备 localStorage，任一处更换即让其他设备的写操作 403。S1b 把"无限弹层"改成了明确报错（缺陷 11），但**服务端没有重置/找回通道**——忘了就只能上服务器改库 | 朋友或第二个自己误改口令 = 看板写侧集体锁死；对外收集需求时会放大 | `admin.js` 403 分支；`roster.js` 只有 `setPassphrase(需旧口令)` | **可修的那一半已修（S5 §3.3，2026-09-19，本机）**：`server/scripts/reset-admin-pass.mjs` —— 无 `--yes` 只报告、带 `--yes` 才把 `meta.admin_pass` 删掉回到"未设置"（默认拒绝），**不碰节点密钥**（三台 Agent 不需重配）；步骤进 `部署说明.md`"S5 上线步骤"第 5 条。**为什么是脚本不是接口**：一个"忘记口令"的 HTTP 路由就是那道 guarding 所有写入的门上的洞，而能跑这个脚本需要的文件系统权限本来就已经等于能直接改库。**未修的一半**：多设备之间仍会互相踢（要口令明文落 localStorage 才会）；真正的解是账号化，随 H11 的第三方凭据治理一起谈 |
| **H14** | **冻结告警的铃声代价**：`unknown_alert_grace_minutes`（默认 5min）内，一个已经消失的演示/模拟节点的 CRIT 会保持活动，而客户端 CRIT/OFFLINE 是**循环铃声** | 每次重启 Server 后最多 5 分钟无意义鸣响；演示机上更常见（mock 随时关） | `alerts.js` `UNKNOWN_GRACE_MS`；`App.vue:setCritLoop` | **低~中**：**S2a 已消掉最常见触发源**——演示机群关闭时其节点退役、告警立刻以 `retired` 闭合，不再冻结 5 分钟（`selftest-roster` G6e）。**仍未做**：给"冻结/陈旧"告警加视觉标记并在声音侧降级、或把 grace 配到 1-2 分钟。**S2c 没并进来**（接入页与它是两条不相干的改动面），改挂 S6 打磨批 |
| **H15** | **接入台账是内存环形缓冲**：`ingest.events` 只留最近 20 条、不落库 → Server 重启即清空；`enroll_codes` 只判过期不删除，死行永久留在表里（行很小但无界） | "昨天谁试过接进来"这种排障问题答不了；对外展示够用（现场演示不看重启前），运维期会不够。**S2c 上线后多一条呈现面风险**：接入页那块卡片如果只写"暂无记录"，重启后的空列表会被读成"从来没人被拒过"——正好相反 | `ingest.js:EVENTS_MAX`；`roster.js` 无码表清理路径 | **S3a 已做主体（2026-09-19）**：拒绝/配对/宽限/指纹漂移四类都落 `events` 表（history 同库），`GET /api/events` 跨进程可读，内存环形缓冲退化为接入页的"本次启动"视图（仍保留）。噪声纪律：同 `(host, code)` 60s 内合并 `count+1`，一台没凭据的机器被 supervisor 反复拉起 ≈ 每小时 1 行而非 300 行（自检 A2/C2）。保留 7d 挂在 `history.onCleanup`。**仍未做**：`enroll_codes` 死行清理（行很小、无界，留 S5 凭据治理批一并做）；S2c 的"本次启动"兜底文案到 S3b 有真事件流后再换 → **S3b 已换（2026-09-19）**：接入页改为持久化事件流（`EventStream lock-kind="ingest"`，24h 可跨重启）＋一行"本次启动累计"，重启后的空列表不再可能被读成"从来没人被拒过"。**`enroll_codes` 死行已由 S5 §5.2 清掉（2026-09-19，本机）**：`roster.purgeExpiredCodes(now)` 挂到 `history.onCleanup` 上（同一个 retention 时钟，不新开第二个定时器），过期超 30d 的码行删除；读侧本来就只看未过期（`activeCodes()`），撤销走 `events` 审计（`code_issued`/`code_revoked`），所以删行不擦掉任何历史。自检 G 段：−31d 删、−29d 与有效码保留 |
| **H16** | **节点密钥明文存储、明文过线**：`nodes.enroll_token` 以明文躺在 `roster.db`，配对时在局域网 WS 上明文传输（无 TLS）。能读到 231 磁盘、或能在链路中间监听者 = 可顶替任意已配对节点上报假数据 | H8 关的是"凭空造节点"，**没有关"冒充已知节点"**；`legacy` 档下连冒充都不需要密钥（见 S2 §3 残留风险） | `roster.js:newNodeKey/keyMatches`；`ingest.js` 模块头 "Honest limits" | **存储侧已收口（S5 G1，2026-09-19，本机）**：落库形态 `sha256$<16B 盐>$<散列>`，逐行独立盐、恒定时间比对；启动一次性迁移老明文行（`#migratePlainKeys`）＋ 迁移后 `wal_checkpoint(TRUNCATE)`+`VACUUM`（否则明文还躺在自由页与 `-wal` 里，"改了 SELECT"是假收口），并在匹配时顺手升级未迁移行。**为什么是 SHA-256 而不是 scrypt**：节点密钥是 128bit 随机十六进制，不是字典里挑的词，暴力成本一样；而 scrypt 每次 Agent 重连都要跑一遍，231 是低功耗小主机——**管理口令那侧继续用 scrypt**，因为它确实是人选的词。**传输侧仍未收口**：配对与上报仍是明文局域网、无 TLS，"过线"这半个 H16 没关，也不做伪装式加密（诚实表述写进 `部署说明.md` 鉴权行）；真收口要 TLS 或每机一次性握手，属成型后工作 |
| **H17** | **"被拒即退出"把重试风暴换成了 supervisor 重启循环**：S2b 让 Agent 收到 `rejected` 后打印中文原因并以退出码 2 结束，不再自己 5 秒重试。但 142 是 `Restart=always`、231 是计划任务崩溃 5 秒拉起 → 一台没凭据的机器会"起—被拒—退"循环，每轮一行中文日志 | 好处：拒绝原因每次都单独打印在退出前，比"伪装成正常运行的静默重试"易读；坏处：supervisor 会反复拉起（systemd 默认配额下可能进 `failed`，也可能一直循环，231 的计划任务一定一直循环），且看板上它只是 OFFLINE，原因**过去**只在本地日志里 | `agent/main.py` `except Denied`；`部署说明.md` §Agent 运行方式 | **S2b 已知代价，本期不修**：改成"退避重试＋首次即报"要动 Agent，而本期不碰三台生产机（迭代节奏约定）。事件流侧的承接条件在 S3a 已经具备。**S3a 把"只在本地日志里"这半彻底消掉了**：每次拒绝都进 `events` 表并按 60s 合并计数，重启 Server 后仍可从 `/api/events` 读出"intruder 接入被拒：未知节点，且未带有效配对码 ×N"（自检 C2 + F4 端到端）。剩余部分（Agent 侧退避重试而非"起—拒—退"循环）仍未做，需要改 Agent 且本期不动生产机。**S3b 补上了呈现**：合并后的 ×N（HH:MM 起）在接入页常驻可见，且不再依赖"本次启动"（`/api/events` 跨重启）。翻 `strict` 前的动作：先确认顶栏 `⚿ 未带凭据 N` 归零（等价于快照 `admin.keyless_agents` 为空），再翻档；翻错用 `HM_INGEST_TOKEN=off` 当场回退（S2 §8）。逐台步骤已写进 `部署说明.md`"里程碑重部署清单" |
| **H18** | **进程内的长静默仍是彻夜红灯**（S3b 真机验证时澄清的语义边界）：缺席只由**名册驱动的合成**产生，而合成条件是"该节点不在实时主机表里"。`store.hosts` 从不清理（`checkTimeouts` 只把 `online` 翻 false），所以一台在本进程运行期间被关掉、过夜的常驻机器**永远走不到合成那条路**，它一直是实时 OFFLINE 记录 → CRIT 卡片 + 循环铃声 + 进健康度 | 与 H12 同源、但场景相反：H12 是"该报警的没报"，H18 是"该淡出的不淡"。7×24 运行的 231 上，关机过夜 = 响一夜，而"它不在这儿"并不是"它出事了"（S3 §4.2 的取舍在这里被绕开了一半） | `store.js:checkTimeouts`（无 remove）+ `#absenceRecords` 的 `liveIds` 条件；实测见 S3 §10.5 | **S5 已做（2026-09-19，本机）**：`store.checkTimeouts()` 里，已确认 + 常驻 + agent 类记录静默超过 `presence.degrade_after_minutes`（默 15）即打 `absent_record=true`、`absent_by='silence'`，**复用 S3 那条缺席通路**（不改生命周期、只加一个标记，所以 H1 已闭合的那半边没被牵动）→ 无原因可评的 OFFLINE 告警以 `cancelled='absent'` 闭合、健康度剔除、仍进在线分母、铃声停。两条阈值各答一问：`absent_after_minutes`=3（跨重启："它今晚在不在"）vs `degrade_after_minutes`=15（进程内："别再为它响了"）。措辞侧新增 `absent_by` 前端分支（"Server 运行期间静默超时" / "自 Server 重启后未再上报"），避免把两种成因印成同一句话。自检：`selftest-roster` presence 段（注入时钟 16min）+ `selftest-events` C6 |
| **H19** | **`identity.json` 存了 `server` 却不参与解析**：配对时把当时的 Server 地址一起写进身份文件（`main.py:147` `save_identity`），但 `config.resolve()` 的优先级链是 CLI > env > `agent.json` > 默认，**identity 只提供 `host_id` 与密钥**，`server` 字段被存而不用。三台生产机现在靠计划任务/unit 里烧死的 `--server` 活着 | 单看今天无害；但它埋了一条很难查的路径：哪天按 S6 的"参数不再烧进任务"做清理（unit 改成裸 `python main.py`），三台会一起回落到默认 `ws://localhost:9100`——Server 在别的机器上，表现为"改了个启动参数，全机群集体失联"，且 `--print-config` 会诚实显示 `server = (from default)`，容易被当成无关噪声略过 | 2026-09-19 里程碑三台 `--print-config` 实测输出；`config.py` 解析链 | **低优先级可选**（与 S6"启动参数瘦身"同一批做，不要单独动）：要么让 identity 的 `server` 参与解析（排在 `agent.json` 之后、默认之前，并在 `--print-config` 里点名来源），要么干脆不存这个字段——**存而不用最坏**，因为它同时具备"看起来会生效"和"实际不生效"两个属性 |
| **H20** | **口令失败预算按"来源地址"计数**（S5 G2 引入的新形态，登记备查，本期不改）：60s 内同一来源 8 次失败 → 该地址 429。当前形态 A（每个浏览器各自直连 231）下正确；**如果将来中间加一层反向代理、或多人经同一出口（公司 NAT / 同一台 ZT 网关）进来，一个人的连输会锁住同地址的其他人** | 低摩擦误伤：被锁的人看到的是"口令未被接受"，猜不到是别人输错的 | `index.js:218-248`（`ADMIN_FAIL_*`，键为 `req.socket.remoteAddress`） | **低优先级可选**：机群或用户数上来后改法有两种——按 `(地址, 会话)` 复合键，或在反代场景信任 `x-forwarded-for`（**只有真的加了可信代理才许信这个头**，否则等于把预算送给伪造头的人）。现在就加"信任转发头"是把误伤换成绕过，不做 |
| **H21** | **在场台账是一份会长久的留存记录**（S5 §4 引入）：`presence_state` 跨重启保留每台对端的 `first_seen`/`last_seen`/`name`，`roster.db` 因此多了"哪些机器什么时候在线"的历史；同时 `zt.json` 内含本机 ZT 端口与排除节点 id | 与"读侧无鉴权"这条既有决策叠加后，能打开 9101 的人 = 能看到这份在场历史（**不含 IP、不含密钥、token 不进任何响应**，这是 S5 G3 已断言的边界）。小圈子用途下已接受；对外发截图/分享包时这页要过一眼 | `roster.js` 的 `presence_state` 表；`/api/presence` 只回 ZT 节点 id（10 位十六进制）而不回 IP/token（hygiene D 段逐端点扫描） | **【不建议现在实现】"在场历史可导出/可清除"这类治理功能**——它是账号化（H11/H13 长期解）之后才有意义的话题。当前只记：分享包不含 `roster.db`、`zt.json` 已 gitignore |

## 待决项（非隐患，属架构记账）

- **历史库体积**：SQLite 无 auto_vacuum，`DELETE` 不回收空间；raw 7d + 1m 30d 量级下可控（3 台 ≈ 数十 MB 量级），但换机群规模后需复核。当前无需动。
- **`getSeries` 30d 区间**：会把区间内所有含 `gpu_json/link_json` 的行全部读入并逐行 `JSON.parse`（3 台 30d ≈ 13 万行）。首屏若默认拉长区间会卡顿；改法=按区间降采样或限制 JSON 列查询。
- **首次 roll 的 catch-up**：`#roll` 一次读入 `rolledThrough` 之后的全部 raw 行（全新安装且已有 7 天数据时是整表）。可分批。
- **`--interval`/`thresholds.json`/`probes.json` 均需重启生效**：改配置=重启 Server，运维成本随节点数上升。若做名册化，顺带把"配置热加载"列入同一改造。
- **演示机群会在历史库里留 30 天尾巴**：道具照常落 `samples_raw`/`samples_1m`（详情页要有曲线），关掉只是停止生成＋退役节点，已写入的行按自然保留期衰减。清理语句在 `S2-细化设计.md` §8；4 台道具 ≈ 每分钟 4 host-minute，roll-up 开销可忽略。**不做**"关演示即删数据"——那是把用户以为已经删掉的东西变成静默破坏。
- **kiosk 的新鲜度横幅依赖标签页定时器**：`KioskView` 的 1s ticker 推动"已停止刷新 X 秒"，而 Chrome 对**隐藏或被遮挡**的标签页把定时器节流到约 1 分钟一次（实测：内置浏览器页面 `document.hidden===true` 时横幅比预期晚出现约 1 分钟）。年龄取自 wall clock 而非 tick 计数，所以只会晚报、不会错报；但"值守屏必须始终前台、不被遮挡"从此是一条部署要求而不只是使用习惯。真机验收时确认一次（副屏 + `?kiosk` 前台）。见 `S4-细化设计.md` §7.4-7。
- **L3 主板 3D 没有本期这套生命周期护栏**：`contextlost` 自愈、`_disposeObject`、`visibilitychange` 暂停只补在 `ClusterTopologyRenderer`（kiosk 与拓扑 tab 共用那一个）。`TopologyRenderer` 同病未动——它是坐着看的视图、白屏代价低一档，且已在讨论下线（收尾段）。若最终保留，需要把同一套护栏搬过去而不是重写一遍。
- **密钥迁移那一次 `VACUUM` 是启动期的整库重写**（S5 G1）：只在"确实有明文行要迁"时跑一次，量级 = 名册库大小（当前数十 KB，秒级）。它换来的是"磁盘上真的没有明文了"，因此不能省；但要知道**它是开机路径上的一次性写放大**，231 上如果将来名册变大（几百台）就要看它是否影响启动。第一次上线现场确认两行日志：`Migrated N plaintext node key(s)` 与随后端口就绪。
- **在场台账给 231 增加了一个每 30s 的读点**：ZeroTier 本地 API 一次 POST + 命中可达时才写 `presence_state`（写侧被 `PRESENCE_WRITE_MS=60_000` 节流，一台稳定在场的机器 ≈ 每分钟一行 UPDATE，不是每 30s）。不装 Agent、不采指标、不进告警评估，所以功耗面与主机采集不在一个量级；但"多一个常驻轮询"这件事本身在低功耗机上是要记的账，`zt.json` 缺文件即完全关闭（连定时器都不建）。
