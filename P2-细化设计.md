# P2 细化设计 — 告警体系（阈值配置、归并防抖、横幅/列表/声音）

> ⚠️ **V1 阶段（P 系列）历史稿，2026-09-20 文档对账时加此声明。** 文中的"现状/契约/阈值数值/配置真源"是 P 批当时的形态，**不代表现役**：V2（S1~S6）已收口并上线（生产 231 = `61bf4db` = tag `v2`）。现役事实以 `README.md` + `部署说明.md`（两篇上线记录）+ `技术隐患清单.md`（H1~H29）为准。三条最容易踩的：**阈值现值一律读 `GET /api/config`**（S6 起运行时真源在 DB，`thresholds.json` 只剩"首次播种"与"回落到播种值"两个用途）；**四态色值真源在 `client/src/lib/palette.js` + `style.css`**（由 `client/scripts/check-tokens.mjs` 把关，`lib/status.js` 里没有颜色），卡片排序键是 `attentionRank` 而不是 CRIT>OFFLINE>WARN>OK；文中出现的"V1.5 / V2 / V3"指**页面层级**，与 V2、V3 两轮迭代无关。

> 依据：`产品设计方案-v1.md` §2（状态机/阈值/防抖抑制/可达性）+ §5 P2
> 基线代码：git `ac6fdd6`（P1 检查点）
> 状态：**评审稿，通过后实施，本轮未改代码**
> 日期：2026-09-18

---

## 0. P2 范围边界

| 属于 P2 | 不属于 P2 |
|---|---|
| 防抖状态机（持续窗口触发/恢复） | 告警持久化、重启不丢（P5） |
| 告警对象模型 + 活动/历史环形缓冲（Server 内存） | IM/webhook 外部通知（一期明确不做） |
| 顶部告警横幅（CRIT/OFFLINE） | 告警确认（ack）流程——个人值守场景不需要 |
| 声音提醒（WebAudio 合成音，CRIT 循环/WARN 单次）+ 静音开关 | 邮件/桌面通知 |
| AlertPanel 升级（活动排序 + 恢复折叠） | 链路类告警（P3 探测上线后自动复用本引擎，零改动） |

---

## 1. 防抖状态机（核心，Server 端）

### 1.1 引擎位置与节拍

- 新增 `server/src/alerts.js`：有状态引擎，**挂在 2s 广播节拍上**（每次 `getSnapshot()` 时先跑一轮引擎），不侵入 status.js（status.js 保持纯函数、逐帧瞬时评定——它是引擎的输入）。
- 节拍 = 2s（BROADCAST_INTERVAL_MS），Agent 默认上报间隔 2s，两者同阶。**"采样周期"从此定义为引擎节拍**。

### 1.2 规则（对应产品方案 §2.3）

- **告警身份（归并键）**：`host_id + metric + source`（如 `infer-142:gpu_temp:gpu1`）。同键永远只有一条活动告警，重复触发不新增，只更新 `latest_value`。
- **触发**：同键连续 `debounce_cycles`（默认 3）个节拍处于超限态 → 状态机 `pending → active`，记 `started_at`。
- **恢复**：`active` 后同键连续 3 个节拍回到阈值内 → `resolved`，记 `resolved_at`，进历史；**5 分钟后从快照中移除**（持久化前的"历史"就是这 5 分钟窗口）。
- **升档立即生效**：同键 `active(WARN)` 期间升到 CRIT → 不等窗口，直接升档并重新通知（温度骤升不能等 6 秒）；**降档走恢复窗口**（防抖对称）。
- **OFFLINE 特例**：心跳缺失 >15s 本身已是持续判定，**不叠加防抖，立即 active**；重新上线即 resolved。
- **节点状态语义变更（重要）**：`host.status.level` 与卡片边框、集群 health、3D 发光，从 P2 起一律取**防抖后**档位（稳定，不闪）；瞬时值仍作为数字读数展示。触发窗口内的"疑似"进 `status.pending`（前端在卡片上以淡角标提示，不告警、不出声）。

### 1.3 配置扩展（thresholds.json 追加段）

```json
"alert": {
  "debounce_cycles": 3,
  "resolved_keep_minutes": 5,
  "history_capacity": 200
}
```

---

## 2. 数据契约（snapshot 增量）

告警对象：

```jsonc
{
  "id": "mock-warn|gpu_temp|gpu1",
  "host_id": "mock-warn",
  "hostname": "mock-warn",
  "metric": "gpu_temp",
  "source": "gpu1",
  "level": "WARN",              // 当前档（可升档）
  "state": "active",            // active | resolved
  "value_at_trigger": 89,
  "latest_value": 91,           // 持续期间跟最新采样
  "started_at": 1737180000000,
  "resolved_at": null
}
```

snapshot 变化：

```jsonc
{
  "cluster": { "health": "WARN", ... },          // 改按防抖后档位
  "alerts": {
    "active":   [ /* 按 level→started_at 排序 */ ],
    "resolved": [ /* 最近 5 分钟，新的在前 */ ]
  },
  "hosts": [{
    "status": {
      "level": "WARN",          // 防抖后
      "pending": [ { "metric": "cpu_temp", "value": 76, "cycles": 1, "needed": 3 } ],
      "components": { ... },    // 不变（瞬时，供数字旁微色）
      "reasons": [ ... ]        // = active 告警的视图（保持 P1 兼容）
    }
  }]
}
```

- REST 增 `GET /api/alerts`（同一份数据，调试/将来 P5 导出用）。
- Server 重启：活动告警全部丢失并重新走窗口——已知并接受（P5 持久化解决）。

---

## 3. 前端（横幅 / 列表 / 声音）

### 3.1 AlertBanner（新组件，V1 顶部固定）

- 出现条件：`alerts.active` 中存在 CRIT 或 OFFLINE；内容如「**2 个节点严重/失联**：infer-142 GPU温度 91°C · db-main 失联 34s」，点击跳转第一个节点详情。
- 可手动收起 → 收成右上角小铃铛图标（带计数角标）；**新告警到来自动再展开**。
- WARN 不进横幅（只在侧栏+单次声音），避免值守横幅常驻化。

### 3.2 AlertPanel 升级

- 活动区：消费 `alerts.active`（Server 已排序），每行显示持续时长（"12m" 实时计）与 latest_value；
- 恢复区：`alerts.resolved` 折叠列表（"最近恢复 (3)"），5 分钟自动消失；
- 卡片上的 reasons 行改由 active 告警驱动（与 P1 的瞬时 reasons 视觉一致但不再闪）。

### 3.3 声音（`client/src/lib/sound.js`）

- **WebAudio 合成音，零音频资产文件**：
  - WARN 触发：**单次**双音"叮"（880→1175Hz，0.3s）；
  - CRIT/OFFLINE 触发：**循环**三音下行（660→550→440Hz），活动期每 2.5s 重复一次，全部恢复即停；
  - 升档（WARN→CRIT）按新档重发。
- **浏览器自动播放策略**：AudioContext 初始 suspended → 顶栏出现「🔇 点击激活声音」提示，用户任意点击后解锁；解锁状态存 `localStorage`。
- **静音开关**：ClusterBar 右侧铃铛按钮，状态持久化；静音只关声音，横幅/颜色不受影响。
- 客户端只在新 `alert.id` 首次进入 active 集（或 level 变化）时发声——以快照 diff 判断，不轮询。

### 3.4 既有组件适配

- HostCard：`pending` 非空时右上角淡黄小点（"观察中"），无边框变色；
- DetailView：3D 发光/报警闪烁改吃防抖后 `status.level`（更稳，见 §1.2 语义变更）；
- 色码/排序逻辑不动。

---

## 4. 自测方案（mock 扩展）

`server/scripts/mock-agent.mjs` 新增两个节点：

| 节点 | 行为 | 验证点 |
|---|---|---|
| mock-flap | CPU 占用每 ~5s 在 78↔84 交替穿越 WARN 阈值 | 防抖生效：**不产生**活动告警，只有 pending 黄点 |
| mock-crit | 磁盘 96% 持续 | 6s 后 CRIT active：横幅+红框+循环音；改值重启后走恢复→历史折叠 |

恢复路径：手改 mock 值重启（不做复杂脚本，验收清单含此项）。

**你的验收清单（约 15 分钟）**：
- [ ] mock-warn 出现后约 6s 才进活动告警（非瞬时）；mock-flap 始终不告警、卡片有黄点
- [ ] mock-crit：横幅自动出现、点击钻取、循环音；静音后无声、横幅仍在
- [ ] 刷新页面 → 声音需重新点击解锁（自动播放策略），横幅/列表不丢（Server 状态还在）
- [ ] 杀掉 mock 进程 → 15s 后 db 失联**立即**灰卡+告警（无 6s 延迟）
- [ ] 恢复 mock-warn 的 mem（改脚本重启）→ 告警转"最近恢复"，5 分钟后消失

---

## 5. 工作量（P2 通过后）

| 步骤 | 内容 | 预估 |
|---|---|---|
| 1 | server/alerts.js 引擎 + snapshot 契约接线 + 配置段 | ~50 分钟 |
| 2 | sound.js + 解锁/静音 + AlertBanner + AlertPanel 升级 + HostCard pending | ~50 分钟 |
| 3 | mock-flap/mock-crit + 全链路自测 | ~30 分钟 |
| 4 | 联调验收 | ~15 分钟（你在场） |

---

## 6. 评审点（请逐条确认或否决）

1. **status.level 语义切换**：卡片边框/集群灯/3D 发光改用防抖后档位（数字仍是瞬时值）——这是 P1 契约唯一的破坏性变更。
2. **触发/恢复窗口 = 3 节拍 ≈ 6 秒**，OFFLINE 不叠加防抖立即告警。
3. **横幅只收 CRIT/OFFLINE**，WARN 静默进列表+单次音。
4. **声音用 WebAudio 合成音**（无音频文件、无外部依赖），首次点击解锁。
5. **"历史"一期 = 内存 5 分钟折叠窗口**，重启即失（P5 才持久化）。
