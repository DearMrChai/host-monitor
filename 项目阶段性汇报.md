# 局域网主机监控系统 — 项目总结与阶段性汇报

> 版本检查点（暂存）：**v0.4 — 界面效果迭代中**
> 日期：2026-09-18
> 状态：**暂停开发**（待重新评审"设备监控信息"的样式与呈现效果）

---

## 一、项目背景与目标

- 由产线数字孪生项目 `galv-3d-viewer`（Vue3 + Three.js + Vite）派生，**不改动原产线项目**。
- 目标：监控局域网内多台主机的 **CPU / 显卡（可多卡）/ 内存（可多条）/ 网络速度**。
- 环境：Windows + Linux 混合；保留 3D 数字孪生风格；小步快跑、分阶段迭代。
- 部署目标机：老机器（Phenom II X4 645 + GT 1030 GDDR5），因此前端采用**程序化几何体**（轻量），渲染发生在"观看者设备"而非被监控主机。

## 二、系统架构（三层 B/S）

```
┌────────────┐   ws:9100    ┌────────────┐  http/ws:9101  ┌────────────┐
│ Agent(Python)│ ─────────▶ │ Server(Node)│ ─────────────▶ │ Client(Vue3)│
│ 采集+拓扑自检 │            │  WebSocket 中继│               │ Three.js 3D  │
└────────────┘              └────────────┘                └────────────┘
     被监控主机                  中继/汇聚                    观看者浏览器
```

- **Agent（Python）**：`psutil` 采集 CPU/内存/网络，`nvidia-ml-py` 采集 NVIDIA GPU；`collectors/topology.py` 开机自检硬件拓扑（Windows 用 PowerShell `Get-CimInstance`，因 `wmic` 在 Win11/25H2 已弃用）。
- **Server（Node.js）**：`ws` 库；9100 收 Agent 上报，9101 对前端提供 REST(`/api/hosts`) + WebSocket 推送；内存态存储（暂无持久化）。
- **Client（Vue3 + Three.js + Vite）**：`TopologyRenderer.js` 依据拓扑 JSON 生成 3D 主板场景；`App.vue` 单视口 + WebSocket 实时流；dev 端口 5173。

## 三、目录结构

```
host-monitor/
├── agent/                  # Python 采集端
│   ├── main.py
│   ├── requirements.txt    # psutil, nvidia-ml-py, websockets
│   └── collectors/         # cpu / gpu / memory / network / topology
├── server/                 # Node 中继
│   └── src/  index.js, store.js
├── client/                 # Vue3 前端
│   └── src/
│       ├── App.vue         # 单 3D 视口 + HUD + 图例
│       ├── style.css       # 浅色（奶白）主题
│       ├── three/TopologyRenderer.js   # 核心 3D 渲染器
│       └── config/         # sample-topology.json, inference-server-topology.json
└── 项目阶段性汇报.md        # 本文档
```

## 四、已完成功能（分阶段）

| 阶段 | 内容 | 状态 |
|------|------|------|
| 1. MVP | 三服务打通；CPU/GPU/内存/网络指标采集与展示 | 完成 |
| 2. 拓扑可视化 | 组件拓扑 JSON 配置；flat / 3d 双风格对比；**选定 3d 立体**（利于后期多机） | 完成 |
| 3. 实时绑定 | 指标驱动颜色/风扇/总线脉冲；硬件自检（PowerShell CIM）；服务端转发拓扑 | 完成 |
| 4. 界面效果迭代 | 真实 PCB 主板、CPU 形象、内存池化容器、浅色主题 | **进行中（本次暂停点）** |

## 五、当前暂存版本的界面效果明细

- **主板**：浅绿 PCB（`#1b7a1b`）+ 铜走线 + 过孔 + SMD 阻容 + 电解电容 + ATX 安装孔 + 丝印框。
- **CPU**（参考真实芯片形象）：
  - 绿色基板**贴合主板、无悬空**；四边金色鸥翼引脚（尖端触板）。
  - 圆角**银色 IHS 顶盖**（ExtrudeGeometry 圆角+倒角，metalness 0.95），顶面 Canvas 印字（CPU + 型号 + 序列行）。
  - 金色定位点（pin-1 标记）。
  - **顶盖颜色 = f(占用率, 温度)**（温度权重更高）；**报警闪烁**：占用 >90% 或 温度 >85°C 时顶盖红色脉冲 + 标签 CSS 闪烁。
  - 无散热鳍片/风扇（按需求移除）。
- **内存（池化容器）**：
  - 抽象为 **2 个无盖窄容器**，与 CPU **同排对齐**（z = CPU 行）。
  - 两容器**液位相同 = 整池占用率**。原因：双通道 interleaving 下数据按 cache-line 跨条分布，**单条"存了多少"不可测、也无需区分**；OS 只报总用量/总容量。容器标签仅标该条容量（slot + GB）。
- **GPU / 存储 / PCH / 网卡**：程序化几何体 + 实时发光/风扇。
- **连接线**：Manhattan 直角 **PCB 走线**（非 3D 管道）+ 起止/拐角过孔焊盘；按总线类型着色（DDR 蓝 / PCIe x16 绿 / x4 青 / DMI 橙 / NVLink 紫）。
- **主题**：奶白浅色背景（`#f5f0e6`），面板/标签/HUD 全部浅色化。

## 六、已知问题与限制

- Windows 存储接口识别不完美（NVMe 可能误报 SATA）；连线正确但接口名可能不准。
- 笔记本 GPU 省电态 `usage_percent` 为 null（已优雅处理，不崩）。
- CPU 温度跨平台差异（Linux sensors / Windows WMI）。
- 内存单条占用不可区分（**设计如此**，池化语义）。

## 七、暂停原因与待决事项（下次恢复时讨论）

- **待重新考虑：设备监控信息的样式 / 呈现效果。**
  候选方向（供评审）：
  1. 信息直接"刻"在 3D 组件上（顶盖印字、容器刻度）vs 悬浮 HTML 标签（CSS2D）。
  2. HUD 面板集中式读数 vs 组件分布式读数。
  3. 颜色/发光编码 vs 数字读数为主、颜色为辅。
  4. 报警的呈现：闪烁 / 边框 / 弹窗 / 声音。
- 多机拓展（主机间网络互联连线）仍为后续目标。

## 八、运行方式

```
# 1) 中继服务
cd server && node src/index.js            # ws:9100 / http+ws:9101
# 2) 前端
cd client && npx vite --host              # http://localhost:5173
# 3) 采集端（被监控机）
cd agent && python main.py --server ws://localhost:9100
```

## 九、恢复开发后的下一步

1. 评审并确定"监控信息样式"方案（本次暂停主题）。
2. 按选定方案重构信息呈现层。
3. 多机拓扑组合 + 网络互联可视化。
