# S6 细化设计 — 设置界面 + 配置单一真源（本期把 V2 收口）

> 排期来源：`V2-迭代讨论纪要-20260919.md` §5.4 的 S6 行。纪要把这一行的**判据留空了**
> （S1~S5 都有），所以本文件的第一件事是替它把判据定出来，第二件事才是设计。
> 入场前提：S5 已过闸（`S5-细化设计.md` §8 的未验证项仍待现场）。

## 0. S6 里做什么、不做什么（先把"做完 V2"定义清楚）

| 项 | 本期 | 理由 |
|---|---|---|
| **observed（SSH 拉取）** | **不做，移 V3** | S5 §7 的 D1，等用户拍板；硬约束纪要 §5.2（第三方凭据治理未落地前不得启动）。已登记，不偷偷做 |
| **access 台账（谁访问了我的设备）** | **不做** | 纪要 §4 明写"实施前仍需他单独点头一次"。没有点头 = 不做 |
| **H9 配置单一真源 + 热加载** | 做 | 它是设置界面的前置：不能写的配置做出一个只读设置页，比没有更糟（多一屏"看起来能改其实不能改"） |
| **H7 per-node 阈值覆盖 + 手动静默完整版** | 做 | 异构机群满载是常态，全局阈值 = 陪朋友编译；这是 §5.3 的"故障感"里最难看的一条 |
| **H6 探测计划按节点派生** | 做 | 跨网机器探的是你家网关，链路结论不可信；且这是"加机器要改文件"这最后一处交互断层 |
| **完整设置界面（owner/site/role/class/改名/退役/静默/阈值/探测）** | 做 | 上面三项没有界面 = 仍是运维工具而不是产品 |
| **H14 冻结告警的铃声与标记** | 做（小） | 唯一一处"重启后自己响一阵"的故障感 |
| 3D 主板下线、注释/文案巡检、H3 探测并发化 | 收尾批（另文） | 与 S6 无耦合 |

## 1. 判据（S6 的"过"长什么样）

| # | 判据 | 机器可验？ |
|---|---|---|
| J1 | 改阈值**不重启 Server**：`POST /api/admin/config/thresholds` 后 2s 内，新帧按新阈值判级；重启后仍是新值（DB 是真源） | 是（自检 B 段） |
| J2 | 全局阈值文件与 DB 只有一处运行时真源：`thresholds.json` 只在**首次播种**时被读；此后任何代码路径都从 config 层取值 | 半（自检断言"改了 DB 行 → 生效"，并用引用边扫描断言除 config.js 外无人 `readFileSync(thresholds.json)`） |
| J3 | per-node 覆盖：一台机器单独把 `cpu_usage` 抬到 95 → 它满载不告警，其他机仍按 80 告警；告警原因行**写明这条用的是该机自定义值** | 是（自检 C 段 + 呈现断言） |
| J4 | 覆盖可撤销：清掉覆盖后回到全局值；界面上能看出"这条是自定义 / 这条继承全局" | 是 |
| J5 | 探测计划按节点：A 站点机器拿到 A 的网关，未派生到专属计划的机器**在看板上标"继承全局探测计划"**（把 H6 的错语义显式化，而不是继续装作它对） | 是（后端）+ 人眼（前端） |
| J6 | 设置界面覆盖全部可写档案字段，且**每个写端点都在 S5 G2 的枚举断言里**（新端点自动被要求带闸，漏了就是红） | 是（复用 hygiene C 段） |
| J7 | 冻结/陈旧告警不再循环鸣响，且卡片上说的是"节点长期未上报"而不是"已恢复" | 半（声音侧代码断言 + 人眼） |
| J8 | 所有既有自检仍绿（S1~S5 的六套一条不许改弱） | 是 |

## 2. 配置层（H9）

### 2.1 形状

- 新 `server/src/config.js`：**不 import 任何本项目模块**（否则 `status → config → roster → events → status` 成环）。
  持久化由 index.js 注入：`config.attach({ get, set })`，get/set 落到 `roster.js` 新增的 `settings(key,value,updated_at)` 表。
- 内存里是一份**活的**对象：`config.thresholds`（模块级导出、**只原地 mutate 永不重新赋值**，这样 `import { thresholds } from './status.js'` 的老读法天然热生效——这是刻意的最小改动面，代价是"不许换对象"写进注释与自检）。
- 生效路径：写 → 落 `settings` → mutate 内存 → 通知订阅者（alerts 用它重算 tick 间隔）。
  **不做定时轮询**：只有一个进程写自己的 DB，多一个时钟就多一处"两个真相"的余地；跨进程改库不是支持用法（要改走界面）。

### 2.2 播种与优先级

```
节点覆盖 (nodes.settings_json.thresholds)
  > 全局运行时值 (settings['thresholds']，界面所写)
    > 文件种子 (config/thresholds.json，只在首次没有 DB 行时读)
```
- 播种发生在 `attach` 时；若 DB 已有行，**文件不再被读**（J2）。文件此时唯一作用是给新装实例一个起点。
- 读不到 `settings.json`（全新库）时行为与今天完全一致 → 升级零迁移风险，也不存在"配置丢了"这种失败模式。

### 2.3 校验在写侧，不在读侧

`setThresholds` 只接受已知键 + 数值 + 区间钳制（warn ≤ crit、比例 1~100、秒数上下界），越界即拒（400 带人话原因），**不静默修正**。理由：配置错了会一路影响告警，静默修正比拒绝更难查。校验集中在 config.js 一处，界面与 API 共用。

## 3. 阈值覆盖（H7）

- 存进 `nodes.settings_json.thresholds = { cpu_usage:{warn,crit}, … }`，与 `mute` 同袋（S1 已确立"节点级设置在这"）。
- `status.js` 的 `evaluateHost(host)` 开头算一次 `const th = thresholdsFor(host)`，其后全部用 `th.*`；`pushReason` 在用了覆盖项时给 reason 加 `custom: true`。
  **为什么不另起一份 per-node 评估函数**：四态判级只有一个实现这条约束（P1 §1.2）比省 20 行重要。
- 呈现：HUD/详情的告警原因行对自定义项加一个"该机自定义"小标；缺省不加。谎话最便宜的形式就是让人以为这条红线是全局共识。

## 4. 探测计划按节点（H6）

- 全局计划（`settings['probes']`，首次从 `probes.json` 播种）新增可选 `sites: { <site>: { gateway, key_hosts } }`。
- `config.probePlanFor(node)` 三级：节点专属（`settings_json.probes`）→ 该节点 `site` 命中 `sites[site]` → 全局 `gateway/key_hosts`。
- register ack 里带 `probe_plan` 与 **`probe_plan_source: 'node'|'site'|'global'`**；Agent 不解释它，只在 `--print-config` 与首行日志里如实打印来源（Agent 侧零逻辑改动）。
- 来源为 `global` 且该节点 `site` 与 Server 不同 → 看板链路臂标"继承全局（可能不是本机的网关）"。这是判据 J5 的后半，也是本期唯一一次把"我不确定"画到界面上。

## 5. 设置界面

- 新 L1 入口"设置"→ 单页两栏：左=**全局**（阈值表、全局探测计划、事件窗口），右=**选中节点**（显示名/owner/site/role/class/静默时长/阈值覆盖/探测计划）。
- 全走 `lib/admin.js:rosterPost()`：403 → 口令弹层 → 一次性回放（S1b 已定的唯一写出口，本期不开第二条）。
- 端点（全部 POST + requireAdmin）：`/api/admin/config/thresholds`、`/api/admin/config/probes`、
  `/api/roster/:hostId/settings`（档案字段合并写）、`/api/roster/:hostId/thresholds`、`/api/roster/:hostId/probes`。
  新增只读 `/api/config`（不设闸：里面没有任何凭据，只有阈值与探测目标名——但**探测目标含真实 IP，故该端点只回名称与掩码，实值只在写请求里去**，S5 G3）。

## 6. 自检计划（`selftest-config.mjs`）

| 段 | 断言 |
|---|---|
| A 播种 | 无 settings 行时=文件值；有行时文件被忽略（写一个"改过的文件"证明没被读） |
| B 热生效 | `setThresholds` 后**不重启**，同一 host 的下一帧判级改变；新进程读同库仍是新值 |
| C per-node | 覆盖只影响该节点；清覆盖回落全局；reason 行带 `custom`；全局 warn/crit 与节点覆盖的区间校验各自生效 |
| D 探测派生 | node>site>global 三级命中与 `probe_plan_source` 正确；未配 probes.json 时仍能工作 |
| E 闸 | 五个新写端点全在 hygiene 的枚举里且带 `requireAdmin`（复用 C 段，不重写） |
| F 泄漏 | `/api/config` 与 `/api/hosts` 不含真实 IP 与密钥（复用 D 段扫描器） |

## 7. 风险与取舍（写下来，别等出事）

1. **"活对象"约定是脆的**：谁将来写 `config.thresholds = {...}`（重新赋值）就会让所有老 import 变旧。缓解：`Object.freeze` 不能用在需要 mutate 的对象上，改为自检 B 段直接断言"写后 status 侧读到的就是新值"——它恰好会因重新赋值而红。
2. **改 `evaluateHost` 取阈值的路径**碰到的是 P1 契约最核心的一段。缓解：J8 要求既有六套自检全绿，四态判级的既有断言一条不改。
3. 设置界面一次性铺开 5 个端点 + 两栏表单，比 S1~S5 任何一批的前端面都大。若中途要砍，**先砍全局阈值编辑器**（留只读展示），保留节点覆盖与档案字段——后者才是"加机器要改文件"这条断层的本体。

## 8. 偏差与未验证清单（实施后回填，不得事前省略）

> 完成批 = S6-1 配置层（`5da1682` 前半）/ S6-2 逐机阈值 / S6-3 探测派生 / S6-4 设置页（`50d57ac`）/ S6-5 H14 冻结（`61bf4db`）。全部本机完成，**未上线**。

### 8.1 实施中发现并当场修掉的（设计里没写，但不修就是坑）

| 侧 | 发现 | 修法与依据 |
|---|---|---|
| 服务端 | **指标档的区间校验与"字符串归一"从未生效**：`numberAt` 的 `src` 已经指向那个 pair，我却又传了 `${key}.${side}` 当路径 → 它在 pair 里找 `pair.cpu_usage.warn`，找不到就静默跳过。结果 `warn:"abc"` 能一路写进 DB，且 warn/crit 的上下界检查整体失效。这正是"表单填错→告警永久失灵"的形态 | 改传一道路径、完整名只作 label；`selftest-config` **B6** 是抓它的那条（它是本期唯一由自检先红后绿抓出的校验漏洞） |
| 服务端 | **`custom` 标记被去抖器丢掉**：`alerts` 的 debounce 用"仍活动的条目"重建 `s.reasons`，重建时只带 metric/level/value → J3 要求的"这条用的是该机自定义值"在**告警面板**上消失（卡片上还在）。J3 恰恰只在看板上能验，所以这条不做完判据就是假的 | 让 `custom`/`threshold` 穿过 new→pending→active→`apply()`→`getLists()` 全链；自检 C 段 + `client` 呈现断言各钉一次 |
| 服务端 | **"设了又清"在合并窗口里渲染成旧措辞**：同 `(host, code)` 60s 合并只 `count+1`、不重写文字 → 清掉覆盖后那行仍写"已设自定义阈值"。合并是对的（防噪声），但**计数与文字必须同时更新**，否则一行"看起来像统计"的谎 | 合并路径改为 `count+1` **且** 用最新事件重写 `text`；自检 **F3** 钉住"这一行说的是最新状态" |
| 前端 | **`deepSet` 只下探不创建**：点"应用"后服务端回 `未知配置项 warn` —— 叶子落在了父级旁边而不是里面。**这是设置页第一次在浏览器里跑出来的实际错误**，不是我推演出来的 | 下探时缺键即建对象（`SettingsView.vue:66-72` 注释留了原话）；此后每条 `errors[]` 都被表单如实显示，不重试、不改数据 |
| 前端 | **改归类后左栏不刷新**：写成功、弹提示，但节点列表里的"阈/探"角标与归类仍来自旧快照，读起来像"没生效" | `setClass` 成功后 `await load()`（`:216`）；同类路径（阈值/计划/回落）本来就各自带 load，这是唯一漏的一条 |
| 前端 | **加载中的空态在撒谎**：表头写死 `{{ nodes.length }} 台在册`，而 `nodes` 首帧是 `[]` → 进页面会闪一下"0 台在册"。零台和"还没读到"是两回事，本项目对这两者的区分是产品契约之一（S3b 的三种沉默三句话） | 表头改 `headNote`：加载中显示"读取中…"，读到后才报数（`:286`） |
| 前端 | **逐机阈值行印了全局的允许区间**：全局表里 `｜界 0~100` 是对的（那是播种来的约束），逐机覆盖表复用同一段模板，会让人以为"这台只能填 0~100"而它其实是在跟全局值比大小 | 逐机表只留单位，区间只在全局表出现（`:436` vs `:314`） |

### 8.2 与本文件原设计的偏离（有意为之，逐条可对判据）

| # | 偏离 | 为什么 |
|---|---|---|
| 偏1 | **§5 的端点清单多了一个：`GET /api/admin/config`（带闸只读）** | 可编辑表单只能来自**未掩码**文档——`/api/config` 对局域网开放并按 S5 G3 掩码，把掩码值回写进表单再"应用"= 悄悄把探测臂换成 `10.9.*.*`。两个要求（读侧掩码 / 表单能编辑）只能靠"全进程唯一一处带闸的读"调和。`selftest-hygiene` **C5 按路径名白名单**钉住它：带闸的读只许有这一个，且必须在断言里写明为什么要口令。这是 J2/J6 的**加强**，不是扩权 |
| 偏2 | **站点层（`sites`）本批只读** | §4 的三级派生里 `sites` 是一等公民，但界面上只能"看见"（列名 + 一句去处），新增站点仍要写 DB 行或改文件。取舍：全局 + 逐机两层已覆盖真实场景（三台机、一个办公站点），而站点编辑要回答"站点改名怎么办、删站点谁受影响"——那是第二张表的量级。**已知缺的那块写在界面上**，不留"看起来能改"的错觉（H6 残留①） |
| 偏3 | **§7 预案"要砍先砍全局阈值编辑器"没有执行，被降级的是站点层** | 全局阈值表是逐机覆盖的**对照基准**：只读的话每次挪红线都要人脑记住全局值是多少，恰好把 H7 想消掉的负担放回人身上 |
| 偏4 | **§3 的 `thresholdsFor(host)` 没有落在 `status.js`**：覆盖由 store 侧读 roster、算好挂到 `host.thresholds_override`，status 只做 `mergeNodeThresholds` | `status.js` 是 P1 契约里最干净的判级层，不该为取配置去 import roster（否则判级层长出依赖，四态判级就不是一个实现了）。代价 = 每快照一次 DB 读（三台 <1ms），可接受 |
| 偏5 | **J7 措辞"节点长期未上报"实现为"该机未再上报"** | 同一行要同时容纳 S3 的"跨重启缺席"与 S5 的"静默降级"，宽限期最短可配到 1 分钟——"长期"在这种行上就是假的。完整解释放进 `title`，不占正文 |
| 偏6 | **Agent 侧加了一处打印**（`main.py:171` 探测计划来源） | §4 说"Agent 零逻辑改动"仍然成立（不解释、不判断），但目标地址只在 Agent 那一侧看得见；不在它启动日志里印出来，H6 的错语义就只有 Server 端的人看得到。顺带让 H19 将来做"裸 `python main.py`"时多一行现场证据 |
| 偏7 | **宽限期从文件常量变成可配标量**（`alert.unknown_alert_grace_minutes`，1~240 分） | §7 风险里那句"或者把 grace 配到 1-2 分钟"顺手做实：不硬改，但也不逼用户为一个数字重启 |

### 8.3 本期未验证（交接时按此核对，别当已验证）

| 项 | 状态 |
|---|---|
| 设置页排版（两栏在窄窗/高分屏下的宽窄、行内换行、按钮文案长度） | **人眼未过**。临时库 harness 里核的是四态与写入闭环，而内置浏览器视口 0×0（同 S5 §8.2 的限制）→ 几何量测无意义。**上线后需要一次正常窗口下的肉眼**，这是本期唯一一处"代码绿了但没看过"的界面。 |
| 冻结行的真实听感 | **未做主观确认**：DOM/计算样式核实了虚线 chip 与整行降透明，声音侧用 AudioContext 计数实测（冻结态 11s 内 0 个 oscillator、正对照 24 个），但"听起来不吵"这件事机器答不了。里程碑第一次重启 231 时留意一下那 5 分钟。 |
| `probe_plan_suspect`（"可疑"标）在真机上从未出现 | 本机三台全在同一站点，D 段/E9 用的是合成节点。**它是 J5 后半的唯一呈现，却没在真实数据上被看过一次** → 接办公室那台（118）或朋友机器进来时顺手确认。 |
| 站点命中的端到端 | 因偏2（只读），E9 里 `sites` 是 DB 直写造的，不是界面写的。界面写站点这条路径本期不存在，所以也无从验证。 |
| **H22 的崩溃路径是读代码确认的，不是实跑复现的** | 我**故意没给它做自检**：钉一条"发局部请求就会崩"的用例，价值低于直接修掉它，而且仓库里不该留一个故意打崩自己的脚本。风险 = 若我对 `replaceInto`/`levelFor` 的推断有一环错，那这条隐患是纸上的。**上线前若要翻这笔账**：本机临时库上手动 curl 一次局部 `POST /api/admin/config/thresholds`（**别在 `server/data` 上**），看 Server 是否真的退出。修法见 H22。 |
| 首次上线的"配置从哪来" | 231 现有 `roster.db` 里**没有** `settings` 行 → 升级后第一次开机走"文件播种"，行为与今天一致（J2 的推论）。但这条只在本机临时库上验过（A 段），**生产库上仍是首次**：上线现场确认两件事——设置页能打开且数值等于 `thresholds.json`，以及改一格不重启就生效。 |

### 8.4 自检结果（本期实跑，全绿）

服务端七套合计 **309/0**：`selftest-roster 100` · `selftest-events 59` · `selftest-credential-hygiene 50` · `selftest-config 48` · `selftest-zt-presence 28` · `selftest-agent-pair 14` · `selftest-history 10`。
客户端三套 **25/0**：`check-tokens 10` · `selftest-sound 9`（本期新增）· `selftest-fps 6`；`vite build` 通过。

J8 的对账方式不是"总数变大就行"：**既有六套的数字与 S5 §8.3 逐条相等，只有一处变大**（hygiene 44→50，是 S6 新增的偏1 白名单与泄漏断言，一条未删、一条未改弱）；`selftest-config` 48 = 初版 40（A~F 段）+ S6-5 的 G 段 8 条。

---

## § 收口销账（2026-09-20 文档对账）

只改文档、未动一行代码。

- **§8 引言"全部本机完成，**未上线**"→ 已被现场推翻**：09-20 01:40 S5+S6 上线 231（生产 = 代码锋面 `61bf4db` = GitHub tag `v2`），02:08 整机重启后自启通过、三台 Agent 全部以 `sha256$` 凭据在 `strict` 下重连（`accepted_legacy:0`）。本批的 DB 真源、设置页、H14 冻结契约现在都跑在生产上。
- **§8.3 "首次上线的配置从哪来"这一格，两件事只做了半件**：①"数值等于 `thresholds.json`"已确认（`/api/config` 逐键对过文件，顺带从现场核出了 `METRICS` 的真实键名——这个读数后来纠正了 H22 的复现例证）；但"设置页能打开"是**从接口看**的，页面本身仍没人眼过。②"**改一格不重启就生效**"**没有在生产上做**——那需要真改一次生产阈值，本期没有拿到这个授权。热生效目前只有本机临时库的自检 B 段背书，**这条要挂着**：在生产上验一次是"要不要动现场"的决定，得用户点头。
- **顺带一条同族的语义坑（H28）**：`untouched_since_seed` 首启 `true`、之后每次开机都是 `false`（播种这个动作本身就写那一行）。`部署说明.md` 与本文件的期望值 09-20 都已按"后者属预期"改过，**V3 只剩代码/文案那一半**，别再去改文档。
- **§8.3 末行"H22 的崩溃路径是读代码确认的，不是实跑复现的"→ 状态未变**：09-20 没有在任何库上 curl 过一次局部 `POST /api/admin/config/thresholds`（本机也没有），所以 H22 至今仍是**推演级证据**（但每一环都有 `config.js:109`、`config.js:150`、`status.js:25`、`index.js:544` 的代码依据）。这次对账改掉的正是它的两处：①**例证键名**原先写 `{mem_usage:{…}}`，那是个不存在的键、请求会在 `config.js:136-139` 被 400 拒掉——照旧例子写回归用例会**误判成"已修"**；②**修法覆盖面**原先只点名"那个 timer 与 `alerts.tick`"，而 `alerts.tick` 是 `store.js:201` 同步调的（同一个点），真正还有一条不经过广播 timer 的入口 `index.js:539`（客户端一连接就 `store.getSnapshot()`）——只包 :544 会被"刷新一次页面"再打死。V3 施工图（含两处 try/catch 落点、`events.record()` 与客户端 `KIND_TABS` 的配套）在 `00-暂存\V3-前置-文档对账.md`。
- **§8.1 当场修掉的七条与 §8.2 偏1~偏7**：09-20 逐条回读过代码，仍然成立（偏1 的 `GET /api/admin/config` 确在全进程唯一一处带闸的读上；`validateThresholds` 里那行"改传一道路径、完整名只作 label"的注释就是 §8.1 首条的现场）。
- **§8.3 "设置页排版人眼未过"→ 变成 V3 首批第 1 项**（`部署说明.md`"明天上眼清单"第 1 条），且 09-20 用户给了**默认放行**（原话"屏幕默认验收我通过"），口径"默认通过、非人眼逐条确认"，见同一份对账文件。
- **`probe_plan_suspect`（"可疑"标）在真机上从未出现**：状态未变。09-20 上线读到的是"三台无逐机覆盖、全在同一站点"，并没有制造出该分支的场景——它仍是 J5 后半唯一没在真实数据上被见过一次的呈现。
- **复跑读数**：2026-09-20 全套 = server 309/0、client 25/0，与本文件 §8.4 逐套件相等。

