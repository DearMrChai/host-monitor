# S2 细化设计 — 配对加机（D0'）+ ingest 凭据强制（H8）+ 演示机群

> 状态：**S2a 服务端已实施并自测通过（2026-09-19）**；S2b Agent / S2c 前端待做，见 §9 实施记录。
> 迭代节奏：本机自测，不重部署 231、不 push GitHub（推送与部署各自单独请示）。
> 上游依据：`V2-迭代讨论纪要-20260919.md` §5.4 的 S2、`技术隐患清单.md` H2/H4/H8。
> 一句话目标：**把"加一台机器"从改文件变成页面上点一下**，同时补上成型期最大的两个掉价点——① 接入端口无凭据（任何 ZT 成员可伪造节点），② 数据单薄（只有 3 台）。

---

## 1. 范围

| 子段 | 内容 | 交付判据 |
|---|---|---|
| S2a 服务端 | 配对码签发/消费、每节点长期密钥、ingest 三档强制（off/legacy/strict）、metrics 帧越权改写封堵、伪造拒绝台账、演示机群进程内生成 | 无凭据的新 host_id 接不进来；演示机群一条环境变量开关 |
| S2b Agent | `agent.json` 配置层（H2）、`--enroll` 一次性配对后自动落盘 node_key、`agent_version` + 机器指纹上报 | 新机只需一条命令；第二次启动不再需要配对码 |
| S2c 前端 | 接入页（生成配对码 + 一行命令 + 等待接入 + 最近拒绝）、演示机群可见性开关、"N 台 Agent 未带凭据"提示 | 一台新机 ≤3 分钟出现在看板；演示机群可一键关 |

**不在本期**（各自有主）：H12 常驻机重启后不重连=隐形（并入 S3 事件流，因为 24h 流正是它的呈现面）、observed/SSH（S5 闸口后）、看板读侧鉴权（决策维持 9101 开放）、完整设置界面（S6）。

---

## 2. 配对模型：一次性的配对码 + 长期节点密钥

不把配对码当成长期凭据用——它会在命令行里、在朋友的历史记录里、在截图里留下痕迹。所以是两段式：

```
看板点"生成配对码"          Agent 首连 register{token=配对码}        之后每次启动
  └─ 12 位随机码            └─ Server 校验/计数/作废                 └─ register{host_id, key=节点密钥}
     TTL 15min                   ├─ 签发 32hex 节点密钥写入 roster       ├─ 校验通过 → 采数
     可用次数 1/5/∞              └─ ack{node_key} → Agent 落 agent.json  └─ 不通过 → 拒绝
```

- 配对码与节点密钥都只存 `roster.db`（码表 `enroll_codes` + `nodes.enroll_token`），**API 只回状态不回明文**（`has_credential` 已是 S1 既有字段）。
- 明文只在"刚生成出来"的那一次出现在响应里——与 S1 口令弹层同样的诚实边界：局域网明文传输，不给它编"已加密"的说法。

## 3. ingest 强制三档（H8）

`HM_INGEST_TOKEN` = `legacy`（默认）｜ `strict` ｜ `off`。判定顺序：有效配对码 → 该 host_id 的节点密钥 → 按档位兜底。

| 档 | 未知 host_id 无凭据 | 已入名册但无密钥（一期 3 台 Agent） |
|---|---|---|
| `off` | 接受（= v1 现状，逃生阀） | 接受 |
| `legacy` | **拒绝** | 接受，但计入"未带凭据"并在看板提示 |
| `strict` | 拒绝 | 拒绝 |

**为什么默认 legacy 而不是 strict**：迭代节奏不许我动 231 上跑着的三台 Agent，`strict` 会把它们当场踢下线。但 legacy 已经关掉了本期真正要防的那个口子——**别人/临时机凭空造出一个节点**。里程碑重部署时的动作是：三台 Agent 换上带 `agent.json` 的新版 → 改 `strict` → 重启。这条写进部署说明，不靠记忆。

legacy 的残留风险如实记录：知道真实 host_id 的人仍可无凭据顶替它（H4 的重写问题）。封堵要靠 strict + 指纹绑定，指纹不一致在 legacy 下只记"漂移"不拒绝（重装系统会换指纹，直接拒绝=自己造故障）。

## 4. 顺手堵掉的一个真洞（不属于 H8 但同源）

`msg.type==='metrics'` 时 `hostId = msg.host_id || hostId` 允许**一条已注册连接改写任意 host_id 的数据**。改为：连接一旦 register 成功，其 metrics 帧的 host_id 必须与连接一致，不一致 → 丢弃 + 记入拒绝台账。

## 5. 演示机群（`模拟-`）

进程内生成器 `server/src/demo.js`，`HM_DEMO=1` 才开——生产默认关，不靠"记得关掉"。

| 决策 | 取法 | 理由 |
|---|---|---|
| 命名 | host_id 一律 `sim-*`，显示名 Server 侧强制加 `模拟-` 前缀（S1 §6 既有实现） | 前缀不能由跑 mock 的人决定 |
| 归并 | **不计入** `cluster.health` / 在线分母 / 聚合负载；只计入新增的 `cluster.demo` | "我的集群 3/3 健康"这句话不能被道具污染；聚合负载是关于我们硬件的事实陈述 |
| 告警 | **照常进**告警面板与横幅 | 演示要看板有内容；条目带 `模拟-` 前缀自证道具身份，且"道具告警不影响真集群健康"本身就是要展示的能力 |
| 历史 | 照常落库（host_id `sim-*` 可整体 `DELETE`） | 详情页曲线要有东西；30 天保留自然衰减；清理语句写文档 |
| 关 | **服务端开关**（`POST /api/admin/demo` + 口令，状态持久在 roster meta），前端只发请求 | 红线：**缺开关的演示不算完成**。原设计写的是"localStorage 过滤 `kind==='demo'`"，实施时否掉了——本地过滤只是**看不见**，生成器仍在跑、仍在写历史库，那等于对自己说谎 |

演示场景写死 4 台：健康的向量库、满载的"朋友的游戏机"、磁盘将满的影音机、失联的临时笔记本——正好演示 ABSENT/退役/静默这套 S1 语义。

## 6. 接入页（D0'）

从集群条的"+ 接入"进入；写操作走 `rosterPost`，没口令时先弹口令层（S1b 既有）。页面四块：配对码 + 有效期倒计时 + 剩余次数；Windows/Linux 两条可复制命令；等待接入（轮询 roster 新增，成功后立刻显示节点名）；最近被拒连接（IP 尾段 + 原因 + 时间）。

一行命令的现实边界：Agent 需要 Python + `pip install -r requirements.txt`。本期给的是"下载/解出 agent 目录 → 一条命令配对"，并明示依赖；`GET /agent.tar`（按需打包 Agent 源码）作为 S2c 的可选项，做完才算"真一行"。

## 7. 验收映射

| 判据 | 怎么验 |
|---|---|
| 拔掉临时机集群仍绿（S1 已交付，本期不回退） | selftest 继续跑 |
| 无凭据的新 host_id 接不进来 | S2a 自测：legacy/strict/off 三档 × 有码/错码/无码 |
| 一台新机 ≤3 分钟出现在看板 | 本机实跑：生成码 → node-repl 模拟 Agent 配对 → 看板出现 |
| 截图能填满一屏 | 演示机群开 + 前端开关生效，逐项检查聚合数字 |
| 演示可关 | 开关切两次 + 刷新后状态保持 |

## 8. 风险与回滚

- 配对码/密钥表只在 `roster.db`，删库=回到无凭据状态（与 S1 同）。
- `HM_INGEST_TOKEN=off` 是逃生阀：任何一档把真机挡住，可当场回退，不需要改代码。
- 演示机群与真实机群共库：清理语句 `DELETE FROM samples_raw WHERE host_id LIKE 'sim-%'`（`samples_1m` 同理）+ `DELETE FROM nodes WHERE host_id LIKE 'sim-%'`，写进部署说明。

---

## 9. 实施记录

### 9.1 S2a 服务端（2026-09-19 完成，本机自测 95/95）

**新增文件**

| 文件 | 职责 |
|---|---|
| `server/src/ingest.js` | 接入授权策略单一真源：`authorize()`（纯决策，无 socket 依赖）、`frameHostMatches()`（§4 封堵）、`recentEvents()`（拒绝台账，地址掩码到 `x.y.*.*`）、三档 `resolveMode()` |
| `server/src/demo.js` | 演示机群进程内生成器：4 台写死场景、`attach(store)` 注入避免 import 环、`setEnabled()` 真开真关、`autoStart()` 启动预置 |

**改造点**

- `roster.js`：新表 `enroll_codes`（码/创建/过期/上限/已用/备注/已配对 id）＋ `issueEnroll/consumeEnroll/activeCodes/revokeEnroll`；`nodes.enroll_token` 只由 Server 铸（`newNodeKey()`），`ensure()` **忽略配对码**、只落长期密钥（否则一次性的码会变成永久凭据）；`keyMatches()` 用 `timingSafeEqual` 且空值永不匹配；`keylessAgents()`、`fingerprintOf()`、`demoNodes()`；口令读写重构到 `#metaGet/#metaSet` 并复用给 `demo_enabled`。
- `status.js`：`evaluateCluster` 先按 `kind==='demo'` 拆 `real`/`props`，健康度、在线分母、`persistent/ephemeral`、链路、静音计数全部只走 `real`；新增 `cluster.demo.{online,total}`。
- `index.js`：register 走 `ingest.authorize()`，拒绝对方回 `{type:'rejected',reason}` 后以 `4001 + reason` 关闭；metrics 帧加连接身份校验；新路由 `POST /api/admin/enroll`（口令，返回明文码＋`host`＋`agent_port`）、`GET /api/enroll`（只回码尾号＋台账）、`POST /api/admin/enroll/revoke`、`POST /api/admin/demo`（口令）。
- `store.js`：快照 `admin` 块补 `ingest_mode` / `keyless_agents` / `demo`，前端不需要额外请求即可渲染提示。

**与设计不同的地方（都已回写上文）**

1. 演示开关从"前端 localStorage 过滤"改成"服务端生成器开关＋持久 meta"。理由见 §5 表格。附带把 `HM_DEMO=1` 定位为**启动预置**：env 优先，其次读上次落盘状态，因此生产环境默认关、且"演示机开着重启服务器"不会被静默改变。
2. 关演示时**顺带退役**这 4 个节点（`presence_class='retired'` + 标记断连），比设计的"停止生成"更严：残留告警按 S1b 语义闭合成"已退役"，不会衰减成"已恢复"。

**验证（`node scripts/selftest-roster.mjs`，全绿 96 项；`selftest-history.mjs` 10/10；`npm run build` 通过）**

- F 段（纯函数层，23 项）：码的签发/逐次消费/过期/用尽/同机重复配对不重复计数；三档行为矩阵（未知节点 legacy 即拒、老 Agent 宽限、strict 拒、off 逃生阀）；跨节点改写被拒；指纹漂移"接受但留痕"；演示 4 台全部 `模拟-` 前缀、道具不移动任何真机数字（`cluster.total`/`health`/`aggregate.cpu.avg` 逐项相等）、越线指标确实是 WARN、临时道具 ABSENT 且零告警、关掉后生成停止＋退役＋分母归零。
- G 段（真起 Server，端到端 16 项）：无凭据机器拿到 `rejected` 后被 4001 断开且不进名册；签发配对码要口令、明文只在签发响应里出现一次；带码注册 → ack 回 `node_key` → 名册记 `confirmed + has_credential`；第二次启动用密钥直连**不再消耗码**（`uses` 仍为 1）；读接口只回尾号；未带凭据的 v1 Agent 在快照里点名；演示开关经 HTTP＋口令生效，等过防抖后道具告警进面板而真机健康度不变，关掉时以 `retired` 闭合；**G7** 接入帧自报 `kind:'demo'` 无效。
- E 段（S1 回归，16 项）：新增 `HM_INGEST_TOKEN=off` 环境变量运行——E 段测的是名册语义，早于凭据层存在；策略另由 F/G 覆盖。这是本次唯一对既有测试基线的改动。

**实施中顺手补的洞（设计里没写，属于 §5 的反面）**

- 原先 `store.register(..., { kind: msg.kind })` 让 Agent 自己声明身份类别 → 一台真机可以自称 `demo` 从而**退出**健康度归并、在线分母与聚合负载。`kind` 改为只由服务端决定（演示生成器是唯一传 kind 的调用方），断言见 G7。
- 测试与注释里的示例地址改用 RFC 5737 文档段（`198.51.100.x` / `203.0.113.x`），保证仓库里不出现任何真实网段地址。

**S2a 已知未闭合（不隐藏）**

- 三台生产 Agent 仍在 `legacy` 宽限内，`strict` 要等 S2b 换版后在里程碑重部署时翻档（动作写 `部署说明.md`，见 §3）。
- 指纹校验目前是**服务端就绪、客户端未上报**：真 Agent 尚不发 `fingerprint`/`agent_version`，所以 H4 的绊线在 S2b 之前是空转的。
- 节点密钥明文躺在 `roster.db`、明文走在局域网 WS 上：能读 231 磁盘或能在链路中间监听者可顶替已配对节点。已登记为 H16，与 H11 同批（S5 闸口）治理。

### 9.2 S2b Agent（待做）

`agent.json` 配置层（H2）＋ `--enroll CODE` 一次性配对并把 ack 的 `node_key` 落盘 ＋ 上报 `agent_version` 与机器指纹（machine-id / 注册表 MachineGuid / 持久 install_id 兜底）。

### 9.3 S2c 前端（待做）

接入页（§6）、演示机群可见性开关、"N 台 Agent 未带凭据"提示；可选 `GET /agent.tar`。
