# S2 细化设计 — 配对加机（D0'）+ ingest 凭据强制（H8）+ 演示机群

> 状态：**S2a 服务端、S2b Agent、S2c 前端均已实施并自测通过（2026-09-19）**；S2 整段完成，剩余未闭合项逐条列在 §9.3 末尾。生产机群仍是旧版 Agent + `legacy` 档（换版与翻档见 `部署说明.md`）。
> 迭代节奏：本机自测，不重部署 231、不 push GitHub（推送与部署各自单独请示）。
> 上游依据：`V2-迭代讨论纪要-20260919.md` §5.4 的 S2、`技术隐患清单.md` H2/H4/H8。
> 一句话目标：**把"加一台机器"从改文件变成页面上点一下**，同时补上成型期最大的两个掉价点——① 接入端口无凭据（任何 ZT 成员可伪造节点），② 数据单薄（只有 3 台）。

---

## 1. 范围

| 子段 | 内容 | 交付判据 |
|---|---|---|
| S2a 服务端 | 配对码签发/消费、每节点长期密钥、ingest 三档强制（off/legacy/strict）、metrics 帧越权改写封堵、伪造拒绝台账、演示机群进程内生成 | 无凭据的新 host_id 接不进来；演示机群一条环境变量开关 |
| S2b Agent | `agent.json` 配置层（H2）、`--enroll` 一次性配对后自动落盘 node_key、`agent_version` + 机器指纹上报 | 新机只需一条命令；第二次启动不再需要配对码 |
| S2c 前端 | 接入页（生成配对码 + 一行命令 + 等待接入 + 最近拒绝）、演示机群可见性开关、"N 台 Agent 未带凭据"提示 | 一台新机 ≤3 分钟出现在看板；演示机群可一键关 |

**不在本期**（各自有主）：H12 常驻机重启后不重连=隐形（并入 S3 事件流，因为 24h 流正是它的呈现面）、observed/SSH（S5 闸口后）、看板读侧鉴权（决策维持 9101 开放）、完整设置界面（S6）。

---

## 2. 配对模型：一次性的配对码 + 长期节点密钥

不把配对码当成长期凭据用——它会在命令行里、在朋友的历史记录里、在截图里留下痕迹。所以是两段式：

```
看板点"生成配对码"          Agent 首连 register{token=配对码}        之后每次启动
  └─ 12 位随机码            └─ Server 校验/计数/作废                 └─ register{host_id, key=节点密钥}
     TTL 15min                   ├─ 签发 32hex 节点密钥写入 roster       ├─ 校验通过 → 采数
     可用次数 1/5/∞              └─ ack{node_key} → Agent 落 agent.json  └─ 不通过 → 拒绝
```

- 配对码与节点密钥都只存 `roster.db`（码表 `enroll_codes` + `nodes.enroll_token`），**API 只回状态不回明文**（`has_credential` 已是 S1 既有字段）。
- 明文只在"刚生成出来"的那一次出现在响应里——与 S1 口令弹层同样的诚实边界：局域网明文传输，不给它编"已加密"的说法。

## 3. ingest 强制三档（H8）

`HM_INGEST_TOKEN` = `legacy`（默认）｜ `strict` ｜ `off`。判定顺序：有效配对码 → 该 host_id 的节点密钥 → 按档位兜底。

| 档 | 未知 host_id 无凭据 | 已入名册但无密钥（一期 3 台 Agent） |
|---|---|---|
| `off` | 接受（= v1 现状，逃生阀） | 接受 |
| `legacy` | **拒绝** | 接受，但计入"未带凭据"并在看板提示 |
| `strict` | 拒绝 | 拒绝 |

**为什么默认 legacy 而不是 strict**：迭代节奏不许我动 231 上跑着的三台 Agent，`strict` 会把它们当场踢下线。但 legacy 已经关掉了本期真正要防的那个口子——**别人/临时机凭空造出一个节点**。里程碑重部署时的动作是：三台 Agent 换上带 `agent.json` 的新版 → 改 `strict` → 重启。这条写进部署说明，不靠记忆。

legacy 的残留风险如实记录：知道真实 host_id 的人仍可无凭据顶替它（H4 的重写问题）。封堵要靠 strict + 指纹绑定，指纹不一致在 legacy 下只记"漂移"不拒绝（重装系统会换指纹，直接拒绝=自己造故障）。

## 4. 顺手堵掉的一个真洞（不属于 H8 但同源）

`msg.type==='metrics'` 时 `hostId = msg.host_id || hostId` 允许**一条已注册连接改写任意 host_id 的数据**。改为：连接一旦 register 成功，其 metrics 帧的 host_id 必须与连接一致，不一致 → 丢弃 + 记入拒绝台账。

## 5. 演示机群（`模拟-`）

进程内生成器 `server/src/demo.js`，`HM_DEMO=1` 才开——生产默认关，不靠"记得关掉"。

| 决策 | 取法 | 理由 |
|---|---|---|
| 命名 | host_id 一律 `sim-*`，显示名 Server 侧强制加 `模拟-` 前缀（S1 §6 既有实现） | 前缀不能由跑 mock 的人决定 |
| 归并 | **不计入** `cluster.health` / 在线分母 / 聚合负载；只计入新增的 `cluster.demo` | "我的集群 3/3 健康"这句话不能被道具污染；聚合负载是关于我们硬件的事实陈述 |
| 告警 | **照常进**告警面板与横幅 | 演示要看板有内容；条目带 `模拟-` 前缀自证道具身份，且"道具告警不影响真集群健康"本身就是要展示的能力 |
| 历史 | 照常落库（host_id `sim-*` 可整体 `DELETE`） | 详情页曲线要有东西；30 天保留自然衰减；清理语句写文档 |
| 关 | **服务端开关**（`POST /api/admin/demo` + 口令，状态持久在 roster meta），前端只发请求 | 红线：**缺开关的演示不算完成**。原设计写的是"localStorage 过滤 `kind==='demo'`"，实施时否掉了——本地过滤只是**看不见**，生成器仍在跑、仍在写历史库，那等于对自己说谎 |

演示场景写死 4 台：健康的向量库、满载的"朋友的游戏机"、磁盘将满的影音机、失联的临时笔记本——正好演示 ABSENT/退役/静默这套 S1 语义。

## 6. 接入页（D0'）

从集群条的"+ 接入"进入；写操作走 `rosterPost`，没口令时先弹口令层（S1b 既有）。页面四块：配对码 + 有效期倒计时 + 剩余次数；Windows/Linux 两条可复制命令；等待接入（轮询 roster 新增，成功后立刻显示节点名）；最近被拒连接（IP 尾段 + 原因 + 时间）。

一行命令的现实边界：Agent 需要 Python + `pip install -r requirements.txt`。本期给的是"下载/解出 agent 目录 → 一条命令配对"，并明示依赖；`GET /agent.tar`（按需打包 Agent 源码）作为 S2c 的可选项，做完才算"真一行"。

> 实施后的差异（不覆盖上面的原设计，只记在这里）：入口是集群条第三个视图按钮"接入"（外加 `演示 N` / `⚿ 未带凭据 N` 两枚可点进来的 chip），块数从四块变六块，"等待接入"改为按配对码的 `paired_ids` 差集判定，配对码列表新增"按 id 撤销"。逐条理由见 §9.3。`GET /agent.tar` 未做。

## 7. 验收映射

| 判据 | 怎么验 |
|---|---|
| 拔掉临时机集群仍绿（S1 已交付，本期不回退） | selftest 继续跑 |
| 无凭据的新 host_id 接不进来 | S2a 自测：legacy/strict/off 三档 × 有码/错码/无码 |
| 一台新机 ≤3 分钟出现在看板 | S2a/S2b：真 Python Agent 一条 `--enroll` 命令 → 名册 `confirmed`＋看板出现真数据（`selftest-agent-pair.mjs` P1/P2/P4b）。S2c 补了页面侧那半：接入页签发 → 把页面上那行命令**原样**贴进真终端 → "等待接入"里出现节点名 → 详情页可进（§9.3 实跑）。**仍未闭合**："≤3 分钟"是给陌生人掐的人工表，需要一台没装过依赖的机器 + 别人操作，本机自测不算。 |
| 截图能填满一屏 | 演示机群开 + 前端开关生效，逐项检查聚合数字：4 台道具在总览铺满一屏、`在线 2/2` 与 `集群正常` 在道具在场时不变（S2c 实跑，§9.3） |
| 演示可关 | 开关切两次 + 刷新后状态保持：接入页按钮 → 服务端档 → 顶栏 `演示 4` 出现/消失 → 关后卡片从 6 张回到 2 张（S2c 实跑）；`POST /api/admin/demo` 的持久性在 S2a 自测 E 段已验 |

## 8. 风险与回滚

- 配对码/密钥表只在 `roster.db`，删库=回到无凭据状态（与 S1 同）。
- `HM_INGEST_TOKEN=off` 是逃生阀：任何一档把真机挡住，可当场回退，不需要改代码。
- 演示机群与真实机群共库：清理语句 `DELETE FROM samples_raw WHERE host_id LIKE 'sim-%'`（`samples_1m` 同理）+ `DELETE FROM nodes WHERE host_id LIKE 'sim-%'`，写进部署说明。

---

## 9. 实施记录

### 9.1 S2a 服务端（2026-09-19 完成，本机自测 96/96）

**新增文件**

| 文件 | 职责 |
|---|---|
| `server/src/ingest.js` | 接入授权策略单一真源：`authorize()`（纯决策，无 socket 依赖）、`frameHostMatches()`（§4 封堵）、`recentEvents()`（拒绝台账，地址掩码到 `x.y.*.*`）、三档 `resolveMode()` |
| `server/src/demo.js` | 演示机群进程内生成器：4 台写死场景、`attach(store)` 注入避免 import 环、`setEnabled()` 真开真关、`autoStart()` 启动预置 |

**改造点**

- `roster.js`：新表 `enroll_codes`（码/创建/过期/上限/已用/备注/已配对 id）＋ `issueEnroll/consumeEnroll/activeCodes/revokeEnroll`；`nodes.enroll_token` 只由 Server 铸（`newNodeKey()`），`ensure()` **忽略配对码**、只落长期密钥（否则一次性的码会变成永久凭据）；`keyMatches()` 用 `timingSafeEqual` 且空值永不匹配；`keylessAgents()`、`fingerprintOf()`、`demoNodes()`；口令读写重构到 `#metaGet/#metaSet` 并复用给 `demo_enabled`。
- `status.js`：`evaluateCluster` 先按 `kind==='demo'` 拆 `real`/`props`，健康度、在线分母、`persistent/ephemeral`、链路、静音计数全部只走 `real`；新增 `cluster.demo.{online,total}`。
- `index.js`：register 走 `ingest.authorize()`，拒绝对方回 `{type:'rejected',reason}` 后以 `4001 + reason` 关闭；metrics 帧加连接身份校验；新路由 `POST /api/admin/enroll`（口令，返回明文码＋`host`＋`agent_port`）、`GET /api/enroll`（只回码尾号＋台账）、`POST /api/admin/enroll/revoke`、`POST /api/admin/demo`（口令）。
- `store.js`：快照 `admin` 块补 `ingest_mode` / `keyless_agents` / `demo`，前端不需要额外请求即可渲染提示。

**与设计不同的地方（都已回写上文）**

1. 演示开关从"前端 localStorage 过滤"改成"服务端生成器开关＋持久 meta"。理由见 §5 表格。附带把 `HM_DEMO=1` 定位为**启动预置**：env 优先，其次读上次落盘状态，因此生产环境默认关、且"演示机开着重启服务器"不会被静默改变。
2. 关演示时**顺带退役**这 4 个节点（`presence_class='retired'` + 标记断连），比设计的"停止生成"更严：残留告警按 S1b 语义闭合成"已退役"，不会衰减成"已恢复"。

**验证（`node scripts/selftest-roster.mjs`，全绿 96 项；`selftest-history.mjs` 10/10；`npm run build` 通过）**

- F 段（纯函数层，23 项）：码的签发/逐次消费/过期/用尽/同机重复配对不重复计数；三档行为矩阵（未知节点 legacy 即拒、老 Agent 宽限、strict 拒、off 逃生阀）；跨节点改写被拒；指纹漂移"接受但留痕"；演示 4 台全部 `模拟-` 前缀、道具不移动任何真机数字（`cluster.total`/`health`/`aggregate.cpu.avg` 逐项相等）、越线指标确实是 WARN、临时道具 ABSENT 且零告警、关掉后生成停止＋退役＋分母归零。
- G 段（真起 Server，端到端 16 项）：无凭据机器拿到 `rejected` 后被 4001 断开且不进名册；签发配对码要口令、明文只在签发响应里出现一次；带码注册 → ack 回 `node_key` → 名册记 `confirmed + has_credential`；第二次启动用密钥直连**不再消耗码**（`uses` 仍为 1）；读接口只回尾号；未带凭据的 v1 Agent 在快照里点名；演示开关经 HTTP＋口令生效，等过防抖后道具告警进面板而真机健康度不变，关掉时以 `retired` 闭合；**G7** 接入帧自报 `kind:'demo'` 无效。
- E 段（S1 回归，16 项）：新增 `HM_INGEST_TOKEN=off` 环境变量运行——E 段测的是名册语义，早于凭据层存在；策略另由 F/G 覆盖。这是本次唯一对既有测试基线的改动。

**实施中顺手补的洞（设计里没写，属于 §5 的反面）**

- 原先 `store.register(..., { kind: msg.kind })` 让 Agent 自己声明身份类别 → 一台真机可以自称 `demo` 从而**退出**健康度归并、在线分母与聚合负载。`kind` 改为只由服务端决定（演示生成器是唯一传 kind 的调用方），断言见 G7。
- 测试与注释里的示例地址改用 RFC 5737 文档段（`198.51.100.x` / `203.0.113.x`），保证仓库里不出现任何真实网段地址。

**S2a 已知未闭合（不隐藏）**

- 三台生产 Agent 仍在 `legacy` 宽限内，`strict` 要等 S2b 换版后在里程碑重部署时翻档（动作写 `部署说明.md`，见 §3）。
- 指纹校验当时是**服务端就绪、客户端未上报**：真 Agent 尚不发 `fingerprint`/`agent_version`，所以 H4 的绊线在 S2b 之前是空转的。→ S2b 已补上报（§9.2），绊线两端都通了；未闭合的是"真机换硬件"实测，记在 §9.2 末尾。
- 节点密钥明文躺在 `roster.db`、明文走在局域网 WS 上：能读 231 磁盘或能在链路中间监听者可顶替已配对节点。已登记为 H16，与 H11 同批（S5 闸口）治理。

### 9.2 S2b Agent（2026-09-19 完成，本机自测 16/16 ＋ 真机端到端 14/14）

**新增文件**

| 文件 | 职责 |
|---|---|
| `agent/config.py` | 配置与身份的单一真源。`resolve(cli, data_dir, env)` 逐字段按 **CLI > env > `agent.json` > 默认** 取值，并记住每项 `sources`（"我到底读的是哪份文件"是远程排障第一问）；`read_json` 对损坏文件降级成 `{}`＋警告而不是崩；`write_json` 用 `fsync`+`os.replace` 原子落盘；`machine_fingerprint()` = `sha256(f"{source}:{raw}")[:16]`，来源链 `/etc/machine-id` → 注册表 `MachineGuid` → `IOPlatformUUID` → 落盘 `install_id` 兜底，原始值不出机器；`save_identity()` 只合并字段 |
| `agent/agent.example.json` | 可以直接贴给朋友看的模板（占位地址＋`_说明`）；真实那份在 `.gitignore` |
| `agent/scripts/config-selftest.py` | 纯函数层 16 项：优先级链、类型/取值降级、两份文件各自损坏、身份合并、`node_key` 与 `host_id` 绑定、指纹稳定与兜底、不留 `.tmp` 残骸 |
| `server/scripts/selftest-agent-pair.mjs` | **真** Server × **真** Python Agent 端到端 14 项（一次性端口 941x、一次性 DB、跑完即删，不碰 `server/data/`） |

**改造点（`agent/main.py`）**

- 参数解析全部下沉到 `config.resolve()`；新增 `--enroll CODE`、`--pair-only`（先配对、后由计划任务负责常驻）、`--data-dir`、`--print-config`。
- 注册帧带上 `agent_version` ＋ `fingerprint` ＋ `token`（配对码或本机长期密钥）；ack 回 `node_key` 时，把它和 `host_id`/`server`/`fingerprint`/`paired_at` **一起**写进 `identity.json`——密钥与身份必须同生，否则下次启动会拿这台机器的密钥去注册另一个 id，被自己的策略拒掉。
- ack `type === 'rejected'` → 打印中文原因（`REASON_CN` 与 `server/src/ingest.js` 的 `REASON_TEXT` 一一对应）并以退出码 **2** 结束，不再进 5 秒重试循环。理由：被拒是"要人来处理"的状态，重试只会把原因埋进日志海里。
- 已配对的机器不再看主机名（C9）：改计算机名不会让节点在名册里变成孤儿；`host_id` 与身份不符时 `node_key` 直接不启用（C10/C10b）。
- 启动即 `sys.stdout.reconfigure(encoding="utf-8", errors="replace")`：231 是 GBK 控制台，而拒绝文案是中文——编码不能成为第二层故障（P4 的 ICMP 解析吃过同一记）。

**与设计不同的地方**

1. 凭据分成两份文件，设计里只说"落盘"没说落哪儿。`agent.json`（真实地址、可贴进群）与 `identity.json`（节点密钥）必须分开，否则"把配置发给朋友"= 送出写权限（H16 的一部分，至少不再随手扩大）。
2. `config.resolve()` 收一个 `env` 参数：自测需要注入环境而不污染 `os.environ`。第一版没有它，结果测试自己把上一行设的 `HM_INTERVAL` 又复原掉了——C3/C4 当时红给我看的是测试的错，不是代码的错。
3. 默认档没动：Agent 无凭据时服务端仍按 `legacy` 放行。所以 S2b **不是**"换版即换档"，翻 `strict` 依旧留在里程碑重部署（§3）。

**验证**

- `python scripts/config-selftest.py` → 16/16。
- `node scripts/selftest-agent-pair.mjs` → 14/14：P1 一条命令带码上线（就是 §1 那句验收原句）、P2 名册里是 `confirmed + has_credential + kind=agent`、P2b `agent_version=1.1.0` 被记住（H2）、P3/P3b 密钥只进 `identity.json`、P4 三个读接口都拿不到明文密钥、P4b 看板上有真数据（cpu=12 / cores=20 / 3 分区 / 探测结果在帧里）、P5＋P5b 第二次启动 `Credential: identity.json` 且配对码 `uses` 不涨、P6 抄错码 → 中文原因＋退出码 2 且不进名册（P6b）、P7 无凭据陌生机器在 legacy 档同样被拒且**没有** Retrying 行、P8 `--print-config` 说清每项来源。
- 回归：`selftest-roster.mjs` 96/96、`selftest-history.mjs` 0 失败、`python -m py_compile main.py config.py prober.py collectors/*.py scripts/*.py` 通过。

**踩到的两个坑（跨平台自测的通用教训）**

- 本机 `python` 是 pyenv-win 的 `.bat` shim，Node 的 `spawn` 走 `CreateProcess`，对 `.bat` 直接 ENOENT——于是端到端套件在这台机器上**静默 SKIP**，"没测出问题"长得和"没有问题"一模一样。修法：先用 `python -c "import sys;print(sys.executable)"`（必要时经 shell）解析出真 `.exe` 再启动；且判定必须看 `status === 0`，因为 shell 找不到命令时也不抛 error，只给退出码 1。
- 两处退出码断言一开始是**空断言**：匹配到拒绝文案后立刻 `kill`，`code` 就成了 `null`，于是 `code !== 0` 恒真。P7 用 `code === 2` 把这个洞照了出来，随后给 `stop()` 加了"先等它自己退出、超时才杀"的宽限窗，P6 也改成精确断言。

**S2b 已知未闭合（不隐藏）**

- 三台生产 Agent 还跑着旧版：没有 `agent.json`、没有配过对。里程碑重部署时要逐台"换代码 → 页面上签发码 → `--enroll` 一次 → 确认 `Credential: identity.json`"，然后才翻 `strict`（动作写 `部署说明.md`）。
- H4 绊线现在两端都就绪了（客户端上报、服务端比对＋留痕），但"同一 id 真换了硬件"这条路径只做过服务端侧模拟（F 段），没做过真机换盘实测。
- `--print-config` 与 `--pair-only` 只在自测里跑过，还没在真实安装脚本/计划任务里用过。

### 9.3 S2c 前端（2026-09-19 完成，`selftest-roster.mjs` 100/100 ＋ 浏览器 × 真 Agent 实跑）

**新增文件**

| 文件 | 职责 |
|---|---|
| `client/src/views/EnrollView.vue` | 接入页六块（§6 的四块 + 右侧两块）：签发配对码（有效期/次数下拉 → 明文只显示一次的码 + 倒计时）· 可改 Server 地址的 Windows/Linux 命令（`127./localhost` 时提示"这只适用于同机"）· 等待接入（按 `paired_ids` 差集出节点名并可直跳详情）· 最近异常接入（原因中文 + 掩码地址 + 相对时间 + 累计计数）· 未带凭据的 Agent · 演示机群开关。后两块超出 §6 清单，理由见下面"与设计不同的地方" 4 |

**改造点**

| 文件 | 改动 |
|---|---|
| `server/src/roster.js` | `activeCodes()` 多带一个 `rowid AS id`；新增 `revokeById(id)`（只吃这个非敏感把手，不认明文） |
| `server/src/index.js` | `/api/admin/enroll/revoke` 同时接受 `{id}` 与 `{code}`，仍 `requireAdmin` |
| `server/scripts/selftest-roster.mjs` | 新增 G8/G8b/G8c/G8d：撤销此前**零覆盖**（S2a 只测了"撤销单个码不影响其它码"，走的是直接喂明文的路径） |
| `client/src/lib/admin.js` | `admin` 单例多出 `ingestMode` / `keylessAgents` / `demo`，`syncAdmin()` 逐字段守卫（`GET /api/hosts` 只带 `passphrase_set`，缺字段不能写成 `undefined`） |
| `client/src/components/ClusterBar.vue` | 第三个视图按钮"接入"；两枚可点进接入页的 chip：`演示 N`（虚线、灰）、`⚿ 未带凭据 N`（橙） |
| `client/src/App.vue` | 挂 `EnrollView`；`ORIGINS = ['topology','enroll']` 取代原来那句三元——**加第三个来源时它会把 from 静默丢成 overview** |
| `client/src/views/DetailView.vue` | 返回按钮标签写死"← 总览"改为 `from` prop + `BACK_TEXT` 映射（详见下面第 5 条） |

**与设计不同的地方**

1. **撤销需要一个不是明文的把手。**设计只说"列出现存配对码（只显尾号）+ 可撤销"，没留意这两句合起来有洞：明文只在签发响应里出现过，页面一刷新就再也没有能撤销那枚码的东西了——过期前无法收回，只能等它自己烂掉。补 `rowid AS id`：id 可以公开（它不证明持有者知道码），撤销仍然要口令。
2. **倒计时在前端算，但档位/名单/演示状态一律来自服务端。**`ttl` 的"剩余 14 分 59 秒"是本地时钟差（服务端只给 `expires_at`）；`ingest_mode` / `keyless_agents` / `demo.{enabled,count}` 全部来自快照（S2a 已放进 `store.getSnapshot().admin`）。这条线是 §3 "前端不重算阈值"的同一件事在凭据层的翻版。
3. **"等待接入"用 `paired_ids` 差集，不用"名册新增"**。设计 §6 原句是"轮询 roster 新增"，实施时改掉了：`legacy` 档下一个未带凭据的老 Agent 上线也会让名册多出/翻动节点，那会被误报成"你刚配好的那台到了"。`paired_ids` 是"这台机器用这枚码进来了"的确证，恰好是这一块的语义。首帧只播种不报喜（`load.seeded`），否则每次刷新都会庆祝一遍历史配对。
4. **多了两块。**未带凭据名单（§3 的硬要求："不许只躺在日志里"）与演示机群开关（§5 的红线："缺开关的演示不算完成"）——写设计的把这两件都归给了 S2c 的交付判据（§1 表），却都塞在"页面四块"之外，所以它们在这里落地。开关本身不是道具的"隐藏"：文案明写"关掉是真的关：生成停止、节点退役、其告警以'已退役'闭合"。
5. **详情页返回按钮原来在说谎。**`from` 只有 overview/topology 两种且标签写死"← 总览"时看不出来；S2c 加了第三个来源后暴露——从接入页进详情，返回按钮仍写"← 总览"，点回去却落在接入页（`App.vue` 那句三元把 `enroll` 判成了非 topology，即 overview）。所以改法不是补一个字符串，而是把"来源"变成一张表（`ORIGINS` + `BACK_TEXT`），下一栏加视图时不会再错一次。

**验证（浏览器实跑，Qoder 内置浏览器；截图通道当时不可用，全部是 DOM/行为核对）**

- 真机全链路：接入页生成码（`70d6…2ff9`，默认 15 分/1 次）→ 把页面上那行 Windows 命令**原样**在真终端执行（不带 `--host-id`，走 S2b 的主机名推导）→ Agent 注册为 `paired`、名册 `confirmed + has_credential`、"等待接入"出现"柴先生的电脑 查看详情 →"、顶栏 `在线 2/2`。这条就是 §1 那句"把加机器从改文件变成点一下"。
- 撤销有牙：页面点"撤销" → 列表行消失 + `已撤销配对码 …2ff9` → 拿**同一枚码**换新 `--data-dir` 再跑 → 服务端拒（中文文案"配对码不存在（是不是抄错了，或已被撤销）"），拒绝台账多一行 `127.0.*.*`。
- 未带凭据提示（这一条之前只由 JS 假 Agent 覆盖过）：先用一次性脚本往临时名册里塞一行 `kind=agent / enroll_token=NULL / confirmed=1`（就是生产三台升完服务端档之后的样子），重启 Server，再用真 Agent 带全新 `--data-dir`、不带任何凭据接入 → 服务端 `accepted_legacy`、顶栏 `⚿ 未带凭据 1`、接入页"未带凭据的 Agent 1 台"列出显示名，点名字直达详情页。
- 演示机群：开 → `演示 4` chip 出现、总览 6 张卡片（4 张 `模拟-` 前缀）、`在线 2/2` 与 `集群正常` 不变；关 → chip 消失、卡片回 2 张。聚合数字不受道具污染这条在 S2a 自测 E/G6 段已有机器核对，这里是眼睛再看一遍。
- 返回标签：接入页 → 详情 = `← 接入`，总览 → 详情 = `← 总览`，返回后落在原视图（`.cb-view button.active` 核对）。
- 实跑中抓到一处**会在重启后说谎的文案**（H15 的呈现面）：`ingest.events` 是内存环形缓冲，Server 一重启就合法地空了，而卡片原来写"暂无记录"——看板上就成了"从来没人被拒过"，与刚发生的拒绝正好相反。改成标题带窗口"只到 Server 本次启动"、空态写"本次启动以来没有拒绝记录"。真正的持久化仍留给 S3 事件流（H15 原计划不变）。
- 同一轮重启还暴露了**名字来源不足**：`keyless_agents` 与 `paired_ids` 都是名册事实，但页面上原来只用实时主机表查显示名，于是重启后（Agent 还没回来）未带凭据那张卡片上写的是裸 `legacy-box`，而看板其它地方叫它"客厅小主机"。接入页因此多拉一份 `/api/roster` 作第二命名源（顺序：主机表 → 名册行 → 裸 id），且它自己的 `try/catch` 不与配对码列表共担失败。点进去的详情页由既有的"节点暂不可见"覆盖层交代，不是死路——这一段是实跑看到的，不是推断。
- 拒绝路径：陌生 `host-id` 无凭据 → Agent 打印中文原因并退出（服务端台账 `unknown_host`），不重试。
- 回归：`selftest-roster.mjs` **100/100**（含新增 G8 四项）、`selftest-agent-pair.mjs` 14/14、`selftest-history.mjs` 10/10（这套现在才有 exit code，见下面"脚手架"）、`python scripts/config-selftest.py` 16/16、`npm run build` 通过。三套 Node 套件跑完 `%TEMP%` 无残留。

**脚手架顺手修的两处（不属于 S2c 范围，但被这次实跑照出来）**

- `selftest-history.mjs` 原来只把 PASS/FAIL 打进 stdout，**永远 exit 0**——T1–T10 里任何一条红了都不会让这批测试失败（和 §9.2 记的"空断言"同一类洞，只是高了一层：这次是套件的裁决本身）。改成 `check()` 统一记账并 `process.exit(fail ? 1 : 0)`；负例实测过（人为把 T10 改坏 → `pass=9 fail=1` 且 exit=1），不是只写完就宣称修好。
- 三个自测套件跑完都把一次性目录留在 `%TEMP%`（今天这台机器攒到 22 个 `hm-s1-*` + 14 对 `hm-p5-*`）。改成**绿了就删、红了保留并打印路径**（调试时真正需要那条路径的只有红的这一次）。Windows 上有个必然的坑：`node:sqlite` 的句柄不关，`rmSync` 就会被文件锁挡下来——第一版因此只留下半个 `roster.db`，于是给 D 段的 `metaDb` 与进程内 `roster.db` 补了关闭。**§9.2 里"跑完即删"那句在写下时并不成立，现在成立。**

**S2c 已知未闭合（不隐藏）**

- `GET /agent.tar`（设计 §6 的"真一行"可选项）**没做**。本期给的仍是"解出 agent 目录 + 一条命令"，`pip install -r requirements.txt` 这一步还要人配合。归到 S6（完整设置界面）或直接砍——它不改数据模型，什么时候补都不会烂。
- 三件"没做过实测"的事：`← 拓扑` 这条标签（拓扑详情只能靠 3D 拾取进入，合成点击没打中节点；它与已验证的两条共用同一张 `BACK_TEXT` 表）、`navigator.clipboard` 在纯 http 下的降级路径（代码有 `execCommand` 兜底，实跑环境是 localhost 安全上下文，走的是主路径）、"≤3 分钟"的人工掐表（见 §7）。
- 顶栏两枚 chip 只在"有内容"时出现，空态不占位——`⚿ 未带凭据` 归零就是翻 `strict` 的前置条件（`部署说明.md` 里程碑清单第 4 步），别把它当成可以忽略的装饰。
- 一次性实跑环境留下的痕迹都已清理：临时 Server/Agent 进程停止、`agent/identity.json` 删除（它指向那台一次性 Server）、`%TEMP%\hm-s2c*` 与旧的 `hm-visual*` 目录删除。名册真源 `server/data/` 未被碰过。
